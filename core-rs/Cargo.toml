[package]
name = "core-rs"
version = "0.1.0"
edition = "2021"
rust-version.workspace = true
publish = false

[dependencies]
core-sys = { path = "../core-sys" }

# The same crate again under dev: the tests compare the wrapper against the raw
# call bitwise, and without direct access to core-sys there would be nothing to
# compare against.
[dev-dependencies]
core-sys = { path = "../core-sys" }

[lints]
workspace = true
