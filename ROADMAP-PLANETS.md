# ROADMAP-PLANETS.md — планети (етап R)

> Рішення проєкту — [PROJECT.md](PROJECT.md), решта робіт — [ROADMAP.md](ROADMAP.md),
> питання до розробника — [QUESTIONS.md](QUESTIONS.md).
>
> **Етап R закрито цілком (R1–R8); M4 закривають R1–R6.** R7 (процедурний
> детайл) — етап R, але не мілстоун. Хід кроків лишається в git; тут — рішення
> й виміряні межі, що діють далі.

Окремий файл тому, що етап R **переписує кадр зсередини**: `frame.rs`, глибину,
шейдери, і вперше в проєкті заводить ассет-пайплайн для текстур. Нумерація
`R*` стала, номери не перевикористовуються.

---

## Правила, що діють далі

Інваріанти планетарного рендера. Кожен виведений з уже ухваленого рішення;
порушення кожного — помилка архітектури, не компроміс.

1. **Патч — одиниця всього.** Рівно та роль, яку в фізиці має ланка: позиція
   патча живе в `f64` на CPU, вершини — `f32` **відносно початку патча**, і на
   патч же вішається все інше — вибір рівня, відбір, відвантаження на GPU,
   тайл DEM.
2. **Похибка вершини — стала частка розміру патча, а не відстані до камери.**
   `f32` дає 6·10⁻⁸ відносних, тож зсув усередині патча похибний на
   `6·10⁻⁸ · d_patch`, і на екрані це ніколи не більше за ту саму частку
   кутового розміру патча. Форма твердження навмисно та сама, що у виміряного
   `Δz ≈ z·6·10⁻⁸`: обидва кажуть, що абсолютні метри тут не аргумент.
3. **Рушій і далі не знає про гру.** Планета в `Scene` — це дані (центр у
   `f64`, радіус, орієнтація, ідентифікатор набору тайлів), а не «Земля».
4. **Жоден біт траєкторії не рухається.** Рельєф — це рендер; у силову модель
   висота з DEM не входить і не увійде побічно. Якщо колись знадобиться
   (посадка), вона прийде **через ассет** тим самим бітовим шляхом на всіх
   платформах, і впреться в уже відоме: під опорною сферою ряд гармонік
   **розбіжний**, тож `harmonics.c` тримає там `(Re/r)` не вище одиниці (K5e).
5. **Тріщина між патчами — топологічна помилка, і ловиться на CPU.** Спільна
   вершина двох сусідніх патчів мусить рахуватися однією формулою й збігатися
   бітово. На знімку тріщина — одна темна лінія в піксель, яку око пропустить,
   а тест на рівність — ні.
6. **Кожна текстура — крізь bindless-масив, з першої.** Семплери індексуються
   **окремо** від текстур (1024 на Metal — найменше з трьох чисел), per-object
   дані лежать у storage-буфері, ніколи в масиві uniform-буферів (D3D12 їх не
   дає).
7. **Перевірка — знімком без вікна плюс числом.** Планета з тріщинами й
   планета без них виглядають однаково, поки не подивишся; але знімок — друга
   половина перевірки, перша завжди число або рівність на CPU.
8. **Дані DEM приходять у репозиторій з README про походження, продукт і
   ліцензію** (прецедент `data/grail`). Повний LOLA (118 ГБ) у git не їде
   ніколи; що саме їде, вирішує **число метрів на піксель**, а не бажання.

---

## Рішення R1–R6, що діють далі

### Кубосфера й патч

- **Куб на сферу — тангенційний варп, не наївна нормалізація.** Відношення
  найдовшого ребра до найкоротшого на сітці 32×32: **2.0748 наївно проти
  1.4136 з варпом**. Шов закритий побудовою, і це три окремі рішення: одна
  таблиця параметрів на всі грані, рівні й патчі; дзеркало **відніманням**, а
  не другим викликом `tan`; кінці таблиці точно ±1, бо
  `tan(π/4) = 0.99999999999999989` (7 мм на радіусі Землі). 372 вершини
  спільні для двох граней, 8 — для **трьох**; саме кути куба ламають наївне
  зшивання.
- **Перестановка осей `u`/`v` в одній грані шва не розриває** — транспонована
  грань дає ту саму множину вершин. Ловить її лише перевірка з патчами
  (відповідність `(i, j)` між сусідами).
- **Патч: початок у `f64`, зсуви від центральної вершини у `f32`.** Зсуви від
  центра вдвічі коротші за зсуви від кута. Правило 2 підтверджене як закон:
  частка розміру патча стоїть на 1.96–2.44·10⁻⁸ при зміні розміру в тисячу
  разів. Розбіжність із пер-вершинним шляхом міряється **кутом**, не метрами:
  1.15·10⁻⁷ рад на 10 м і 6.82·10⁻¹² на 4.05·10⁸ м — відстані немає в рівнянні.
- **Порядок в обчисленні початку патча і є суть:** `центр + R·(q·s) − око`,
  усе у `f64`. Множення на радіус **після** віднімання камери повернуло б
  катастрофу скорочення. `f32`-матриця моделі безпечна саме тому, що масштаб
  виносить множник, а не додає до великого числа мале.
- **Геометрія патча — одинична сфера, спільна для всіх тіл**; радіус і поворот
  тіла їдуть матрицею моделі, виклик малювання — на тіло. Початки патчів — у
  storage-буфері. Геометрія кешується **за патчем** (від камери й тіла не
  залежить); місткість кеша вдвічі більша за потребу кадру, витіснення —
  курсором по колу, тож у пошуку немає гілки «а якщо ні».
- **Сітка рівня `L+1` містить сітку рівня `L` у своїх парних вузлах, бітово.**
  На цьому стоїть зшивання рівнів.
- **Ближня площина міряється від найближчого тіла сцени.** Камера за 100 км
  над Місяцем, яка міряє висоту від Землі, отримала б `near` у мільйони метрів,
  тобто кадр без Місяця.
- **Поворот гладкої сфери показати не можна**, і це властивість, а не вада:
  силует і нормаль переходять самі в себе. Перевіряється те, що варте
  перевірки — поворот застосовано **однаково** до геометрії й до нормалей.
- **Збоку сфера проєктується в еліпс** — за 41° від осі диск Землі виміряний як
  35×26 пікселів. Кругла формула там міряє не ту фігуру.

### Вибір рівня й зшивання

- **Рівень обирається за екранною похибкою, не за відстанню:** відстань не знає
  ні про поле зору, ні про роздільність, ні про радіус тіла. Стріла прогину —
  `1 − |середина хорди|` на одиничній сфері, без тригонометрії; радіус —
  множник наприкінці. Найгірша клітинка лежить біля центральної лінії грані,
  тож обхід — 64 клітинки на патч замість 1024.
- ⚠ **Відстань до патча міряється його конусом, і п'ять вузлів тут не
  наближення, а вада** (борг D13). Мінімум по чотирьох кутах і центру
  правильний рівно над **центром грані** — і саме туди його ставила вся
  фікстура, тож вада прожила від R2a до R7a невидимою. За двадцять градусів
  убік вибірка казала 758 км там, де під ногами 170. Тепер відстань іде до
  сферичної шапки, у яку патч вписаний (`Patch::cone`) — того самого конуса,
  що й у `cull::beyond_limb`: шапка більша за патч, тож помилитись вибір може
  лише в бік зайвого поділу. Око над шапкою — окрема гілка `d − R`, бо теорема
  косинусів там скорочує дванадцять цифр на висоті метра.
- **Вибір рівня питає і про сферу, і про рельєф** (R7c). Стріла прогину падає з
  рівнем квадратично, похибка рельєфу — лінійно, тож здалеку вирішує перша,
  зблизька друга; беруться через `max`, не суму — джерела незалежні. Без
  другої критерій зупинявся на клітинці завширшки з екран (8309 px на 100 м),
  і жодна деталь не мала де жити.
- **Здалеку планета — рівно шість граней** (допуск 1 px, поле зору 60°), на
  300 км — 9 патчів. Похибка падає рівно вчетверо на рівень (4.000 на семи
  рівнях) — але лише якщо порівнювати **ту саму ділянку** поверхні.
- **Стеля `MAX_LEVEL = 12` повертає `Selection::clamped` окремим полем:** стеля,
  що ріже якість мовчки, — та сама помилка, що й її відсутність.
- **Сусідство через ребро куба виводиться з `AXES`, а не з таблиці на 24
  рядки.** У `AXES` знак має лише нерухома вісь, тож **індекс уздовж спільного
  ребра не перевертається ніколи**: вузол `k` з одного боку — вузол `k` з
  другого.
- **Зшивання — одна підміна індексу, а не шістнадцять тріангуляцій.** На ребрі
  з грубішим сусідом непарний вузол не існує, і скрізь, де він згаданий, стоїть
  попередній парний. Кути патча не є окремим випадком **за парністю**. Ціна —
  `SIDE / 2` вироджених трикутників на зшите ребро з 2048.
- **Вирівнювання рівнів тільки подрібнює** (огрубити дрібний бік означало б
  мовчки віддати якість), і воно **циклічне**: одного поділу грубого сусіда на
  ребро досить, поки критерій міняється плавно — стріла прогину саме така, — а
  рельєф між сусідами стрибає, і різниця в три рівні з'явилась першим кадром.
- **Оракул зшивання — топологія, у формі, що не знає про рівні:** кожне
  неорієнтоване ребро сітки належить рівно двом невиродженим трикутникам, і
  `V − E + F = 2`. Виміряно: 33 патчі / 67200 трикутників / 384 вироджених на
  100 км, жодного самотнього ребра.
- **Знімок тріщини між рівнями не бачить, і це число:** незшитий стик — це
  **0.060 пікселя**, бо саме цю величину критерій тримає під допуском в один
  піксель. Знімки лишаються сторожем рівно проти одного — **видимого патча,
  якого немає в кадрі**.

### Відбір

- **Відбір іде після вибору рівня, а не всередині нього.** Маска зшивання
  рахується з набору; викинутий під час обходу сусід став би «дрібнішим боком»,
  і тріщина з'явилась би рівно на лімбі — на силуеті.
- **Критерій лімба — без тригонометрії в кадрі:** `cos(β − α) = cos β cos α +
  sin β sin α` для конуса патча, узятого по чотирьох кутах; перебір усіх вузлів
  дає запас **рівно нуль** — кут не «близько» до межі, він і є межа.
  ⚠ **Око всередині конуса — окремий випадок** (борг D14): косинус парний, тож
  формула тихо повертає кут до краю шапки замість нуля. Це рід D13 і його ж
  виправлення, застосоване наполовину.
- **Horizon важливіший за frustum — числом.** Лімб прибирає 88% патчів на
  100 км, 56% на 300 км, 33% на 2000 км, 17% з відстані до Місяця; frustum
  понад це додає **0, 0, 1 і 0** патчів. Він почне заробляти, коли DEM підніме
  кількість патчів.
- **Два радіуси тіла, а не один.** `occluder_radius_m` стоїть поруч із
  `radius_m` уже сьогодні, хоч вони й рівні: перший каже, де вершини, другий —
  що затуляє погляд.
- **Поворот тіла входить і у вибір рівня, і у відбір.** Конус патча живе в
  системі тіла, камера — у системі світу; поки орієнтація одинична, різниці
  немає, і саме тому помилка цього класу не знаходиться тоді, коли її зробили.
  Око переводиться в систему тіла раз на тіло. Сторож — симетрія: поворот тіла
  й камери на той самий кут дає той самий набір поштучно.
- **Відбір живе у compute, кількість — у `draw_indirect`.** Оракул, заради
  якого R3 робився на CPU: на 24 камерах (вісім кутів куба × три висоти) GPU
  намалював рівно стільки патчів, скільки лишив CPU. Точно, без допуску.
- **Порядок вижилих недетермінований** — атомарна компактація. Для кадру це
  байдуже (непрозорі патчі, глибина), для звірки порівнюється кількість.
  Записано вголос, бо в цьому проєкті недетермінізм зазвичай помилка.
- **Вершинних буферів у планети немає.** Геометрія, початки й список малювання —
  storage-буфери, номер вершини й інстансу дає конвеєр. Причина не в моді на
  GPU-driven: зшивання це підміна **індексу** вузла, і поки вершина приходила
  вибраною, підміну доводилось робити шістнадцятьма індексними наборами й
  викликом на патч.

### Проходи, глибина, тайли

- **Прохід кадру — список структур, а не граф.** `wgpu` розставляє бар'єри сам,
  тож від класичного графа лишається порядок, а порядок — це список. Проєкція
  своя на прохід (динамічний зсув у одному буфері, крок 256), глибина
  очищується завжди, колір — лише в першому.
- **Діапазони глибини не купують роздільності. Виміряно.** Найменший
  роздільний зазор однаковий для нескінченної проєкції й для скінченного
  діапазону з будь-якого його кінця: 0.5 м на 10⁷ м, 4.0 м на 10⁸ м, 16 м на
  4·10⁸ м. Причина — катастрофічне скорочення в `z_clip` біля далекої площини.
  Виправдані вони **scaled space**, а не точністю (питання Q2, закрите
  розробником: діапазони лишаються).
- **Кількість проходів виводиться з розмаху сцени:** один діапазон на сім
  порядків, не більше чотирьох. Сцена зондів дає один прохід, і кадр лишився
  бітово тим самим від R4a до R6b — sha256 `30812bf2…` через чотири перезаписи
  того, як кадр видається.
- **Рівень тайлів вирішує число, а не бажання:** LDEM_4 дає 7581 м на відлік,
  тобто 47 екранних пікселів на відлік при камері на 100 км. Це рівень, який
  вміщається в репозиторій (2.0 МБ проти 33 і 531); підняти його — це **дані,
  а не код**.
- **Тайл лежить вузол у вузол із сіткою патча.** Звідси головне: рельєф не
  додає тріщин узагалі — вершина на спільному ребрі бітово одна, отже напрямок
  вибірки один, отже висота одна (0.000000 м на межі тайлів). Зшивання рівнів
  не потребувало жодної правки.
- **Висоти — `int16` без стиснення**, у тих самих пів метра, що й джерело.
  Квантування насичує, а не загортає: загорнута гора на 33 км стала б
  правдоподібною западиною.
- **Перша текстура в рушії пішла крізь bindless-масив**, і три речі довелося
  з'ясувати замість припустити: `naga` не приймає Slang-ового
  `array<texture_2d<…>>` (потрібні `binding_array` і `enable`), `wgpu` не
  дозволяє масиву й буферу з динамічним зсувом жити в одній групі, а обидва
  пайплайни ділять макет — тож група масиву мусить бути прив'язана й тоді, коли
  шейдер до неї не звертається.
- **Рельєф має власну нормаль, і аналітичної в нього немає:** поверхня — це
  набір фасеток розміром із клітинку DEM, тож нормаль береться з похідних
  світової позиції. Виміряно на термінаторі: 39191 повної варіації яскравості
  проти 4041 у гладкої сфери.

---

## Рішення R7 (процедурний детайл)

**Навіщо він, видно очима:** гра малює Місяць до 200 м висоти, і на останніх
кілометрах поверхня стає **гладкою** — дані LOLA скінченні, а патчі ні.
Інтерполяція між вузлами тайла нових висот не додає.

**Три правила шуму, ухвалені до першого рядка, і всі три з перевіркою:**

1. **Шум — функція позиції, і нічого більше.** Аргумент — **одиничний напрямок
   вершини**, не позиція відносно патча: патч віднімає камеру від свого
   початку, тож «позиція» в кожного своя, а напрямок лежить у буфері геометрії
   нормаллю і на спільному ребрі **бітово один**. Неперервність деталі не
   доводиться — вона успадкована.
2. **Затухання октави — від позицій вузла й камери**, ніколи від рівня патча.
   Довжина хвилі в пікселях, `λ·focal/d`, і відстань береться **на вершину**:
   `length(origin)` була б стала для всього патча.
3. ⚠ **Довжина хвилі найгрубішої октави НЕ береться з глибини піраміди.**
   «Деталь починається там, де кінчаються дані» читається красиво й порушує
   правило 1: глибина піраміди — параметр **кукера**, тобто стан генератора.
   Перекукування ассета з іще одним рівнем переграло б гори. Береться
   `радіус/512` — на Місяці 3393 м, практично той самий масштаб, що вузол
   LOLA, але виведений з тіла.

**Амплітуда — від нахилу DEM, і різниця береться на сталій метричній базі**, а
не по сусідніх вузлах того тайла, який патч читає: вирівнювання дозволяє
сусідам накриватися тайлами різних рівнів, а їхні вузли різняться вдвічі — той
самий вузол на спільному ребрі дістав би дві амплітуди.

**Дві передумови, яких план не мав:**

- **Адресація підпрямокутника чужого тайла (R7a).** До неї LOD не спускався
  глибше за піраміду тайлів: патч глибшого рівня читав би тайл предка за
  **своїми** локальними координатами. `Terrain::window` виділено з
  `Terrain::height_m` (шейдер — її дослівний двійник), `PatchData` виріс з 16
  до 32 байтів і несе зсув та масштаб вікна.
- **Ореол в один вузол по краю тайла (R7b).** Градієнт у вузлі на межі тайла
  потребує сусіда з іншого тайла; затиснути його — дістати на двох боках межі
  різні амплітуди, тобто тріщину рівно там, де її прибрало зшивання. Тайл
  35×35 замість 33×33: сітка патча недоторкана, плюс кільце з першого ряду
  сусіда. **Копія, а не посилання** — у шейдера один тайл на патч. Ціна:
  4 464 404 → 5 020 916 байтів, множник 1.1247 проти передбачених 1.1250.
  Вибірка **висоти** в ореол не заходить свідомо: поверхня — це сітка патча.

**Оракули, і два уроки з них:**

- Нахил на живих LOLA — **бітово одне число з обох боків ребра** на 1008
  вузлах, з них 72 сусідства через ребро куба; на лінійній рампі збігається з
  аналітичним із нульовою відносною похибкою.
- Шум: та сама точка завжди дає ту саму висоту; амплітуда падає монотонно й
  стає **рівно нулем** на 6.3·10⁵ м — це і є «зникає, а не мерехтить».
- ⚠ **Пандус як фікстура не міряє нічого.** Лінійне поле відтворюється точно,
  але його нахил 2·10⁻⁴ не змінив жодного пікселя проти гладкої сфери. Поле
  мусить бути шумом, інакше бітова рівність тримається тому, що обидва кадри
  однаково порожні.
- ⚠ **Одна камера не бачить половини помилок адресації.** Перестановка
  `origin.x` і `origin.y` у шейдері змінила **нуль** пікселів при повороті 0° і
  1 047 163 при 90°. Тому камер чотири.

**Відкрите питання етапу — Q3** (смуга навколо кута куба, де нахил двозначний;
39% розбіжності на ≈5.3 км навколо восьми кутів). Робота пішла далі під
припущенням «лишити як є».

---

## Що показало закриття (R8)

- **Кадр подорожчав удвічі там, де сцена не змінилась**: 0.064 → 0.153 мс на
  10⁷ м, при тому що патчів там стало п'ять проти шести. Платить не геометрія,
  а прохід compute плюс вибір рівня на CPU — ціна R2–R6 однакова на всіх
  висотах. Числа — ROADMAP.md, «Вимір продуктивності».
- **CPU-прохід camera-relative не повернувся**: 0.027 мкс на тіло, те саме
  число, що на R1e.
- **Рельєф мільйона вершин у кадр не приніс,** і це відповідь на D7 з другого
  боку: 33 патчі на 100 км — це 36 тисяч вершин. Причина не в щедрості, а в
  критерії: рівень вибирається за екранною похибкою, тож кількість вершин
  обмежена **розміром кадру**, а не тривалістю гри. Ламана такої межі не має —
  у цьому вся різниця між нею й рельєфом.
- **Етап не під'єднав зроблене до гри** — це помітили пізніше, на U8: борг D12.

---

## Чого етап R свідомо не зробив

- **Атмосфери й матеріалів PBR** — це M5 (атмосферу закрив етап S).
- **Океану, рослинності, хмар** — там же, і кожне вартує власного кроку.
- **Колізії з рельєфом і посадки.** Це фізика, а етап R не рухає жодного біта
  (правило 4).
- **Кольорових мозаїк високої якості.** Спочатку геометрія: колір, накладений
  на неправильну форму, виглядає правдоподібно, і це найгірше поєднання.
- **Кількох планетних систем.** Тайли Місяця й Землі досить, щоб довести
  конструкцію; решта тіл — це дані, не код.
