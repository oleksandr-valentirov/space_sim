# ROADMAP-PLANETS.md — планети (етап R)

> Послідовність робіт над планетарним рендером — те, що закриває **M4**.
> Рішення проєкту — [PROJECT.md](PROJECT.md), решта робіт — [ROADMAP.md](ROADMAP.md),
> інтерфейс — [ROADMAP-UI.md](ROADMAP-UI.md), питання до розробника —
> [QUESTIONS.md](QUESTIONS.md). Дат немає свідомо, як і скрізь тут.
>
> Файл живе за тими самими правилами, що ROADMAP.md: тримає **рішення й
> виміряні межі, що діють далі**, а не звіт про зроблене. Історія кроків
> лишається в git.

---

## Чому окремий файл і чому «R»

Окремий — з тієї ж причини, що й [ROADMAP-UI.md](ROADMAP-UI.md), тільки
дзеркальної. Етап U не рухає жодного біта траєкторії й тому може йти
паралельно з ядром. Етап R теж не рухає жодного біта — але він **переписує
кадр зсередини**: `frame.rs`, глибину, шейдери, і вперше в проєкті заводить
ассет-пайплайн для текстур. Це найбільший обсяг правок в одному крейті за всю
історію репозиторію, і тримати його в ROADMAP.md означало б, що дві паралельні
сесії правлять один розділ. Так ROADMAP.md має рівно один рядок-покажчик сюди.

**Літера R, а не M4.** `M*` — це мілстоуни з PROJECT.md §10; етап — це робота,
що мілстоун закриває. A–L зайняті послідовно, `U` — інтерфейсом, `P` —
розвідками (P0, P1), `D` — боргами, `Q` — питаннями. Лишається `R` (render),
і нумерація `R*` **стала**: номери не перевикористовуються.

---

## Що виміряно перед плануванням

Не припущення. Кожен рядок або прочитано в коді, або взято з уже виміряного
в ROADMAP.md.

| Питання | Відповідь | Звідки |
|---|---|---|
| Скільки проходів у кадрі сьогодні | **Один.** Сфера й ламані — в одному `begin_render_pass`, одна depth-текстура, обидва вкладення `Clear` | `engine/src/frame.rs:413` |
| Чи є frame graph | **Немає жодного.** Порядок проходів прописаний у `Frame::draw` | `engine/src/frame.rs:369` |
| Скільки текстур у рушії | **Нуль.** `sphere.wgsl` не має ні текстури, ні семплера; єдиний `png` у крейті — це запис знімка | `engine/shaders/sphere.wgsl`, `engine/Cargo.toml` |
| Яка зараз сфера | UV-сфера 64×128 = **8385 вершин**, позиції в `f64`, camera-relative **per-vertex на CPU щокадру** | `engine/src/sphere.rs:25`, `main.rs:260` |
| Скільки це коштує | кадр **0.109 мс**, з них **чверть** — той самий CPU-прохід | ROADMAP «Вимір продуктивності» |
| Скільки порядків тримає один буфер глибини | **~7** (`Δz ≈ z·6·10⁻⁸`, `near` скорочується) | ROADMAP «Рендер», F3 |
| Скільки порядків треба планеті | 1 м деталі поверхні при Місяці в кадрі на 4.05·10⁸ м — **8.6 порядків** | арифметика з рядка вище |
| Чи є радіус тіла на межі | **Ні.** Межа — вісім функцій, з них у `core-rs` відкриті `body_state`, `run`, `run_stm`, `lambert_solve` | `core-rs/src/lib.rs`, ROADMAP «Межа C↔Rust» |
| Чи є орієнтація тіла на межі | **Ні.** `eph_body_orientation` існує в C (K3b) і читається полем усередині ядра; крізь межу не проходить | `core/ephemeris.h`, `core-rs/src/lib.rs` |
| Чи знає `Scene` про тіла | **Ні, і свідомо**: сфера одна, в початку координат, з радіусом Землі з рушія | `engine/src/scene.rs` |
| Чи є DEM у репозиторії | **Немає жодного.** Прецедент вендорингу даних — `data/grail` (140 КБ + README про походження) | `data/`, K5a |
| Чи є кукер ассетів на Rust | **Ні.** Єдиний кукер — на C (`make cook`, ефемерида); `/tools` містить лише зонди | `Makefile:406`, `tools/` |
| Ліміти bindless | семплерів **1024 на Metal**, 2048 на D3D12; **D3D12 не дає масивів uniform-буферів** | P0, PROJECT.md §7 |

Три висновки, які вже змінили план:

- **Frame graph не може бути першим кроком.** Сьогодні прохід один, і граф над
  одним проходом — це рівно та «структура, жодне поле якої не читається»,
  яку CLAUDE.md називає гіршою за свою відсутність. Але й відкладати його
  нікуди: тригер названий числом — 8.6 порядків проти 7, — і спрацьовує він
  тоді, коли в кадрі водночас поверхня й друге тіло. Тобто граф іде **після**
  геометрії й **перед** тим, як на цю геометрію дивляться зблизька: R4.
- **Bindless не можна відкласти «на потім».** Перша текстура в рушії з'явиться
  саме тут — сьогодні їх нуль, — а PROJECT.md §7 прямо забороняє «спочатку
  класично, потім перепишу». Отже перший тайл DEM приїжджає вже крізь масив
  (R5c), і масив з одного елемента нічого не доводить: перевірка вимагає двох.
- **Відбір на CPU — не чернетка, а майбутній оракул.** R3 робить horizon
  culling на CPU не тому, що так простіше, а тому, що R6 порівнює з ним
  кількість намальованих патчів. Пропустити R3 означало б, що GPU-відбір
  нема з чим звіряти, і його помилка виглядатиме як «десь щось не намалювалось».

---

## Принцип упорядкування

Той самий, що в ROADMAP.md: **за спаданням імовірності успіху**, з винятком
для дешевих розвідок. Кожен крок має **перевірку** (як дізнатися, що зроблено)
і **розвилку** (що робити, якщо не вийшло). Позначки ризику ті самі:
🟢 низький, 🟡 середній, 🔴 високий.

Дрібний крок — один коміт із власним оракулом. Великий крок закривається
повним локальним гейтом і пушем (CLAUDE.md, «Порядок роботи»).

---

## Правила, що діють на весь етап

Інваріанти планетарного рендера. Кожен виведений з уже ухваленого рішення,
а не вигаданий тут; порушення кожного — помилка архітектури, не компроміс.

1. **Патч — одиниця всього.** Рівно та роль, яку в фізиці має ланка: позиція
   патча живе в `f64` на CPU, вершини — `f32` **відносно початку патча**,
   і на патч же вішається все інше — вибір рівня, відбір, відвантаження на
   GPU, тайл DEM. Наслідок, який видно одразу: per-vertex camera-relative
   зникає, а `--perf-probe` міряє цей прохід окремим числом саме заради
   цього дня (ROADMAP «Вимір продуктивності», `engine/src/perf_probe.rs:63`).

2. **Похибка вершини — стала частка розміру патча, а не відстані до камери.**
   `f32` дає 6·10⁻⁸ відносних, тож зсув усередині патча похибний на
   `6·10⁻⁸ · d_patch`, і на екрані це ніколи не більше за ту саму частку
   кутового розміру патча. Форма твердження навмисно та сама, що у
   виміряного `Δz ≈ z·6·10⁻⁸`: обидва кажуть, що абсолютні метри тут не
   аргумент.

3. **Рушій і далі не знає про гру.** Планета в `Scene` — це дані (центр у
   `f64`, радіус, орієнтація, ідентифікатор набору тайлів), а не «Земля» й не
   «Місяць». Те саме рішення, що вже записане в `scene.rs`, тільки тепер
   список тіл нарешті має читача — і саме тому він з'являється.

4. **Жоден біт траєкторії не рухається.** Рельєф — це рендер. У силову модель
   висота з DEM не входить, і не увійде побічно: якщо колись знадобиться
   (посадка), вона прийде **через ассет** тим самим бітовим шляхом на всіх
   платформах, і це буде окреме рішення з власним оракулом, а не наслідок
   того, що тайл уже лежить у пам'яті. Заборона `libm` в циклі інтегрування
   (CLAUDE.md, інваріант 3) етапу R не стосується взагалі: генерація сітки
   й кукер — це офлайн і CPU поза інтегратором.

5. **Тріщина між патчами — топологічна помилка, і ловиться на CPU.** Спільна
   вершина двох сусідніх патчів мусить рахуватися **однією формулою** й
   збігатися бітово. Пікселі про тріщину кажуть надто пізно й надто тихо: на
   знімку вона — одна темна лінія в один піксель, яку око пропустить, а тест
   на рівність — ні.

6. **Кожна текстура — крізь bindless-масив, з першої.** Семплери індексуються
   **окремо** від текстур (1024 на Metal — найменше з трьох чисел), per-object
   дані лежать у storage-буфері, ніколи в масиві uniform-буферів (D3D12 їх не
   дає). PROJECT.md §7, розвідка P0.

7. **Перевірка — знімком без вікна плюс числом.** «Кадр не знає про вікно»
   діє тут дослівно, і причина та сама, з якої існує `engine::shot`: планета
   з тріщинами й планета без них виглядають однаково, поки не подивишся. Але
   знімок — друга половина перевірки, не перша: перша завжди число або
   рівність на CPU.

8. **Дані DEM приходять у репозиторій з README про походження, продукт і
   ліцензію** — прецедент `data/grail` (K5a). Повний LOLA (118 ГБ) у git не
   їде ніколи; що саме їде, вирішує R5a **числом метрів на піксель**, а не
   бажанням.

---

## Стан

| Крок | Зміст | Ризик | Стан |
|---|---|---|---|
| R1 | Кубосфера: проєкція, патч як одиниця, планета в сцені | 🟢 | не почато |
| R2 | LOD без тріщин: вибір рівня, зшивання, знімки | 🟡 | не почато |
| R3 | Horizon culling на CPU (майбутній оракул для R6) | 🟢 | не почато |
| R4 | Frame graph і чотири діапазони глибини | 🟡 | не почато |
| R5 | DEM: дані, кукер, тайли крізь bindless | 🟡 | не почато |
| R6 | GPU-driven: storage-буфер патчів, відбір у compute, indirect | 🔴 | не почато |
| R7 | Процедурний детайл, зшитий з градієнтом DEM | 🔴 | не почато |
| R8 | Закриття: перемір, борги, M4 | 🟢 | не почато |

**M4 закривають R1–R6.** R7 — етап R, але не мілстоун: PROJECT.md §10 називає
в M4 «кубосфера LOD, DEM-тайли, horizon culling, frame graph», і процедурний
детайл туди не входить.

---

## R1. Кубосфера й патч як одиниця 🟢

Найдешевший великий крок: усе тут — арифметика на CPU з точними оракулами,
жодного нового шматка GPU-конструкції.

### R1a. Куб → сфера, і шов, якого немає 🟢

Проєкція грані куба на сферу з **тангенційним варпом**, не наївною
нормалізацією: наївна дає різницю площ між центром грані й кутом, тобто
вершини витрачаються не там, де потрібні. Ціна — `tan` при генерації сітки,
і це дозволено (правило 4).

**Перевірка:** три числа й одна рівність.
1. Відношення найбільшого ребра до найменшого в сітці 32×32 на грань —
   записати **для обох проєкцій**; варп має дати менше. Одна цифра без другої
   нічого не означає.
2. `|r|` кожної вершини дорівнює радіусу з відносною похибкою `< 1e-15`.
3. **Вершина на спільному ребрі двох граней збігається бітово** — бо
   рахується однією формулою, а не двома симетричними.

Мутація, що валить саме третє: переставити місцями осі в одній грані. Вона
лишає перші два числа зеленими, і в цьому весь сенс третього.

**Розвилка:** якщо генерація сітки з `tan` виявиться помітною в профілі (вона
не в кадрі, а при появі патча) — таблиця варпу на рівень. Але не «прибрати
варп»: нерівномірність повернеться як удвічі більше вершин на ту саму
екранну похибку.

### R1b. Патч: початок у `f64`, вершини у `f32` 🟢

`Patch { face, level, i, j, origin: [f64; 3] }` і сітка вершин як зсуви від
`origin`. Це той крок, після якого правило 1 стає кодом.

**Перевірка:** позиція, відновлена як `origin + зсув`, відрізняється від
прямого обчислення в `f64` не більше ніж на `6·10⁻⁸` **розміру патча** — на
рівнях 0, 5 і 10, тобто твердження перевіряється як закон, а не як одне
число. Друга половина, обов'язкова: камера за 10 м від патча й камера за
4·10⁸ м від нього дають той самий силует — це той самий тест, що F4 робив
per-vertex, і саме він доводить, що зсув по патчах не втратив того, заради
чого camera-relative існує.
**Розвилка:** якщо на глибоких рівнях `f32` виявиться замалим (він не має
бути — див. правило 2), то помилка не в типі, а в тому, що зсув рахується
від чужого початку; шукати там, а не піднімати точність.

### R1c. Планета в сцені — дані, не «Земля» 🟢

`Scene` отримує список тіл: центр у `f64`, радіус, орієнтація, набір тайлів.
Радіус і орієнтація — це те, чого на межі C↔Rust **немає**; звідки вони
приходять:

- **Радіус — `eph_body_radius`, дев'ята функція межі.** Її вже планує U2a з
  власною причиною (висота в HUD). Хто дійде першим, той її й приносить;
  другий крок тоді порожніє, а не робить її вдруге.
- **Орієнтація — `eph_body_orientation` крізь межу** (K3b дав її в C).
  Без неї Місяць у кадрі не повернутий, тобто маскони й рельєф дивляться в
  різні боки. Це десята функція межі, і приносить її саме етап R.

**Перевірка:** знімок із двома тілами (Земля й Місяць у справжніх позиціях зі
снапшоту) — обидва на своїх місцях, розміри відносяться як радіуси; тест на
межі друкує кватерніон через `%.17g` і звіряється бітово, як усі вісім
попередніх функцій.
**Розвилка:** якщо орієнтація крізь межу потягне за собою похідну (кутову
швидкість) — не тягнути: рендеру похідна не потрібна, а D8 у ROADMAP.md уже
описує, як обидві читаються за один прохід **усередині** ядра.

### R1d. Кубосфера в кадрі замість UV-сфери 🟢

Фіксований рівень, без LOD, без DEM. Один крок — одна заміна.

**Перевірка:** знімок 1280×720 тією самою камерою до і після; порівнюється
**маска «планета / небо»**, не кольори (правило 7 і вже записане рішення про
sRGB), розбіжність — не більше ніж піксель по краю. І число: `--perf-probe`
друкує CPU-прохід camera-relative, і він має **впасти на порядки** (замість
8385 вершин щокадру — по одному зсуву на патч). Записати обидва числа.
**Розвилка:** якщо силует поїхав більше ніж на піксель — це не допуск, це
вісь або порядок обходу; дивитись у R1a, а не послаблювати перевірку.

---

## R2. LOD без тріщин 🟡

### R2a. Вибір рівня за екранною похибкою 🟢

Критерій — **геометрична похибка патча в пікселях** (стріла прогину, спроєктована
в екран), а не відстань до камери: відстань не знає ні про поле зору, ні про
роздільність, ні про радіус тіла.

**Перевірка:** монотонність — наближення камери ніколи не знижує рівень
жодного патча (перевіряється на послідовності з 100 позицій, а не на двох);
детермінізм — той самий кадр дає той самий набір патчів; і два записані
числа — скільки патчів при камері на 300 км і на 4·10⁵ км.
**Розвилка:** якщо кількість патчів вибухне на низькій орбіті — стеля на
рівень і на кількість, і стеля **називається числом у логах**, а не тихо
ріже якість.

### R2b. Зшивання: сусіди різняться не більше ніж на рівень 🟡

Правило «різниця рівнів сусідів ≤ 1» плюс варіанти індексних наборів на краї
(їх 16 — по одному на комбінацію чотирьох країв, і це відомо наперед, а не
з'ясується під час налагодження).

**Перевірка (правило 5, топологічна):** для кожної пари сусідніх патчів у
згенерованому наборі спільні вершини збігаються **бітово**, включно з кутом,
де сходяться **три** грані куба — там сусідів троє, а не четверо, і саме там
ламається наївне зшивання.
**Розвилка:** спідниці (skirt) по краю патча замість індексних наборів, якщо
комбінаторика країв почне розповзатись. Це нормальний інженерний вибір, не
поразка: ціна спідниць — кілька зайвих пікселів на краю, ціна індексів —
16 наборів. Вирішує вимір, а не смак.

### R2c. Знімки з восьми напрямків 🟢

Топологія не ловить усього: патч може бути зшитий і все одно провалитись,
якщо рівень обрано в один бік для геометрії й в інший для відбору.

**Перевірка:** вісім знімків (по кутах куба) при камері на 100 км; усередині
силуету планети **нуль пікселів кольору неба**. Це те, що ловить тріщину,
яка прослизнула повз рівність вершин.
**Розвилка:** якщо піксель неба знайдеться — не збільшувати спідницю, а
знайти пару патчів, яка його дала; знімок тут — детектор, а причина завжди
в R2a або R2b.

---

## R3. Horizon culling на CPU 🟢

PROJECT.md §7: **horizon culling важливіший за frustum** — половина планети
завжди за лімбом. Тут це стає числом.

### R3a. Критерій лімба 🟢

Патч видно, якщо його найближча точка лежить перед площиною дотику конуса з
камери до сфери **мінімального** радіуса тіла — не середнього. Різниця не
академічна: за середнім радіусом гора за лімбом зникає, а вона видна.

**Перевірка:** аналітична, обидва боки — патч, що торкається лімба, не
відкидається; патч на кілометр за ним — відкидається. І записане число:
скільки відсотків патчів відпало при камері на 300 км (очікується ~половина,
але важливе саме виміряне значення).
**Розвилка:** коли R5 принесе тайли, мінімальний радіус має братися **з
тайла** (там уже є min/max висоти), а не з тіла; поки тайлів немає — з тіла,
і це записано тут, щоб не забулось.

### R3b. Frustum після горизонту, і скільки він додає 🟢

**Перевірка:** число — скільки патчів прибирає frustum **понад** горизонт, на
тих самих камерах. Це перетворює «horizon важливіший» з цитати на вимір;
якщо виявиться навпаки, то це знахідка, а не помилка.

---

## R4. Frame graph і чотири діапазони глибини 🟡

Тригер названий числом, а не бажанням: прохід тримає ~7 порядків, а поверхня
з деталлю в метр разом із другим тілом у кадрі вимагає 8.6.

### R4a. Прохід стає даними 🟡

Мінімальний граф: проходи оголошують читання й записи, порядок і бар'єри
розставляє граф. Проходів на цей момент рівно стільки, скільки є читачів:
сцена, ламані — і UI, якщо U1b уже сів (див. «Паралельно з етапом U»).

**Перевірка — найдешевша можлива й точна:** знімок **бітово той самий**, що
до графа. Граф не міняє картинки; він міняє, хто вирішує порядок. Будь-яка
різниця в пікселях тут означає, що змінилось щось іще.
**Розвилка:** якщо граф почне вимагати узагальнень заради двох проходів —
зупинитись і лишити список проходів списком. Граф виправданий чотирма
діапазонами, LUT атмосфери й compute для LOD (PROJECT.md §7); до появи
другого з них він має право бути тупим.

### R4b. Чотири діапазони 🟡

Розбиття за відстанню, глибина очищується між проходами, композиція
back-to-front (PROJECT.md §7).

**Перевірка:** сторожа D4 (`a_metre_at_ten_million_is_exactly_one_ulp` у
`engine/src/depth.rs`) лишається зеленою — вона про арифметику, і діапазони
її не скасовують. Новий тест — **обидва боки**: дві поверхні, розділені
метром на 10⁸ м, дають різні пікселі, коли лежать у своєму діапазоні, і
однакові, коли їх свідомо запхати в чужий. Тест лише на перше був би зелений
на будь-чому. Плюс знімок «поверхня під ногами й Місяць у кадрі одночасно» —
той самий кадр, у якому 8.6 порядків.
**Розвилка:** якщо межі діапазонів дадуть видимий шов на силуеті — рухати не
`near` (він скорочується, F3 це виміряв), а межу між проходами, і виміряти,
де саме шов зникає.

---

## R5. DEM: дані, кукер, тайли 🟡

### R5a. Принести дані й прочитати їх 🟢

Та сама форма, що K5a: спершу дані в репозиторії й читач із оракулом, і лише
потім щось із ними робиться.

Продукт — **LOLA** (Місяць): він і найкраще документований, і Місяць у цій
грі найважливіший. Який саме рівень — вирішує число «метрів на піксель при
висоті 100 км»; README поруч із файлом описує походження, продукт, одиниці й
ліцензію, як `data/grail/README.md`.

**Перевірка:** min і max висоти, надруковані читачем, збігаються з
опублікованими для продукту. Це оракул, який ловить одразу три класичні
помилки — переплутаний порядок байтів, забутий масштаб (LOLA зберігає
цілими в частках метра) і зсув на пів пікселя в сітці.
**Розвилка:** якщо потрібний рівень не вміщається в розумний розмір
репозиторію — брати нижчий і записати, скільки метрів на піксель це дає в
грі; правило 8 про це прямо й каже.

### R5b. Кукер: перший інструмент ассет-пайплайну на Rust 🟡

`/tools` отримує кукер (PROJECT.md §7: «НЕ завантажуємо PNG у рантаймі»).
Тайли ріжуться по патчах кубосфери, формат власний, версія в заголовку —
рівно як у ассета ефемериди (він уже на версії 6).

**Перевірка:** SHA скукованого тайлсету **стабільний між прогонами**, і
висота в центрі тайла збігається з висотою, прочитаною з джерела в тій самій
точці **іншим шляхом** (пряма вибірка з сирого файлу за широтою й довготою).
Два шляхи, одне число — та сама форма оракула, що в K5e.
**Розвилка, названа наперед:** BC4 для висот дає видимі сходинки на пологих
схилах. Якщо вони з'являться — висоти лишаються `int16` без стиснення (тайл
малий), а BC7/BC6H відкладаються до кольору. Стиснення геометрії й стиснення
кольору — різні задачі, і плутати їх дорого.

### R5c. Тайл у кадрі крізь bindless 🟡

Масив текстур, семплери окремо (правило 6), висота зсуває вершину вздовж
нормалі патча.

**Перевірка:** висота, прочитана назад на CPU тим самим кодом вибірки,
збігається з ассетом; і **два різні тайли дають різні пікселі** — масив з
одного елемента не доводить, що індексація працює, а саме індексація тут
нова. Знімок на термінаторі: рельєф видно тінями від нахилу, і це те, заради
чого крок робиться.
**Розвилка:** якщо bindless упреться в ліміт на якійсь із трьох цілей — це не
налаштування, а зміна конструкції; тоді зупинитись і повернутись до таблиці
P0, бо саме її числа обіцяли, що впертись нема куди.

---

## R6. GPU-driven 🔴

### R6a. Дані патчів у storage-буфері 🟡

Один буфер на всі патчі, одна draw-команда. **Не** масив uniform-буферів:
D3D12 їх не дає (PROJECT.md §7), і це не «поки що», а назавжди.

**Перевірка:** кадр бітово той самий, що при малюванні патчів поодинці.

### R6b. Відбір у compute й indirect draw 🔴

Horizon і frustum переїжджають у compute-шейдер, малювання —
`draw_indexed_indirect`.

**Перевірка, заради якої R3 робився на CPU:** кількість намальованих патчів
**збігається з CPU-відбором** на тих самих камерах, для всіх восьми напрямків
із R2c. Два незалежні шляхи, одне число. Без цього помилка GPU-відбору
виглядає як «десь щось не намалювалось» і шукається очима.
**Розвилка:** 🔴 якщо збіг не досягається через різницю в арифметиці (GPU
рахує у `f32` те, що CPU рахував у `f64`) — не підганяти допуск, а звузити
твердження: збіг має бути точним на патчах, віддалених від межі відбору, і
різниця дозволена лише на самій межі. Тоді тест міряє те, що справді
гарантоване.

---

## R7. Процедурний детайл 🔴

Поза M4, всередині етапу R. PROJECT.md §7: фрактальний детайл, **зшитий з
градієнтом DEM** — не накладений поверх.

**Головне рішення ухвалюється тут і зараз, а не потім:** шум є функцією
**позиції на тілі**, ніколи не функцією кадру, часу чи стану генератора.
Інакше та сама гора виглядатиме інакше після завантаження сейву, і це
помітять — гра, у якій траєкторія відтворюється бітово, не має права
переграти рельєф.

**Перевірка:** на межі тайла деталь неперервна (та сама точка з двох сусідніх
тайлів дає ту саму висоту); при віддаленні деталь **зникає в мипі, а не
мерехтить** — знімок серії відстаней.
**Розвилка:** якщо зшивання з градієнтом дасть видимі «килими» на рівнинах —
амплітуда від нахилу, і це вимір, а не смак.

---

## R8. Закриття етапу 🟢

- **Перемір кадру** (скіл `perf-probe`): планети — істотна нова вартість, а
  0.109 мс і чверть кадру на CPU-прохід уже виміряні. Числа лягають у ROADMAP
  «Вимір продуктивності» **новим блоком, не поверх старого**.
- **CPU-прохід camera-relative має зникнути з профілю** — це записана обіцянка
  (`engine/src/main.rs:238`, `perf_probe.rs:63`, скіл `engine-render`). Якщо
  не зник — назвати, що лишилось і чому.
- **D7 бачиться з другого боку.** Борг говорить про мільйон вершин ламаної;
  тут з'явиться мільйон вершин рельєфу, і `Lines::upload` перестане бути
  найдорожчим у кадрі. Записати, що саме побачили. Черга самого D7 — після
  етапів U і R (рішення розробника, ROADMAP «Відкриті борги»).
- **Переоцінка боргів і питань** (CLAUDE.md, пункт 6) тим самим комітом.
- **M4 оголошується закритим** у ROADMAP.md і PROJECT.md §10, коли R1–R6
  зелені.

---

## Паралельно з етапом U

Обидва етапи правлять кадр, і це єдине справжнє перетинання.

1. **Спільний файл рівно один і він великий — `engine/src/frame.rs`.** U1b
   додає прохід egui останнім; R4a перетворює проходи на дані. Правило
   просте: **хто прийшов другим, той підлаштовується**. Якщо U1b уже сів,
   граф R4a приймає egui як звичайний прохід і доводить це тим самим
   бітовим знімком. Якщо першим сів граф — U1b оголошує прохід у ньому, а не
   поруч із ним.
2. **Обидва етапи міряють кадр.** Число часу кадру без назви етапу поруч стає
   нічиїм: у ROADMAP «Вимір продуктивності» кожен новий блок підписується,
   що саме в сцені (UI, патчі, тайли) і на якому профілі збірки.
3. **Дев'ята функція межі (`eph_body_radius`) належить тому, хто дійде
   першим** — U2a або R1c. Другий не робить її вдруге; десята
   (`eph_body_orientation`) — завжди R1c.
4. **Пушить одна сесія за раз.** Гейт — близько чверті години, CI — 17 хвилин.
   Друга перебазовується й проганяє локальний гейт **після** перебазування.

---

## Чого етап R свідомо не робить

- **Атмосфери** (Hillaire 2020) і **матеріалів PBR** — це M5. Тут є форма
  й висота, не повітря й не поверхневі властивості.
- **Океану, рослинності, хмар** — там же, і кожне вартує власного кроку.
- **Колізії з рельєфом і посадки.** Це фізика, а етап R не рухає жодного
  біта (правило 4). Посадка означатиме, що висота входить у модель через
  ассет — окреме рішення з власним оракулом.
- **Кольорових мозаїк високої якості.** Спочатку геометрія: колір, накладений
  на неправильну форму, виглядає правдоподібно, і це найгірше поєднання.
- **Кількох планетних систем.** Тайли Місяця й Землі — цього досить, щоб
  довести конструкцію; решта тіл — це дані, не код.

---

## Борги, яких торкається етап

Своїх боргів етап R не заводить, доки не з'явиться робота, якої ніхто не
зробив. Наявні, які він бачить:

- **D7 (історія росте без межі)** — R8 називає, що саме побачили; закриття
  боргу стоїть у черзі після етапів U і R.
- **D8 (обертання тіла читається двічі)** — R1c виносить орієнтацію на межу
  й тому проходить поруч, але не чіпає: D8 живе всередині `accel_field`, а
  рендер читає орієнтацію окремим викликом і в кадрі, а не в циклі.
