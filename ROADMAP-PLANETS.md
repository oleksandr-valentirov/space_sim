# ROADMAP-PLANETS.md — планети (етап R)

> Послідовність робіт над планетарним рендером — те, що закриває **M4**.
> Рішення проєкту — [PROJECT.md](PROJECT.md), решта робіт — [ROADMAP.md](ROADMAP.md),
> інтерфейс — [ROADMAP-UI.md](ROADMAP-UI.md), питання до розробника —
> [QUESTIONS.md](QUESTIONS.md). Дат немає свідомо, як і скрізь тут.
>
> Файл живе за тими самими правилами, що ROADMAP.md: тримає **рішення й
> виміряні межі, що діють далі**, а не звіт про зроблене. Історія кроків
> лишається в git.

---

## Чому окремий файл і чому «R»

Окремий — з тієї ж причини, що й [ROADMAP-UI.md](ROADMAP-UI.md), тільки
дзеркальної. Етап U не рухає жодного біта траєкторії й тому може йти
паралельно з ядром. Етап R теж не рухає жодного біта — але він **переписує
кадр зсередини**: `frame.rs`, глибину, шейдери, і вперше в проєкті заводить
ассет-пайплайн для текстур. Це найбільший обсяг правок в одному крейті за всю
історію репозиторію, і тримати його в ROADMAP.md означало б, що дві паралельні
сесії правлять один розділ. Так ROADMAP.md має рівно один рядок-покажчик сюди.

**Літера R, а не M4.** `M*` — це мілстоуни з PROJECT.md §10; етап — це робота,
що мілстоун закриває. A–L зайняті послідовно, `U` — інтерфейсом, `P` —
розвідками (P0, P1), `D` — боргами, `Q` — питаннями. Лишається `R` (render),
і нумерація `R*` **стала**: номери не перевикористовуються.

---

## Що виміряно перед плануванням

Не припущення. Кожен рядок або прочитано в коді, або взято з уже виміряного
в ROADMAP.md.

⚠ **Це знімок стану до етапу, і читати його треба саме так.** Половина рядків
описує світ, якого вже немає: проходів у кадрі стільки, скільки просить
розмах сцени (R4a); текстури в рушії є, і йдуть вони bindless-масивом (R5c);
DEM у репозиторії є (`data/lola`); кукер на Rust є (`tools/dem-cook`).
Знімок лишається, бо з нього виведені три висновки нижче, і без нього вони
висіли б у повітрі.

**А один рядок виявився хибним, і це важливіше за решту.** «Скільки порядків
треба планеті — 8.6» стояло підставою для чотирьох діапазонів глибини. R4b
виміряв, що **жодна кількість діапазонів цих 8.6 не дає** (роздільність
однакова в усіх варіантах), і що потреби такої немає: метр на 4.05·10⁸ м —
це 3·10⁻⁶ пікселя. Тобто число правильне, а висновок із нього — ні. Питання
до розробника — Q2 у [QUESTIONS.md](QUESTIONS.md).

| Питання | Відповідь | Звідки |
|---|---|---|
| Скільки проходів у кадрі | **Один** (плюс egui з U1b). Порядок прописаний у `Frame::draw`, **frame graph немає жодного** | `engine/src/frame.rs` |
| Скільки текстур у рушії | **Нуль.** Шейдери не мають ні текстури, ні семплера; єдиний `png` у крейті — це запис знімка | `engine/shaders/`, `engine/Cargo.toml` |
| Скільки порядків тримає один буфер глибини | **~7** (`Δz ≈ z·6·10⁻⁸`, `near` скорочується) | ROADMAP «Рендер», F3 |
| Скільки порядків треба планеті | 1 м деталі поверхні при Місяці в кадрі на 4.05·10⁸ м — **8.6 порядків** | арифметика з рядка вище |
| Чи є DEM у репозиторії | **Немає жодного.** Прецедент вендорингу даних — `data/grail` (140 КБ + README про походження) | `data/` |
| Чи є кукер ассетів на Rust | **Ні.** Єдиний кукер — на C (`make cook`, ефемерида); `/tools` містить лише зонди | `Makefile`, `tools/` |
| Ліміти bindless | семплерів **1024 на Metal**, 2048 на D3D12; **D3D12 не дає масивів uniform-буферів** | P0, PROJECT.md §7 |
| Що біля Місяця коштує поле | **4.1 мкс** на `accel_field` проти 0.73 на точкових масах (GRAIL до ступеня 24) | ROADMAP «Вимір продуктивності» |

Три висновки, які вже змінили план:

- **Frame graph не може бути першим кроком.** Сьогодні прохід один, і граф над
  одним проходом — це рівно та «структура, жодне поле якої не читається»,
  яку CLAUDE.md називає гіршою за свою відсутність. Але й відкладати його
  нікуди: тригер названий числом — 8.6 порядків проти 7, — і спрацьовує він
  тоді, коли в кадрі водночас поверхня й друге тіло. Тобто граф іде **після**
  геометрії й **перед** тим, як на цю геометрію дивляться зблизька: R4.
- **Bindless не можна відкласти «на потім».** Перша текстура в рушії з'явиться
  саме тут — сьогодні їх нуль, — а PROJECT.md §7 прямо забороняє «спочатку
  класично, потім перепишу». Отже перший тайл DEM приїжджає вже крізь масив
  (R5c), і масив з одного елемента нічого не доводить: перевірка вимагає двох.
- **Відбір на CPU — не чернетка, а майбутній оракул.** R3 робить horizon
  culling на CPU не тому, що так простіше, а тому, що R6 порівнює з ним
  кількість намальованих патчів. Пропустити R3 означало б, що GPU-відбір
  нема з чим звіряти, і його помилка виглядатиме як «десь щось не намалювалось».

### Що приносить із собою повна модель сил

- **Ціна поля біля Землі й біля Місяця відрізняється на порядок** (0.75 проти
  4.1 мкс). Звичку «гармоніки майже безкоштовні» сюди переносити не можна:
  якщо R десь рахуватиме траєкторії пачками — прев'ю спуску, вибір місця
  посадки, — це вже інша ціна.
- **`FieldCtx` ніхто не збирає руками:** є `field_all_bodies`,
  `field_all_but`, `field_exclude`, а заповнення полів напряму — це UB, яке
  живе доти, доки структура не виросте (ROADMAP «Детермінізм і збірка»).
- **D8 стане помітнішим саме тут:** апарат біля тіла з обертанням висить
  довго, а обертання читається двічі за одне обчислення поля.

---

## Принцип упорядкування

Той самий, що в ROADMAP.md: **за спаданням імовірності успіху**, з винятком
для дешевих розвідок. Кожен крок має **перевірку** (як дізнатися, що зроблено)
і **розвилку** (що робити, якщо не вийшло). Позначки ризику ті самі:
🟢 низький, 🟡 середній, 🔴 високий.

Дрібний крок — один коміт із власним оракулом. Великий крок закривається
повним локальним гейтом і пушем (CLAUDE.md, «Порядок роботи»).

---

## Правила, що діють на весь етап

Інваріанти планетарного рендера. Кожен виведений з уже ухваленого рішення,
а не вигаданий тут; порушення кожного — помилка архітектури, не компроміс.

1. **Патч — одиниця всього.** Рівно та роль, яку в фізиці має ланка: позиція
   патча живе в `f64` на CPU, вершини — `f32` **відносно початку патча**,
   і на патч же вішається все інше — вибір рівня, відбір, відвантаження на
   GPU, тайл DEM. Наслідок, який видно одразу: per-vertex camera-relative
   зникає, а `--perf-probe` міряє цей прохід окремим числом саме заради
   цього дня (ROADMAP «Вимір продуктивності», `engine/src/perf_probe.rs:63`).

2. **Похибка вершини — стала частка розміру патча, а не відстані до камери.**
   `f32` дає 6·10⁻⁸ відносних, тож зсув усередині патча похибний на
   `6·10⁻⁸ · d_patch`, і на екрані це ніколи не більше за ту саму частку
   кутового розміру патча. Форма твердження навмисно та сама, що у
   виміряного `Δz ≈ z·6·10⁻⁸`: обидва кажуть, що абсолютні метри тут не
   аргумент.

3. **Рушій і далі не знає про гру.** Планета в `Scene` — це дані (центр у
   `f64`, радіус, орієнтація, ідентифікатор набору тайлів), а не «Земля» й не
   «Місяць». Те саме рішення, що вже записане в `scene.rs`, тільки тепер
   список тіл нарешті має читача — і саме тому він з'являється.

4. **Жоден біт траєкторії не рухається.** Рельєф — це рендер. У силову модель
   висота з DEM не входить, і не увійде побічно: якщо колись знадобиться
   (посадка), вона прийде **через ассет** тим самим бітовим шляхом на всіх
   платформах, і це буде окреме рішення з власним оракулом, а не наслідок
   того, що тайл уже лежить у пам'яті. І воно впреться в уже відоме: під
   опорною сферою ряд гармонік **розбіжний**, тож `harmonics.c` тримає там
   `(Re/r)` не вище одиниці (K5e). Це свідома заглушка, а не «майже точно»,
   і рельєф, що заходить під опорний радіус, силовій моделі нічого чесного
   не скаже. Для рендера це байдуже; для посадки — ні. Заборона `libm` в циклі інтегрування
   (CLAUDE.md, інваріант 3) етапу R не стосується взагалі: генерація сітки
   й кукер — це офлайн і CPU поза інтегратором.

5. **Тріщина між патчами — топологічна помилка, і ловиться на CPU.** Спільна
   вершина двох сусідніх патчів мусить рахуватися **однією формулою** й
   збігатися бітово. Пікселі про тріщину кажуть надто пізно й надто тихо: на
   знімку вона — одна темна лінія в один піксель, яку око пропустить, а тест
   на рівність — ні.

6. **Кожна текстура — крізь bindless-масив, з першої.** Семплери індексуються
   **окремо** від текстур (1024 на Metal — найменше з трьох чисел), per-object
   дані лежать у storage-буфері, ніколи в масиві uniform-буферів (D3D12 їх не
   дає). PROJECT.md §7, розвідка P0.

7. **Перевірка — знімком без вікна плюс числом.** «Кадр не знає про вікно»
   діє тут дослівно, і причина та сама, з якої існує `engine::shot`: планета
   з тріщинами й планета без них виглядають однаково, поки не подивишся. Але
   знімок — друга половина перевірки, не перша: перша завжди число або
   рівність на CPU.

8. **Дані DEM приходять у репозиторій з README про походження, продукт і
   ліцензію** — прецедент `data/grail` (K5a). Повний LOLA (118 ГБ) у git не
   їде ніколи; що саме їде, вирішує R5a **числом метрів на піксель**, а не
   бажанням.

---

## Стан

| Крок | Зміст | Ризик | Стан |
|---|---|---|---|
| R1 | Кубосфера: проєкція, патч як одиниця, планета в сцені | 🟢 | ✅ R1a–R1e |
| R2 | LOD без тріщин: вибір рівня, зшивання, знімки | 🟡 | ✅ R2a–R2c |
| R3 | Horizon culling на CPU (майбутній оракул для R6) | 🟢 | ✅ R3a, R3b |
| R4 | Frame graph і чотири діапазони глибини | 🟡 | ✅ R4a, R4b |
| R5 | DEM: дані, кукер, тайли крізь bindless | 🟡 | ✅ R5a–R5c |
| R6 | GPU-driven: storage-буфер патчів, відбір у compute, indirect | 🔴 | ✅ R6a, R6b |
| R7 | Процедурний детайл, зшитий з градієнтом DEM | 🔴 | 🟡 R7a ✅, лишились R7b, R7c |
| R8 | Закриття: перемір, борги, M4 | 🟢 | ✅ |

**M4 закривають R1–R6, і вони закриті** — мілстоун оголошений у PROJECT.md
§10. R7 — етап R, але не мілстоун: PROJECT.md §10 називає в M4 «кубосфера
LOD, DEM-тайли, horizon culling, frame graph», і процедурний детайл туди не
входить. Отже етап R закриває один крок, і він лишається попереду.

---

## Рішення R1–R6, що діють далі

Хід кроків лишається в git; тут — те, що зобов'язує наступний крок.

- **Куб на сферу — тангенційний варп, не наївна нормалізація.** Відношення
  найдовшого ребра до найкоротшого на сітці 32×32: **2.0748 наївно проти
  1.4136 з варпом**. Шов закритий побудовою, і це три окремі рішення: одна
  таблиця параметрів (`cubesphere::parameter`) на всі грані, рівні й патчі;
  дзеркало **відніманням**, а не другим викликом `tan`; кінці таблиці точно
  ±1, бо `tan(π/4) = 0.99999999999999989` (7 мм на радіусі Землі).
  372 вершини спільні для двох граней, 8 — для **трьох**; саме кути куба
  ламають наївне зшивання.
- **Перестановка осей `u`/`v` в одній грані шва не розриває** — транспонована
  грань дає ту саму множину вершин. Ловить її лише перевірка з патчами
  (відповідність `(i, j)` між сусідами), тобто R2b.
- **Патч: початок у `f64`, зсуви від центральної вершини у `f32`.** Зсуви від
  центра вдвічі коротші за зсуви від кута, а `f32` тримає сталу **відносну**
  похибку. Правило 2 етапу підтверджене як закон: частка розміру патча стоїть
  на 1.96–2.44·10⁻⁸ при зміні розміру в тисячу разів. Розбіжність із
  пер-вершинним шляхом міряється **кутом**, не метрами: 1.15·10⁻⁷ рад на 10 м
  і 6.82·10⁻¹² на 4.05·10⁸ м — відстані немає в рівнянні.
- **Сітка рівня `L+1` містить сітку рівня `L` у своїх парних вузлах, бітово.**
  На цьому стоятиме зшивання рівнів (R2b).
- **Порядок в обчисленні початку патча і є суть:** `центр + R·(q·s) − око`,
  усе у `f64`. Множення на радіус **після** віднімання камери повернуло б
  катастрофу скорочення. `f32`-матриця моделі безпечна саме тому, що масштаб
  виносить множник, а не додає до великого числа мале.
- **Геометрія патча — одинична сфера, спільна для всіх тіл**; радіус і поворот
  тіла їдуть матрицею моделі, виклик малювання — на тіло. Початки патчів — у
  **storage-буфері**, не в масиві uniform-буферів (D3D12 їх не дає).
- **Ближня площина міряється від найближчого тіла сцени.** Камера за 100 км
  над Місяцем, яка міряє висоту від Землі, отримала б near у мільйони метрів,
  тобто кадр без Місяця.
- **Ціна, заради якої все:** CPU-прохід планети 26.7 мкс (UV-сфера, 8385
  вершин щокадру) → **0.027 мкс** на тіло (шість початків із поворотом).
  Силует проти UV-сфери — 217884 пікселі диска, розбіжність 28.
- **Поворот гладкої сфери показати не можна**, і це властивість, а не вада:
  силует і нормаль переходять самі в себе. Перевіряється те, що варте
  перевірки — поворот застосовано **однаково** до геометрії й до нормалей;
  контроль (зсув центра на радіус) стоїть поруч, бо «нічого не змінилося» без
  нього означало б сліпе порівняння. Очима поворот стане видно з R5.
- **Збоку сфера проєктується в еліпс** — за 41° від осі диск Землі виміряний
  як 35×26 пікселів. Кругла формула там міряє не ту фігуру; радіус звіряється
  з `asin(R/d)` лише в центрі кадру.
- **Рівень LOD обирається за екранною похибкою, не за відстанню:** відстань не
  знає ні про поле зору, ні про роздільність, ні про радіус тіла. Стріла
  прогину — `1 − |середина хорди|` на одиничній сфері, без тригонометрії;
  радіус — множник наприкінці. Найгірша клітинка лежить біля центральної
  лінії грані, тож обхід — 64 клітинки на патч замість 1024 (перебором
  підтверджено).
- **Відстань до патча міряється його конусом, і п'ять вузлів тут не
  наближення, а вада** (перегляд R2a, борг D13, 2026-08-15). Мінімум по
  чотирьох кутах і центру правильний рівно над **центром грані** — і саме
  туди його ставила вся фікстура вибору рівня, тож вада прожила від R2a до
  R7a невидимою. За двадцять градусів убік вибірка казала 758 км там, де під
  ногами 170, і грань не ділилась узагалі. Тепер відстань іде до сферичної
  шапки, у яку патч вписаний (`Patch::cone`) — того самого конуса, що й у
  `cull::beyond_limb`: шапка більша за патч, тож помилитись вибір може лише
  в бік зайвого поділу. Око над шапкою — окрема гілка `d − R`, бо теорема
  косинусів там скорочує дванадцять цифр на висоті метра.
- **Здалеку планета — рівно шість граней** (допуск 1 px, поле зору 60°), на
  300 км — 9 патчів; набір міняється через одне подвоєння роздільності, і це
  арифметика, а не поріг: рівень коштує вчетверо меншу похибку, подвоєння
  кадру купує вдвічі більшу. Похибка падає рівно вчетверо на рівень (4.000 на
  семи рівнях) — але лише якщо порівнювати **ту саму ділянку** поверхні.
- **Стеля `MAX_LEVEL = 12` повертає `Selection::clamped` окремим полем:**
  стеля, що ріже якість мовчки, — та сама помилка, що й її відсутність.
- **Сусідство через ребро куба виводиться з `AXES`, а не з таблиці на 24
  рядки.** Вийти за грань через `a == +1` означає, що слот `u` дорівнює `+1`;
  сусідня грань — та, у якої цей слот нерухомий із тим самим знаком. У `AXES`
  знак має лише нерухома вісь, тож **індекс уздовж спільного ребра не
  перевертається ніколи**: вузол `k` з одного боку — вузол `k` з другого.
- **Зшивання — одна підміна індексу, а не шістнадцять тріангуляцій.** На
  ребрі з грубішим сусідом непарний вузол не існує, і скрізь, де він
  згаданий, стоїть попередній парний. Край стає точно хордою сусіда, між
  бітово тими самими вершинами; кути патча не є окремим випадком **за
  парністю** (непарний вузол лежить строго всередині свого ребра). Ціна —
  `SIDE / 2` вироджених трикутників на зшите ребро з 2048.
- **Вирівнювання рівнів тільки подрібнює.** Огрубити дрібний бік означало б
  мовчки віддати якість, яку попросив критерій. Виміряно: 12 патчів з 33 на
  100 км, 0 на 300 км і вище.
- **Оракул зшивання — топологія, і він у формі, що не знає про рівні:** кожне
  неорієнтоване ребро сітки належить рівно двом невиродженим трикутникам, і
  `V − E + F = 2`. Виміряно: 33 патчі / 67200 трикутників / 384 вироджених на
  100 км, 6 / 12288 / 0 на 2000 км, жодного самотнього ребра.
- **Знімок тріщини між рівнями не бачить, і це число:** незшитий стик — це
  **0.060 пікселя**, бо саме цю величину критерій тримає під допуском в один
  піксель. Вісім знімків з кутів куба лишаються сторожем проти грубого
  промаху — і рівно проти одного: **видимого патча, якого немає в кадрі**
  (23310 пікселів неба з 65536). До R3a вони не бачили й цього: дірка
  показувала дальній бік тієї самої планети.
- **Геометрія патча кешується за патчем**, бо не залежить ні від камери, ні
  від тіла. Місткість кеша — вдвічі більша за потребу кадру, витіснення —
  курсором по колу: при такому запасі слот, не потрібний цього кадру,
  знаходиться завжди, і в пошуку немає гілки «а якщо ні».
- **Відбір іде після вибору рівня, а не всередині нього.** Маска зшивання
  рахується з набору; викинутий під час обходу сусід став би «дрібнішим
  боком», і тріщина з'явилась би рівно на лімбі — на силуеті. Втрачається
  при цьому мало: дальня півсфера за ~2R і так дістає найгрубіші рівні.
- **Критерій лімба — без тригонометрії в кадрі:** `cos(β − α) =
  cos β cos α + sin β sin α` для конуса патча. Конус береться по чотирьох
  кутах, і перебір усіх вузлів дає запас **рівно нуль** — кут не «близько» до
  межі, він і є межа.
- **Два радіуси тіла, а не один.** `occluder_radius_m` стоїть поруч із
  `radius_m` уже сьогодні, хоч вони й рівні: перший каже, де вершини, другий
  — що затуляє погляд. З тайлами другий стане найнижчою точкою тайла, і
  жодна формула не зміниться.
- **Horizon важливіший за frustum — тепер числом.** Лімб прибирає 88% патчів
  на 100 км, 56% на 300 км, 33% на 2000 км, 17% з відстані до Місяця;
  frustum понад це додає **0, 0, 1 і 0** патчів (надир і погляд уздовж лімба).
  Він почне заробляти, коли DEM підніме кількість патчів.
- **Поворот тіла входить і у вибір рівня, і у відбір.** Конус патча живе в
  системі тіла, камера — у системі світу; поки орієнтація одинична, різниці
  немає, і саме тому помилка цього класу не знаходиться тоді, коли її
  зробили. Око переводиться в систему тіла **раз на тіло**, транспонованою
  матрицею. Сторож — симетрія: поворот тіла й камери на той самий кут дає той
  самий набір поштучно.

---

- **Прохід кадру — список структур, а не граф.** `wgpu` розставляє бар'єри
  сам, тож від класичного графа лишається порядок, а порядок — це список.
  Проєкція своя на прохід (динамічний зсув у одному буфері, крок 256),
  глибина очищується завжди, колір — лише в першому.
- **Діапазони глибини не купують роздільності. Виміряно.** Найменший
  роздільний зазор однаковий для нескінченної проєкції й для скінченного
  діапазону з будь-якого його кінця: 0.5 м на 10⁷ м, 4.0 м на 10⁸ м, 16 м на
  4·10⁸ м. Причина — катастрофічне скорочення в `z_clip` біля далекої
  площини. Те саме стосується межі між проходами. Виправдані вони **scaled
  space**, а не точністю, і це питання Q2 до розробника.
- **Кількість проходів виводиться з розмаху сцени:** один діапазон на сім
  порядків (межа, виміряна на F3), не більше чотирьох. Сцена зондів дає один
  прохід, і кадр лишився бітово тим самим від R4a до R6b — sha256
  `30812bf2…` через чотири перезаписи того, як кадр видається.
- **Рівень тайлів вирішує число, а не бажання:** LDEM_4 дає 7581 м на відлік,
  тобто 47 екранних пікселів на відлік при камері на 100 км. Це рівень, який
  вміщається в репозиторій (2.0 МБ проти 33 МБ і 531 МБ); підняти його — це
  **дані, а не код**.
- **Тайл лежить вузол у вузол із сіткою патча.** Звідси головне: рельєф не
  додає тріщин узагалі. Вершина на спільному ребрі бітово одна (R2b), отже
  напрямок вибірки один, отже висота одна — виміряно 0.000000 м на межі
  тайлів. Зшивання рівнів не потребувало жодної правки.
- **Висоти — `int16` без стиснення**, у тих самих пів метра, що й джерело.
  Квантування насичує, а не загортає: загорнута гора на 33 км стала б
  правдоподібною западиною.
- **Перша текстура в рушії пішла крізь bindless-масив** (правило 6), і три
  речі довелося з'ясувати замість припустити: `naga` не приймає Slang-ового
  `array<texture_2d<…>>` (потрібні `binding_array` і `enable`), `wgpu` не
  дозволяє масиву й буферу з динамічним зсувом жити в одній групі, а обидва
  пайплайни ділять макет — тож група масиву мусить бути прив'язана й тоді,
  коли шейдер до неї не звертається.
- **Рельєф має власну нормаль, і аналітичної в нього немає:** поверхня це
  набір фасеток розміром із клітинку DEM, тож нормаль береться з похідних
  світової позиції. Саме це показує рельєф тінями — виміряно на термінаторі
  39191 повної варіації яскравості проти 4041 у гладкої сфери.
- **Вершинних буферів у планети немає.** Геометрія, початки й список
  малювання — storage-буфери, номер вершини й інстансу дає конвеєр. Причина
  не в моді на GPU-driven: зшивання це підміна **індексу** вузла, і поки
  вершина приходила вибраною, підміну доводилось робити шістнадцятьма
  індексними наборами й викликом на патч.
- **Відбір живе у compute, кількість — у `draw_indirect`.** Оракул, заради
  якого R3 робився на CPU: на 24 камерах (вісім кутів куба × три висоти) GPU
  намалював рівно стільки патчів, скільки лишив CPU. Точно, без допуску.
- **Порядок вижилих недетермінований** — атомарна компактація. Для кадру це
  байдуже (непрозорі патчі, глибина), для звірки порівнюється кількість.
  Записано вголос, бо в цьому проєкті недетермінізм зазвичай помилка.

---

## R7. Процедурний детайл 🟡

**Єдиний крок етапу, що лишився.** Поза M4 (PROJECT.md §10 його там не
називає), всередині етапу R. Зроблено R7a (передумова 1); лишились R7b
(ореол) і R7c (сам шум).

**Навіщо він потрібен, тепер видно очима, а не з викладки.** Після D13 гра
малює Місяць до 200 метрів висоти включно — і на останніх кілометрах видно,
що поверхня стає **гладкою**: дані LOLA скінченні, а патчі ні. Інтерполяція
між вузлами тайла — це та сама поверхня дрібнішою сіткою, нових висот вона
не додає. Саме цю порожнечу й має заповнити R7c.

PROJECT.md §7: фрактальний детайл, **зшитий з градієнтом DEM** — не
накладений поверх.

**Головне рішення ухвалюється тут і зараз, а не потім:** шум є функцією
**позиції на тілі**, ніколи не функцією кадру, часу чи стану генератора.
Інакше та сама гора виглядатиме інакше після завантаження сейву, і це
помітять — гра, у якій траєкторія відтворюється бітово, не має права
переграти рельєф.

### Дві передумови, знайдені в R5c і R6, і жодної з них план не мав

Обидві виявились під час роботи, обидві коштують окремої правки, і саме тому
крок не почато, а не зроблено наполовину.

1. ✅ **Адресація підпрямокутника чужого тайла (R7a, коміт `60a59b3`).**
   До цього кроку LOD не спускався глибше за піраміду тайлів
   (`Terrain::levels − 1`), бо патч глибшого рівня читав би тайл предка за
   **своїми** локальними координатами, тобто не в тому місці. Стелю знято:
   `Terrain::window` виділено з `Terrain::height_m` (одна формула, шейдер —
   її дослівний двійник), `PatchData` виріс з 16 байтів до 32 і несе зсув та
   масштаб вікна, `patch.slang` вибирає білінійно. Три юніт-тести на
   арифметику; спільне ребро сходиться бітово і всередині одного предка, і
   через межу між предками.

   ⚠ **GPU-половину оракула тоді написати не вдалось** — рівень завжди
   виходив нульовим, тож патчі, глибші за піраміду, не виникали взагалі. Це
   був борг D13, і його закрито (`74f7101`): рівень тепер доходить до 6 на
   висоті кілометра, і перевірку в кадрі можна дописати. **Це перше, що має
   зробити R7b.**

2. 🔴 **Ореол в один вузол по краю тайла.** Амплітуда шуму мусить іти від
   градієнта DEM, а градієнт у вузлі на межі тайла потребує сусіда **з
   іншого тайла**. Затиснути його — значить дістати на двох боках межі різні
   амплітуди, тобто тріщину рівно там, де R2b її прибрав. Правильна
   відповідь — зберігати в тайлі ореол шириною в один вузол; на ребрі куба
   він лежить на сусідній грані, і брати його треба через
   `Patch::neighbour` (R2b), а не затисканням індексу. Це версія 2 формату
   `engine::tiles` і +13% розміру тайла.

**Ще одне рішення, яке варто ухвалити наперед: чим гасити октаву.** Затухання
не може залежати від **рівня патча** — сусіди різняться на рівень, і на
спільному ребрі вони дали б різні висоти. Воно мусить залежати від того, що
обидва боки рахують однаково: **позиції вузла й камери**. Тоді спільний вузол
дає бітово те саме число з обох боків, а деталь гасне з відстанню — тобто
рівно те, чого вимагає перевірка нижче.

**Перевірка:** на межі тайла деталь неперервна (та сама точка з двох сусідніх
тайлів дає ту саму висоту, **бітово**, як у R2b); при віддаленні деталь
зникає, а не мерехтить — знімок серії відстаней.
**Розвилка:** якщо зшивання з градієнтом дасть видимі «килими» на рівнинах —
амплітуда від нахилу, і це вимір, а не смак.

---

## R8. Що показало закриття

Крок зроблено; тут лишається те, що він з'ясував, а не звіт про нього.

- **Кадр подорожчав удвічі там, де сцена не змінилась**: 0.064 → 0.153 мс на
  10⁷ м, при тому що патчів там стало п'ять проти шести. Платить не
  геометрія, а прохід compute плюс вибір рівня на CPU — тобто ціна R2–R6
  однакова на всіх висотах. З низької орбіти кадр коштує 0.25–0.36 мс, у 46
  разів менше за бюджет 60 Hz на 1080p. Числа — ROADMAP.md, «Вимір
  продуктивності».
- **CPU-прохід camera-relative не повернувся**: 0.027 мкс на тіло, те саме
  число, що на R1e. LOD і тайли не принесли per-vertex роботи іншим шляхом —
  саме це й треба було перевірити.
- **`--perf-probe` тепер міряє дві висоти**, і це не косметика: з появою LOD
  один рядок перестав описувати кадр, бо набір патчів залежить від камери.
- **D7 з другого боку.** Борг говорить про мільйон вершин ламаної; рельєф
  мільйона вершин у кадр **не приніс**. Патч коштує 1089 вершин, а відбір
  лишає одиниці й десятки патчів: 33 патчі на 100 км — це 36 тисяч вершин,
  тобто на два порядки менше за ламану, про яку йдеться в D7. Причина не в
  щедрості, а в критерії: рівень вибирається за екранною похибкою, тож
  кількість вершин обмежена **розміром кадру**, а не тривалістю гри. Ламана
  такої межі не має — і в цьому вся різниця між нею й рельєфом. Отже
  `Lines::upload` лишається найдорожчим у кадрі, і черга D7 не змінилась.
- **Новий борг етап не завів.** Те, що лишилось незробленим, — це крок R7, а
  не борг: він стоїть у плані з власними передумовами й перевіркою.

  **Уточнення, зроблене пізніше (D12):** етап справді не завів боргу в
  рушії — але й не під'єднав зроблене до гри. `game/src/view.rs` ставить
  `TileSet::Smooth` усім тілам, тож рельєф видно в демо й у тестах, а в грі
  ні. Помітили це не тут, а на U8, коли шукали, чим насправді зайнятий кадр.
- **Питання Q2** (чи лишається вимога чотирьох діапазонів глибини) відкрите й
  нікого не блокує.

---

## Паралельно з етапом U

Обидва етапи правлять кадр, і це єдине справжнє перетинання.

1. **Спільний файл рівно один і він великий — `engine/src/frame.rs`.** Прохід
   egui уже сидить у ньому останнім, отже граф R4a приймає його як звичайний
   прохід і доводить це тим самим бітовим знімком.
2. **Обидва етапи міряють кадр.** Число часу кадру без назви етапу поруч стає
   нічиїм: у ROADMAP «Вимір продуктивності» кожен новий блок підписується,
   що саме в сцені (UI, патчі, тайли) і на якому профілі збірки.
3. **Пушить одна сесія за раз.** Гейт — близько чверті години, CI — 17 хвилин.
   Друга перебазовується й проганяє локальний гейт **після** перебазування.

---

## Чого етап R свідомо не робить

- **Атмосфери** (Hillaire 2020) і **матеріалів PBR** — це M5. Тут є форма
  й висота, не повітря й не поверхневі властивості.
- **Океану, рослинності, хмар** — там же, і кожне вартує власного кроку.
- **Колізії з рельєфом і посадки.** Це фізика, а етап R не рухає жодного
  біта (правило 4). Посадка означатиме, що висота входить у модель через
  ассет — окреме рішення з власним оракулом.
- **Кольорових мозаїк високої якості.** Спочатку геометрія: колір, накладений
  на неправильну форму, виглядає правдоподібно, і це найгірше поєднання.
- **Кількох планетних систем.** Тайли Місяця й Землі — цього досить, щоб
  довести конструкцію; решта тіл — це дані, не код.

---

## Борги, яких торкається етап

Своїх боргів етап R не заводить, доки не з'явиться робота, якої ніхто не
зробив. Наявні, які він бачить:

- **D7 (історія росте без межі)** — R8 називає, що саме побачили; закриття
  боргу стоїть у черзі після етапів U і R.
- **D8 (обертання тіла читається двічі)** — R1c виносить орієнтацію на межу
  й тому проходить поруч, але не чіпає: D8 живе всередині `accel_field`, а
  рендер читає орієнтацію окремим викликом і в кадрі, а не в циклі.
