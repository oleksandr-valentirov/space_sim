//! Аналог `make flags` на боці cargo.
//!
//! Існує рівно заради розвилки з ROADMAP D1: якщо хеші розійшлися, перше, що
//! треба зробити, — покласти поруч дві командні строки. Без цього довелося б
//! вигрібати їх із `cargo build -vv`, а це чотири екрани логу, у яких
//! потрібен один рядок.
//!
//!     make flags
//!     cargo run -q --example flags
//!
//! Виводить те, що `build.rs` фактично прочитав із `core/cflags.txt`. Повний
//! виклик компілятора, з тим, що `cc` домішує від себе, — окремою командою:
//!
//!     CC_ENABLE_DEBUG_OUTPUT=1 cargo build -vv

fn main() {
    println!("{}", env!("CORE_CFLAGS"));
}
