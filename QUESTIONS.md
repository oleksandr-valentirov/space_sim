# Невирішені питання

Питання, на які може відповісти лише розробник, і які **не спинили роботу**.
Порядок роботи, частиною якого є цей файл, — [CLAUDE.md](CLAUDE.md),
«Порядок роботи», пункт 5.

Правила короткі:

- **Нумерація стала** (Q1, Q2, …), номери не перевикористовуються: коміти на
  них посилаються, як на борги `D*` у [ROADMAP.md](ROADMAP.md). Закрите
  питання звідси зникає, а рішення переїжджає у ROADMAP «Рішення, що діють
  далі» або в PROJECT.md — тут не лишається закреслених рядків.
- **Кожне питання несе припущення, під яким робота пішла далі**, і те, що
  зміниться від іншої відповіді. Питання без припущення означає зупинену
  роботу, і таких має бути якнайменше — це і є сенс файлу.
- Питання, яке блокує конкретний крок ROADMAP, **називає цей крок**. Перед
  початком кроку перевіряється саме це.

---

## Q1. Чи лишати valgrind у CI на кожен пуш?

**Контекст.** Крок `make valgrind` (K7b) уперше спіймав клас помилок, якого
ASan не бачить взагалі — читання неініціалізованої пам'яті. Ціна виміряна:
джоб `linux-gcc` виріс з 8м45с до 17м10с і став найдовшим у гейті вдвічі
(решта платформ — 2–5 хв).

**Припущення, під яким робота триває:** лишаємо на кожен пуш. Пуш
відбувається раз на великий крок, а не раз на коміт, тож ці 17 хвилин
трапляються в кілька разів рідше, ніж коли крок додавався.

**Що зміниться від іншої відповіді.** Valgrind переїде на розклад (раз на
добу) або на зміни в `/core`. Тоді треба буде вирішити окремо, що робить
пуш, який чіпає лише Rust: зараз він теж проганяє C-тести під valgrind, і це
не безглуздо — `core-sys/build.rs` збирає те саме ядро.

**Кроків не блокує.**

---

## Q2. Чи лишається вимога чотирьох діапазонів глибини (PROJECT.md §7)?

**Контекст.** R4b реалізував діапазони й **виміряв, що вони не купують
роздільності**. Найменший роздільний зазор на відстані `z` однаковий для
нескінченної проєкції й для скінченного діапазону з будь-якого його кінця:

```text
    z         нескінченний   z біля far   z біля near
    10⁷ м        0.5000        0.5000        0.5000
    10⁸ м        4.0000        4.0000        4.0000
    4·10⁸ м     16.0000       16.0000       16.0000
```

Викладка обіцяла інше — `Δz ≈ z·2⁻²⁴·(1 − z/far)`, тобто ідеальну
роздільність біля далекої площини. З'їдає обіцянку арифметика `f32`:
`z_clip = A·z_view + B` біля далекої площини скорочується катастрофічно.
Те саме стосується межі між проходами: площина відсікання стоїть на тій
самій арифметиці, тож нею не розділити поверхонь ближче, ніж `z·6·10⁻⁸`.

Отже рядок таблиці «скільки порядків треба планеті — 8.6» більше не є
підставою для діапазонів: жодна їх кількість цих 8.6 не дає. Причому й
потреби такої немає — метр на 4·10⁸ м це 3·10⁻⁶ пікселя.

**Припущення, під яким робота триває:** механізм лишається, бо він потрібен
**scaled space** — праву малювати далеке на вигаданій відстані, де фальшива
геометрія зіткнулася б із справжньою. Кількість проходів при цьому не стала:
один діапазон на кожні сім порядків розмаху сцени (межа, виміряна на F3), не
більше чотирьох. Сцена зондів дає рівно один прохід, і кадр лишився бітово
тим самим.

**Що зміниться від іншої відповіді.** Якщо чотири проходи потрібні
беззастережно — `DECADES_PER_PASS` зникає, `plan` завжди дає чотири, і кадр
починає коштувати чотири перемальовки замість однієї. Якщо ж діапазони
визнати непотрібними до появи scaled space — `plan` спрощується до одного
проходу, а `reversed_finite` лишається без читача й має піти.

**Кроків не блокує.** R5 і R6 працюють з будь-якою кількістю проходів.
