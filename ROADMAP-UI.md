# ROADMAP-UI.md — інтерфейс гри (етап U)

> Послідовність робіт над інтерфейсом. Рішення проєкту — [PROJECT.md](PROJECT.md),
> решта робіт — [ROADMAP.md](ROADMAP.md), питання до розробника —
> [QUESTIONS.md](QUESTIONS.md). Дат немає свідомо, як і скрізь тут.
>
> Цей файл живе за тими самими правилами, що ROADMAP.md: тримає **рішення й
> виміряні межі, що діють далі**, а не звіт про зроблене. Історія кроків
> лишається в git.

---

## Чому окремий файл

Не тому, що ROADMAP.md завеликий. Тому що етап U — єдиний, який **не рухає
жодного біта траєкторії**, і це його головна властивість, а не побічна.
Все, що тут описано, читає снапшот і надсилає команди; жоден крок не входить
у цикл інтегрування, не міняє ассет і не зсуває хеш детермінізму. Отже етап U
можна вести паралельно з роботою в `/core`, і саме так він і вівся.

Нумерація `U*` **стала**, номери не перевикористовуються — те саме правило,
що для боргів `D*` і питань `Q*`.

---

## Принцип упорядкування

Той самий, що в ROADMAP.md: **за спаданням імовірності успіху**, з винятком
для дешевих розвідок. Кожен крок має **перевірку** (як дізнатися, що зроблено)
і **розвилку** (що робити, якщо не вийшло). Позначки ризику ті самі:
🟢 низький, 🟡 середній, 🔴 високий.

Дрібний крок — один коміт із власним оракулом. Великий крок закривається
повним локальним гейтом і пушем (CLAUDE.md, «Порядок роботи»).

---

## Правила, що діють на весь етап

Це інваріанти UI. Порушення кожного — помилка архітектури, а не компроміс,
і кожен виведений із уже прийнятого рішення, а не вигаданий тут.

1. **UI не володіє станом світу.** Він читає снапшот і надсилає команди.
   Власний стан UI — рівно те, чого не існує поза екраном: що виділено, що
   розгорнуто, куди тягнеться миша. Наслідок для перевірки: **будь-яке число
   на екрані виводиться зі снапшоту**, а не накопичується в панелі. Інакше
   два види показували б різне «зараз», і це та сама помилка, від якої
   `app::draw` бере снапшот рівно один раз за кадр.

2. **Жодне поле вводу не стає `t_end`** (CLAUDE.md, інваріант 9). Час маневру,
   введений гравцем, — це **план**, а межу ланки дає план. Але «показати ще
   тридцять діб уперед» не перетворюється на `t_end` ніколи: горизонт
   міряється ланками (`LEAD_LEGS`), і J2 виміряв, що котючий горизонт валить
   усі три перевірки частоти кадрів, не падаючи сам.

3. **UI перевіряється без вікна.** Прямий наслідок «кадр не знає про вікно»:
   `egui::Context` приймає синтетичний `RawInput`, `egui-wgpu` малює в будь-яку
   текстуру, а `engine::shot` уже вміє її зберегти. Отже клік перевіряється як
   «синтетична подія → конкретна команда в каналі», а вигляд — як PNG.
   Причина та сама, з якої існує `engine::shot`: **панель із правильними
   числами й панель із NaN виглядають однаково**, поки на них не подивитись.

4. **Ввід має власника на кожен кадр.** Якщо egui подію спожив, гра її не
   бачить. Без цього тягнення повзунка одночасно обертає камеру — помилка,
   яку інакше знаходять пальцями, а не тестом.

5. **Панель не кличе ефемериду й не пропагує.** Ні `eph_body_state` на рядок
   HUD, ні `prop_run` усередині кадру. Усе, що показано, або лежить у
   снапшоті, або прийшло з планувальника. `view.rs` уже відмовився від
   четвертого виклику ефемериди на кадр заради невидимого — тут та сама межа,
   тільки її легше порушити.

6. **Довге рахування — у нитці планувальника** (PROJECT.md §6). Porkchop —
   це 10⁴ розв'язків Ламберта; у кадрі це секунди чорного екрана. Нитка вже
   є, і в неї вже є скасування між одиницями роботи (J5).

7. **Рядки — через таблицю ключів, з першого рядка UI.** Рішення розробника:
   англійська як основна мова, шар локалізації одразу. Причина не в перекладі,
   а в тому, що розкидані по віджетах літерали доводиться потім вишукувати
   поодинці.

8. **UI не має права зробити стан гри недосяжним.** Кнопка, що надсилає
   команду, не знає, чи її приймуть: `commit_plan` може відмовити
   (`PlanRejected`), сейв може не записатися. Відповідь приходить **подією в
   каналі**, і панель показує саме її, а не власне припущення про успіх.

---

## Стан

| Крок | Зміст | Ризик | Стан |
|---|---|---|---|
| U1 | Оболонка: egui у кадрі, ввід, знімок без вікна | 🟡 | ✅ (U1a–U1c) |
| U2 | HUD: час, warp, апарат; радіус тіла на межі | 🟢 | ✅ (U2a–U2c) |
| U3 | Розклад: маркери подій і перемотування до них | 🟢 | ✅ (U3a, U3b) |
| U4 | Редактор маневрів: список, поля, тягнення вузла | 🟡 | ✅ (U4a, U4b) |
| U5 | Porkchop-плот: межа без колбеків, сітка, вибір вікна | 🟡 | ✅ (U5a–U5d) |
| U6 | Карта: обертовий фрейм і криві нульової швидкості | 🔴 | ✅ (U6a–U6c) |
| U7 | Стиль і мова: палітра, шрифт, таблиця рядків | 🟢 | ✅ (U7a–U7c) |
| U8 | Закриття: перемір кадру, борги, M3 | 🟢 | не почато |

**M3 закривають U1–U6, і вони зелені.** U7 і U8 — це етап U, але не мілстоун:
стиль і мова не входять у «Lambert, диференціальна корекція, porkchop-плот,
редактор маневрів, криві нульової швидкості».

---

## Рішення U1–U7, що діють далі

Хід кроків лишається в git; тут — те, що зобов'язує наступний віджет.

### Стиль і мова (U7)

- **Кожен рядок — через `text::tr`, кожен колір — через `palette`.** Літерал
  у віджеті проходить тест таблиці й валить тест «панель говорить обома
  мовами»; власна константа кольору поруч із палітрою тихо розійдеться з нею.
- **Акцент інтерфейсу дорівнює кольору прогнозу** (`palette::ACCENT ==
  PREDICTION`), дія — кольору прев'ю, дорогий кінець porkchop — тому самому
  бурштину. Заводити п'ятий змістовний колір означає спершу сказати, що він
  означає в **сцені**.
- **Палітра — вісім біт на канал і жодної гамми на жодному шляху.**
  `Colour::scene` ділить на 255, `Colour::egui` віддає байт як є; ціль кадру
  не sRGB, і знімок це підтверджує побайтово. Перетворення, додане «як
  правильно» на одному з двох шляхів, розведе один колір на два.
- **Панель набрана моноширинним цілком**, і це можна лише тому, що кирилиця
  в цьому сімействі своя й тієї самої ширини (виміряно, U7b).
- **Тести панелей малюють через `palette::apply`.** Панель із типовим egui
  має інші відступи й розміри кнопок, тобто тест клікав би не туди, куди
  гравець.
- **Світлої теми немає.** Стиль ставиться обом гілкам теми, і `set_theme`
  для цього недостатньо: він каже, яку тему брати, а не що в ній лежить.

### Оболонка й ввід

- **Провід тримає `engine::ui`, віджети пише `game`.** `engine` бере
  `egui`/`egui-wgpu`/`egui-winit` і **реекспортує `egui`** — двох версій не
  може виникнути за побудовою. Рушій від цього не дізнається про гру:
  `egui::Context` такий самий сторонній тип, як `wgpu::Device`.
- **Прохід egui — останній, у ту саму текстуру, `load` замість `clear`, без
  глибини.** Ціна виміряна: порожній прохід **+0.017 мс**, панель із рядком
  тексту ще **+0.006**. Отже панелі дешеві, а **прохід має лишитися одним**.
- **`RawInput` приходить ззовні** (у вікні — `egui-winit`, у тесті —
  `Viewport::quiet_input()`). Саме це робить правило 3 здійсненним.
  `Viewport` несе розмір і масштаб однією структурою: два `u32` підряд
  переставляються місцями мовчки.
- **Дизеринг вимкнено** — він додає шум у пікселі, тобто робить «бітово те
  саме» недосяжним саме там, де нічого не намальовано. І порожній контекст
  усе одно віддає текстуру (атлас шрифта): «нічого не намальовано» і «нічого
  не приїхало» — різні стани.
- **Власника вводу питають у контексту, не в `egui-winit`:**
  `Ui::wants_pointer()` = `is_using_pointer() || is_pointer_over_egui()`.
  Перша половина липка навмисно — доки кнопку не відпущено, миша належить
  інтерфейсу й за межами панелі, інакше повзунок губиться під рукою. Тест,
  що тисне кнопку щокадру, отримає `true` будь-де: наміри `Hover`/`Click`/
  `Hold` існують саме тому. Вужчий `pointer_over_panel` — для піккінгу в
  сцені.

### Панелі

- **Панель повертає команди й дії, а не надсилає їх.** Хто надсилає, той знає
  про канал; панель знає лише про те, що намалювала. Це й робить оракул
  кроку перевіркою вектора — без вікна й без GPU.
- **Кнопки й рядки мають сталі адреси** (`hud::PAUSE` і далі). Автоматичні
  `Id` ззовні не відтворити, а тест, що клікає підібраними пікселями, від
  першої зміни відступів клікає в порожнечу — і лишається зеленим. Адреса
  рядка розкладу — **номер маркера, не час**: `f64` у складі `Id` перетворив
  би найменше уточнення інтерполяції на інший віджет.
- **Числа виносяться з малювання** (`hud::VesselReadout`): оракул — «значення
  в панелі збігається з незалежно порахованим зі снапшоту», а число з
  пікселями не звіриш.
- **Висота міряється від `eph_body_radius`, і від нічого іншого.** У тіла є
  щонайменше три різні радіуси — середній з ассета, опорний радіус гармонік і
  намальована сфера; для Місяця перші два розходяться на **470 м**. Подія
  `Altitude` міряє від першого, отже HUD мусить брати те саме число.
- **Календар — властивість UI й назад у фізику не повертається.**
  Цілочисельний `civil_from_days`, без залежності; TT−UT1 тримається сталим
  (63.8286 с), тож дата точна до секунди-двох на століття, а епоха ассета —
  J2000 **TDB** (різниця з TT ≤ 1.7 мс).
- **Пауза читається зі `stall`, не з warp:** `Clock::warp()` віддає заданий
  множник і в паузі не обнуляється.
- **Таблиця рядків діє з першої панелі** (правило 7); U7a лишається за
  наповненням і кирилицею.

### Розклад і курсор

- **Подія для ока — мінімум відстані по трьох сусідніх семплах**, а не знак
  `d(r·v)`: `r·v` вимагає швидкості відносно тіла, а семпл несе лише позицію.
  Час уточнюється параболою по тих самих трьох точках, бо семпли — прийняті
  кроки інтегратора, а не сітка. Збіг з озброєною подією — **у межах 60 с**
  при кроках у тисячі секунд.
- **Розклад показує лише події попереду курсора:** рядок про вже пролетіле
  був би кнопкою, яка завжди відмовляє.
- **`SeekTo` має рівно дві відмови, обидві названі:** `Backwards` (курсор не
  ходить назад ніколи) і `NotComputedYet { computed_to }` — без другої поле
  інтерфейсу вирішувало б `t_end`, прямо проти інваріанта 9.

### План і маневри

- **`PlanAction::Preview(Plan)` і `Commit(Plan)` несуть сам план**, той самий
  об'єкт: «лінія, яку бачив, і є лінія, якою полетиш» стає буквальним
  твердженням. `PlanDraft` — власний стан UI, і саме той, який дозволяє
  правило 1.
- **Панель, якої ніхто не чіпав, не просить нічого**, і порожній план прев'ю
  не просить. Без цього оракула решта перевірок зелені й на панелі, що
  щокадру топить планувальник.
- **Відмова перевіряється по намальованому, а не по логу:** панель, яка після
  `PlanRejected` мовчки лишила старий план, не відрізняється від тієї, що
  прийняла його.
- **Тягнення вузла — ручки по осях VNB, не вільне тягнення.** Осі VNB,
  спроєктовані на екран, **не ортогональні**, тож розкладання довільного
  тягнення міняло б `prograde` при русі вздовж `normal` (виміряно: 2 м/с
  витоку). Про `Camera::to_screen`: ближня площина в неї не входить взагалі,
  точка позаду камери екранної координати **не має** (інакше піккінг хапає
  вузли з протилежного боку планети), а вісь, що дивиться в камеру,
  вироджується в точку й ручки не має.
- **Вузли будуються з уже порахованих семплів** і перераховуються наприкінці
  кадру: подія миші приходить між кадрами, і порівнювати їй треба з тим, що
  гравець бачив.

### Porkchop

- **Рахує планувальник, одиниця роботи — рядок сітки.** Сітка 100×100 крізь
  межу — 22 мс, один рядок — 0.24 мс: у кадр не влазить, але й «секунд
  чорного екрана» немає. Два види роботи йдуть **одним каналом** запитів, бо
  правило скасування має лишитися одне; відповіді — різними.
- **Скасування несе найновіший запит нагору.** Перевірка, яка виймала його з
  каналу й викидала, губила саме той, після якого гравець дивиться на екран.
- **Плот — текстура, піксель на клітинку, `NEAREST`.** Дірка прозора (будь-який
  непрозорий колір ліг би на ту саму шкалу, що й ціни); шкала обрізана до ×4
  найдешевшого вікна (справжній розмах — чотири порядки); вісь `tof` росте
  вгору, а рядки зображення вниз — переворот живе рівно у двох функціях і має
  власний тест.
- **Клітинка несе сам маневр**, а не оцінку вартості: другий розв'язок
  Ламберта на кліку означав би два числа, які мусять збігатися, і ефемериду
  в кадрі.
- **Вісь відходу міряє `computed_to`, а не бажання:** `leg::state_at` за
  горизонтом віддає крайню точку, і сітка на ній показувала б переліт із
  місця, де апарата не буде.
- **Розгортка — від апарата й у координатах центрального тіла, у Rust над
  `lambert_solve`.** Урок, який це коштувало, більший за саму функцію: **збіг
  двох шляхів через межу не доводить нічого про вибір системи координат** —
  однакова помилка стоїть з обох боків і скорочується. Оракулу потрібне третє
  число ззовні. Обгортка тіло→тіло лишилась без викликача — борг D10.
- **Сітка обирає вікно, а не обіцяє траєкторію.** Найкраще вікно фікстури
  (halo 1151 → Місяць): 205 м/с, переліт 9.5 доби; політ повною моделлю дає
  найближчий підхід **2495 км** проти 43544 км без маневру — але на 3.17-й
  добі, а в мить обіцяного приходу апарат за 95602 км. Уточнює диференціальна
  корекція, і саме тут вона вперше по-справжньому потрібна.

### Карта: фрейм і ZVC

- **Перетворення фрейму — на CPU у `f64`, в одному проході з camera-relative**
  (`game::view`; рушій про фрейми не знає). «Ключовий трюк» з вершинним
  шейдером виміру не витримав, і PROJECT.md §7 змінено: `f32`-шлях коштує
  **132.5 м** найгіршого відхилення (17 пікселів на вигляді завширшки 10 км),
  а проходу він не економить — той є щокадру. Ціна: 2.69 → 10.56 нс на точку,
  тобто **D7 у режимі карти настає вчетверо раніше**.
- **Масштаб синодичних координат — сталий** (`SYNODIC_SCALE_M`). Множення на
  теперішню відстань Земля-Місяць повертає в картинку рівно те, що фрейм
  щойно прибрав: `L` гуляє між 3.63 і 4.06·10⁸ м, і Місяць у карті **їде**.
  Спіймали це пікселі, а не числа.
- **«Замкнена петля» оракулом бути не може:** за 42.5 доби прогнозу
  (стеля `LEAD_LEGS`) halo-орбіта не замикається в жодному фреймі — вона
  нестійка. Оракул — звірка з `engine::trajectory::rotating_position`
  (0.54 м на 4·10⁸, тобто рівень різниці `μ` ассета й `μ`-константи рушія).
  Заради чого карта й вмикається: розмах утричі менший.
- **Перемикач віддає фрейм, а не команду** — вибір вигляду не змінює в світі
  жодного числа; кнопка одна, бо пара означала б стан «жоден не натиснутий».
- **Карта — не окремий режим, а той самий кадр** (цим закрито питання Q2).
  Панелі лягли накладками поверх одного кадру, жодна не попросила другої
  сцени, а тіла в `Scene` зробили «карту» ще менш схожою на окремий екран:
  Земля й Місяць малюються тим самим кадром і в обертовому фреймі просто
  стоять на місці. Отже `ViewMode` у грі не з'являється, а перемикач —
  **параметр сцени**.
- **Крива нульової швидкості жива, а не зріз.** Дрейф `C` виміряний: 0.007% за
  добу, 0.076% за місяць — дихання на очах немає. Але за межами околу пари
  розмах стрибає до 82%: там `C` перестає бути `C`, і підпис каже це вголос.
  Криву будують промені **від кожного тіла**, не з барицентра (з барицентра
  промінь упирається в лоб Землі й до Місяця не доходить ніколи); промінь без
  відповіді розриває ламану — у цьому розриві й видно ворота; зовнішньої
  вітки немає, бо її промені йшли б ззовні всередину.
- **Застереження показується завжди, а не в поганому випадку:** крива,
  показана як стіна хоч раз, уже збрехала, і гравець не має способу
  дізнатися, коли їй вірити.
- **Пайплайн ламаних має `blend`** — до U6b3 альфа ігнорувалась, тобто
  «напівпрозорий шар» із PROJECT.md §7 не працював.

### Перевірка на Windows

- **GPU-тести йдуть через WARP.** `Gpu::new` після невдалого запиту повторює
  його з `force_fallback_adapter` (на Windows це WARP, змінних середовища не
  треба), `Gpu::for_tests` друкує адаптер і перетворює пропуск на **помилку**
  при `SPACE_SIM_REQUIRE_GPU` — саме воно стоїть у Windows-джобі, інакше крок
  зелений і тоді, коли WARP не знайшовся. Допуски розширювати не довелося:
  пікселеві тести пройшли з тими самими, що на апаратному Vulkan.
- **Порівняння настінного часу не буває оракулом у гейті.** Тест «з
  перетворенням дорожче, ніж без» упав на windows-mingw, де в debug обидва
  проходи коштують ~272 нс замість 2.69 і 10.56: час там задає не арифметика,
  а накладні збірки. Ціна живе там, де вона щось означає — у зонді, release,
  одна машина, один прогін.

---

## U7. Що показав крок

Зроблено; тут лишається те, що крок з'ясував, а не звіт про нього.

- **U7a приїхав наполовину зробленим, і це наслідок правила 7.** Таблиця
  ключів з'явилася разом із першою панеллю U1, тож писати її не довелося —
  але в неї **не було читача**: `Language::default()` англійська, і жоден
  шлях не вів до другої таблиці. Сорок п'ять українських рядків були мертвим
  кодом, який тест «кожен ключ щось каже» справно тримав зеленим. Урок
  ширший за крок: перевірка існування не є перевіркою досяжності.
- **Назви мов — ендоніми в обох таблицях,** і це рішення, а не оформлення:
  кнопка, що пропонує «Ukrainian» англійською, читається лише тим, хто вже
  читає англійською. Побічний наслідок важливіший за кнопку — «Українська»
  тепер лежить в **англійській** таблиці, тобто кирилиця потрібна незалежно
  від перекладу.
- **Шрифт перевірено не знімком, і план тут виявився слабшим за реальність.**
  Знімок каже «щось намальовано», а тофу — теж щось: порожній прямокутник це
  теж пікселі. Оракул натомість такий: egui підставляє **один і той самий**
  растр заміни, тож символ із приватної зони Unicode дає його поіменно, і
  жоден гліф наших рядків не має права на нього збігтися. GPU не потрібен
  зовсім, тобто перевірка не пропускається там, де адаптера немає.
- **Кирилицю покрито, шрифт додавати не довелось:** 1108 гліфів обох таблиць
  в обох сімействах, включно з ґ є і ї (ті, що випадають із «російського»
  набору) і Δ × —. Розвилка кроку — шрифт у `assets-src/` — не знадобилась.
- **Моноширинне сімейство лишається моноширинним і на кирилиці** — 8.43 точки
  на кожен символ. Це окремий вимір, бо перший його не доводить: egui,
  не знайшовши гліфа, бере його з пропорційного, і виходить справжній растр
  **іншої ширини**. Саме це зробило U7c безкоштовним рішення «панель
  моноширинна цілком».
- **Палітра виводиться з траєкторій, а не навпаки.** Кольори ліній існували з
  H5 і вже несли зміст (бурштин — прогноз, синій — історія, зелений — прев'ю,
  білий — апарат); інтерфейс, пофарбований окремо, заговорив би **другою**
  мовою кольору поверх першої. Тепер акцент панелі **дорівнює** кольору
  прогнозу, і це перевіряється рівністю, а не схожістю.
- **Контраст — формула, а не смак.** WCAG-відношення для кожної пари
  «текст на поверхні», 4.5:1 для основного й 3:1 для другорядного. «Мені
  видно» на одному моніторі не доводить нічого про інший.
- **Тест упіймав справжню помилку, а не підтвердив вибір:** перша заливка
  панелі (12, 16, 24) виявилася **світлішою** за небо (5, 8, 20), тобто
  панель світилася б на тлі космосу. Стало (4, 6, 12), і причина записана:
  під панеллю буває не лише космос, а й освітлений диск планети.
- **«Один колірний простір» доведено пікселями.** Той самий колір, покладений
  у сцену `[f32; 4]` нашим шейдером і в панель `Color32` шейдером egui,
  зустрічається в одній текстурі **тими самими байтами**. Без цього виміру
  рівність акценту й лінії прогнозу була б обіцянкою в коментарі.

---

## U8. Закриття етапу 🟢

- **Перемір кадру** (скіл `perf-probe`): UI — істотна нова вартість, а
  0.109 мс без нього вже виміряно. Числа лягають у ROADMAP «Вимір
  продуктивності» **новим блоком, не поверх старого**.
- **D7 стане видимим саме тут.** Борг каже: мільйон вершин — ~3 мс CPU і
  24 МБ на кадр, тобто перша стеля кадру. Етап U його не закриває, але робить
  так, що в нього впираються очима: довга місія на екрані — це і є той
  мільйон. Записати, що саме побачили.
- **Переоцінка боргів і питань** (CLAUDE.md, пункт 6) тим самим комітом.
- **M3 оголошується закритим** у ROADMAP.md, коли U1–U6 зелені: редактор
  маневрів є, porkchop є, ZVC є, солвери кличуться з інтерфейсу.

---

## Чого етап U свідомо не робить

- **Ручного керування** — виведення, стикування, посадка (PROJECT.md §8).
  Це три різні режими вводу з власною фізикою й власним кадром; жоден із них
  не потрібен M3, і кожен вартий свого етапу.
- **Оболонки агенції** — список місій, календар, меню сейвів, налаштування.
  Половина з цього планувала б те, чого в моделі ще немає: кілька апаратів у
  грі вже можливі, а місія як сутність — ні. «Структура, жодне поле якої не
  читається, гірша за свою відсутність» діє й для екранів.
- **Візуалізації невизначеності** — еліпси, сеанси зв'язку. Це M6, і в ядрі
  для нього є лише прототип (`core/uncertainty.c`), який уже показав межу
  своєї моделі.
- **Проріджування історії** (D7) — етап U його бачить, але не закриває:
  борг вимагає, щоб сейв після проріджування відтворювався бітово, а це
  робота в `/game`, не в UI.

---

## Борги, яких торкається етап

- **D7 (історія росте без межі)** — U8 називає, що саме побачили на екрані.
  Друга половина боргу (`Lines::upload` щокадру переганяє кожну вершину) — це
  рівно те, у що впреться довга місія з відкритою картою, і в обертовому
  фреймі вчетверо раніше.
- **D10 (`porkchop_compute_eph` без викликача)** — заведений тут: розгортка
  переїхала в Rust, а функція на межі лишилась. Умови закриття — ROADMAP.md.
