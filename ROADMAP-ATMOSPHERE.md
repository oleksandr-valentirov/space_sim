# Атмосфера й матеріали — етап S (M5)

> Джерело істини — [PROJECT.md](PROJECT.md) §7 («Атмосфера», «Освітлення»);
> загальний порядок етапів — [ROADMAP.md](ROADMAP.md).
>
> **Етап S закрито цілком 2026-08-16 (S1–S7).** Хід кроків лишається в git;
> тут — те, що діє далі, і те, чого M5 ще винен.

---

## Що M5 винен

PROJECT.md §10 називає п'ять речей; таблиця оновлена після етапів V і T:

| пункт M5 | стан | чим заблокований |
|---|---|---|
| Hillaire 2020 (небо й повітря) | ✅ S1–S7 | — |
| PBR корабля | ✅ етап T | — |
| Небо без повітря: зорі й видиме світило | 🟢 | нічим — етап `Z`, і це єдиний незаблокований пункт |
| Факел двигуна | 🔴 | **тяги в `/core` немає** — маневри імпульсні, тобто горіння не має тривалості |
| Вхід в атмосферу | 🟡 | чекає на тягу разом з факелом; повітря (S) і фізика опору (K7) вже є |

Корабель у `Scene`, якого чекали три з цих пунктів, приніс етап V; PBR
корпусу — етап T. Лишилось те, що впирається в тягу, і те, що не впирається ні
в що (розбір етапу `Z` — ROADMAP.md, «Далі»).

⚠ **Факел без тяги малювати не можна.** Імпульсний маневр триває нуль секунд;
факел довелося б показувати за вигаданим таймером, тобто малювати те, чого у
фізиці немає — та сама помилка, що структура, якої ніхто не читає, тільки
видима на екрані.

**Чого етап свідомо не робив:** об'ємних хмар (PROJECT.md §7: не в v1),
кількох атмосфер (Земля доводить конструкцію; Марс і Титан — це дані, не код),
автоекспозиції (стосується всієї сцени, і без корабля в кадрі міряти її нема
на чому).

---

## Рішення S1–S7

Небо, повітря й аеральна перспектива за Hillaire 2020: чотири таблиці, один
марш, один повноекранний прохід. Земля має повітря, Місяць — ні, і кадр Місяця
бітово той самий, що до етапу (`--shot` — `30812bf2…`, як від R4a).

### Ціна не залежить від кількості апаратів

Жоден LUT не належить місії, і це відповідь на питання «чи рахувати повітря
спрощено для місій поза кадром» — рахувати нема чого:

| LUT | від чого залежить | як часто |
|---|---|---|
| Transmittance | лише параметри повітря | раз назавжди |
| Multiple scattering | лише параметри повітря | раз назавжди |
| Sky-view | позиція камери + напрямок на Сонце | раз на кадр |
| Aerial perspective | фрустум камери | раз на кадр |

Тридцять апаратів коштують стільки ж, скільки один. Там, де «не в кадрі — не
платимо» справді дає мілісекунди, — це борг D15 у ROADMAP.md.

### Геометрія: змінна — `rho² = r² − bottom²`, а не радіус

**Найдорожче рішення етапу: три помилки поспіль, і всі три виглядали як
помилки фізики.** Обидві відстані — до поверхні й до верхньої межі —
виражаються через `rho²` без віднімання великих чисел. У `f32` при
`r ≈ 6.4·10⁶` одиниця останнього розряду квадрата дорівнює 4·10⁶, тобто біля
поверхні й біля дотику знак різниці `r² − bottom²` дає округлення, а не
геометрія. Хто знає `rho²` точніше (параметризація таблиці, висота над
поверхнею) — той і мусить його передати.

Що це коштувало, доки не було зроблено:

- **S2:** крайній стовпець таблиці пропускання (дотичний промінь) обрізався
  вдесятеро й розходився з оракулом на 0.21 замість 10⁻³;
- **S3:** промінь униз із поверхні має `rho² = 0`, і `−r·mu − √(r²mu²)` у `f32`
  виходить то трохи додатним, то трохи від'ємним. Від'ємне викликач читає як
  «поверхні попереду немає» — промінь ішов крізь планету й освітлювався
  зсередини неї; рядок 0 таблиці був **утричі** яскравіший;
- **S3, друге:** та сама різниця, порахована з радіусів замість висоти, дала
  рядку 0 удвічі більше за двійник, тоді як решта сходилася на 0.05%.

Звідси `shell_squared` добутком `(top − bottom)·(top + bottom)`, а не різницею
квадратів, і `distance_to_ground`, що ніколи не повертає від'ємного: відстань
не буває від'ємною, а викликач читає від'ємне як «немає».

### Ланцюг оракулів: жодна ланка не перевіряє себе сама

1. **Замкнена форма ⇄ CPU.** Оптична товща вертикального променя крізь
   експоненційний шар — `β·H·(exp(−h₀/H) − exp(−h₁/H))`, крізь трикутний
   озоновий — різниця первісних трикутника. Юніт-тест, без GPU.
2. **CPU ⇄ GPU.** `engine::atmosphere` у `f64` проти `shaders/sky.slang` у
   `f32`: 16 384 текселі пропускання (< 10⁻³), 1024 текселі розсіювання (0.1%
   удень), 1296 напрямків неба з трьох камер (< 5%).
3. **GPU ⇄ фізика в кадрі.** Знімки з числами: горизонт удвічі яскравіший і
   біліший за зеніт, захід удесятеро червоніший за полудень, смуга лімба
   спадає з висотою шкали.

Без першої ланки два чисельні інтегрування зійшлися б і на спільній помилці;
без другої шейдер не перевірений узагалі; без третьої перевірені таблиці, а не
кадр.

**Параметризація кладе кінці одиничного діапазону в центри крайніх текселів**
(`atmosphere::unit_to_texture`). Без цього стовпець `u = 0` таблиці пропускання
несе `mu = 0.98` замість `1`, тобто вертикалі в таблиці немає й замкненій формі
нема з чим порівнюватися. Побічно це тримає білінійну вибірку в межах таблиці.

### Числа, які варто пам'ятати

- **500 кроків у таблиці пропускання, не 40 зі статті.** Промінь від землі під
  малим кутом іде 560 км кроками по 14 км при висоті шкали 8 км; правило
  середньої точки помиляється на 5·10⁻³, увосьмеро більше за крок half-float.
  П'ятсот дають 3.6·10⁻⁵ — таблицю обмежує **формат**, а не арифметика.
- **Найбільша частка повторного розсіювання — 0.32.** `ψ = L₂/(1 − f)`
  збігається лише при `f < 1`, і повітря Землі до межі не підходить близько.
- **Профіль розсіювання по висоті не монотонний**, і це озон: максимум на
  6.5 км, провал на 35 км, **другий підйом** до 58 км і лише тоді спад. Тест
  пише це трьома твердженнями, бо «спадає з висотою» було б простіше й
  неправдою.
- **e-складання смуги лімба — 8.1 км проти 8.0 км висоти шкали Релея.** Міряти
  треба в прозорій частині (35–55 км): нижче дотичний промінь має оптичну
  товщу порядку одиниці, і там та сама підгонка дає 12.5 км — насичення, а не
  інша фізика.
- **Шов між двома шляхами шейдера — 8 одиниць з 255.** Марш із 16 кроків до 48
  не міняє нічого, отже це кутова роздільність таблиці неба біля горизонту:
  більша таблиця його зменшить, точніше інтегрування — ні.
- **Експозиція — стала 8, виміряна:** яскравість у зеніті опівдні 0.048 на
  одиницю освітленості Сонця, вісім ставить її на 0.38. Число рухається тоді,
  коли з'явиться автоекспозиція, і тим самим кроком.
- **Альбедо поверхні — нуль.** У статті відбиття від землі справжнє (небо над
  снігом світліше), але кольору поверхні в `Scene` немає, а вигаданий дав би
  небу відтінок, якого в грі нема звідки взяти.

Ціна кадру — ROADMAP.md, «Вимір продуктивності / Повітря (S7)».

### Дві речі, які знайшов лише CI

Обидві пройшли повз локальний гейт з тієї самої причини: на машині розробника
один пристрій, а в CI — три інші.

**⚠ Ніякої індексації вектора змінною й ніяких циклів по каналах у шейдері.**
HLSL не вміє індексувати `float3` неконстантним індексом узагалі — компілятор
зобов'язаний розгорнути такий цикл, а всередині маршу FXC відмовляється:
`error X3511: forced to unroll loop, but unrolling failed`. Спіймано на WARP
(D3D12), де Vulkan і Metal мовчали. Отже вся арифметика по каналах — векторна,
а замість `if (sigma_e[c] <= 0) continue;` стоїть ділення на `max(sigma_e,
1e-30)`: де ослаблення нульове, `1 − exp(0)` теж нуль, тобто внесок нульовий, а
ділення визначене. Правило стосується **будь-якого** майбутнього шейдера.

**⚠ Біля термінатора допуск не буває відносним.** Тінь — це сходинка: `lit`
означає «промінь до Сонця не зустрів поверхні». Один ULP у геометрії перекидає
цілий семпл маршу з одного боку сходинки на інший, і два пристрої мають повне
право вирішити по-різному. Там, де значення й складається з кількох таких
семплів (нічний бік, одиниці 10⁻⁶), різниця виходить порядку самого значення.
Тому в допусках S3 і S4 є абсолютний доданок 3·10⁻⁶, і він **не про
half-float**. Той самий клас, що «нічию на GPU не перевіряють» (F3), лише
сходинка не в глибині, а в тіні.

Локально другий клас не ловиться взагалі: lavapipe на машині розробника не
створює пристрій («Parent device is lost»), хоч і перелічується. Другий
пристрій у цього оракула є лише в CI, і це записано, а не забуто.

### Конструкція кадру

- **Небо — повноекранний трикутник першим у найдальшому діапазоні**, без запису
  глибини: далі геометрія лягає зверху звичайним тестом.
- **Два пайплайни неба, вибір на CPU.** Усередині повітря — один `SampleLevel`
  з таблиці; поза ним таблиця не працює й не через точність: її вісь зеніту
  ділить напрямки горизонтом спостерігача, а з орбіти горизонту в цьому сенсі
  немає — повітря займає малий диск, а не півсферу.
- **Одна реалізація маршу на всі шляхи.** Таблиця неба будує позицію й напрямок
  із кутів і кличе ту саму функцію; дві копії розійшлися б рівно там, де камера
  перетинає межу.
- **Небо додає світло, а не заміщає.** Заміщення вигризало з фону чорну дугу на
  нічному лімбі: розсіювати там нема чого, і нуль ставав кольором. Повна
  композиція — `фон·T + L`.
- **Аеральна перспектива — окремий прохід одразу після свого діапазону
  глибини**, і окремий саме тому, що читає глибину: та сама текстура не буває
  одночасно ціллю й ресурсом. Після свого — бо кожен діапазон чистить глибину.
- **Композиція двома викликами, `дст·T` і `дст + L`.** За один прохід це
  вимагало б dual-source blending — ще однієї фічі пристрою й `@blend_src` у
  WGSL, якого компілятор Slang не друкує. З тієї ж причини пропускання — повний
  RGB-об'єм, а не одне усереднене число: усереднене зробило б захід крізь
  повітря сірим замість червоного.
- **Шари об'єму натягнуті на повітря, а не «від камери».** Виміряно: з 1000 км
  увесь стокілометровий шар потрапляв менш ніж в один шар об'єму з тридцяти
  двох — тридцять один ішов на порожнечу перед ним.

### Умова, за якої повітря не малюється

**Одна на всю роботу з повітрям, і вона в пікселях кадру:** шар малюється, лише
коли його товщина в кадрі не менша за піксель. Для Землі це перетинає одиницю
на 6.24·10⁷ м. Той самий вид критерію, що `lod::error_px`, і з тієї самої
причини: метри довелося б підбирати на кожен радіус тіла окремо.

Дві умови там були спершу — об'єм пропускався, а таблиця неба ні, — і різницю
виміряно: на 10⁹ м вони коштували 0.05 мс за диск повітря завширшки в
шістнадцяту пікселя. Режиму «видно наполовину» тут не існує.
