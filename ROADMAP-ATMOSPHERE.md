# Атмосфера й матеріали — етап S (M5)

> Послідовність робіт над атмосферою й матеріалами — те, що закриває **M5**.
> Джерело істини — [PROJECT.md](PROJECT.md) §7 («Атмосфера», «Освітлення»);
> загальний порядок етапів — [ROADMAP.md](ROADMAP.md).

**Літера S, а не M5.** `M*` — це мілстоуни з PROJECT.md §10; етап — це робота,
яку роблять. Так само розведені U/M3 і R/M4.

Окремий файл із тієї ж причини, що ROADMAP-UI.md і ROADMAP-PLANETS.md: етап
великий, а два паралельні етапи не мають правити один розділ.

---

## Що M5 обіцяє, і що з цього можна робити сьогодні

PROJECT.md §10 називає чотири речі: **Hillaire 2020, PBR корабля, факел, вхід
в атмосферу**. Перевірено читанням коду, а не пам'яттю:

| пункт M5 | стан | чим заблокований |
|---|---|---|
| Hillaire 2020 (небо й повітря) | ✅ зроблено (S1–S7) | — |
| PBR корабля | 🔴 заблокований | у `Scene` немає корабля як геометрії взагалі |
| Факел двигуна | 🔴 заблокований подвійно | немає корабля; і **тяги в `/core` немає** — маневри імпульсні, тобто горіння не має тривалості, до якої факел міг би кріпитись |
| Вхід в атмосферу | 🔴 заблокований | потребує і повітря (пункт 1), і корабля (пункт 2); фізика опору вже є (K7) |

**Звідси порядок етапу, і він не про смак.** Половина M5, яка стосується
повітря, не заблокована нічим і робиться першою. Друга половина чекає на
корабель у сцені — а це перший крок етапу «вигляд від корабля», де розробник
уже назвав порядок: **спершу третя особа** (ROADMAP.md, «Далі — грубо»).

Тобто M5 закривається двома етапами, не одним, і це виявлено, а не вирішено:

1. **Етап S (тут)** — небо, повітря, аеральна перспектива. Закриває
   «Hillaire 2020» цілком.
2. **Корабель у сцені** (окремий етап) — після нього PBR і вхід в атмосферу
   стають кроками, а не мріями. Факел додатково чекає на тягу в `/core`, і
   це вже фізика, а не рендер: PROJECT.md §4 обіцяє тягу в моделі сил, але
   `accel.h` про неї має лише коментар.

⚠ **Факел без тяги малювати не можна, і це не педантизм.** Імпульсний маневр
триває нуль секунд; факел, приліплений до нього, довелося б показувати за
вигаданим таймером, тобто малювати те, чого у фізиці немає. Це рівно та
помилка, від якої застерігає CLAUDE.md про структури, яких ніхто не читає, —
тільки видима на екрані.

---

## Що етап S робить

Hillaire 2020 — це набір невеликих LUT, які перераховуються в реальному часі,
і один шейдер, що працює і з поверхні, і з орбіти. Останнє для нас не
зручність, а вимога: перехід між масштабами в грі безперервний, і двох різних
атмосфер бути не може.

### Правила етапу

1. **Параметри атмосфери — властивість тіла, не виклику.** Те саме рішення,
   що з гармоніками й радіусом (CLAUDE.md): повітря Землі — властивість
   Землі. Отже вони їдуть у `Scene::Body`, а не в uniform кадру.
2. **Оракул — число, а не «схоже на небо».** Кожен LUT має аналітичну
   перевірку хоча б в одній точці: вертикальний промінь крізь експоненційну
   атмосферу дає оптичну товщу в замкненій формі, і саме з нею звіряється
   перший LUT.
3. **Один шейдер з поверхні й з орбіти.** Кожен крок, який дає картинку,
   перевіряється **двома** знімками — з землі й з орбіти, — інакше він
   закриває половину задачі й не каже, яку.
4. **Кадр лишається бітово відтворюваним там, де атмосфери немає.** Місяць
   без повітря має дати той самий кадр, що й до етапу; це найдешевша
   сторожа проти «пройшло крізь усе».
5. **Вартість атмосфери не залежить від кількості апаратів, і це треба
   пам'ятати, щоб не оптимізувати те, чого немає.** Питання розробника
   (2026-08-16) було «чи не рахувати повітря спрощено для місій поза
   кадром»; відповідь — рахувати нема чого, бо жоден LUT не належить місії:

   | LUT | від чого залежить | як часто |
   |---|---|---|
   | Transmittance | лише параметри повітря | раз назавжди |
   | Multiple scattering | лише параметри повітря | раз назавжди |
   | Sky-view | позиція камери + напрямок на Сонце | раз на кадр |
   | Aerial perspective | фрустум камери | раз на кадр |

   Камера одна, тіло з повітрям сьогодні одне (Земля; у Місяця `air: None`,
   S1). Тридцять апаратів коштують стільки ж, скільки один.

   **Але пропускати те, чого не видно, все одно треба — і питання тут інші
   два:** чи камера всередині повітря і чи є в кадрі поверхня, видима крізь
   нього. З 10⁹ м aerial perspective не потрібен узагалі — шейдити крізь
   повітря нема чого. Це умова кроку S5, і вона мусить бути **виміряна**, а
   не оголошена: скільки коштує об'єм, який нікому не знадобився.

   Там, де «не в кадрі — не платимо» справді дає мілісекунди, — це борг D15
   (ламані не відбираються ні за фрустумом, ні за чим), і він записаний у
   ROADMAP.md, а не тут.

### Кроки

**Усі закриті (2026-08-16).** Що з них діє далі — розділ «Рішення S1–S7».

- **S1. Параметри атмосфери в `Scene::Body`.** ✅ Реєй, Мі, висоти шкали, радіус
  верхньої межі, поглинання озоном. Разом із першим викликачем — тілом Землі
  в `game::view`; тіло без атмосфери лишається `None` і не платить нічого.
  **Перевірка:** кадр Місяця бітово той самий, що до кроку.
- **S2. Transmittance LUT.** ✅ 256×64, compute, раз на набір параметрів.
  **Перевірка:** аналітична — оптична товща вертикального променя крізь
  експоненційну атмосферу дорівнює `β·H·(1 − exp(−h/H))` у замкненій формі;
  збіг у межах кроку інтегрування, на десятках висот і кутів.
- **S3. Multiple-scattering LUT.** ✅ 32×32. **Перевірка:** енергія не росте —
  друге й наступні розсіювання додають менше за перше, і сума збігається.
- **S4. Sky-view LUT і небо в кадрі.** ✅ — двома кроками: S4a таблиця, S4b
  кадр. **Перевірка:** два знімки (з поверхні
  й з орбіти), і в кожному — число: колір у зеніті проти кольору біля
  горизонту, захід сонця дає червоніше за полудень.
- **S5. Aerial perspective, і умова, за якої його не рахують.** ✅ Об'єм
  32×32×32 перед камерою; поверхня, видима крізь повітря, синіє з відстанню.
  **Перевірка:** той самий патч на різній відстані — контраст падає
  монотонно; **і друге число** — час кадру з камерою за 10⁹ м, де об'єм
  пропущено, проти нього ж порахованого даремно.
- **S6. Лімб і тінь планети.** ✅ Атмосфера на краю диска з орбіти й нічний бік.
  **Перевірка:** знімок лімба; товщина світної смуги проти висоти шкали.
- **S7. Перемір і закриття.** ✅ Ціна кадру з повітрям і без, на трьох висотах;
  переоцінка боргів і питань.

### Чого етап S свідомо не робить

- **Об'ємних хмар.** PROJECT.md §7 прямо каже «не в v1»; спочатку двовимірні
  шари, і навіть вони — не цей етап.
- **PBR корабля, факела, входу в атмосферу.** Заблоковані вище; етап S їх не
  розблоковує й не імітує.
- **Кількох атмосфер.** Земля доводить конструкцію; Марс і Титан — це дані,
  не код.
- **Автоекспозиції.** Вона стосується всієї сцени, а не повітря, і без
  корабля в кадрі міряти її нема на чому.

---

## Рішення S1–S7

Те, що діє далі. Хід кроків лишається в git; сюди винесене лише те, про що
найлегше спіткнутися, і кожне з числом.

### Що етап зробив

Небо, повітря й аеральна перспектива за Hillaire 2020: чотири таблиці, один
марш і один повноекранний прохід. Земля має атмосферу, Місяць — ні, і кадр
Місяця бітово той самий, що до етапу (`--shot` — `30812bf2…`, як від R4a).
Пункт M5 «Hillaire 2020» закрито цілком.

### Геометрія: змінна — `rho² = r² − bottom²`, а не радіус

**Найдорожче рішення етапу, і воно коштувало трьох помилок поспіль.** Обидві
відстані — до поверхні й до верхньої межі — виражаються через `rho²` без
жодного віднімання великих чисел. У `f32` при `r ≈ 6.4·10⁶` одиниця останнього
розряду квадрата дорівнює 4·10⁶, тобто біля поверхні й біля дотику знак різниці
`r² − bottom²` дає округлення, а не геометрія. Хто знає `rho²` точніше —
параметризація таблиці, висота над поверхнею — той і мусить його передати.

Що це коштувало, доки не було зроблено:

- **S2:** крайній стовпець таблиці пропускання (дотичний промінь) обрізався
  вдесятеро й розходився з оракулом на 0.21 замість 10⁻³;
- **S3:** промінь униз із самої поверхні має `rho² = 0`, і `−r·mu − √(r²mu²)`
  у `f32` виходить то трохи додатним, то трохи від'ємним. Від'ємне викликач
  читає як «поверхні попереду немає» — промінь ішов крізь планету й
  освітлювався зсередини неї. Рядок 0 таблиці виходив **утричі** яскравішим;
- **S3, друге:** та сама різниця, порахована з радіусів замість висоти, дала
  рядку 0 удвічі більше за двійник, тоді як решта сходилася на 0.05%.

Звідси й `shell_squared` добутком `(top − bottom)·(top + bottom)`, а не
різницею квадратів, і `distance_to_ground`, що ніколи не повертає від'ємного:
відстань не буває від'ємною, а викликач читає від'ємне як «немає».

### Ланцюг оракулів: жодна ланка не перевіряє себе сама

1. **Замкнена форма ⇄ CPU.** Оптична товща вертикального променя крізь
   експоненційний шар — `β·H·(exp(−h₀/H) − exp(−h₁/H))`, крізь трикутний
   озоновий — різниця первісних трикутника. Юніт-тест, без GPU.
2. **CPU ⇄ GPU.** `engine::atmosphere` у `f64` проти `shaders/sky.slang` у
   `f32`: 16 384 текселі пропускання (розбіжність < 10⁻³), 1024 текселі
   розсіювання (0.1% удень), 1296 напрямків неба з трьох камер (< 5%).
3. **GPU ⇄ фізика в кадрі.** Знімки з числами: горизонт удвічі яскравіший і
   біліший за зеніт, захід удесятеро червоніший за полудень, смуга лімба
   спадає з висотою шкали.

Без першої ланки два чисельні інтегрування зійшлися б і на спільній помилці;
без другої шейдер не перевірений узагалі; без третьої перевірені таблиці, а не
кадр.

**Параметризація кладе кінці одиничного діапазону в центри крайніх текселів**
(`atmosphere::unit_to_texture`). Без цього стовпець `u = 0` таблиці пропускання
несе `mu = 0.98` замість `1`, тобто вертикалі в таблиці немає зовсім і
замкненій формі нема з чим порівнюватися. Побічно це ще й тримає білінійну
вибірку в межах таблиці.

### Числа, які варто пам'ятати

- **500 кроків у таблиці пропускання, не 40 зі статті.** Промінь від самої
  землі під малим кутом іде 560 км кроками по 14 км при висоті шкали 8 км;
  правило середньої точки помиляється на 5·10⁻³, увосьмеро більше за крок
  half-float. П'ятсот дають 3.6·10⁻⁵ — тобто таблицю обмежує **формат**, а не
  арифметика.
- **Найбільша частка повторного розсіювання — 0.32.** `ψ = L₂/(1 − f)`
  збігається лише при `f < 1`, і повітря Землі до цієї межі не підходить
  близько. Число живе в альфі таблиці розсіювання саме заради перевірки.
- **Профіль розсіювання по висоті не монотонний**, і це озон: максимум на
  6.5 км, провал на 35 км, **другий підйом** до 58 км уже над шаром, і лише
  тоді спад. Тест пише це трьома окремими твердженнями, бо «спадає з висотою»
  було б простіше й неправдою.
- **e-складання смуги лімба — 8.1 км проти 8.0 км висоти шкали Релея.**
  Міряти треба в прозорій частині (35–55 км): нижче дотичний промінь має
  оптичну товщу порядку одиниці, і там та сама підгонка дає 12.5 км —
  насичення, а не інша фізика.
- **Шов між двома шляхами шейдера — 8 одиниць з 255.** Камери за десять метрів
  усередині й ззовні верхньої межі читають таблицю неба й марширують промінь
  відповідно. Причина названа: підняти марш із 16 кроків до 48 не міняє
  нічого, отже це кутова роздільність таблиці неба біля горизонту. Більша
  таблиця його зменшить, точніше інтегрування — ні.

### Конструкція кадру

- **Небо — повноекранний трикутник першим у найдальшому діапазоні**, без
  запису глибини: далі геометрія лягає зверху звичайним тестом, ближчі
  діапазони домальовують поверх.
- **Два пайплайни неба, вибір на CPU.** Усередині повітря — один
  `SampleLevel` з таблиці; поза ним таблиця не працює взагалі, і не через
  точність: її вісь зеніту ділить напрямки горизонтом спостерігача, а з орбіти
  горизонту в цьому сенсі немає — повітря займає малий диск, а не півсферу.
- **Одна реалізація маршу на всі шляхи.** Таблиця неба будує позицію й
  напрямок із кутів і кличе ту саму функцію; дві копії розійшлися б, і небо з
  поверхні перестало б збігатися з небом з орбіти рівно там, де камера
  перетинає межу.
- **Небо додає світло, а не заміщає.** Заміщення видно було одразу: нічний
  край лімба з орбіти вигризав із фону чорну дугу, бо там розсіювати нема чого
  і нуль ставав кольором. Повна композиція — `фон·T + L`; другий множник
  прийшов з аеральною перспективою.
- **Аеральна перспектива — окремий прохід одразу після свого діапазону
  глибини**, і окремий саме тому, що читає глибину: та сама текстура не буває
  одночасно ціллю й ресурсом. Після свого — бо кожен діапазон чистить глибину.
- **Композиція двома викликами, `дст·T` і `дст + L`.** За один прохід це
  вимагало б dual-source blending — ще однієї фічі пристрою й `@blend_src` у
  WGSL, якого компілятор Slang не друкує. Два повноекранні трикутники коштують
  менше, ніж залежність від того й того. З тієї ж причини пропускання — повний
  RGB-об'єм, а не одне усереднене число: усереднене зробило б захід крізь
  повітря сірим замість червоного.
- **Шари об'єму натягнуті на повітря, а не «від камери».** Виміряно: з висоти
  1000 км увесь стокілометровий шар потрапляв менш ніж в один шар об'єму з
  тридцяти двох, бо тридцять один ішов на порожнечу перед ним.

### Умова, за якої повітря не малюється

**Одна на всю роботу з повітрям, і вона в пікселях кадру:** шар малюється,
лише коли його товщина в кадрі не менша за піксель. Для Землі це перетинає
одиницю на 6.24·10⁷ м. Той самий вид критерію, що `lod::error_px`, і з тієї
самої причини: метри довелося б підбирати на кожен радіус тіла окремо.

Дві умови там були спершу — об'єм пропускався, а таблиця неба ні, — і різницю
виміряно: на 10⁹ м таблиця з повноекранним проходом коштували 0.05 мс за диск
повітря завширшки в шістнадцяту пікселя. Режиму «видно наполовину» тут не
існує.

### Ціна кадру (1280×720, RTX 4070 Ti SUPER, release)

| висота | повітря | без, мс | з повітрям, мс | різниця | разів |
|---|---|---|---|---|---|
| 10 км | малюється | 0.388 | 0.497 | +0.108 | 1.28 |
| 500 км | малюється | 0.137 | 0.522 | +0.386 | 3.82 |
| 10⁷ м | малюється | 0.117 | 0.321 | +0.204 | 2.74 |
| 6.0·10⁷ м | малюється | 0.116 | 0.236 | +0.119 | 2.03 |
| 6.5·10⁷ м | пропущено | 0.109 | 0.114 | +0.005 | 1.05 |
| 10⁹ м | пропущено | 0.104 | 0.108 | +0.005 | 1.05 |

Найдорожче — 500 км: об'єм марширує крізь найгустіше повітря на повну довжину.
Уп'ятеро дорожче за кадр без повітря і все одно 0.52 мс, тобто 3% бюджету
60 Hz. **Кількість апаратів на це не впливає взагалі** — жоден LUT не належить
місії (правило 5 етапу).

### Експозиція — стала, і це рішення етапу

Множник 8, виміряний: яскравість у зеніті опівдні — 0.048 на одиницю
освітленості Сонця, і вісім ставить її на 0.38. Автоекспозиція стосується всієї
сцени, а не повітря, і без корабля в кадрі міряти її нема на чому. Число
рухається тоді, коли вона з'явиться, і тим самим кроком.

### Альбедо поверхні — нуль, і це теж рішення

У статті відбиття від землі є, і воно справжнє: небо над снігом світліше. Але
кольору поверхні в `Scene` немає взагалі, а вигаданий дав би небу відтінок,
якого в грі нема звідки взяти.
