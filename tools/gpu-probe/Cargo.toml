[package]
name = "gpu-probe"
version = "0.1.0"
edition = "2021"
rust-version.workspace = true
publish = false

# ROADMAP P0 reconnaissance, not part of the game. Lives in tools/ with the
# rest of the offline kit: it renders nothing and does not enter the build.
[dependencies]
pollster = "1.0.1"
wgpu = "30"

[lints]
workspace = true
