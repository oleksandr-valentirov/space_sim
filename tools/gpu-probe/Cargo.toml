[package]
name = "gpu-probe"
version = "0.1.0"
edition = "2021"
rust-version.workspace = true
publish = false

# Розвідка ROADMAP P0, а не частина гри. Живе в tools/ разом з рештою
# офлайнового приладдя: воно нічого не рендерить і в білд не потрапляє.
[dependencies]
pollster = "1.0.1"
wgpu = "30"

[lints]
workspace = true
