# ROADMAP.md — послідовність робіт

> Що робити далі й у якому порядку. Рішення проєкту — у [PROJECT.md](PROJECT.md).
> Дат немає свідомо: важливий порядок, не швидкість.
>
> Цей файл тримає **рішення й виміряні межі, що діють далі**, а не звіт про
> зроблене. Історія кроків із повними вимірами, знахідками й розборами
> помилок лишається в git — `git log`, `git show <commit>:ROADMAP.md`.

---

## Принцип упорядкування

Кроки впорядковані **за спаданням імовірності успіху**, а не за важливістю.
Спочатку те, що майже напевно спрацює: воно будує інструменти, на яких потім
діагностуються складні кроки.

**Єдиний виняток — дешеві розвідки.** Якщо архітектурне рішення ризиковане,
але перевірити його можна за години, перевірка йде рано.

Кожен крок має **перевірку** (як дізнатися, що зроблено) і **розвилку**
(що робити, якщо не вийшло). Розвилка — не аварійний план, а очікуваний
варіант розвитку.

Позначки ризику: 🟢 низький — питання часу; 🟡 середній — очікуйте день-два
на пошук причини; 🔴 високий — може змінити план.

---

## Стан

| Етап | Зміст | Стан |
|---|---|---|
| A | Фундамент: збірка, каркас детермінізму, базова математика | ✅ |
| B | Ефемерида: Horizons, Чебишев, RK4, DOP853, ассет | ✅ |
| C | CR3BP, halo-орбіти, multiple shooting, гейт детермінізму | ✅ **M0** |
| D | Межа C↔Rust: `build.rs`, `core-sys`, `core-rs` | ✅ **M1** |
| E | Розвідки: bindless (P0), Slang (P1) | ✅ |
| F | Перша картинка: вікно, reversed-Z, camera-relative, сфера, траєкторії | ✅ **M2** |
| G | Планування: Lambert, porkchop, диференціальна корекція, ZVC | ✅ (частина M3) |
| H | Жива межа: `prop_run` з подіями від C до рушія | ✅ |
| I | Інтерактивний кадр: сфера у `Frame`, орбітальна камера, перемір | ✅ |
| J | Гра й потоки: `/game`, курсор часу, план, нитки, сейв | ✅ |
| K | Повна модель сил (M3.5) | ✅ **M3.5** |
| L | Закриття боргів: D6, D3, D1 | ✅ (D3 чекає на залізо) |
| U | Інтерфейс гри — решта M3 ([ROADMAP-UI.md](ROADMAP-UI.md)) | ✅ **M3** (U1–U8) |
| R | Планети — M4 ([ROADMAP-PLANETS.md](ROADMAP-PLANETS.md)) | ✅ цілком (R1–R8), **M4** на R1–R6 |
| N | D7: слід перестає рости з ігровим часом | ✅ цілком (N1–N5), **борг D7 закрито** |
| S | Атмосфера — M5 ([ROADMAP-ATMOSPHERE.md](ROADMAP-ATMOSPHERE.md)) | ✅ цілком (S1–S7), **пункт M5 «Hillaire 2020» закрито** |
| V | Корабель у сцені — третя особа, решта M5 після нього | ✅ **етап V** |
| T | Матеріали й поверхня: колір, тайли, PBR, перша модель корабля | ✅ цілком (T1–T9) |
| W | Запечений нахил: відповідь на Q3, варіант 1 | 🚧 W1–W6 |

**Етап T закрито цілком** (T1–T9): колір, тайли, PBR корпусу, Земля з двох
джерел, корабель з референсу.

**Поточна робота — етап `W`:** розробник відповів на Q3 (2026-08-16) варіантом
1, запіканням, і нахил переїжджає з кадру в ассет. Етап не належить жодному
мілстоуну — це борг конструкції R7c, який етап T зробив утричі дорожчим,
додавши нахилу третього споживача. Розбір і кроки — «Етап W» нижче.

Після нього — етап `Z` (зорі й видиме світило), п'ятий пункт M5 і єдиний з
решти, не заблокований нічим; розбір у «Далі».

Мілстоуни M0–M4 закриті, M5 — наполовину (Hillaire 2020 етапом S, PBR
корабля етапом T). Що з кожного етапу діє далі — у
«Рішеннях» нижче й у файлах етапів; сам хід кроків лишається в git.

---

## Рішення, що діють далі

Усе нижче — виміряне, не проголошене. Порушувати без нового виміру не можна.

### Детермінізм і збірка

- **Valgrind біжить за умовою, і умова однакова в CI та локально** — рішення
  розробника, 2026-08-15, закриває Q1. Кроки переїхали з `determinism.yml` в
  окремий `.github/workflows/valgrind.yml` з фільтром `paths` на `core/**`,
  `core-sys/**`, `core-rs/**`, `Makefile`; локальний гейт (CLAUDE.md, пункт 4)
  повторює ту саму умову рукою.

  **Чому не «або CI, або локально», як питалося.** `make valgrind` — це
  юніт-тести на C, і саме його має локальний гейт. Другий крок — пам'ять межі
  C↔Rust (D3) — **локального еквівалента не має взагалі**. Тобто прибрати з
  CI означало б втратити половину перевірки, а прибрати локально — лишити
  17 хвилин на місці. Умова знімає обидві половини задачі: на кроці, що не
  чіпає C (весь етап U, N, більша частина R), valgrind не біжить ніде.

  **Два виміряні наслідки.** Перший: 8м25с з 17м10с ішли на valgrind
  **усередині** найдовшого джоба, тож окремий workflow дає ще й паралельність
  — коли C змінився, чекати доводиться максимум, а не суму. Другий:
  `determinism.yml` отримав `paths-ignore: '**.md'`, і правка документа
  перестала коштувати повного гейта — правило «ніякого запушу лише документи»
  цим і знімається.

  ⚠ Workflow з фільтром **не створює джоб, а не створює зелений**. Required
  check у branch protection з такого робити не можна без джоба-заглушки.
- **`core/cflags.txt` — єдине джерело FP-прапорців.** `Makefile` і
  `core-sys/build.rs` читають його й більше нізвідки; `cc` не додає нічого
  свого (`no_default_flags`). Виняток один — `-fPIC`: плоский список не
  виражає «крім Windows», на арифметику не впливає (перевірено збігом хешів
  з `make`-збіркою без нього).
- **Хеші бітово однакові при `-O1`, `-O2`, `-O3`, `-Os`, `-march=native`,
  `-funroll-loops`; з `-ffast-math` — розходяться.** Тобто в ядрі немає
  нічого, що компілятор міг би переасоціювати, а звірка має зуби.
- **Збірка вимагає щонайменше `-O1`.** При `-O0` gcc кличе `sqrt` як функцію
  libm, і сценарії, що свідомо лінкуються без `-lm`, не злінкуються.
  «Зібрати без оптимізації для дебагу» тут не працює; `-O1 -g` дає бітово
  те саме.
- **Windows: потрібен тулчейн `x86_64-pc-windows-gnu`.** `cflags.txt`
  написаний під gcc; `build.rs` виявляє MSVC і падає з поясненням, а не
  збирає щось інше мовчки. Перевірено на реальному залізі: `cargo` з
  mingw-gcc дає той самий бітовий результат, що еталон з Linux, і з D2 те
  саме крутиться в CI. Задається **`RUSTUP_TOOLCHAIN`**, не `rustup default`
  і не `override set`: перші два програють `rust-toolchain.toml`, який каже
  `stable` і резолвиться в msvc. І `ucrt64\bin` мусить стояти попереду в
  `PATH` — інакше `cc` і лінкер візьмуть інший gcc, ніж `make`, і
  порівнювались би хеші двох різних збірок ядра.
- **Перевірка, яка існує заради ловіння тихих змін, мусить бачити всі свої
  входи.** Цей клас діри спіймано тричі: `watch()` у `build.rs` не стежив за
  каталогами (лише за файлами, що там були); `Makefile` не мав заголовків у
  залежностях (розсинхрон `sizeof` зруйнував купу); `$(ACTUAL)` не залежав
  від ассета (після `make cook` звірявся старий `actual.txt`). Усі три
  виправлені; правило лишається.
- **Межу «нижче X» не можна перевіряти одним прогоном.** Незбіжне число, що
  вийшло замалим, виглядає як успіх. Збіжність доводиться **згодою двох
  допусків** — саме такий `CHECK` стоїть у `test_nbody.c`.
- **Структури ядра ніхто не збирає руками.** Є `field_all_bodies`,
  `field_all_but`, `field_exclude`; заповнювати `FieldCtx` полями напряму —
  це UB, яке живе доти, доки структура не виросте. Так само `VesselParams`
  ініціалізується списком у дужках, а не полем за полем:
  `-Wmissing-field-initializers` ловить лише першу форму. Обидві помилки
  трапилися в K7b, обидві — «структура виросла, а екземпляр ні».
- **ASan і valgrind ловлять різне, і потрібні обидва.** ASan не бачить
  читання неініціалізованої пам'яті взагалі. `make asan` і `make valgrind` —
  дві різні перевірки, обидві в CI на linux-gcc.
- **Дії GitHub тримаємо на актуальному мажорі, одному на всі три workflow**
  (зараз `checkout@v7`, `upload-artifact@v7`; D5). Причина — не свіжість:
  раннер уже форсує Node 24 під діями, зібраними під 20, тобто працює не те,
  що написано в тезі, і попередження в кожному прогоні — єдине, що про це
  каже. Одна версія на всі workflow, бо різні — це три різні відповіді на
  питання «а на чому воно там виконується».
  ⚠ `checkout@v7` **блокує checkout форкового PR** для `pull_request_target`
  і `workflow_run`. Нас це не чіпає: тригери — `push` і звичайний
  `pull_request`. Якщо колись з'явиться `pull_request_target` — дивитися сюди
  першим, а не в саму дію.

### Ефемерида й ассет

- **Сирі дані контенту живуть у файловій системі, а не в git** (відповідь на
  Q5, розробник, 2026-08-16). Скуковані ассети були поза git і раніше
  (`/assets/` у `.gitignore`); тепер туди ж ідуть і **джерела** — DEM,
  мозаїки, текстури. Наслідок, заради якого це й вирішувалось: **рівень
  тайлсета більше не обирається тим, що вміщається в репозиторій**, а числом —
  пам'яттю, кількістю тайлів у bindless-масиві й часом кукання.

  **Межа проведена не за розміром, а за роллю: у git лишається те, що є входом
  для перевірки.** Ассет-фікстура ефемериди — бо на ній стоїть звірка хешів і
  скукана на іншій машині вона була б інша; оракули Horizons і GRAIL; і
  `data/lola/ldem_4.img`, доки він працює фікстурою читача PDS3 у чотирьох
  тестах. Дані, які ніхто не перевіряє, а лише малює, у git не потрапляють.

  **Чого немає й не буде мовчки:** якщо джерела немає на диску, кукер і тести
  кажуть, чого бракує й звідки воно береться, і **пропускаються**, а не
  падають — той самий прецедент, що `Gpu::for_tests`, який повертає `None` без
  адаптера. README про походження в кожному каталозі `data/` уже є (правило 8
  етапу R); до нього додається SHA-256 очікуваного файлу, щоб «у мене працює,
  а в тебе ні» зводилося порівнянням, а не переказом.

  Механізму завантаження цей крок не заводить — це борг **D18**.
- **Ассет мусить включати всі великі тіла.** Кілька тіл несуть лише частину
  імпульсу системи, тож їхній спільний барицентр лінійно дрейфує крізь SSB —
  мільярди метрів, запечені в файл. З усіма десятьма похибка Землі падає
  у 3226 разів.
- **Барицентр закріплений** (`nbody_anchor_barycentre`, типово ввімкнено).
  Прибирається лише **швидкість** (3.35 мм/с — це хибні GM барицентрів
  газових гігантів, підтверджено чутливістю): дрейф за 10 років 1.05·10⁶ м →
  9.1 мкм. Зсув від початку SSB лишається свідомо — стала не накопичується.
- **Допуск кукера — 1e-6 м, не 1 м**, і обирається за **найшвидшим тілом**.
  Метр не діяв узагалі (крок задавали приземлення на вузли підгонки), а там,
  де діяв, брехав у приємний бік: 9.32·10⁴ м проти збіжних 1.549·10⁵ м по
  Місяцю — на 40% нижче за правду, бо числова похибка гасила відсутню фізику.
- **Похибка ассета проти JPL — це фізика, якої бракує, не інтегратор.**
  `похибка ~ t^k` дає k = 1.02 (Земля) і 0.87 → 1.01 після K2 (Місяць).
  Стала відсутня сила дає k = 1; заокруглення несимплектичного інтегратора
  дало б k = 2 (і дає — оборотність росте квадратично, 16.1 м за 200 років).
  Одне число розводить два механізми.
- **Похибка чебишевської підгонки зі спаном не росте** (4.6·10⁻² → 7.6·10⁻² м
  і виходить на полицю) — вона властивість інтервалу й ступеня. Довший ассет
  **продовжує** коротший бітово; розбіжність лише на шві між інтервалами
  (2.55 см), яка є в кожному ассеті на кожній межі.
- **Кватерніон, не матриця, і ступінь 36, не 14.** Дев'ять компонент матриці,
  підігнаних незалежно, дрейфують від ортогональності; у чотирьох лишається
  один інваріант, і відновити його — `sqrt`. Позиція на інтервалі — крива,
  обертання — **хвиля**: між ступенем 24 (1.4 радіана, тобто нічого) і 26
  (6.9·10⁻⁷) обрив, не погіршення. `EphBuildReport::max_orient_error_rad`
  існує саме тому, що ступінь, обраний упритул за обривом, виглядав би
  бездоганно доти, доки хтось не подовжить інтервал.
- **Меридіан Землі виводиться з ERA (IAU 2000 B1.8), не з наближеного виразу
  NAIF.** Той помиляється на 169″ на J2000, і головне — його **швидкість**
  відхилена на 833″/століття, тобто 1842″ до J2200. Стало: 0.0″ / 1.7″ / 7.0″
  на J2000 / J2100 / J2200.
- **UT1 біжить із фіксованим зсувом від ефемеридного часу** (TT−UT1 =
  63.8286 с). Реальний зсув дрейфує ~0.7 с/рік і **непередбачувано** — де
  впаде наступна високосна секунда, не знає ніхто. Це не зрізаний кут, а
  частина, якої не існує.
- **Тіла без моделі обертання не несуть каналів узагалі** й читаються як
  точна тотожність — вісім з десяти, чотири п'ятих вартості блоку.
- **Кукер рахує на машині розробника, ассет комітиться.** Дві машини скукали б
  різні ассети (підгонка — це `cos()`), і звірка падала б на кукері, а не на
  рантаймі.

**Версії формату ассета:** 2 — гармоніки потільно; 3 — радіус і світність
потільно; 4 — ступінь орієнтації в заголовку, блок кватерніонів після
позиційного; 5 — таблиця атмосфери потільно; 6 — коефіцієнти зберігаються
нормованими (конверсія на льоту зникла), поле GRAIL до ступеня 24.

### Межа C↔Rust

- **Переліки на межі — `c_int` з константами, не `#[repr(C)] enum`.**
  Rust-енум зі значенням поза переліком — UB, а не помилка, яку видно. Сирий
  шар віддає рівно те, що сказав C; перетворення на справжній енум — робота
  `core-rs`.
- **Кожна нова функція межі друкує свій вихід у `core-sys/oracle.c`
  (`%.17g`), і тест звіряє біти.** Помилка на межі не падає: переплутані поля
  `State`, `int` замість `size_t`, переставлені `tol_m` і `h_max_s` — усе це
  компілюється й повертає числа, схожі на координати. Оракул лінкується
  **без `libm`**, як сценарії детермінізму.
- **Ефемерида в пропагаторі — `Arc`, а не лайфтайм.** Лайфтайм у структурі
  заразив би собою все, що зберігає пропагатор (CLAUDE.md: жодних лайфтаймів
  у структурах).
- **Порожній зріз ≠ порожній буфер.** Порожній зріз у Rust — вирівняний
  висячий вказівник; C визнав би це помилкою викликача, і цикл ланок не
  закінчився б ніколи. Порожність перекладається явно.
- **Пам'ять перевіряє valgrind, не Miri й не ASan-в-Rust.** Алокації живуть у
  C, Miri їх не бачить, а ASan вимагав би прапорців, відмінних від
  `cflags.txt`. (Окремо: C-тести самі ганяються під ASan/UBSan — це інша
  збірка, не та, що дає хеші.)
- **`compile_fail`-доктест проходить із будь-якої причини**, тож кожен
  прогнано ще раз без порушувального рядка — обидва мусять зібратися.

Межа несе **вісімнадцять** функцій: `eph_load`, `eph_free`, `eph_body_state`,
`eph_body_radius`, `eph_body_mu`, `eph_body_orientation`, `prop_create`,
`prop_free`, `prop_run`, `prop_run_stm`, `frame_synodic`, `frame_from_inertial`,
чотири CR3BP — `cr3bp_mu`, `cr3bp_jacobi`, `cr3bp_lagrange`, `cr3bp_zvc_radius`
— і дві поза зоною детермінізму, в окремій `libcore_planning.a` з власним
оракулом на `-lm`: `lambert_solve` і `porkchop_compute_eph` (без викликача в
грі — борг D10).

- **Геттери ассета не повертають `CoreResult`** (`eph_body_radius`,
  `eph_body_mu`): це читання поля контексту, без часу й без способу не
  вдатися. Невідоме тіло дає той самий нуль, що й тіло без розміру в ассеті —
  обидва означають «ассет не каже», і два різні значення для одного випадку
  довелося б розрізняти кожному викликачеві.
- **Синодична швидкість — фізика, а не камера.** `frame_synodic` і
  `frame_from_inertial` перетнули межу тому, що `−ω × r` поверх повороту не
  має рахуватися в Rust поруч із камерою.

- **Функції CR3BP працюють у безрозмірних одиницях, і це угода, якої не
  перевіряє компілятор** (U6b2). Метри сюди подавати можна, і константа Якобі
  вийде схожою на правду. Тому в них є числа ззовні: L4 з точної формули,
  відстані L1 і L2 від Землі (3.26·10⁸ і 4.49·10⁸ м), порядок
  `C(L1) > C(L2) > C(L3)`, і падіння `C` рівно на `v²`.
- **`CORE_ERR_TOLERANCE_NOT_MET` від `cr3bp_zvc_radius` — це відповідь, а не
  збій:** уздовж такого променя область або цілком заборонена, або цілком
  відкрита. Викликач, що трактує її як невдачу, домалює криву там, де її
  немає.
- **Два `μ` для тієї самої пари, і різницю варто знати:** з `mu` ассета
  (DE440) виходить 0.0121505843, класична стала — 0.0121505856. Різниця
  1.1·10⁻⁷ відносно, і саме вона пояснює ті 0.54 м, на які синодичні
  координати гри розходяться з формулою рушія (U6a2).

- **Бітова звірка не перевіряє змісту, і це виявилось двічі поспіль.**
  `eph_body_orientation` (R1c): спряжений кватерніон збігається з правильним
  у всьому, крім знака трьох компонент, лишається одиничним і обертає так
  само добре — в інший бік. Оракул на `%.17g` цього не бачить, бо порівнює
  межу саму з собою. Ловлять лише числа **ззовні**: полюс Землі в J2000,
  RA нульового меридіана (280.194°) і зоряна швидкість 15.0411°/год.
  Те саме, тільки дорожче, показав U5b — там однакова помилка системи
  координат стояла по обидва боки звірки й скорочувалась (ROADMAP-UI.md,
  U5b).

- **Колбек через межу не проходить, і розв'язок для цього — у C** (U5a).
  `porkchop_compute` бере два `BodyStateFunc`; замість того щоб пускати їх
  крізь межу, поруч стоїть тонка обгортка, яка сама подає `eph_body_state`.
  Колбеки лишаються **всередині** бібліотеки — рівно як `StepObserver` у
  `dop853`, — а крізь межу їде пакет: Rust дав буфер, C заповнив і повернув
  кількість. Функція з колбеками нікуди не дівається: нею ганяють тести на
  синтетичних орбітах, і саме заради них вона така.
- **`libcore_planning.a` вперше посилається на `libcore.a`** (та сама
  обгортка кличе `eph_body_state`). Детермінізму це не торкається: сценарії
  лінкують саму `libcore.a` й планування не бачать, а обидва шляхи збірки вже
  лінкують архіви в порядку, який це розв'язує.

### Рендер

- **Геометрія повітря стоїть на `rho² = r² − bottom²`, а не на радіусі** (етап
  S). Обидві відстані — до поверхні й до верхньої межі — виражаються через
  нього без віднімання великих чисел; у `f32` при `r ≈ 6.4·10⁶` одиниця
  останнього розряду квадрата дорівнює 4·10⁶, тобто біля поверхні й біля
  дотику знак тієї різниці дає округлення, а не геометрія. Коштувало трьох
  помилок поспіль, перш ніж стало правилом — подробиці й числа
  в ROADMAP-ATMOSPHERE.md.
- **Повітря малюється, лише коли його товщина в кадрі не менша за піксель**
  (S5, S7). Одна умова на всю роботу з ним — таблицю неба, об'єм і обидва
  проходи. Той самий вид критерію, що `lod::error_px`: екранна похибка, а не
  метри, бо метри довелося б підбирати на кожен радіус тіла окремо.
- **Кадр не знає про вікно.** `engine::frame` пише в текстуру; далі той самий
  кадр іде або в поверхню вікна, або в PNG. Перевіряти рендер інакше не можна:
  чорний кадр і правильний виглядають однаково. Depth-текстура належить
  `Frame`, не `app`, — інакше та сама логіка знадобилась би двічі.
- **Роздільність глибини — стала частка відстані:** `Δz ≈ z·6·10⁻⁸`, і **`near`
  скорочується**. Один прохід тримає ~7 порядків. Підбирати `near` заради
  z-fighting безглуздо.
- **Діапазони глибини лишаються, але не заради точності** — рішення
  розробника, 2026-08-15, закриває Q2. R4b виміряв, що роздільності вони не
  купують узагалі; тримає їх те, що в грі буде вигляд від корабля — з кабіни
  (можливо, з ілюмінатором) і від третьої особи, причому третьою
  користуватимуться частіше. Такий кадр несе сантиметрову деталь і тіло за
  10⁸–10¹¹ м одночасно, тобто 12–13 порядків проти семи на буфер.
  Обґрунтування цілком — PROJECT.md §7; тут лише те, об що найлегше
  спіткнутися:
  - **Проходів виходить два, а не чотири.** `plan` рахує їх з розмаху, і
    кабіна з Сонцем у кадрі дає рівно два. Решту до чотирьох додає **scaled
    space** — інша причина, не розмах: у реальному масштабі Марс займає 0.09
    пікселя на 1080p, Юпітер — 0.23, і видимими лишаються самі лише Місяць
    із Сонцем (по ~9). `MAX_PASSES = 4` тому лишається стелею, а не нормою.
  - **Ілюмінатор не додає проблеми з глибиною**, хоч і виглядає так.
    Між корпусом (~100 м) і поверхнею (4·10⁵ м) лежить порожнеча, і межа
    двох проходів лягає в неї сама — при `near` 0.05 м і `far` 1.5·10¹¹ м
    вона стає на 87 км. Кабіна з вікном коштує дорого в іншому: темний
    інтер'єр проти яскравої планети в одному кадрі (експозиція) і скло як
    прозорий матеріал. Це M5, і плутати його з Q2 не варто.
  - **`near_for` цього кадру ще не пропускає** — умова кабіни, тобто тієї
    роботи, яка її принесе; не борг.
- **Негативні твердження про глибину («цього не розрізняє») — юніт-тести в
  строгому f32, не GPU.** Клітинка, де обидві поверхні дають один біт,
  вирішується округленням драйвера (FMA, апаратний `rcp`), і той самий тест
  давав 0.000 на llvmpipe, 1.000 на RADV і на Metal. На GPU лишаються тільки
  клітинки, де різниця справді є.
- **Метр на 10⁷ м — рівно 1 ULP, і ця клітинка залежить від мантиси `near`,
  не лише від відстані** (D4). Виміряно: `near` 0.1, 0.2, 1.0 → 1 ULP;
  `near = 0.15` → **0 ULP**, клітинка зникає, і GPU-тест лишається зеленим,
  нічого не перевіряючи. Степені двійки від 0.1 бітово тотожні — це та сама
  «`near` скорочується» як факт. Отже ризик тут не нове залізо, а зміна
  ближньої площини на не-степінь-двійки; сторожа —
  `a_metre_at_ten_million_is_exactly_one_ulp` в `engine/src/depth.rs`, і
  червону дошку читати треба з нього, залізо — тільки якщо він зелений.
- **Camera-relative потрібен уже з 10⁸ м**, а не з астрономічної одиниці:
  наївний шлях там не тремтить, а **зникає** (обидві координати лягають на
  одну ґратку ULP, різниця стає нулем, об'єкт опиняється в камері).
  Camera-relative дає однакове число до останньої цифри від 10³ до 10¹¹ м.
- **Camera-relative — per-vertex, не per-object.** Камера може стояти за 10 м
  від однієї вершини планети й за 1.6·10⁷ м від протилежної одночасно. M4 це
  замінив на зсув по патчах (R1d, R1e): камера віднімається раз на патч, у
  `f64`, а зсув вершини лишається у `f32` і множиться на радіус тіла вже в
  шейдері. Прохід по мільйонах вершин на CPU щокадру неможливий.
- **Перетворення фрейму — у вертексному шейдері, дві точки входу, не гілка.**
  Траєкторія зберігається в інерціальних координатах; перемикання фрейму —
  вибір готового пайплайна на CPU. (Побічно: `mix()` з uniform у вершинній
  стадії на RADV/ACO мовчки давав порожній кадр — дві точки входу заодно
  обходять це.)
- **Колір ламаної — атрибут вершини, не uniform.** Усі записи в чергу
  відбуваються до проходу: з uniform виграв би останній запис.
- **Anchor для довгих траєкторій — тіло-в-момент-семпла, не камера.**
  Земля пролітає мільярди метрів геліоцентрично за час місії; атрибути це
  `vessel − earth` і `moon − earth` для того самого `t`.
- **Шейдери компілюються офлайн, WGSL комітиться.** Інакше кожна збірка
  вимагала б 24 МБ Slang. CI перезбирає й дивиться `git diff --exit-code`.
- **Ліміти bindless, що визначають конструкцію** (P0, wgpu 30.0): семплерів
  у масиві — **1024 на Metal, 2048 на D3D12** (мільйон на Vulkan), тож
  семплерів має бути кілька за режимами й індексуватися окремо від текстур;
  **D3D12 не дає масивів uniform-буферів** (storage — дає), тож переносити
  per-object дані в масив uniform-буферів не можна ніколи. Цілі — Vulkan,
  D3D12, Metal; GL відпадає, у нього немає жодної фічі.
- **Slang → WGSL, не SPIR-V:** `naga` не приймає capability `DrawParameters`,
  яку Slang оголошує заради `SV_VertexID`. Модулі й дженерики — властивість
  мови, а не цілі, тож вибір нічого не коштує.
- **Контрольні точки перевірки — всередині трикутника, не на вершинах**
  (растеризатор законно не фарбує піксель точно на вершині), і звіряється
  **переважання каналу**, не точний колір (поверхня вікна sRGB, ціль знімка
  лінійна).

### Фізика й пропагація

- **Ланка (`prop_run` до заповнення буфера) — одиниця всього:** інтегрування,
  поділу зі снапшотом, інвалідації при правці плану, відвантаження на GPU,
  зерна сейву. Вона несе **крок, з яким прогін її залишив**.
- **Прогноз, порізаний буфером, бітово дорівнює одному прогону** — разом із
  перенесеним кроком. Перевірено на всіх рівнях: C (H1), `core-rs` (H4),
  гра (J1, з іншим розміром ланки — 256 проти 64, щоб звірка не була
  тавтологією).
- **Ціна викинутого кроку залежить від частоти скидання:** ×70 роботи, якщо
  скидати щоланки (7148 семплів замість 101); 1.8% на десяти великих ланках;
  1.4% на маневрі. Але траєкторія щоразу **інша**, і це головне.
- **Стеля кроку (`h_max_s`) мусить бути заданою.** При нулі інтегратор бере
  за стелю довжину ланки, і зшитий прогін дає бітово ту саму траєкторію, але
  **інший перенесений крок** — наступний виклик продовжить кроком, якого
  ніхто не обирав.
- **Семпли — це прийняті кроки інтегратора, а не рівномірна сітка.**
  Приземляти кроки на обрані часи означає забрати крок у контролера похибки
  (виміряно на кукері: з примусовими вузлами зміна допуску на два порядки не
  міняла нічого). Адаптивний крок сам густішає там, де траєкторія
  загинається — рівно там, де ламаній потрібні вершини.
- **Зупинка на події не залежить від розміру кроку:** зменшення стелі вчетверо
  зсуває перицентр на **8.3 мкс** при кроках у тисячі секунд. Ціна названа
  чесно: зупинка змінює послідовність кроків після себе — це задумана
  поведінка, бо той самий план відтворюється однаково.
- **Подія, на якій прогін стартував, — не подія.** Класифікація з запасом у
  вісім порядків: `|g| < 10⁻¹²·|d|·|ḋ|`. Інакше гравець, що крокує від
  апоцентра до апоцентра, не зрушив би з місця.
- **Озброюються лише події, потрібні плану.** Маркери для ока шукаються
  скануванням уже порахованих семплів: озброєна подія змінює послідовність
  кроків.
- **`prop_run_stm` дає бітово ту саму траєкторію, що `prop_run`** — контролер
  кроку рахує норму похибки лише по опорному блоку, тож шість варіаційних
  блоків їдуть тією ж послідовністю, не голосуючи за неї. Планувальник, що
  виправляє маневр матрицею від трохи іншої траєкторії, цілив би туди, де
  апарата немає.
- **`prop_run_stm` не має ні подій, ні буфера семплів.** Питання «куди дійде
  зміна початкового стану до `t_end`» стосується однієї ланки з двома
  кінцями; подія обірвала б її там, де викликач не просив.
- **Горизонт прицілювання — пів оберту, не наступний вузол.** Ціль у
  наступний вузол коштує 5.5·10⁹ м/с, пів оберту — 1e-4. Дев'ять порядків за
  одне ціле число, і ніщо в рівняннях на це не натякає.
- **Індекс стабільності каталогу JPL — це (λ + 1/λ)/2, а не λ.** Орбіта 1151:
  індекс 297, множник за оберт 594.
- **Слабко нестійкі орбіти не тікають експоненційно:** одинична пара власних
  значень дефектна (жорданова клітина), тож типове зміщення дрейфує лінійно
  й надовго перекриває малий множник.
- **Чим орбіта нестійкіша, тим легше вона коригується** в повній ефемериді —
  протилежно до очевидного здогаду. Половина сімейства (орбіти 0 і 383) не
  піддається multiple shooting узагалі, і це зафіксовано тестом як відмова.
- **Бюджет station-keeping лінійний у похибці виведення** (1 км → 0.035 м/с
  на рік; 10 км → 0.348) з точністю до відсотка.
- **Межа детермінізму проходить по пропагації, не по плануванню.** Lambert,
  porkchop і диференціальна корекція можуть давати трохи різні числа: їхній
  результат — це дані (`час, Δv`). Але `target_hit` живе в `core/` верхнього
  рівня, а не в `core/planning/`: у ньому немає тригонометрії, а детермінізм —
  властивість коду, не викликача.
- **Lambert: `z` має жорстку стелю трохи нижче 4π².** `C(z) ≥ 0` для будь-якого
  `z > 0` — підручникове читання про «еліптичну гілку» хибне; без стелі
  Ньютон стрибає в корінь **іншого** рівняння (гілки з повним обертом), який
  так само проходить усі перевірки знаків. Розв'язано брекетингом плюс
  safeguarded Ньютоном усередині.
- **`prograde` — це знак z-компоненти моменту імпульсу**, а не «коротка чи
  довга гілка».
- **Клітинка porkchop, де Lambert не зійшовся, пропускається.** Заборонені
  зони — очікувана частина плоту, і плот існує саме щоб їх побачити.
- **Власна гравітація точки старту дає NaN.** `r1` — це буквально позиція
  Землі з ассета, отже `mu/0³`. Контекст поля збирається без тіла
  відправлення й прибуття: їхня яма — окрема, не змодельована фаза.

### Модель сил (K)

- **Гармоніки — рекурсія Пайнса, не Каннінгема** (той ділить на
  `sqrt(x²+y²)`, сингулярне на полюсі). Напрямні косинуси `s,t,u`, похідні
  трикутників (`A'`, `A''`) — продиференційована та сама рекурсія, не окреме
  джерело помилки.
- **Рекурсія нормована, і це не стиль запису, а межа існування.** У
  ненормованій формі прискорення стає NaN приблизно з 44 ступеня (`Re^n`
  виходить за `DBL_MAX` на n = 50 при місячному радіусі), тож `(Re/r)^n`
  рахується разом, а не `Re^n` і `r^-n` окремо. Стеля — `HARMONICS_MAX_DEGREE`
  50. Бітової рівності з ненормованою формою не буває **за побудовою**
  (нормування множить на `sqrt` ірраціональних), тож критерій там — збіг на
  рівні заокруглення плюс незалежний аналітичний J2.
- **Під опорною сферою ряд розбіжний**, і `harmonics.c` тримає там `(Re/r)`
  не вище одиниці. Це свідома заглушка: рельєф, що заходить під опорний
  радіус, силовій моделі нічого чесного не скаже.
- **Поле рахується в системі самого тіла:** `r` повертається кватерніоном з
  ассета, прискорення повертається назад, гессіан — `R H Rᵀ` двома проходами
  `quat_rotate`. Полюс Місяця відхилений від z на **23.46°**, тобто без
  повороту це різниця між полем і шумом; для Землі — 6.4·10⁻⁷ від самого J2.
  Тіло без каналів обертання лишається бітово тим самим (тотожність додає
  літеральний нуль).
- **Робочий ступінь Місяця в ассеті — 24, і він обраний виміром** (оберт на
  100 км проти повного поля 50): 38 м похибки за 4.1 мкс проти 0 за 13.5 мкс.
  Файл GRAIL несе всі 50; підняти константу в кукері й перекукувати — уся
  зміна, якщо місія колись літатиме нижче.
- **Тіла в кукері рухаються під точковими масами плюс J2 Землі.** Дати
  Місяцю його поле означало б погіршити ефемериду (2108 → 2453 м проти JPL
  за 210 діб): орбіту формує тривісність **у парі з лібрацією**, а
  `body_rotation.c` лібрації не має (D9). Кукер при цьому повертає поле у
  власну систему тіла — саме відсутність повороту зробила місячне поле
  шкідливим.
- **Гессіан виведений групуванням у п'ять груп, не розкриттям у 27 доданків.**
  Кожна група симетрична за (i,j) сама по собі, тож симетрія результату —
  властивість виведення; кожна однорідна за степенем `r`, тож доданок із
  неправильним степенем виглядає як невідповідність розмірності.
  Безкоштовні перевірки без еталона: симетрія (бітово) і слід = 0 (Лаплас).
- **Коефіцієнти — параметр ассета тіла, не поле `PropConfig`.** Форма Землі —
  властивість Землі, а не налаштування виклику; інакше два викликачі літали б
  біля двох різних Земель. `field_set_harmonics` видалено з цієї ж причини;
  лишився `field_clear_harmonics` — для вимірювання, не для конфігурації.
- **`eph_build` пише саме ті коефіцієнти, з якими інтегрував.** Апарат і тіла
  не можуть розійтися у формі Землі — не «домовились», а немає способу.
- **J2 Землі тягне Місяць — і тягне назад Землю.** Реакція явна в `nbody.c`;
  пропустити її означало б непомітно ламати збереження імпульсу.
  `nbody_energy` має відповідний доданок, виведений із тієї самої похідної.
- **Безмасова пробна частинка не відстежує Землю точно, і це фізика.** Справжня
  Земля в ассеті відчуває реакцію свого J2 на всі тіла, а частинка — ні.
  Спроба ввімкнути гармоніки Землі для частинки, що стоїть на Землі,
  відхиляється (`CORE_ERR_INVALID_ARG`), а не приймається мовчки.
- **J2 Землі дав Місяцю ×4 точності** (1.396·10⁵ → 3.454·10⁴ м за 10 років) і
  очистив показник росту з 0.87 до 1.01 — тобто лишився один ясний підпис
  відсутньої сили, і це поле Місяця вищих степенів (K5).
- **Нахил полюса в зональному полі не має значення** — виміряно тимчасовим
  нахилом усього десятирічного куку на найгірші 3.3′: похибка Місяця
  3.454·10⁴ → 3.485·10⁴ м, **менше за 1%**. Зональне поле питає лише полюс;
  меридіан скорочується. Для тесеральних членів K5 і атмосфери K7 — ні.
- **`field.c` обертання ще не читає.** K5 буде першим викликачем.
- **SRP без тригонометрії коштувало роботи, а не обережності.** Частка диска,
  закрита тілом, — площа перетину двох кругів, класично через арккосинуси.
  `srp_acos` — власний многочлен (A&S 4.4.46), похибка **виміряна проти
  `libm`**: 2.179·10⁻⁸ рад. Модель тіні звірена не з тією ж плоскою формулою,
  а з **точною сферичною геометрією**, проінтегрованою в тесті: 1.262·10⁻⁴
  найгірше (апарат просто над поверхнею, Сонце рівно на лімбі).
- **Градієнт SRP свідомо не диференціює саму тінь.** Відкинутий член на шість
  порядків більший за лишений, але на шість порядків менший за точково-масовий
  градієнт Землі, і живе рівно ті секунди, поки апарат перетинає півтінь.
  Гладкий член лишено, щоб `field_gradient` лишався точною похідною
  `accel_field`, а не похідною сусіднього поля.
- **Тінь від найтемнішого затуляча**, не добуток (заниження) і не сума
  (безглуздя). Межа точна, доки два тіла не закривають Сонце з різних боків.
- **Світність потільно, не поле «яке тіло є Сонцем».** Апарат відчуває тиск
  від кожного тіла з додатною світністю; поняття «Сонце» в коді немає, тож
  його нема з чим переплутати. Радіус нуль = «ассет не каже», і таке тіло не
  кидає тіні замість кидати тінь вигаданого розміру.
- **`VesselParams` їде на кожен прогін, не в `PropConfig`.** Маса змінюється
  при горінні, а `/game` тримає один пропагатор на всі апарати. `NULL` =
  безмасова пробна частинка, бітово те, що було.
- **SRP коштує +5.9% на `accel_field`** (2105 → 2230 нс). Побічно виміряно
  головніше: одне `accel_field` — 2.1 мкс проти 0.641 мкс на **крок** у
  `make bench`, тобто ціну задає ефемерида (десять чебишевських обчислень),
  а не інтегратор, і рядок продуктивності нижче міряє не те, що платить гра.
- **Структура, жодне поле якої не читається, гірша за свою відсутність.**
  `VesselParams` не існувала до K6a саме тому; `Scene` не має списку тіл, поки
  в ассеті немає радіусів; `Plan` не з'явився в J1, бо його не було чим читати.
- **Густина — пошарова експонента з власним `exp`, за прецедентом `srp_acos`.**
  Лінійна інтерполяція ρ між вузлами дає велику **відносну** похибку (густина
  падає на ~14 порядків), а інтерполяція `log ρ` вимагала б `exp()` назад.
  Ряд рахує `exp(+x)` і ділить, а не альтернує (при x = 1 члени альтернативного
  ряду сягають 1 при сумі 0.368); зведення діапазону — половинням, бо `×0.5`
  точне; коефіцієнти `1/k!` не виписані, а виходять діленням на індекс циклу.
  Похибка проти `libm::exp` **виміряна**: 2.477·10⁻¹⁴. Відсічка на шести
  половиннях стоїть там, де значення нульове (2·10⁻²⁸ кг/м³), а не там, де
  скінчилось терпіння. Дані — US Standard Atmosphere 1976 до 1000 км.
- **Смуги таблиці — розриви, і це стосується кожного, хто біля них міряє.**
  Модель на базі смуги розривна до 10⁻³ відносних, тож скінченна різниця
  через шов міряє **стрибок, а не нахил** (0.46 замість 6·10⁻⁹). Сама
  таблиця перевіряє себе: 27 швів мусять сходитися й сходяться до 9.6·10⁻⁵.
- **Швидкість відносно атмосфери, не інерціальна** (`v_rel = v − ω × r`).
  Знак `ω` не закріплює в кодовій базі ніщо інше — його тримає оракул
  «апарат, що летить разом із повітрям»: у нього `v_rel` точно нульовий, тож
  прискорення з `cd` **бітово дорівнює** прискоренню без нього.
- **`∂a/∂r` від опору несиметричний, і це звуження обіцянки `field_gradient`.**
  Позиція входить у силу лише через густину, тобто зовнішній добуток рангу 1;
  `∂a/∂v` навпаки симетричний. Тест вимагає, щоб матриця **була**
  несиметричною, інакше пізніше «причісування» віддзеркалило б її половину.
- **`CORE_EVENT_ALTITUDE` міряє від середнього радіуса ассета** — того самого,
  від якого міряє висоти атмосфера, не від опорного радіуса гармонік (для
  Землі це різні числа, для Місяця вони розходяться на 470 м). Тіло без
  радіуса — `CORE_ERR_INVALID_ARG` при озброєнні: нуль мовчки перетворив би
  подію на `DISTANCE`. Розрив таблиці події не заважає — вона не читає
  густину, а Ньютон користується `g` і `g'`, тоді як стрибок сидить у `g''`.
- **`density_scale` — стала на прогін, і нуль відхиляється.** Множник живе в
  `PropConfig`, їде в сейв (завантажена гра, що летить крізь іншу густину, —
  те саме порушення, що свіжий крок інтегратора) і чекає на галочку космічної
  погоди в грі. Модель сонячного циклу — це `sin`, тобто гра, не фізика.
  «0 = типове значення» вважало б незадану структуру правильною, тому всі
  `PropConfig` ініціалізуються дужками.
- **Липкий прапорець, якого ніхто не читає, — не запобіжник.** `sc_trajectory`
  ганяв multiple shooting крізь `accel_field_var`, який відмовив і повертав
  нулі; траєкторія вийшла пряма, Ньютон збігся, хеш порахувався — перевірка
  детермінізму стабільно відтворювала однакову дурницю. Тепер сценарії
  перевіряють `field.failed`.

### Планування й імпульс

- **Планування живе в окремій `libcore_planning.a` з власним оракулом, який
  лінкується з `-lm`.** Злити його з наявним означало б утратити те, заради
  чого той лінкується без libm: саме лінкування є перевіркою рантаймової зони.
- **`lambert.o` не залежить від `libcore.a`** — `vec3_*` там `static inline`.
  Порядок архівів у лінкуванні тому не має значення (перевірено `nm -u`), хоч
  і мав би, якби ці помічники були зовнішніми.
- **Перестановка *імен* однотипних параметрів у `extern`-блоці не є мутацією.**
  Аргументи передаються позиційно. Тест виглядає перевіреним, не будучи ним;
  мутація мусить міняти те, що перетинає межу, а не як воно зветься.
- **Δv у 11.7 км/с там, де має бути 4.5, — це не помилка Ламберта, а площина.**
  Орбіта відльоту, що не містить цілі, платить половину Δv за поворот площини,
  і Ньютон на такій задачі не збігається взагалі. Тест будує орбіту в площині
  перельоту, як і реальне проєктування місій.
- **Ньютон по Δv біля Місяця вимагає половинного кроку.** З повним промахи
  стрибають (1.2·10⁷ → 4.4·10⁶ → 2.2·10⁶ → 6.2·10⁶), з половинним падають
  монотонно до 10⁴ м за сім ітерацій. Це та сама нелінійність, через яку
  реальні місії роблять корекції, а не один точний імпульс.
- **Оракул на базис VNB не може бути тією ж зовнішньою задачею.** Перекласти
  інерціальний Δv у VNB тим самим базисом, яким гра його розгортає, — і
  помилка скоротиться сама із собою. Тому оракул тут двотільний і
  алгебраїчний: прямий імпульс дає `|v|+Δv`, нормальний — `sqrt(|v|²+Δv²)`,
  і переставити їх, не впавши, неможливо.
- **Власний дрейф гри проти одиночного прогону — 25 м** на тридобовому
  перельоті до Місяця: світ ріже прогін на ланки по заповненому буферу, прогін
  ішов одним викликом, і біля Місяця різниця підсилюється. Помилка в 10⁻⁶ від
  Δv дає вже 1194 м. Обидва числа виміряні мутацією, і поріг тесту стоїть між
  ними.

### Гра й потоки

- **Годинник не входить в інтегратор.** Майбутнє рахується **до** того, як до
  нього дійде час; час повзе курсором по вже порахованій ламаній. Наслідок:
  фіксований крок симуляції не потрібен взагалі, а частота кадрів, warp і
  пауза не можуть змінити жодного біта (перевірено в чотирьох варіантах:
  5 fps проти 500, смикані кадри, змінний warp, пауза).
- **`t_end` ніколи не походить від годинника.** Зуби перевірено: котючий
  горизонт «30 діб уперед від зараз» — найприродніший спосіб це написати —
  валить усі три перевірки частоти кадрів, і не падає сам, а просто робить
  траєкторію функцією fps.
- **Горизонт міряється в ланках** (`LEAD_LEGS = 4`). Скільки це діб, залежить
  від траєкторії; число вирішує, **скільки** прогнозу існує, і ніколи — які в
  нього числа.
- **Порядок усередині кроку: спершу порахувати, потім рушити час.** Навпаки —
  гра затиналася б через кадр при цілком достатній пропускній здатності.
- **Тік рахує поступ, а не спробу.** Апарат, що не рухається (вимкнений
  маневр, `t_end` на місці), зациклював прогін назавжди, і зависання
  читається як «щось задумалось», а не як помилка.
- **Тік по колу між апаратами**, а не першому в списку: інакше дев'ятий апарат
  гравця не рахувався б ніколи. Розмір бюджету на числа не впливає взагалі.
- **Інтерполяція Ерміта, не пряма:** посередині між семплами хорда відхиляється
  на **17.4 км**. Саме тому кожен вузол несе швидкість. Тест вимагає обох
  тверджень — точності у вузлі й **не**виродженості посередині.
- **`Leg` несе `entry`** — стан, з якого ланка почалася. Без цього перший
  відрізок після маневру брав би лівим вузлом швидкість **до** імпульсу й
  малював дугу, якою апарат не летів.
- **Історія й прогноз — одні й ті самі ланки**, розділені порівнянням
  `sample.t` з курсором. Ні перерахунку, ні копіювання, ні руху в сторі.
- **Правки в минулому відхиляються** — на цьому й стоїть недоторканність
  історії: переписуються лише ланки прогнозу, бо інших правити не дозволено.
- **Фрейм Δv — обов'язково відносно тіла** (`Frame::Vnb { body }`). У
  барицентричних координатах швидкість апарата біля Землі — це переважно
  швидкість Землі навколо Сонця, і «вперед» означало б уздовж земної орбіти.
- **Курсор не ходить назад ніколи.** Доти снапшот чесно каже `Stall::Horizon` —
  «рахую», а не «підгальмовую».
- **Зерно сейву — остання межа ланки не пізніша за курсор**, і жодне з трьох
  слів не зайве: не кінець порахованого (прогноз у сейв не входить, інакше
  завантажена гра стрибає вперед на весь горизонт), не сам курсор (з довільної
  точки продовжити бітово неможливо).
- **`applied` лежить у файлі числом.** Стан до й після імпульсу мають однаковий
  час, тож «застосувати все, що не пізніше» виконало б маневр удвічі, а «все,
  що раніше» — жодного разу.
- **Числа в сейві — шістнадцяткові біти, десяткові поруч коментарем.**
  Десятковий друк `double` — справа форматувальника, а не IEEE (той самий
  аргумент, з якого CSV не входять у звірку детермінізму).
- **Читання снапшоту — 13.8 мкс у найгіршому випадку** при насиченій нитці
  (1.9 млн читань за 300 мс). Під `Mutex` тут було б очікування кадру на тік
  симуляції.
- **Команди вичерпуються до `world.step`**, тож коміт плану ніколи не приходить
  усередину інтегрування. Саме тому `commit_plan` може безпечно переписувати
  `tip` і `applied`.
- **Планувальник не має свого сегментного циклу** — той самий `World`, та сама
  ефемерида, той самий `PropConfig`, той самий `step`, спільна точка
  перезапуску (`leg::restart_at`). Дві реалізації того самого правила
  розійшлися б там, де ціна найвища. Прев'ю бітово дорівнює майбутньому
  польоту — без цього планувальник безглуздий.
- **Скасування — між ланками, а не після прогону.** Гравець тягне вузол, і
  запити летять десятками за секунду; доходить рівно останній.
- **У тесті з нитками не можна припускати, коли команда подіє.** Двічі
  спіймано: перевірка проходила **до** обробки коміту (чекала на кількість
  ланок, яка й так уже була — чекати треба на слід нової роботи, якого не
  могло бути раніше), і тест із круглим номером доби впав на повільнішому
  раннері, бо курсор устиг його проїхати.
- **Гонки даних немає не тому, що її шукали**, а тому що спільного
  мутабельного стану немає: світ належить нитці, назовні — незмінні снапшоти,
  всередину — команди каналом. `Propagator` — `Send`, але не `Sync`, тож
  компілятор просто не дав би поділити його між нитками.

### Слід і історія (етап N, борг D7)

- **Слід — вікно навколо курсора, і міряється воно обертами**
  (`world::HISTORY_REVOLUTIONS`, типово 20), не добами й не мегабайтами. Доба
  оманлива — 5100 семплів на LEO проти 720 на місячному перельоті, — а
  обертами вікно підлаштовується під режим само: двадцять обертів це доба з
  гаком на низькій орбіті й **двадцять місячних місяців** на halo біля L2.
  Мегабайти ж гравець не може співвіднести ні з чим; він мислить витками.
- **Оберт міряється сумою кутів між сусідніми геоцентричними
  радіус-векторами**, а не перицентрами: працює і на замкненій орбіті, і на
  перельоті, і не питає, де перицентр у halo-орбіти навколо L2 — питання, на
  яке немає доброї відповіді.
- **Проріджування обмежує вершини на виток, а не на місію.** Станція намотує
  ~1450 витків за сто діб, і кожен на карті — окрема дуга; критерій чесно
  лишає на неї 4–8 вершин. Тому слід, що покриває всю місію, лінійний за
  ігровим часом **за будь-якого представлення**; сталим його робить лише
  вікно. Виміряно: сам критерій дав ×3.2, вікно — ×70.
- **Три речі на ланку недоторканні, доки ланка жива:** `entry`, останній семпл
  і `step_out`. На них стоїть `restart_at`, тобто сейв, — і саме тому оракул
  проріджування це **сейв байт у байт**, а не кількість семплів. Вікно ж
  викидає ланку цілком, тож `Trajectory::start` переїжджає на `entry`
  найстарішої вцілілої.
- ⚠ **Двері в один бік, і це не про код.** Інваріант 5 забороняє інтегрувати
  минуле вдруге, тож викинуте не відновлюється ніколи. `N` обертів — не
  налаштування продуктивності, а вибір того, скільки минулого гравець хоче
  бачити.
- **Позиція семпла в кадрі не залежить від часу кадру — і в обертовому фреймі
  теж.** Синодичний базис будується з лінії Земля-Місяць **самого семпла**, а з
  кадру бере лише сталі `SYNODIC_SCALE_M` і `μ`. На цьому стоїть кеш
  прорідженого сліду, і перевірено це бітово: історія, намальована на 20-й
  добі, збігається з намальованою на 5-й вершина в вершину, хоч лінія пари за
  цей час повернула на 200°.
- **Допуск проріджування — у метрах, не в пікселях.** У пікселях він залежав
  від напрямку погляду, тож кеш помирав від кожного оберту камери мишею. У
  метрах — `tol_px · d / focal_px`, і просторове відхилення ніколи не менше за
  екранне, тож допуск консервативний за побудовою. Ціна консервативності
  виміряна: ×3.2 стало ×1.6, і обмін свідомий — без кеша сам критерій коштує
  415 мс на кадр проти 9 мс, які він економить.
- **Попіксельне порівняння оракулом для ламаної бути не може.** Лінія
  завтовшки в піксель, зсунута на дозволені пів пікселя, міняє більшість своїх
  пікселів: 358 з 1226 при формі, що не змінилась. Оракул питає інше — чи
  лишився **біля** кожного засвіченого пікселя одного кадру засвічений піксель
  другого.
- **Пенсія ланок — політика (`World::set_retirement`), а не завжди-увімкнено.**
  Її вимикають тести, що звіряють потік семплів; жоден із них не про фізику —
  біти, які порахували, пенсія не міняє. Побічний наслідок виміряно: маркери
  подій у минулому грубішають, бо парабола по трьох семплах на хордах у сотні
  секунд точнішою бути не може (395 с розбіжності проти 60).
- **Чорної скриньки немає, бо немає читача**, і коли з'явиться — одиниця в ній
  та сама, оберти. Вимкнення — двері в один бік, і скринька v1 живе лише в
  сесії: інакше сейв виросте рівно на ті гігабайти, яких J6 у нього не пускає.

---

## Далі

Деталізувати наперед немає сенсу: G1 показав, що навіть один крок M3 вартий
свого підрозділу; решта віддається на той самий розгляд, коли до неї дійде
черга. Закриті етапи розписані окремими файлами — [U](ROADMAP-UI.md) (M3),
[R](ROADMAP-PLANETS.md) (M4), [S](ROADMAP-ATMOSPHERE.md) (частина M5); окремі
вони тому, що два паралельні етапи не мають правити один розділ.

### Що з планування етапу V лишилось чинним

Сам розбір переїхав у розділ «Етап V» — там кроки, числа й те, чим вони
скінчилися. Тут лишається одне рішення, ширше за етап.

**Слід малюється на карті, а не з корабля** — рішення розробника, 2026-08-16.
Вигляд від першої й третьої особи траєкторій не показує взагалі: ламана на пів
неба з кабіни — це не інформація, а сітка перед очима. Наслідок записаний
наперед, бо режимів сьогодні двоє лише на папері (`ViewFrame` перемикає фрейм
карти, а не камеру): **найдорожча робота кадру стає умовною**, тобто D15 і
числа сліду — це числа карти, а не гри взагалі. Карта може дозволити собі
більше за бюджет 60 Hz — гравець на ній не пілотує; це не привід не
проріджувати, але привід не платити за карту в польоті.

### Що з планування етапу T лишилось чинним

Сам розбір переїхав у розділ «Етап T» — там кроки, числа й те, чим вони
скінчилися. Тут лишається те, що ширше за етап і що етап свідомо не робив.

**Космодроми варто робити спочатку як дані, не як моделі.** Таблиця
майданчиків (широта, довгота, висота, азимути) → початковий стан у системі
тіла, яка вже обертається. Це дає геймплей одразу: **широта майданчика
визначає досяжне нахилення**. Модель майданчика впирається у дві речі, яких
немає: місцевість метрового масштабу під нею (при 9.8 км на вузол вона стояла
б на плямі) і **тяга** — злітати з неї нічим.

**Два пункти, названі при плануванні й не зроблені**, обидва — текстура, а не
геометрія, і обидва чекають на рішення розробника, а не на код:

- **двовимірний шар хмар** — уже в плані (PROJECT.md §7: об'ємні не в v1);
  найбільший візуальний ефект на гроші з усього списку;
- **вогні міст** — формально «без міст» вони не порушують, бо це текстура;
  але «просто природа» прочитано як позицію, тож вирішує розробник.

### Решта M5

З п'яти пунктів M5 (Hillaire 2020, PBR корабля, факел, вхід в атмосферу,
**небо без повітря**) етап S закрив перший, етап T закриває другий. Лишаються
факел, вхід і небо; факел чекає на **тягу в `/core`**, якої немає: `accel.h`
має про неї лише коментар, а імпульсний маневр триває нуль секунд, тож факел
довелося б малювати за вигаданим таймером. Небо не чекає ні на що —
розбір нижче. Подробиці — ROADMAP-ATMOSPHERE.md, «Що M5 винен».

### Етап Z — зорі й видиме світило (M5, п'ятий пункт)

Питання розробника, 2026-08-16: скайбокс і Сонце, видиме в камеру, — це M5 чи
окремий M5.5? **Рішення: п'ятий пункт M5, окремим етапом `Z`; M5.5 не
заводимо.** Три причини, і третя практична:

- **M5 — це «атмосфера і матеріали», тобто небо й світло.** Зоряне тло є
  небом там, де повітря немає, а видиме Сонце — джерелом світла всього кадру,
  яке досі живе в `Scene` лише напрямком (`Scene::sun`). Одна система.
- **Дробовий мілстоун заводять, коли робота міняє модель.** M3.5 таким був:
  після повної моделі сил інші всі числа. Тут не міняється нічого — фізиці
  байдуже, чи намальовані зорі.
- **Це єдиний шматок M5, не заблокований нічим.** Решта чекає на корабель і
  тягу; цей етап можна брати одразу після T. Виносити його в окремий
  мілстоун означало б, що M5 після T стоїть цілком.

Літера `Z` (зорі) вільна. Кроки — коли до етапу дійде черга; тут те, що вже
відомо й що дорого з'ясовувати заново:

- **Сонце як джерело світла вже є, нове — диск і його яскравість.** Диск на
  порядки яскравіший за освітлену поверхню, а кадр має **один** прохід
  тонмапера (Reinhard, T6). Без експозиції диск упреться в білий і матиме
  той самий колір, що й пустеля під ним. Тобто етап тягне за собою експозицію
  — ту саму, якої чекають кабіна й скло.
- **Зорі мусять гаснути в повітрі.** Прохід неба (етап S) уже рахує
  пропускання; зоряне тло малюється **під** ним і множиться на нього, інакше
  вдень зорі світять крізь блакить. Це головна точка зчеплення, і саме вона
  вирішує, куди прохід лягає у frame graph.
- **Сонце — не `Body`.** Тіло в `bodies` тягне патчі, LOD і відбір за лімбом;
  на 1.5·10¹¹ м це не потрібне нікому. Диск малюється на нескінченності, як і
  небо.
- **Каталог, а не шум** — правило 8 етапу R (дані з походженням і ліцензією)
  діє й тут. Yale BSC5 — ≈9 тис. зір до 6.5ᵐ, дрібний асет, і сузір'я на
  своїх місцях. Процедурний шум дав би зорі, але не небо.
- **Інерційне тло — безкоштовний оракул орієнтації камери.** На чорному кадрі
  помилку в обертанні камери не видно взагалі, на зоряному видно одразу. Той
  самий клас, що фікстура в несиметричній точці (D13, D14): перевірка коштує
  стільки ж, а ловить те, чого чорний кадр не ловить ніколи. Це аргумент
  робити етап **рано** серед решти M5, а не останнім.

### M6 — механіка невизначеності

Прототип уже є в ядрі (`core/uncertainty.c/h`, `Φ P Φᵀ` через уже перевірену
STM), і він показав межу моделі: сталий коефіцієнт стиснення проходу —
недостатня модель трекінгу біля сильно нестійкої орбіти (σ сягає 10¹⁴ м до
кінця семиобертової місії). Реальні місії тому стежать частіше ближче до
критичної події. Виміряна відповідь на приклад PROJECT.md §8: стиснення еліпса
в 3.16× дає **втричі дешевшу корекцію** (1.947 → 0.616 м/с на рік). Чого
прототип не робить: жодної моделі вимірів, жодного orbit determination.

---

## Відкриті борги

**Розбір за моментом закриття, 2026-08-16.** Борг закривають не тоді, коли він
дратує, а тоді, коли з'являється крок, якому він і так по дорозі:

| борг | коли | чому саме тоді |
|---|---|---|
| **D19** масив текстур коштує щокадру | з наступним тайлсетом: поріг «друге тіло» вже пройдено (T7h) | **1.85–2.07 мс при бюджеті 16.7** на двох тілах — найбільша стала стаття кадру; лікується резидентним набором, а не меншим асетом |
| **D18** віддалене сховище даних | коли дані знадобляться другій машині або CI | сьогодні джерело качає розробник руками один раз; ціна з'явиться там, де його доведеться дістати без нього |
| **D17** смуги аеральної перспективи | з наступною роботою над повітрям, і тепер вона на черзі | **виміряно з 400 км** (T7h, `--example earth_orbit`): у надирі це концентричні кільця розмахом 5.9 байта проти 2.2 у тієї самої сцени без повітря — найпомітніша річ у кадрі з орбіти |
| **D8** обертання двічі | з тягою в `/core` | той крок і так переписує `accel_field`; бітово не змінює нічого |
| **D15** ламані без відбору, **D11** ZVC щокадру, **D12** поверхню не видно у вікні | зі стадією карти | усі три — ціна **режиму карти**, а слід малюється на карті, не з корабля; D11 і D12 до того ж вимагають фікстури, якої немає (камера біля станції, камера з вибором цілі) |
| **D9** лібрація Місяця | коли розвилка ефемериди стане живою | єдина названа відсутня фізика на 200-річному горизонті; сьогодні від неї не залежить нічого |
| **D10** `porkchop_compute_eph` | коли стане відомо про міжпланетний етап | видалити чи лишити — питання про гру, не про код |
| **D3** Metal на залізі | коли з'явиться Mac | зробити не можна нічого; конструкція вже стоїть на гіршому з відомих чисел |

Нумерація стала — коміти на них посилаються. Закритий борг звідси зникає, а
те, що він з'ясував, переїжджає в «Рішення, що діють далі»; номер не
перевикористовується.

Питання, на які може відповісти лише розробник, живуть окремо —
[QUESTIONS.md](QUESTIONS.md), з тими самими правилами й нумерацією `Q*`. Різниця
проста: борг — це робота, яку ніхто не зробив; питання — це рішення, якого
ніхто не ухвалив. Обидва списки переглядаються наприкінці кожного великого
кроку (CLAUDE.md, «Порядок роботи», пункт 6).

- **D19. Bindless-масив коштує за своїм розміром, а не за тим, що намальовано.**
  Виміряно на T8: колірна піраміда Місяця (8190 текстур) додає **0.39 мс до
  кадру навіть тоді, коли тіло займає кілька пікселів**, і ціна росте лінійно
  з кількістю текстур (≈51 нс на текстуру за кадр) — а час кодування
  командного буфера при цьому не міняється взагалі, тобто платить драйвер при
  прив'язці. У кадрі малюються десятки патчів, тож прив'язується на два
  порядки більше текстур, ніж читається.

  **Переміряно на T7h, з двома тілами:** Місяць 10 236 текстур дає 0.71–0.73
  мс, Земля 16 380 — 1.00–1.03, обидва разом 26 616 — **1.85–2.07 мс, тобто
  11–12% бюджету**. Ставка виявилась вищою за виміряну на одному тілі (61–78
  нс проти 51), а кадр із двома тілами коштує ще й трохи більше за суму двох
  однотільних. Тобто борг більше не дрібний: він найбільша стала стаття кадру.

  **Чому все ще не зараз.** Робота тут не дрібна:
  резидентний набір означає перепакування масиву щокадру (які тайли вибрав
  LOD) плюс перевідображення індексів у шейдері, тобто новий інваріант між
  CPU і GPU. Купувати його за 2.5% рано.

  **Поріг пройдено.** Земля прийшла з тією ж глибиною й чотирма каналами;
  ціна прив'язки від розміру текселя не залежить, тож подвоїло її саме
  **друге тіло з тайлами**, а не канали — рівно як передбачалось. Числа —
  вище в цьому ж пункті.

- **D18. Джерела даних лежать на диску, і взяти їх нізвідки.** Відповідь на
  Q5 (2026-08-16) вивела сирі дані контенту з git, і це рішення; чого воно не
  зробило — то це способу їх дістати. Сьогодні `data/lola/ldem_4.img` ще в
  git як фікстура, але мозаїка Землі приїде на диск розробника руками, і на
  другій машині її не буде взагалі.

  **Що його закриє:** реліз GitHub + SHA-256 у репозиторії + скрипт
  завантаження — рівно патерн `scripts/fetch_slang.sh`, який у проєкті працює
  рік, тільки з хешем замість тегу. Трафік релізів для публічного репо
  безкоштовний, на відміну від LFS; чому саме не LFS — п'ять причин зважено
  в Q5, і вирішальна перша: сім чекаутів на пуш з'їдають місячну квоту, тобто
  гейт починає падати з причини, що не стосується коду.

  **Коли:** коли дані знадобляться другій машині або CI. Доки кукер запускає
  розробник на своїй, борг нічого не коштує — і саме тому він борг, а не крок.

  **Чому це борг, а не питання:** вибір уже зроблений (2a в Q5), лишилась
  робота.

- **D17. Об'єм аеральної перспективи смугує поверхню з низької орбіти.**
  Знайдено демо-анімацією V2, а не етапом S, і причина в тому, чого етап S не
  міг побачити: його знімки дивилися **на** планету або **з** поверхні, а
  анімація вперше поставила камеру на 400 км і повела погляд уздовж лімба, де
  об'єм марширує крізь повітря на всю довжину, а поверхня займає пів кадру.

  **Що саме виміряно:** той самий кадр з `air: None` — гладкий, з
  `Some(Atmosphere::EARTH)` — у концентричних смугах на океані. Тобто це не
  затінення сфери й не рельєф (його там немає взагалі), а **32 шари** об'єму
  (`atmosphere::AERIAL_SIZE`).

  **Переміряно на T7h числом** (`--example earth_orbit`, Земля з тайлами й
  повітрям з 400 км): у надирі лишок після зняття гладкого тренду має розмах
  **5.9 байта проти 2.2** у тієї самої сцени без повітря. Кільця концентричні
  навколо надира, тобто йдуть по сталій **дальності**, — це і є межі шарів
  об'єму. З орбіти вони найпомітніша річ у кадрі, помітніша за саму мозаїку.

  **Що його закриє:** або більше шарів, або джитер позиції семпла в шарі —
  але **вибирати треба виміром, а не смаком**: обидва коштують у місці, яке
  S7 виміряв як найдорожче (500 км, 0.39 мс), і обидва мають оракул у
  пікселях, бо смуги видно.

  **Чому це борг, а не питання:** рішення тут ухвалювати нема чого — смуг
  бути не має. Ніхто просто не дивився з цього ракурсу.

- **D15. Ламані не відбираються ні за фрустумом, ні за чим іншим.** Патчі
  планети відбирає compute-прохід за лімбом і фрустумом (R6b,
  `Frame::draw` → `planet.cull`); у ламаних відбору немає взагалі.
  `Lines::upload` (`engine/src/frame.rs`) проганяє **кожну** вершину **кожної**
  ламаної крізь `camera.relative` і переписує весь буфер, а `Lines::draw`
  видає `draw` на кожну ламану цілком. GPU відкидає позаекранне після
  вершинного шейдера — тобто CPU-прохід і смуга пам'яті сплачені повністю.

  **Знайдено питанням розробника під час N2, а не виміром**, і це різні речі:
  на фікстурі N1 борг не проявляється взагалі — камера на 10⁹ м тримає весь
  флот у фрустумі, тож відбір там зекономив би нуль. Проявиться він там, де
  камера опускається до станції: halo-траєкторія за 4·10⁸ м коштуватиме тих
  самих мілісекунд, лишаючись невидимою.

  **Це не те саме, що N2, і одне одного не замінює.** Проріджування прибирає
  вершини, які видно, але вони зайві (сусідні ближче за пів пікселя); відбір —
  ті, яких не видно взагалі. На далекій камері працює перше, на близькій —
  друге.

  **Що його закриє:** відбір ламаної за габаритом (`AABB` на ланку) до
  `upload`, і число, зняте на камері біля станції — тобто **нова фікстура
  виміру**, бо наявна його не бачить. Робити його всередині ланки не варто:
  ланка — одиниця всього (CLAUDE.md), і половина ланки в буфері зробила б
  `draw` на ламану неправильним.

- **D3. Metal на справжньому GPU** не перевірений: у таблиці P0 на цьому
  рядку — `Apple Paravirtual`, пристрій віртуалки раннера.

  **CI цього не закриє, і це з'ясовано, а не припущено.** Раннери macOS у
  GitHub — віртуальні машини й на arm64 теж; перезапуск `gpu-probe` дасть
  той самий рядок. Отже борг чекає на залізо, і єдине, що можна зробити без
  нього, — назвати, **що саме його спростує**.

  Спростує рівно одне з двох: реальний Metal показує **менше за 1024**
  текстури в масиві, або в нього немає `PARTIALLY_BOUND`. Тільки ці два
  числа міняють конструкцію. Решта таблиці — ні: 1024 семплери вже
  найменший ліміт із трьох цілей, і саме тому семплери індексуються окремо
  від текстур (PROJECT.md §7). Тобто конструкція вже побудована на гіршому
  з відомих чисел, і краще число нічого в ній не зрушить.

  Що робити, коли з'явиться Mac: прогнати `gpu-probe`, звірити ці два поля,
  замінити рядок у таблиці P0. Якщо котресь не збіглося — це не уточнення
  таблиці, а зміна конструкції bindless, тобто новий крок, а не правка
  цього рядка. Борг відкритий свідомо: це названий ризик із перевіркою, а не
  пробіл, і закриє його перший запуск на живому Apple GPU, більше нічого.

- **D9. Модель Місяця без фізичної лібрації, і тепер це видно.**
  `body_rotation.c` несе середнє обертання; для **поля апарата** це коштує
  0.1% масконного сигналу, тобто нічого (кадр, зсунутий на кутову хвилину,
  дає 3.5·10⁻⁸ м/с² проти 2.8·10⁻⁵ самого масконного сигналу). Але в
  **кукері** поле Місяця без лібрації робить ефемериду гіршою проти JPL
  (2108 → 2453 м за 210 діб), бо орбіту формує тривісність у парі з
  лібрацією.

  **Що його закриє:** фізична лібрація в `body_rotation.c` — і тоді те саме
  порівняння з JPL має **покращитись**, інакше причина не в ній. Доти поле
  Місяця в кукері лишається вимкненим, і це записано в `cook_fixture.c`.

- **D8. Обертання тіла читається двічі за одне обчислення поля.** Опір
  бере `eph_body_angular_velocity`, гармоніки — `eph_body_orientation`, для
  того самого тіла в той самий момент, і кожне з них розгортає чебишевські
  канали ступеня 36 по чотирьох компонентах. Виміряно окремо: ~310 нс (K7b)
  і ~200 нс (K5d) на обчислення поля, при 725 нс на точкові маси. Біля
  Місяця вага цього падає — там саме поле коштує 4.1 мкс (K5e).

  **Що його закриє:** одне читання на тіло за виклик `accel_field`, з якого
  беруться і кватерніон, і його похідна (`cheb_eval_deriv` уже дає обидва за
  один прохід). Бітово не змінює нічого — ті самі числа, менше разів, — і
  саме тому це борг, а не крок фізики.


- **D12. Поверхню, яку гра вмикає, у вікні не побачити.** Борг почався з
  іншого — гра ставила `TileSet::Smooth` кожному тілу, — і **та половина
  закрита**: `game/src/app.rs` завантажує при старті обидва тайлсети Місяця
  (T3b) і обидва тайлсети Землі (T7g) і вмикає їх через
  `view::attach_terrain`.

  **Лишилось те, що виявилось окремою роботою:** орбітальна камера гри
  (`engine::orbit`) дивиться тільки на Землю здалеку, тож Місяць з неї —
  кілька пікселів, а Земля показана з відстані, з якої рельєфу немає сенсу.
  Побачити поверхню сьогодні можна лише знімком (`--shot --moon <км>`,
  `--example earth_orbit`). Камера з вибором цілі — це інтерфейс, тобто
  окремий крок, і саме він закриє борг.

  **Чим це коштувало вже зараз:** усі числа U8 зняті зі сцени **без
  планетного рельєфу**, тобто висновок «панелі найдорожчі в кадрі» правдивий
  рівно для того кадру, який гра малює сьогодні. З рельєфом планета додасть
  0.25–0.36 мс (R8), а два тайлсети — ще 1.85–2.07 мс прив'язки (D19).

- **D11. Крива нульової швидкості перераховується щокадру.** Знайдено виміром
  U8, а не читанням коду: у обертовому фреймі `view::build_in` коштує 0.323 мс
  проти 0.015 в інерціальному, і **0.249 мс з них — це ZVC**, тобто 77%
  усього проходу підготовки сцени. На вершину виходить 689 нс проти 10.56 нс
  у точки траєкторії — крива не малюється дорого, вона дорого **рахується**
  (пошук перетину променя з рівнем `C` для кожного променя).

  **Чому це борг, а не властивість.** Крива залежить рівно від `μ` і `C`:
  перше — константа пари тіл, друге змінюється лише тоді, коли апарат
  маневрує. Тобто щокадрове перерахування дає той самий результат, і його
  ніхто не просив — це робота, якої ніхто не зробив (кеш), а не ціна, яку
  хтось обрав.

  **Що його закриє:** крива, яка перераховується при зміні `C` (і `μ`), а не
  щокадру, і вимір, що `view::build_in` в обертовому фреймі впав до порядку
  інерціального. Обережність одна: `C` — число з плаваючою комою, і ключем
  кешу має бути саме воно, а не «змінився план», інакше кеш проґавить дрейф.

  **Чого борг не означає.** Це не привід рахувати криву на GPU й не привід
  її спрощувати: 362 вершини — правильна густина, питання лише в тому, скільки
  разів на секунду їх шукати.

- **D10. `porkchop_compute_eph` лишився без викликача в грі.**
  (У комітах U5b/U5c названий «D9» — номер уже належав місячній лібрації.
  Історію не переписуємо: у тих повідомленнях «D9» означає саме цей борг.)
  Функція була правильна для того, чим здавалася задача: розгортка тіло→тіло
  в координатах ассета. Гра питає інше — переліт **від апарата** й у
  координатах **центрального тіла**, — а стани відходу приходять із
  траєкторії, не з ассета; розгортка переїхала в Rust, над `lambert_solve`.

  Отже на межі є функція, яку кличуть лише її власні тести, а це рівно те,
  що CLAUDE.md називає гіршим за відсутність. **Але видаляти її зараз
  передчасно:** міжпланетне вікно (Земля→Марс, PROJECT.md §8) — це саме
  тіло→тіло, і там центр ассета за 1.1·10⁹ м від Сонця на тлі 1.5·10¹¹ м,
  тобто наближення чесне.

  **Що його закриє:** або перший справжній викликач (міжпланетний плот), або
  видалення разом із `porkchop.c`, його тестами й згадкою в «Межа C↔Rust».
  Вирішувати не раніше, ніж стане відомо, чи буде в грі міжпланетний етап.

  **Не борг, а урок, і він більший за саму функцію:** збіг двох шляхів через
  межу не доводить нічого про вибір системи координат, бо однакова помилка
  скорочується з обох боків. Оракулу потрібне третє число ззовні.

**Закрито** — те, що вони з'ясували, лежить у «Рішення, що діють далі»:

- **D1** оракул Ламберта й фізика імпульсу; **D2** cargo determinism у CI на
  Windows; **D4** сторожа під однобітову клітинку глибини; **D5** дії GitHub на
  Node.js 20; **D6** бенчмарк силової моделі.
- **D7** (слід росте з ігровим часом) — 2026-08-16, етапом N цілком. Не
  «повільніше», а полиця: на сцені з 31 апарата історія на 50-й добі більша,
  ніж на 101-й. 23.7 → **0.33 мс** на кадр, 82.4 → **1.95 МіБ**, 830 920 →
  **11 948** вершин. Зробили це чотири речі, і жодної не було в первісному
  плані повністю: фікстура, у якій борг видно; екранний критерій із кешем на
  ланку; пенсія ланок; **вікно в обертах** — і лише останнє зробило «не росте»
  правдою.
- **D13** (тіло зникає з кадру при наближенні) — 2026-08-15, `74f7101`.
  `lod::error_px` міряла відстань по чотирьох кутах і центру патча; над
  **центром грані** це випадково правильно, а за двадцять градусів убік
  вибірка казала 758 км там, де під ногами 170. Найгірша похибка на 32
  напрямках: 35.57 px → 0.996 px.
- **D14** (відбір викидав патч, над яким стоїть камера) — 2026-08-16,
  `e9f94a7`. `cos(β − α)` при оці **всередині** конуса дає кут до краю шапки
  замість нуля; грань нульового рівня має `α = 54.7°`, тож умова спрацьовувала
  для всього, що ближче за `1.7 R`. Рід D13 і його ж виправлення, застосоване
  наполовину.

⚠ **Спільний урок D13 і D14 більший за обидва:** уся фікстура рушія ставила
камеру рівно над центром грані куба — єдину точку, де хибна відстань до патча
давала правильну відповідь. Вада була не в тому, що перевірок мало, а в тому,
що вони **всі однакові**. Нова перевірка геометрії тіла мусить мати хоч один
несиметричний напрямок **і хоч одну малу висоту**: помилки цього роду
вимагають широкого конуса й близької камери водночас.

---


## Етап W. Запечений нахил 🚧

**Відповідь розробника на Q3, 2026-08-16: варіант 1 — запікання.** Нахил
перестає бути обчисленням кадру й стає полем ассета: кукер бачить усі грані
одразу й розв'язує кут куба **один раз, офлайн**, а шейдер читає нахил так
само, як висоту. Нумерація `W*` стала.

Чому саме зараз, а не «колись»: нахил читають уже **три** споживачі — амплітуда
процедурної деталі (R7c), вибір рівня (R7c) і правило матеріалу (T4), — і в
смузі навколо восьми кутів куба всі три двозначні. Запікання розв'язує їх одним
полем; будь-яка інша відповідь довелося б повторювати тричі.

### Що зонд показав до планування, і чим це гірше за формулювання Q3

Q3 казав «на куті сходяться три грані, не чотири». Виміряно точніше
(тимчасовий зонд по `Patch::halo_node`, кут `(1,1,1)`, грані 0, 2, 4):
навколо кутового вузла лежать **рівно три** сусіди на відстані вузла — по
одному на кожному з трьох ребер куба, що сходяться. А стенсил кожної грані
має чотири арми, тож **дві з них падають в один і той самий вузол**:

| грань | −a | +a | −b | +b |
|---|---|---|---|---|
| 0 (+X) | N₁ | N₂ | N₃ | **N₂** |
| 2 (+Y) | N₂ | N₁ | N₃ | **N₁** |
| 4 (+Z) | N₂ | N₃ | N₁ | **N₃** |

Тобто центральна різниця на куті **не центральна**: кожна грань рахує
`√((hᵢ − hⱼ)² + (hᵢ − h_k)²)` зі своїм півотом `i`, і три відповіді різні не
через похибку, а через вироджений стенсил. Ті 39% — це не «дві грані
розійшлись», це три різні формули. Отже середнє трьох теж не відповідь:
усереднювати нема чого.

**Правильна відповідь на куті — підгонка градієнта по трьох сусідах.** Три
напрямки в дотичній площині кута лежать рівно під 120° (тривісна симетрія куба
навколо `(1,1,1)` точна, а не наближена), тож найменші квадрати дають
замкнену формулу `g = (2/3L)·Σ hᵢ·d̂ᵢ`, у якій висота самого кута
скорочується — рівно як у звичайної центральної різниці. Вона **точна для
лінійного поля**, тобто має той самий клас оракула, що й уся решта.

⚠ Оракулом не може бути теперішня рампа: її фікстура лінійна за **параметрами
грані**, а на куті параметризації трьох граней різні, тож там вона не є
гладким полем узагалі. Потрібна друга фікстура — **лінійна в 3D**
(`h = A·(d·k)`), у якої аналітичний нахил `(A·scale/R)·sin θ` відомий у
кожній точці, включно з кутом.

### Кроки

| Крок | Зміст | Ризик | Стан |
|---|---|---|---|
| W1 | Ціна формату: `Rg16Sint` і сітка без ореолу на обох вендорах | 🟢 | ✅ |
| W2 | Версія 4: нахил у вузлі, `slope_at` читає замість рахувати | 🟡 | |
| W3 | Кут куба розв'язаний один раз, і фікстура, що це доводить | 🟡 | |
| W4 | Кадр читає нахил: `Rg16Sint`, один `Load` замість дванадцяти | 🟡 | |
| W5 | Ореол зникає з обох тайлсетів — **за умовою W1** | 🟡 | |
| W6 | Перекукати, переміряти, закрити документи й борги | 🟢 | |

#### W1. Скільки коштує другий канал, і чи економить ореол ✅ (2026-08-16)

`--tile-probe` уже вмів питати це в заліза; додано `Rg16Sint` і **сітку як
вимір таблиці** (35×35 з ореолом проти 33×33 без нього). Відповідати на це
припущенням не можна було: T2 виміряв ×3.34 від даних, і арифметика з цього
множника обіцяла подвоєння пам'яті DEM обох тіл.

**Ореол не коштує нічого — і це головна знахідка кроку.** 33×33 і 35×35 дають
**той самий байт на тайл**: у кожному з трьох форматів, на кожній із трьох
глибин, на обох вендорах. Ті 12.5%, які ореол важить у файлі, у пам'яті GPU не
існують узагалі — обидві сітки лягають в один блок гранулярності. Отже W5
лишається **лише спрощенням формату й диском**, і купувати пам'ять ним ніхто
не буде.

**Другий канал коштує рівно один крок гранулярності, і вендори різні:**

| формат | NVIDIA (дискретна) | RADV (інтегрована) |
|---|---|---|
| `R16Sint` — висоти нині | 8192 | 8192 |
| `Rg16Sint` — висоти + нахил | **12 288** | **16 384** |

Тобто ×1.5 і ×2, і платить із пам'яті гри саме інтегрована. У числах сцени:
Місяць 16 → 24/32 МіБ, Земля 64 → 96/128 МіБ, **разом +40 МіБ (NVIDIA) і
+80 МіБ (RADV)**. Це і є ціна відповіді на Q3, названа до того, як формат
змінився; розвилка «нахил окремим байтом у колірний тайл» не знадобилась.

⚠ **Час прив'язки масиву від формату не залежить взагалі** — 1.0–1.1 мс на
8190 текстур у всіх семи рядках. Це підтверджує сам механізм боргу **D19**
з іншого боку: драйвер платить за **кількість** текстур, а не за їхній розмір,
тож запечений нахил не додає до D19 нічого.

#### W2. Нахил у вузлі: версія 4

`SSDEM` версії 4 несе на вузол пару `(i16 висота, i16 нахил)`. Одиниця нахилу —
стала рушія, не поле заголовка: нахил безрозмірний, і його діапазон однаковий
у будь-якого тіла (виміряні медіани — 0.035 Місяць, 0.0030 суша Землі), тож
поле заголовка було б параметром, який ніхто ніколи не змінить.

**Рахує нахил `Terrain::build`, а не кукер.** Записувач формату вже отримує
сітки **з ореолом** і бачить усю піраміду, тож інваріант «нахил — одне число»
належить йому, і всі чотири кукери (Місяць, Земля × висота) дістають його
задарма, без жодної правки.

⚠ **Запечене значення бітово дорівнює теперішньому в вузлах, і це не збіг.**
Теперішній `slope_at` бере різницю на базі **найдрібнішого** кроку піраміди й
перераховує її в координати свого тайла (`delta_nodes`); у цілому вузлі
білінійна вибірка на `±δ` дає рівно `δ·(h[x+1] − h[x−1])`, а множники
скорочуються в центральну різницю на **власному** кроці тайла. Тобто W2 не
міняє чисел, він міняє, хто їх рахує — і саме тому наявні оракули лишаються
чинними без правки.

Різниця з'являється лише **між** вузлами: патч, глибший за піраміду, тепер
інтерполює нахил білінійно, а не диференціює інтерпольовану висоту. Обидва
неперервні, обидва однакові у вузлах.

Наслідки в тому ж кроці: `delta_nodes` і `units_at` помирають, `slope_rise`
перетворюється на одиницю квантування нахилу.

#### W3. Кут куба — один раз, офлайн

Пост-прохід у `Terrain::build`: для кожного з восьми кутів куба на кожному
рівні зібрати три вузли, що на ньому лежать (по одному на грань), порахувати
нахил **один раз** підгонкою по трьох сусідах і записати те саме число в усі
три тайли. Бітова однаковість тоді за побудовою — це копія, а не збіг
обчислень.

Довжина арми береться **номінальна** (`step_m` рівня), як і скрізь: варп
кубосфери робить вузли нерівними до 1.4×, і теперішня формула цим уже нехтує
свідомо. Брати на куті справжню довжину означало б зробити кут точнішим за
середину грані — тобто ввести нову неоднорідність замість знятої.

Оракули:

- **фікстура, лінійна в 3D** (`h = A·(d·k)`): нахил у кутовому вузлі
  відхиляється від аналітичного не більше, ніж нахил у звичайному вузлі
  поруч — «кут не гірший за середину грані»;
- **кутовий вузол несе бітово одне число в усіх трьох гранях**;
- наявний `the_slope_is_one_number_from_both_sides_of_an_edge` **втрачає
  виняток `tainted`**: смуги більше немає, збігатися мусить усе.

#### W4. Кадр читає нахил

`Texture2D<int2>`, `Rg16Sint`, `sample_height` і `sample_slope` зливаються в
одну вибірку: висота й нахил лежать у тому самому текселі, вікно в них одне,
тож чотири `Load` дають обидва числа замість двадцяти. `units_at` зникає й з
шейдера, `PatchData::window_delta` — з буфера, `uniforms.detail.y` міняє сенс
з `slope_rise` на одиницю квантування.

Перевірка — наявний оракул «GPU і CPU дають те саме число» плюс кадр зондів:
байти мусять лишитись тими самими скрізь, **крім** смуги навколо кутів.

#### W5. Ореол — за умовою W1

Після W4 ореол не читає ніхто: висоту він ніколи не ніс (`sample_height`
свідомо затискає по сітці патча), колір бере його лише вагою нуль, а
градієнт переїхав у кукер. Отже `STORED` перестає бути геометрією **файлу** й
лишається геометрією **входу `Terrain::build`**, а обидва тайлсети худнуть на
12.5%.

Робиться це, лише якщо W1 покаже, що воно щось купує, або якщо W4 покаже, що
без цього формат читається важче. Інакше — рядок у «Чого етап свідомо не
робив»: 12.5% файлу не варті версії обох форматів одразу.

#### W6. Перекукати, переміряти, закрити

`make cook-dem`, `make cook-earth` (ассети в git не їдуть, гейт вони не
міняють), кадр зондів, `--perf-probe` і `--tile-probe` — і числа в документи.
Тут же переоцінка D19: масив тепер несе стільки ж текстур, але важчих, і
виміряти треба саме прив'язку, а не пам'ять.

---


## Етап T. Матеріали й поверхня ✅

Що з планування етапу лишилось чинним — «Далі»; тут кроки. Нумерація `T*` стала.

Порядок диктував одне: доки в `Scene` не було кольору, ні тайлам, ні альбедо,
ні PBR не було куди приїхати. Далі йшов шлях Місяця — його висоти вже були
локальні, кукер уже був, — а Земля остання, бо їй потрібні обидва джерела
разом, DEM і колір.

| Крок | Зміст | Ризик | Стан |
|---|---|---|---|
| T1 | Колір — поле `Body`, а не стала рушія | 🟢 | ✅ |
| T2 | Колірний тайлсет: формат і кукер (Місяць) | 🟡 | ✅ |
| T3 | Колір у кадрі: другий bindless-масив тим самим шляхом | 🟡 | ✅ |
| T4 | Процедурна деталь кольору — правило матеріалу | 🟡 | ✅ |
| T5 | Матеріали й PBR: перша справжня модель корабля | 🔴 | ✅ |
| T6 | Планетне сяйво на корпус | 🟡 | ✅ |
| T7 | Земля: DEM і колір — обидва джерела разом | 🔴 | ✅ |
| T8 | Перемір і закриття | 🟢 | ✅ |
| T9 | Корабель з референсу: форма й фарба | 🟡 | ✅ |

### T1. Колір — поле тіла, а не стала рушія ✅ (2026-08-16)

`frame::COLOUR` було `[0.2, 0.6, 0.9, 1.0]`, зашите в кадр від F5 — тобто **всі
тіла світу мали один колір за побудовою**. Тепер це `Body::colour`, як радіус
і орієнтація (R1e). Стала лишилась публічною, але вже лише як колір фікстур:
зонди й тести мусять давати той самий кадр, що до кроку.

- **Оракул вимагає двох тіл, і це не надмір.** Колір їде в uniform із
  динамічним зсувом на прохід, тож помилка «останній викликач виграв» на одному
  тілі не видна взагалі — та сама, через яку колір ламаних став атрибутом
  вершини (J1). Тест малює два тіла й вимагає, щоб кожен колір лишився на
  своєму, а тоді міняє кольори місцями й вимагає, щоб кадр помінявся теж.
- ⚠ **Бік екрана оракул питає в камери, а не виводить з осей.** Перший підхід
  припустив, що `+y` світу лягає ліворуч; насправді праворуч, і тест упав на
  власному здогаді. Тепер очікувану колонку дає `Camera::to_screen` — та сама
  функція, якою V2 міряв силует корабля.
- **Гра вибирає колір за індексом тіла** — тим самим шляхом, що й повітря (S1),
  і з тієї ж причини: в ассеті кольору немає. Місяць сірий, Земля синя;
  невідоме тіло дістає сірий, а не чорний, бо чорна планета читається як діра
  в небі, тобто як помилка рендера.
- Кадр зондів рушія лишився `30812bf2…`.

На це поле чекала не лише поверхня: альбедо неба стояло в нулі з поясненням
«кольору поверхні в `Scene` немає взагалі», і зняв його саме звідси T7h.

### T2. Колірний тайлсет: формат і кукер ✅ (2026-08-16)

Дрібні кроки: **T2a** — стеля масиву й рівень (✅), **T2b** — джерело на диску
й читач, **T2c** — формат версії 3, **T2d** — кукер і його оракул.

#### T2a. Стеля масиву виміряна, рівень названий ✅ (2026-08-16)

`engine/src/tile_probe.rs`, `cargo run --release -p engine -- --tile-probe`:
піраміди на 5, 6 і 7 рівнів у трьох форматах, на **кожному апаратному
адаптері** — дискретна NVIDIA і інтегрована RADV, бо в першої своя VRAM, а
друга бере ту саму системну пам'ять, з якої вже живе гра.

**Заявлена стеля виявилась не при чому.** `max_binding_array_elements` —
1 048 576 (NVIDIA), 8 388 606 (RADV), 1 000 000 (llvmpipe); найменше з трьох
у тридцять разів більше за найглибшу піраміду, яку ми взагалі розглядали.

**Вирішує інше число: тайл коштує ×3.34 від того, що несе.** Однакове на
обох вендорах, у всіх трьох форматів і на всіх трьох глибинах: 1225 байтів
даних у `R8Unorm` → 4096 у пам'яті, 2450 у `R16Sint` → 8192. Тобто 35×35 —
**не «маленька текстура», а чотирикілобайтова**, і це властивість
гранулярності драйвера, а не формату.

| рівнів | тайлів | км/вузол | дані R8 | пам'ять | резерв | створення |
|---|---|---|---|---|---|---|
| 5 | 2046 | 7.6 | 2.4 МіБ | 8 МіБ | 0 | 11 мс |
| **6** | **8190** | **3.8** | **9.6 МіБ** | **32 МіБ** | 0 | **38 мс** |
| 7 | 32 766 | 1.9 | 38 МіБ | 128 МіБ | 256 МіБ | 165 мс |

**Узято рівень 6, один канал.** Рівень 7 купує вдвічі дрібнішу сітку за
вчетверо більшу пам'ять і вчетверо довше завантаження, і зверху резервує
256 МіБ замість нуля. А головне — **екрана не досягає жоден із них**: при
камері на 100 км і кадрі 720 пікселів один екранний піксель накриває 160 м
поверхні, тобто рівень 6 дає 24 пікселі на вузол, рівень 7 — 12. Різницю
однаково закриває правило матеріалу (T4), і платити за половину недосяжної
відстані 256 МіБ — найгірший обмін у таблиці.

**Один канал, а не три, і це властивість джерела, а не економія.** Глобальна
мозаїка LROC WAC монохромна (643 нм), тож RGB зберігав би той самий байт
тричі. Виміряна ціна цього: `Rgba8Unorm` бере 12 288 байтів на тайл у NVIDIA
і 16 384 у RADV проти 4096 в одноканального. ⚠ І трибайтового формату
текстури **не існує взагалі** — ні `Rgb8` у wgpu, ні відповідника у Vulkan,
який можна семплювати без розширень. Отже «35²·3» — це розмір у файлі; у
пам'яті колір округлиться до чотирьох каналів.

Наслідок для коду, який доведеться внести в T3: `MAX_TILES` у `frame.rs` — 4096,
а `Gpu::new` просить `max(default, 4096)` елементів. Обидва числа взяті під
2046 тайлів рельєфу й під 8190 не проходять.

#### T2b. Джерело на диску й читач ✅

⚠ **Колірне джерело в git не лежить** — на відміну від висот, і за Q5 воно
туди й не потрапить.

Продукт названий і перевірений: `WAC_GLOBAL_E000N1800_016P.IMG`, LROC RDR
BDR, 16 пікселів на градус (**1.9 км/піксель**), 66 МБ — тобто вдвічі
дрібніший за вузол рівня 6, як і належить джерелу. README з походженням і
SHA-256 — за правилом 8 етапу R.

⚠ **Етикетка вбудована, а не окрема.** `.LBL` поруч дає 404; PDS3-заголовок
лежить у голові самого `.IMG`. Читач `data/lola` розбирає **окремий** `.lbl`,
тож спільним кодом це не стане без правки — і саме тут, а не в кукері.
Друга відмінність: `SAMPLE_BITS = 32`, значення — відбивна здатність
дійсними числами, а не цілими.

#### T2c. Формат кольору ✅ (2026-08-16)

**Розвилку закрито виміром:** окремі тайлсети зі спільною геометрією піраміди.
Причина числова, а не смакова — у висот і кольору **різні глибини** (5 проти 6)
і різні типи відліку (`i16` проти `u8`), тож один файл із двома каналами мусив
би або зрівняти глибини (вчетверо роздути висоти нічим), або нести дірки.

⚠ **Версія рельєфу при цьому не росте, і це виправлення плану, а не недогляд.**
План казав «версія до 3»; він писався до того, як розвилка закрилась. Файли
розділені підписом (`SSCOL` проти `SSDEM`), тож переплутати їх неможливо, а
підняти `VERSION` означало б відкинути кожен уже скукований `.dem` заради
зміни, якої в ньому немає. Колірний формат починається зі своєї версії 1.

Спільне винесене у **вільні функції** `tiles::{count, index, covering,
window}` — параметром `levels`, а не методом. Дженерик тут заборонений стилем
і не потрібен: спільного рівно одне — де в піраміді лежить тайл патча.

- **Кількість каналів — поле заголовка**: Місяцю один, Землі (T7) чотири. Три
  не буває: трибайтового формату текстури немає ні в wgpu, ні у Vulkan без
  розширень, і **у файлі лежить рівно те, що піде в текстуру** — той самий
  принцип, що вже несуть висоти (R5c).
- **Відлік цілий, `scale` у заголовку.** Джерело дійсне, і зберігати його так
  означало б учетверо більший тайл заради динамічного діапазону, якого в
  поверхні немає. Виміряно на всій мозаїці: медіана 0.044, p90 = 0.076,
  p99.9 = 0.197, хвіст до 0.599 — це 0.09% пікселів. При `scale = 0.25` крок
  становить 0.00098, тобто **23 градації на контраст море-материк**, а хвіст
  насичується в білий. Витратити 96% шкали на 0.09% пікселів було б гірше.
- **Тайл кольору не несе меж (min/max), на відміну від рельєфу**: у висот їх
  читає радіус затуляння (R3a), у кольору читача немає, а поле, якого ніхто не
  читає, гірше за свою відсутність.

#### T2d. Кукер і оракул ✅ (2026-08-16)

`cargo run --release -p dem-cook -- --colour` → `assets/moon.col`: **6 рівнів,
8190 тайлів, 9.6 МіБ**, рівно як обіцяв T2a. Обхід спільний із висотами
(`cook::direction`), і це не заощадження: вузол кольору мусить лежати рівно
там, де вузол висоти, інакше вони зсунулись би один відносно одного на пів
вузла — і виглядало б це як помилка джерела.

**Оракули ті самі, що в R5b, і три з чотирьох не потребують джерела:** `Albedo`
складається руками з полів, тож детермінізм, «два шляхи — одне число» і
відсутність шва (ребро + ореол) перевіряються всюди, а не лише там, де на диску
лежать 66 МБ. Обидва зламані навмисне варіанти — переставлені `Edge::AMin`/
`AMax` і зсув на вузол усередині патча — тести спіймали.

⚠ **Насичення шкали є на будь-якій глибині піраміди, і це не властивість
дрібності.** Грубий рівень нічого не усереднює — він бере ту саму точкову
білінійну вибірку, просто рідше. Перша версія перевірки очікувала на двох
рівнях нуль і впала на чотирьох вузлах. Виміряно: 4 з 36 750 на двох рівнях
(0.011%) і **552 з 10 032 750 на шести (0.0055%)** — обидва на порядок менші
за 0.09% сирих пікселів понад 0.2, бо вибірка **між** пікселями джерела
усереднює четвірку сусідів.

Висоти лишаються на 5 рівнях, доки їх джерело — `ldem_4` (7.6 км на піксель):
глибша піраміда з нього не дістане нічого, чого в ньому немає.

### T3. Колір у кадрі ✅ (2026-08-16)

Дрібні кроки: **T3a** — стеля масиву, **T3b** — другий масив і шейдер,
**T3c** — усереднення грубих рівнів.

#### T3a. Стеля масиву під найглибшу піраміду ✅

`MAX_TILES` було 4096 (під 2046 тайлів висот), кольору треба 8190; те саме
число просить `Gpu::new`. Запасу нуль **свідомо**: рівень 7 — це 32 766
тайлів, і T2a виміряв, що він того не вартий, а саме оголошення стелі не
безкоштовне — дескрипторний набір росте разом із нею.

Перевірка на відмову стелі не стерегла ніколи — вона проходила б і при 4096, і
при 64. Тепер поруч стоїть близнюк, який 8190 тайлів **вантажить**. Обидва
беруть піраміду з нулів: питання про кількість текстур, а не про вміст, і сім
рівнів із LOLA означали б сорок мільйонів вибірок у тесті, який жодної не
читає (набір тестів рельєфу з цього схуд до 2.9 с).

#### T3b. Другий масив, друге вікно ✅

Колір — `binding_array` поруч із висотами, **у тій самій групі 1** (⚠ bindless
не ділить групу з буфером, що має динамічний зсув, R7a). Одна група — один
хендл: два незалежні дозволяли б стан «колір цього асета з рельєфом чужого».

- **Патч несе два вікна, і це те, заради чого формати розділені.** Піраміди
  різної глибини, тож той самий патч законно читає висоту в предка, а колір —
  у власному тайлі.
- ⚠ **Порядок полів у `PatchData` диктує std430:** `vec2` вимагає
  восьмибайтового вирівнювання, тож скаляри йдуть першими, і структура лишається
  48 байтів замість 64. Звірка — у згенерованому `patch.wgsl`.
- ⚠ **Крок буфера зламав `cull.slang`**, який тримає власну копію структури —
  рівно як попереджає коментар у ньому, включно з тим, який тест упаде.
  Він і впав, першим же кадром.
- **Оракул шва довелося переробити.** З ореолом, пофарбованим як продовження
  рампи, викинутий зсув на `HALO` не спіймав **жоден** з трьох тестів: вузол —
  це 0.35° широти, тобто 0.6 одиниці яскравості, менше за будь-який поріг,
  що відрізняє шов від самої рампи. Тепер ореол фарбується кольором, якого
  немає в сітці, і та сама помилка малює світлу лінію по кожному ребру патча.
- **Сірий на екрані = одиниця зберігання, і це заглушка з названою ціною.**
  Фізична відбивна здатність Місяця 0.02…0.18; намалювати її як є означало б
  майже чорний диск, бо експозиції в кадрі немає взагалі. Ділення на `scale`
  зберігає **контраст**, тобто те, що крок додає. Справжня експозиція — T5, і
  забрати заглушку треба саме там.
- Кадр зондів лишився `30812bf2…`.

#### T3c. Грубі рівні усереднюють, а не тицяють ✅

⚠ **Усі інваріанти трималися, а картинка була не та.** Здалеку Місяць виходив
плямистим шумом замість карти, на якій видно моря, і причина була в кукері:
вузол рівня 0 накриває 85 км, тобто дві тисячі пікселів мозаїки, а кукер брав
із них один. Жоден оракул цього не бачив — усі вони звіряють ту саму точкову
вибірку саму з собою.

Стало: **ланцюг удвічі грубіших сіток**, і рівень читає найгрубішу з тих, чий
піксель ще не більший за його вузол. Вибір **кутовий**, тож радіус тіла
скорочується й те саме правило працює для Землі. Інваріант «вузол на спільному
ребрі бітово один» цілий: вибір залежить лише від рівня, тож обидва сусіди
читають ту саму сітку в тій самій точці.

Новий оракул — шахівниця з періодом у піксель, тобто деталь, якої грубий вузол
не здатен нести: правильна відповідь там одна, середнє. Точкова вибірка
розкидає її **53…151 навколо 102**. І стара перевірка «два шляхи, одне число»
тихо перестала питати про грубі рівні — вона звіряла з найдрібнішою сіткою, а
плавна фікстура переживає усереднення без зміни байтів; тепер вона звіряє з
тією сіткою, яку рівень справді взяв.

### T4. Процедурна деталь кольору ✅ (2026-08-16)

Дрібні кроки: **T4a** — правило числом на CPU, **T4b** — двійник у шейдері й
чотири оракули в кадрі, **T4c** — межа рівнів і поріг нахилу з ассета.

Деталь, яку дав R7c, належить **висоті**. Тайлсет несе 5330 м на вузол, колір —
2.7 км, а процедурні октави доводять форму до ~106 м; отже зблизька гори гострі,
а колір поверх них рівний. Другої текстури для цього не існує, тож відповідь —
правило: `множник = 1 + нахил·(SLOPE_GAIN + RELIEF_GAIN·шорсткість)`,
`engine::material` і його двійник у `patch.slang`.

- **Нахил і рельєф — так, висота над сферою — ні.** Висота вже **є** в мозаїці:
  моря лежать нижче й темніші, і це виміряно. Правило з власною залежністю від
  висоти малювало б поверх даних другу відповідь на те саме питання, а на Землі
  розійшлося б з батиметрією видимо (T7). Нахилу такого дубля немає: на масштабі
  вузла мозаїки його не видно взагалі, а фізика за ним справжня — на крутому
  схилі реголіт зсувається, оголюючи світліший незрілий матеріал.
- **Рахується у вершині, і саме тому шва немає.** Обидва аргументи вже пораховані
  `vertex_terrain` і на спільному вузлі двох патчів різних рівнів **бітово одні**.
  ⚠ Спокуса взяти нахил з `ddx/ddy` у фрагменті (справжня геометрична нормаль,
  безкоштовно) хибна: то нормаль **трикутника**, тобто функція рівня патча, і шов
  ліг би рівно там, де R2b його прибрав.
- ⚠ **Висота деталі як джерело кольору дала ±1% — менше за квант шкали.** Не
  через множник: у висоті кожна октава зважена своєю довжиною хвилі (інакше
  дрібні брижі були б заввишки з гори), тож найгрубіша важить у 32 рази більше
  за найдрібнішу, і колір ніс би пляму завширшки 3.4 км — рівно те, від чого
  крок і мав позбавити. Стало: `detail::sample` віддає **два** числа з одного
  проходу — метричну висоту для форми й **шорсткість**, у якій усі октави важать
  порівну. Один прохід, не два: усе дороге в тому циклі — хеші. Виміряно ±1.8%.
- **Обидва доданки помножені на нахил**, тож на рівнині множник рівно одиниця, а
  колірний шум з'являється лише там, де є рельєф, який його виправдовує.
- ⚠ **Поріг насичення взято з ассета, а не з фізики.** Кут природного укосу
  реголіту (0.3, тобто 17°) — правдиве число про **іншу** величину: він про
  місцевий схил, а `slope_at` міряє на базі найдрібнішого вузла піраміди, тобто
  згладжено. Розподіл по `assets/moon.dem` (`--example slope_histogram`, 31 104
  вузли): медіана 0.035, 90% 0.128, 99% 0.221, максимум 0.410. При 0.3 правило
  вмикалося б на одній тисячній тіла. Стоїть 0.15.
- ⚠ **На справжньому Місяці правило зсуває яскравість на 1%, і винен не воно.**
  Кадр Місяця живе в байтах 16…25: відбивна здатність 0.06…0.10 пишеться в
  `Rgba8Unorm` як є, тож усе тіло займає чотири відсотки шкали, і п'ятивідсоткова
  модуляція падає під квант. На фікстурі, де поверхня стоїть на байті 160, те
  саме правило дає 10%. Це **другий незалежний** вимір, що вказує на T5.

**Перевірка** — п'ять оракулів, кожен зламано навмисно:

- множник у кадрі проти передбаченого на CPU: рампа з аналітичним нахилом,
  сталий колір, камера так далеко, що деталі рівно нуль (перевірено, не
  припущено). Передбачено 1.0987 з дифузною домішкою нахилу, у кадрі 1.0990;
- рельєф фарбує зблизька й не фарбує здалеку: розкид 0.27 проти 5.99 одиниці;
- **дві глибини піраміди дають той самий колір** (найгірша різниця — один квант).
  Це і є названа наперед перевірка кроку. ⚠ Оракул на «однаковість» сліпий до
  помилок, що гасять сигнал у **обох** гілках: множник на `window_step` він
  пропускає, бо той 2⁻¹⁰ в одній піраміді й 2⁻⁹ в другій;
- межа рівнів дає один колір з обох боків — ланка за ланкою: напрямок вузла,
  нахил, шорсткість, множник;
- п'ять констант існують у шейдері дослівно.

⚠ **Фікстура зі сталим нахилом неминуче накопичує рельєф** (0.05 на чверть
великого кола — 136 км), і перша її версія поставила камеру на п'єдестал у 98 км.
Дві речі міряються від **опорної сфери**, а не від ґрунту: `Frame::near_for` і
`distance` у шейдері (береться по незсунутій вершині). Тому камера за 30 км від
ґрунту гасила октави як камера на 128 км, і правило виглядало зламаним. Рампу
зсунуто в нуль під камерою.

Кадр зондів лишився `30812bf2…` — сцена зондів без тайлів.

### T5. Матеріали й PBR: перша справжня модель корабля ✅ (2026-08-16)

Найбільший крок етапу. Дрібні кроки:

| крок | зміст | стан |
|---|---|---|
| **T5a** | Передавальна функція: знімок пише те саме, що вікно | ✅ |
| **T5b** | Відбивна здатність замість заглушки `terrain.y = 1` | ✅ |
| **T5c** | Матеріал як дані `Scene` + GGX проти аналітичного двійника | ✅ |
| **T5d** | Пайплайн мешів і модель з Blender | ✅ |

Blender 5.2.0 LTS стоїть **через Steam**
(`~/snap/steam/common/.steam/steam/steamapps/common/Blender/blender`),
запускається headless без Steam і **в `PATH` його немає** — тому шлях задає
змінна `BLENDER` у Makefile, а не `command -v`. Конвенції, оракули й пастки
— у скілі `blender-assets`; **читати його перед роботою з моделями
обов'язково**.

⚠ Цей абзац двічі містив хибні твердження («каталогу не існує», «Blender не
встановлений»), написані **без** читання скіла, у якому все це вже було. Урок
вужчий за «читай документацію»: стан машини не встановлюється командою
`which` — інструмент, поставлений через Steam, snap чи flatpak, у `PATH` не
з'являється, і відповідь «немає» там означає лише «немає в `PATH`».

**Три дрібні кроки, кожен зі своїм оракулом:**

| крок | зміст | оракул | стан |
|---|---|---|---|
| **T5d1** | Модель і експорт: скрипт у Blender, `.blend` + `.gltf` + `.bin` у git | знаковий об'єм з `bmesh` і габарити — у файлі поруч з асетом; два прогони побайтово однакові | ✅ |
| **T5d2** | Формат `SSMSH` і кукер `tools/mesh-cook` | наш об'єм проти об'єму з Blender (10⁻⁷ відносних), `min`/`max` з акесора проти нашого читача `.bin`, обидва типи індексів | ✅ |
| **T5d3** | Рушій малює скукований меш замість процедурного | силует міняється від повороту (V1/V4), `extent` веде `near`, кадр не порожній | ✅ |

Оракул моделі їде **разом з нею**: скрипт пише поруч файл із числами, які
порахував Blender, а не наш код. Це те саме правило, що з етикеткою PDS3 — за
об'єм відповідає інший інструмент, інакше оракул перевіряв би сам себе.

**Що вийшло (2026-08-16).**

- **Модель будує скрипт, а не миша** (`tools/blender/ship.py`): ручний
  експорт не відтворюється, не переглядається в рев'ю й не переганяється
  після зміни конвенції. `--factory-startup` обов'язковий — без нього вихід
  залежить від того, які аддони ввімкнені сьогодні, тобто ассет перестає
  бути функцією входу. Скрипт **стверджує `bpy.app.version`**: Steam оновлює
  Blender сам, а версія друкується в `.gltf`.
- **Конвенція осей підтвердилася виміром**: ніс уздовж `−Y` у Blender →
  `+Z` у glTF, тобто вже `Scene::Ship`, і кукер не перетворює нічого.
  Оракул на це є рівно тому, що габарити в `ship.oracle.json` записані **в
  осях Blender** — щоб їх відтворити, читач мусить пройти ту саму
  перестановку. На симетричній моделі перевірка не означала б нічого.
- **Числа моделі:** 426 вершин у glTF проти 314 у Blender (розриви
  нормалей), 600 трикутників, індекси `UNSIGNED_SHORT`, знаковий об'єм
  **19.727593 м³** проти 19.727592602 з `bmesh` — 2·10⁻⁸ відносних, тобто
  межа `f32`, у якому Blender тримає координати. `extent` — **0.647 висоти**,
  помітно більше за половину: стабілізатори виступають далі за ніс.
- **Формат `SSMSH` живе в рушії** (`engine::mesh`), як і тайли: записувач і
  читач одного формату мусять бути одним кодом. У файлі меш **одиничної
  висоти** плюс два числа — `height_m` (довідка про модель) і `extent`
  (радіус обмежувальної сфери в одиницях висоти). Масштаб лишається за грою.
- **JSON читається бібліотекою** (`serde_json`), `.bin` — руками. Межа та
  сама, що в CLAUDE.md: декодери чужих форматів не пишемо, а масив чисел —
  не формат. Кукер офлайновий, тож залежність у гру не потрапляє.
- **Обидва типи індексів** перевірені на синтетичному glTF з одного
  трикутника: щоб дістати `UNSIGNED_INT` природним шляхом, потрібно 65 536
  вершин, тобто мегабайти в git заради двох рядків читача.
- **Меш у кадрі один на всі кораблі** (`Frame::load_ship`), без хендла:
  у гри один тип апарата, а структура без другого викликача гірша за свою
  відсутність. Другий корпус приведе хендл разом із собою.
- **`extent` дійшов до камери:** `ship_demo::scene_at` бере його з моделі, бо
  на ньому стоять і `near`, і відстань третьої особи. Немає асета — лишається
  заглушка V1 (`/assets/` не в git, і на чистому клоні корпусу немає
  законно).

⚠ **Ціна CPU-перетворення на великому меші так і не виміряна.** Поріг
«~10⁴ вершин» — оцінка з виміру ламаних, а в цієї моделі їх 426, тобто
питання не постало. Виміряти доведеться з першою моделлю, у якої вершин
десятки тисяч (скіл `blender-assets`, розділ «що не виміряно»).

T5a–T5c від Blender не залежать узагалі: вони про **пікселі**, а не про
геометрію, і перевіряються на тілах і процедурному корпусі V1, які вже є.

**Роль Blender названа наперед** (рішення розробника, 2026-08-16), бо від неї
залежить, що саме крок будує:

- **геометрія й параметри матеріалу — звідти, як дані.** `base color`,
  `roughness`, `metallic` їдуть числами поруч із мешем, тим самим експортом;
  кукер їх перекладає, рушій читає. Ніякого нового шляху — те саме, що висоти
  й тайли;
- **еталон вигляду — звідти ж, але з фіксованим колірним керуванням.** Там,
  де відповіді немає в жодній формулі (яке альбедо в реголіту, наскільки
  шорсткий корпус). ⚠ Без фіксації тонмапера порівняння беззмістовне: це той
  самий клас, що «нічию на GPU не перевіряють» — розбіжність буде, а хто в ній
  винен, невідомо;
- **числові оракули — двійники в Rust, не Blender.** У проєкті цей патерн уже
  несе `engine::atmosphere` проти `sky.slang` і `engine::cull` проти
  `cull.slang`. Аналітичний GGX дає число без семплювання, експозиції й
  тонмапера, тобто розбіжність означає помилку, а не інші налаштування;
- **шейдери пишемо в Slang, і це не смак.** Дорога Slang → WGSL має свій
  перелік пасток (X3511 на FXC, uniform у вершинній стадії на ACO,
  `binding_array` для naga); з node graph туди дороги немає. Плюс правила, які
  графом не виражаються взагалі: ніякої індексації вектора змінною, повітря —
  дослівний двійник CPU-коду, вершини camera-relative з reversed-Z по
  діапазонах. І головне — це була б **друга правда про ті самі пікселі**.

#### T5a. Передавальна функція ✅

⚠ **Це не «додати гамму», а закрити розбіжність двох шляхів до одного кадру.**
`window.rs` вибирає поверхню вікна фільтром `is_srgb()`, тобто вікно **вже**
кодує гамму апаратно. `shot::FORMAT` — `Rgba8Unorm`, тобто знімок пише лінійні
значення просто в байти. Отже вікно й PNG показували різні картинки від F1, а
рушій увесь цей час стверджував, що це один кадр двома шляхами.

Виміряно з двох боків. Відбивна здатність Місяця з мозаїки: 0.2 лінійних
показуються як 0.2, хоча в sRGB це 0.48. І з T4: кадр Місяця в PNG живе в
байтах 16…25, тобто все тіло займає чотири відсотки шкали, і правило матеріалу,
яке на фікстурі з байтом 160 дає 10%, там дає 1% — модуляція падає під квант.

**Що робить крок:** `shot::FORMAT` стає `Rgba8UnormSrgb`, тобто той самий
апаратний механізм, що вже діє у вікні. ⚠ Наслідки, кожен свідомий: `CLEAR_BYTES`
перестає дорівнювати лінійним значенням `CLEAR`; будь-який оракул, що ділить
**байти** на байти, починає ділити нелінійні числа й мусить декодувати назад;
бітова звірка `--shot` (`30812bf2…`) ламається — це очікувано, і еталон
оновлюється разом із поясненням.

**Виміряне на кроці, і воно виявилось ширшим за формат цілі:**

- ⚠ **`palette::Colour::scene` була хибною у вікні від U7a**, і знімок цього не
  бачив. Палітра підібрана оком, тобто в sRGB, а віддавала `байт/255` у сцену
  як лінійне число: у вікні апаратура кодувала його ще раз, і байт 200 виходив
  на екрані байтом 229, тоді як та сама панель інтерфейсу лишалась 200. Тобто
  акцент панелі **не був** кольором лінії прогнозу у вікні, хоч і був у PNG.
  Перевірка U7c2 цього не бачила, бо дивилась у PNG. Зміна формату знімка
  перевела розбіжність із вікна в тест, і той упав першим же прогоном. Стало:
  `scene()` кличе `srgb::to_linear`, `egui()` віддає байти як були.
- **`CLEAR_BYTES` лишився `[5, 8, 20]`, а `CLEAR` став лінійним відповідником.**
  Первинним оголошено байт — той вигляд неба, що обрано оком, — бо на ньому
  стоїть половина перевірок рушія й гри («це небо чи щось намальоване»). Пару
  звіряє тест; інакше вони розійшлися б мовчки.
- **Два оракули стали міряти гамму замість фізики, і обидва це визнали:**
  `sky::a_sunset_is_redder_than_noon` ділив байти (кодування стискає
  десятикратну різницю до двох-з-половиною-кратної), а весь `tests/material.rs`
  порівнював яскравості. Обидва тепер декодують. ⚠ Допуски там теж мусили
  змінитися: у кадрі зі сталим полем **усі** пікселі округляються в один бік,
  тож похибка кванта не усереднюється жодною їх кількістю — допуск виражений
  через ширину байта, а не через «одну одиницю».
- **Еталон `--shot` тепер `a64bad03…`** замість `30812bf2…`. Це очікувана
  зміна, а не регресія: вона рівно та, яку етап обіцяв наперед.

**Тонмапер сюди не входить, і причина названа:** сьогодні в кадрі немає нічого,
що перевищує одиницю — джерело одне, `shade` обмежений альбедо. Тонмапер без
значень понад одиницю — це тотожність, тобто структура без читача. Він
приходить з T5c, де дзеркальний відблиск GGX уперше дає понад одиницю.

#### T5c. Матеріал, GGX і тонмапер ✅ (2026-08-16)

Дрібні кроки: **T5c1** — двійник у Rust, **T5c2** — матеріал як дані й GGX у
шейдері, **T5c3** — прохід стиснення.

`Ship` несе `roughness` і `metallic` поруч із базовим кольором, обидва —
атрибути вершини (з тієї ж причини, що й колір: усі записи в чергу йдуть **до**
проходу, тож з uniform виграв би останній корабель). Формулювання —
Karis/Filament: `α = roughness²`, Сміт з кореляцією висот, Шлік; кожен вибір
збігається з glTF і Blender, звідки параметри й приїдуть.

**Дві справжні помилки, і жодну не було б видно без числового оракула:**

- ⚠ **Кадр освітлював корпус у двох різних просторах.** Позиції вершин корабля
  camera-relative **і повернуті** в осі камери (`Camera::relative`), а нормалі й
  світило лишалися світовими. Доки шейдер рахував лише `n·l`, це було
  самоузгоджено; T5c бере напрямок на око з позиції (`view = −position`), і
  `n·v` почав змішувати простори. Стало: нормаль і світило повертає той самий
  прохід CPU.
- ⚠ **Пайплайн корабля малює без відсікання граней**, тобто внутрішній бік
  оболонки — законний фрагмент. Без розвороту нормалі до ока BRDF відповідає на
  нього нулем, і корпус дістає чорні плями там, де мусить бути освітлена
  поверхня. Те саме робить `patch.slang` з нормаллю фасетки.

**Оракули — числові, і межі кожного виміряні зламом:**

- `engine::brdf` проти аналітики: розподіл інтегрується в одиницю (1.000005 при
  шорсткості 0.1), взаємність Гельмгольца тримається на 3.6·10⁻¹⁶. Енергія
  обмежена згори й **свідомо не знизу**: шорсткий метал віддає 0.32 — це відома
  втрата одноразового розсіяння GGX, не помилка;
- кадр проти `engine::brdf` на центрах трикутників: медіана 1 байт, 84…96%
  граней у межах 2 байтів, понад 200 граней на кожну з восьми комбінацій.
  ⚠ Медіана, а не максимум: стабілізатори перекривають частину граней, і центр
  такої падає в чужий піксель. ⚠ Один байт, а не нуль: точка порівняння — центр
  **у просторі**, а растеризатор інтерполює перспективно-коректно.
  ⚠ **Дзеркальний пік з порівняння викидається** — там `D` вищий за схил у
  сотні разів, і різниця нормалі в частку градуса дає сотні байтів;
- ⚠ **фікстура спершу стояла камерою на осі**, де базис камери збігається зі
  світовим і `Camera::rotate` — тотожність. Навмисний злам «світило у світових
  осях» вона пропустила. Камера навскіс — і ловить.

**T5c3, прохід стиснення.** Сцена малюється в `Rgba16Float`, стиснення —
один повноекранний прохід наприкінці. Окремим проходом, а не рядком у кожному
шейдері, бо композиція аеральної перспективи **читає** вже намальоване (`дст·T`,
`дст + L`): стиснуті числа там перестали б бути фізикою.

Крива — тотожність до коліна `K = 0.8`, далі `1 − (1−K)²/(x − 2K + 1)`,
неперервна разом з першою похідною. ⚠ **Тотожність нижче коліна — головна
властивість, а не спрощення:** уся дифузна робота етапу T живе там, і Reinhard,
ACES чи PBR Neutral зрушили б кожне з уже виміряних чисел, зокрема звірку «байт
проти відбивної здатності» (T5b). Перевірено кадром: 0.02 → 39, 0.1 → 89,
0.35 → 160, 0.79 → 230 — тобто бітово те саме, що до кроку.

Понад коліном: 0.95 → 242, 1.5 → 250, 2.0 → 252, 3.7 → 254 — усі різні, тоді як
без проходу всі четверо були б 255. Джерело значень понад одиницю в оракулі —
`Body::colour`, а не відблиск: пік GGX при дотичних кутах законно доходить до
сотень, а крива прямує до одиниці асимптотично, тож зрізання там є завжди й
нічого не доводить.

⚠ **Пастка, що коштувала години:** `shot::take_scene` створював кадр
тимчасовим виразом. З появою в кадру власної проміжної текстури це стало
помилкою, але виглядало не як помилка — картинка малювалася правильно, просто
стиснення не діяло. (Насправді причина була ще простіша й гірша: `git checkout`
на файлі під час перевірки зламом змів незакомічену обв'язку. Урок той самий —
**перевіряти зламом можна тільки те, що вже закомічено.**)

Еталон `--shot` — **`09df6423…`** замість `a64bad03…`: у фікстурному тілі синій
канал 0.9 виходить за коліно, і стиснення його чіпає. Різниця — 0.84% пікселів,
найбільша 4 байти.

#### T5b. Відбивна здатність замість заглушки ✅ (2026-08-16)

`terrain.y = 1` (T3b) розтягувала `unit` у 0.08…0.72 замість фізичних
0.02…0.18: на лінійній цілі справжнє альбедо давало майже чорний диск. Після
T5a причини не лишилось — множник став `Colour::scale`.

**Оракул, який став можливим лише тут: байт у кадрі проти виміряного числа.**
Доти кадр малював одиниці зберігання, і питати про фізику не було сенсу.
Фікстура прибирає з дороги все інше — рельєф плаский (множник матеріалу рівно
одиниця), мозаїка стала, світло вздовж погляду й тіло далеко, тож у центрі
кадру дифузний член рівно один і сталі `0.05 + 0.95·cos` у передбачення не
входять. Лишається `байт = srgb(unit · scale)`:

| одиниця | відбивна здатність | чекали | у кадрі |
|---|---|---|---|
| 45 | 0.0441 | 59 | 59 |
| 160 | 0.1569 | 110 | 110 |
| 255 | 0.2500 | 137 | 137 |

Байт у байт на всіх трьох. Зламано двічі — поверненням заглушки й вилученням
sRGB з передбачення; обидва рази падає.

Центр диска Місяця в анімації підльоту: середній байт **50** проти 33 до
етапу, при діапазоні 34…63. Медіана мозаїки 0.044 — це байт 59, і саме його
кадр і дає.

**Перевірка** — два оракули, і обидва вже названі:
- **меш**: знаковий об'єм проти таблиць і «жоден поворот, крім тотожного, не
  лишає корабель на місці» (V1). Імпортована модель мусить пройти те саме, що
  процедурна, інакше конвенція осей чи одиниць виїде тихо;
- **матеріал**: та сама сфера під набором кутів світла дає криву, яку можна
  звірити з аналітикою GGX, а не «виглядає металево».

### T6. Планетне сяйво на корпус ✅ (2026-08-16)

PROJECT.md §7 вимагає його прямо: тіньова сторона корабля чорна, підсвічена
лише альбедо планети знизу.

**Опромінення має замкнену форму, і в ній немає вільного множника.** Для
ламбертівського диска з півкутом `θ` це `π·L·sin²θ`, а яскравість поверхні
при одиничному опроміненні — `A·cos/π`. Разом `E = A·cos·sin²θ`; біля самої
поверхні `sin θ → 1`, тобто сяйво прямує рівно до альбедо — і саме це число
ловить перший юніт-тест, бо помилка на `π` тут найлегша.

**Альбедо береться з того самого місця, звідки його бере поверхня в кадрі.**
Тіло з колірним тайлсетом малюється відліком асета, а `Body::colour` у нього
не бере участі взагалі (`surface_albedo`), тож сяйво від `Body::colour`
означало б корабель, підсвічений планетою одного кольору над планетою іншого.
Для цього знадобилось обернене відображення «напрямок → грань і вузол»
(`cubesphere::locate`, T6b): усе інше в кубосфері рахує в один бік —
перелічує вузли, — а тут питання йде від точки простору до асета.

**Вибірка — з найгрубішого рівня піраміди, і це рішення.** Джерело тут диск
понад 800 км упоперек (100 км над Місяцем), тож найдрібніший рівень пообіцяв
би роздільність 1.3 км, якої в задачі немає, і сяйво мигтіло б від кожного
кратера. Нульовий рівень усереднений за побудовою (T3c), а його вузол — 85 км,
тобто якраз масштаб контрасту море-материк.

**Перевірка (`tests/shine.rs`), усі три твердження ROADMAP:**

| твердження | у кадрі |
|---|---|
| тіньовий бік не чорний | освітлено **0.65** його пікселів |
| над морем і материком по-різному | **5.71** проти 5.71 з асета (0.12 / 0.021) |
| на нічному боці гасне | освітлено **рівно 0** |

Дві третини, а не всі — правильна відповідь: сяйво приходить знизу, тож
площадки, повернуті від планети, лишаються чорними за тим самим косинусом,
що темнить їх проти світила.

⚠ **Море проти материка перевіряється поворотом тіла, а не переїздом
корабля.** Тоді все інше в сцені лишається бітово тим самим, і відношення
яскравостей є відношенням відбивних здатностей, а не сумішшю з новим кутом
світила й новим «вниз». Побічно це єдиний оракул на поворот у систему тіла:
без нього обидва кадри однакові.

Маска тіньового боку береться **з кадру без планети**: після T5c ці пікселі
рівно `[0, 0, 0]`, і відрізнити їх від тіні за кольором неможливо — та сама
поправка, яку довелося внести в `tests/sun.rs`.

**Чого крок не робить:** форму диска. Три спрощення (диск замість півсфери,
напрямок на центр замість тілесного кута, освітленість у підкорабельній
точці) названі в `planetshine.rs` з ціною кожного; знімає їх одна робота —
SH-проба з інтегруванням по шапці, і видно її буде лише там, де сяйво
перестане входити в кадр одним напрямком і трьома числами.

### T7. Земля: DEM і колір ✅ (2026-08-16)

Обидва джерела приїжджають на диск разом, і разом же кукаються: колір на
ідеальній сфері робити не можна — «колір, накладений на неправильну форму,
виглядає правдоподібно, і це найгірше поєднання» (ROADMAP-PLANETS.md). Отже
ETOPO 2022 (з батиметрією) і Blue Marble разом, а не по черзі.

**Відповідь на Q6 (2026-08-16): обидва продукти, і кожен на своїй ролі** —
ETOPO задає форму (висоти з батиметрією), Blue Marble лягає на неї шкірою
(альбедо). Решта вибору після цього — розрахунок, а не рішення розробника, і
він зроблений нижче.

#### Рівень піраміди — 6, обом тайлсетам Землі

Крок вузла на кубосфері сталий по грані (рівнокутна параметризація,
`cubesphere::parameter`): `(π/2 · R) / (32 · 2^(рівнів−1))`. Пам'ять — з
виміру T2a (12 КіБ на тайл `Rgba8Unorm` у NVIDIA, 16 КіБ у RADV, 8 КіБ у
`R16Sint`), ціна кадру — з виміру T8 (≈51 нс на текстуру за кадр, D19).

| рівнів | км/вузол Землі | тайлів | кадр (D19) | колір у пам'яті | DEM у пам'яті |
|---|---|---|---|---|---|
| 5 | 19.6 | 2046 | +0.10 мс | 24 МіБ | 16 МіБ |
| **6** | **9.8** | **8190** | **+0.42 мс** | **96 МіБ** | **64 МіБ** |
| 7 | 4.9 | 32 766 | +1.67 мс | 384 МіБ | 256 МіБ |

Сьомий рівень тут коштує **вдесятеро** проти шостого за пам'яттю й ще 1.25 мс
кадру — тобто ту саму відмову, яку T2a ухвалив для Місяця, лише з ціною,
вчетверо більшою. Екрана при цьому не досягає жоден: при камері на 100 км і
кадрі 720 пікселів один піксель накриває 160 м, тож рівень 6 дає 61 піксель на
вузол Землі (Місяць мав 24). Різницю закриває процедурна деталь — R7c у формі,
T4 у кольорі, — і саме для цього вона й писалася.

⚠ **Земля вдвічі-вчетверо грубіша за Місяць у метрах, і це свідомо.** Радіус
у 3.7 раза більший, а рівень той самий: 9.8 км на вузол проти 2.7 км. Купити
паритет можна лише сьомим рівнем, тобто 384 МіБ і 1.67 мс — за деталь, якої
екран не бачить.

#### Продукти, названі й перевірені

| роль | продукт | сітка | розмір |
|---|---|---|---|
| форма | **ETOPO 2022 v1, 60″, ice surface, GeoTIFF** | 21600×10800, `int16` | 466 МБ |
| колір | **BMNG w/ Topography and Bathymetry, липень 2004** | 21600×10800, JPEG RGB | 27 МБ |

- **Сітки збігаються пікселе-в-піксель**, і це головна причина взяти саме цю
  пару: 21600×10800, проста циліндрична, 1.85 км/піксель на екваторі в обох.
  Отже висота й колір читаються **однією геометрією**, і берегова лінія DEM не
  може розійтися з береговою лінією мозаїки на пів пікселя — а розійшлася б
  вона видимо, бо саме на березі колір міняється стрибком.
- **60″, а не 30″** (1.59 ГБ): при вузлі 9.8 км навіть 60″ уже вп'ятеро
  дрібніший за вузол, тобто кукер і так усереднює ланцюгом грубіших сіток
  (T3c). 30″ купив би роботу читачеві й нічого — кадру.
- **`surface`, а не `bed`:** з космоса видно лід Гренландії й Антарктиди, а
  не скелю під ним. Батиметрія в обох варіантах та сама.
- **Липень** — місяць мозаїки є параметром URL і міняється одним рядком у
  README; узятий той, у якому найменше морського льоду на півночі.

Походження, URL і SHA-256 — `data/etopo/README.md` і `data/bmng/README.md`,
за зразком `data/wac/`. Сирі файли поза git (Q5); механізм завантаження —
борг D18.

#### Дрібні кроки

| крок | зміст | стан |
|---|---|---|
| T7a | джерела на диску: README, SHA-256, `.gitignore` | ✅ |
| T7b | читач ETOPO (GeoTIFF) | ✅ |
| T7c | читач BMNG (JPEG) | ✅ |
| T7d | кукер DEM Землі, 6 рівнів → `assets/earth.dem` | ✅ |
| T7e | кукер кольору, чотири канали → `assets/earth.col` | ✅ |
| T7g | друга поверхня в кадрі | ✅ |
| T7f | правило матеріалу Землі: вода; смугастість патчів | ✅ |
| T7h | альбедо неба з поверхні, перемір D19 | ✅ |

⚠ **T7f і T7g помінялися місцями**, і це виправлення плану: правило матеріалу
не має де показатися, доки Землі немає в кадрі, а порогів у нього два —
нахил і висота, — і обидва хочеться дивитися очима, а не лише гістограмою.

**Що вже стоїть на диску (T7a–T7e):**

| асет | рівнів | тайлів | розмір | у пам'яті GPU |
|---|---|---|---|---|
| `assets/earth.dem` | 6 | 8190 | 19.2 МіБ | 64 МіБ |
| `assets/earth.col` | 6 | 8190, 4 канали sRGB | 38.3 МіБ | 96 МіБ (NVIDIA) |

Найнижчий вузол рельєфу — −6163 м (Маріанська западина, усереднена вузлом у
9.77 км), середній колір — 0.0631 0.0638 0.0784 у лінійному світлі.

**Три рішення, які крок ухвалив по дорозі:**

- **ланцюг грубіших сіток тепер потрібен і висотам.** У Місяця джерело
  грубіше за вузол, тож точкова вибірка чесна скрізь; ETOPO вп'ятеро дрібніше
  за вузол найглибшого рівня і в тридцять тисяч разів — за вузол нульового.
  Заразом ланцюг перестав ділити строго навпіл: `10800 = 2⁴·675`, тобто
  ділення надвоє спинялося за чотири кроки, і найгрубіша сітка виходила
  вдесятеро дрібнішою за вузол рівня 0. Крок тепер — найменший дільник з
  {2, 3, 5}; Місяць від цього не змінився бітово (`moon.col` = `d579db9c…`);
- **колірний тайл несе простір відліку** (`srgb`, версія формату 2). Байт
  «колір» означає різне для двох тіл: у Місяця це лінійна відбивна здатність
  з WAC, у Землі — sRGB з картинки для ока. Лінійне значення в байті віддало б
  океану (0.0015) нуль; sRGB розкодує GPU при вибірці, а на CPU —
  `Colour::reflectance`, тож сяйво планети далі отримує світло, а не байти;
- **читачі чужих форматів беруть чужі крейти** (`tiff`, `jpeg-decoder`), і
  кукер перестав бути безбібліотечним. Своє в них лише інтерпретація тегів
  прив'язки — того, чого жодна бібліотека не знає.

**Друге тіло з тайлами не коштувало кадру нічого нового** (T7g): `Frame` уже
тримає список слотів поверхні, кожен зі своїми bindless-масивами, а тіло
показує на слот. `MAX_TILES` теж лишився 8192 — стеля рахується **на масив**,
а в найбільшому з них 8190 тайлів. Коштувало іншого: формат текстури тепер
береться з асета (`Colour::srgb` і кількість каналів), а шейдер вибирає між
кольором і яскравістю за uniform — тип ресурсу в WGSL для обох форматів
однаковий, тож спитати саму текстуру не можна.

**T7f зробив дві речі, і перша була не тією, що планувалася.**

⚠ **Смугастість виявилась не в правилі матеріалу, а в нормалі.** Гіпотеза, з
якою крок починався, — `sample_slope` і `window_delta` — не витримала виміру
взагалі: плато в кадрі рівно сталі, завширшки 135 пікселів, зі стрибком 3%,
межі прямі з діагоналлю в куті (**трикутники, не патчі**), а множник матеріалу,
поміряний на CPU-двійнику по всіх рівнях піраміди, ніде не сягає більшого за
1.012. Підміна фасетки нормаллю сфери прибрала всі стрибки — тобто винний
`fragment_terrain`: він брав нормаль з екранних похідних, тобто нормаль
**трикутника**, а та несе не лише нахил рельєфу, а й злам кривини самої сфери
на теселяції. Здалеку, де клітинка сітки більша за рельєф під нею, лишається
рівно цей злам: Місяць виходив гранчастим м'ячем, Земля — плиткою.

Ліки: з фасетки віднімається фасетка **незсунутої** сітки. Злам теселяції в
них однаковий і скорочується, лишається чистий нахил рельєфу, доданий до
аналітичної нормалі сфери; на пласкому тілі різниця тотожно нульова.
⚠ Правку довелося робити двічі, і перша спроба варта запису: `input.normal`
приходить помноженою на **радіус тіла** (`model` = поворот × радіус), тож
поправка завдовжки 0.05 у сумі з 1.7·10⁶ зникала без сліду, і кадр виходив
точнісінько як у гладкої сфери.

**Старий оракул проходив завдяки помилці.** `on_the_terminator_the_relief_shows_as_shade`
давав 39179 проти 4745 (×8.25), і майже все те число робили грані, а не
рельєф; з чистою нормаллю лишається 9100 проти 4745, тож поріг опустився з ×3
до ×1.7 — це чесна частка, яку дає сам нахил. Натомість з'явився оракул саме на
артефакт: `a_flat_tileset_shades_like_a_smooth_sphere` — нульові висоти **є**
сфера, тож обидва пайплайни зобов'язані намалювати той самий кадр. Допуск —
один байт глибини (рельєфна гілка нормалізує двічі, гладка один раз, і один
ULP на схилі яскравості дає один байт); гранчастий м'яч давав сім.

**Друга річ — вода, і поріг для неї не вибраний, а виміряний.** Правило
підсвічує схил, а під водою в кадрі видно поверхню моря, не схил дна. Дно при
цьому **крутіше** за сушу: медіана 0.0071 проти 0.0030, дев'яностий процентиль
0.0333 проти 0.0201 — тобто правило малювало серединні хребти поверх рівної
води, і яскравіше за гори на суходолі.

- **Рівень моря живе в ассеті, а не в `Scene::Body`.** Він виражений у датумі
  **цього** DEM, тож поле сцени дозволило б стан «гра каже нуль, а ассет міряє
  від іншого нуля» — і море стояло б за кілометр від берегової лінії мозаїки,
  мовчки. `SSDEM` версії 3; сухе тіло несе сентинел `NO_SEA`, нижче за який не
  буває жодна висота в `i16`, тож Місяць малюється як до кроку.
- **Нуль для Землі теж не домовленість:** ETOPO міряє висоти від геоїда, а
  одиниця зберігання — метр. Скукований ассет дав 72.0% вузлів нижче нуля при
  справжній частці океану 71% — незалежна перевірка, що датум пережив
  пайплайн.
- **Земля не дістає власного `SLOPE_REF`.** Її розподіл учетверо пологіший
  (медіана 0.0058 проти 0.035), але правило існує там, де немає колірних
  даних, а в Землі мозаїка лежить у **тій самій** сітці, що й DEM. Поріг під
  земний розподіл дав би горам повні +30% поверх виміряного альбедо, тобто
  замалював би дані правилом. Зі сталою 0.15 правило на Землі тихе (1.06 на
  дев'яностому процентилі суші), і саме цього від нього тут і треба.
- **Льоду окремої гілки немає** — у мозаїці він уже білий. Планувалося «вода,
  суша, лід»; лишилась одна вода, бо решта два були б другою відповіддю на
  питання, на яке дані відповіли.

**T7h: небо дістало землю, від якої відбивається.** Етап S поставив альбедо
поверхні в таблиці багаторазового розсіювання нулем і записав чому — кольору
поверхні в сцені не було взагалі, тож будь-яке число було б вигаданим. T3 і
T7e ту причину зняли.

- **Число береться з ассета** (`Colour::mean` по найгрубішому рівню) і
  кешується **раз на завантаження**: середнє розкодовує sRGB у 22 050 вузлах,
  тобто десятки тисяч `powf`, і щокадру такого робити не можна. Тіло без
  колірного тайлсета так і лишається з нулем.
- **Зміщення кубосфери виміряне, а не злякане.** Середнє незважене, а
  рівнокутна сітка розтягує клітинку до кута грані приблизно вдвічі. Кукер
  тепер друкує **два** середні поруч: своє по сітці широта-довгота
  (0.0631/0.0638/0.0784) і піраміди (0.0595/0.0595/0.0732). Різниця 6%.
- **Обидва двійники дістали по два доданки** на удар променя в землю:
  ламбертове `albedo/π · T · cos` у другий порядок і просте `albedo` у частку
  (ізотропна одинична яскравість на ламбертовій поверхні віддає рівно
  альбедо). Без другого відбиття рахувалося б лише в другому порядку.
- **Тести неба тепер біжать з ненульовим альбедо**, і це не косметика: з
  нулем звірка GPU з двійником не перевіряла б новий доданок узагалі — він
  зникав би однаково в обох гілках. Перевірка збіжності (`f < 1`) лишилась
  зеленою, а вона тут найкрихкіша: відбиття від землі живить той самий ряд.
- **Виміряний ефект:** небо світлішає в 1.02…1.15 разу залежно від кута Сонця
  й погляду.

**D19 виміряно на його власному порозі.** Борг називав тригером друге тіло з
пірамідою; T7 його привів. Обидва тіла в кадрі завширшки кілька пікселів
(10⁹ м), тож різниця — прив'язка масивів, а не малювання:

| сцена | текстур | кадр, мс | нс/текстуру |
|---|---|---|---|
| лише Місяць | 10 236 | 0.71–0.73 | 70–72 |
| лише Земля | 16 380 | 1.00–1.03 | 61–63 |
| **обидва** | **26 616** | **1.85–2.07** | 70–78 |

Тобто **11–12% бюджету 60 Hz** проти передбачених на T8 1.36 мс: там ставка
була 51 нс на текстуру, а виміряно 61–78, і кадр з двома тілами коштує ще й
трохи більше за суму двох однотільних — це другий прохід тіла понад прив'язку.
Борг лишається відкритим, але тепер він **найбільша стала стаття кадру**, і
його момент — не «колись», а наступна робота, яка додасть третій тайлсет.

### T8. Перемір і закриття ✅ (2026-08-16)

**Головне з кроку — вимір не підтвердив того, заради чого його робили.** План
питав, скільки коштує друга вибірка на фрагмент; відповідь — що ціна кольору
взагалі не у вибірці. Колірна піраміда додає **0.39 мс до кадру навіть тоді,
коли Місяць займає кілька пікселів на 10⁹ м**, і росте лінійно з кількістю
текстур у bindless-масиві (≈51 нс на текстуру за кадр), а не з накритою
площею. Час кодування командного буфера при цьому не міняється, тобто платить
драйвер при прив'язці. Таблиці — «Вимір продуктивності», розділ про колірні
тайли; наслідок — новий борг **D19**.

Побічний наслідок, який коштував рівно нічого: `--perf-probe` тепер читає
скуковані тайли з диска й міряє їх сам, замість окремого прогону руками.
Немає асетів — рядки просто пропускаються.

### T9. Корабель з референсу — форма й фарба ✅

Замовлення розробника, 2026-08-16: перемалювати корабель з мультяшної ракети
(картинка й [модель на TurboSquid](https://www.turbosquid.com/ru/3d-models/3d-cartoon-spaceship-simple-1380185))
і **одразу накласти кольори**. Сторінка сама не відкривається (403), тож
джерелом розмірів був рендер, а не файл.

| крок | що зроблено | оракул | стан |
|---|---|---|---|
| **T9a** | форма з референсу в `tools/blender/ship.py` | габарити й об'єм з Blender, перевірка кожної оболонки | ✅ |
| **T9b** | фарба: `COLOR_0` → `SSMSH` v2 → кадр | реєстрація фарби на геометрії, круговорот файлу | ✅ |

**Числа зняті з картинки, а не вибрані.** `RADIUS = 0.113` — це 87 пікселів
півширини на 767 пікселів висоти; кільця профілю — силует, зчитаний що сто
пікселів. Найкорисніше з цього: носовий конус виявився **восьмою** частиною
ракети, хоч на око читається як третина. Модель, зроблена «на око», мала б
довгий конус і не була б схожа.

Рішення, які варто пам'ятати:

- **Модель займає рівно `0…1` уздовж осі, разом з п'ятами стабілізаторів.**
  Рушій нормалізує меш **його ж** габаритом (`Model::from_metres`), тож
  стабілізатор, що вилазить за хвіст, робив би довжину в оракулі й дільник у
  кукері двома різними числами. Тепер вони одне за побудовою.
- **Обідок ілюмінатора сидить на поверхні корпусу, а не на дотичній
  площині.** Він завширшки 0.65 радіуса, а на такій опуклості плоска
  пластина відстає від корпусу по краях на третину радіуса.
- **Фарба їде `COLOR_0` на єдиному примітиві, а не діапазонами за
  матеріалами.** Рішення формату, яке читач glTF відкладав з T5d2, лишається
  неухваленим: один примітив, один виклик малювання, одне нове поле. Ціна
  відома й заплачена — розрив кольору розщеплює вершину так само, як розрив
  нормалі: 603 вершини в Blender проти 1115 у файлі. Чого `COLOR_0` не везе —
  шорсткість і метал; вони лишаються сталими на корабель, а скло ілюмінатора
  обійшлося кольором, тож у діапазонів досі немає викликача.
- **`HULL_METALLIC` 1.0 → 0.0.** Фарба — діелектрик. З одиницею базовий колір
  ставав `F0`, тобто відтінком самого відблиску, і біла емаль малювалася
  темним хромом — рівно так виглядав перший кадр T9a.
- **Перевірка оболонок замінила глобальне «перевернути, якщо об'єм
  від'ємний».** Тепер кожен компонент окремо: кожне ребро пройдене двічі й у
  різні боки, потім знак об'єму. Вона окупилася в першому ж прогоні — кришки
  призми стояли поверненими всередину **від T5d**, і побачити це було ніяк:
  вони всередині корпусу, а `bmesh.calc_volume` рахує суму по всій моделі,
  тож одна вивернута оболонка ховається за рештою.
- **Тест кукера перевіряє реєстрацію, а не значення.** `COLOR_0` — це той
  самий `.bin`, який ми й читаємо, тож звірити його з собою не означає
  нічого. Довести можна інше: кожна червона вершина лежить або на конусі, або
  на стабілізаторі й ніколи між ними, а кожна жовта — всередині диска на
  правому борті. Помилка кроку в акесорі лишає палітру й переносить фарбу.

Дивитись — `cargo run --release -p engine --example ship_stills -- build/ship`
(чотири боки нерухомо; `--ship-demo` відповідає на інше питання — як корабель
обертається).

**Чого T9 свідомо не робить:** заклепок і панельних ліній, крім однієї —
на референсі це текстура, а текстур через межу ще не їде жодна. Антена в
моделі лишилась, хоч на референсі її немає: без неї корабель має дзеркало
`z → −z`, а дзеркало ховає помилку ліворукості осей повністю.

### Борги, питання й межі етапу T

**Заведено D19** (масив текстур коштує за своїм розміром), і він же на етапі
переміряний: два тіла з тайлами дають 1.85–2.07 мс, тобто 11–12% бюджету.
**Закрито жодного борга не було.** D17 етап не чіпав, але дав йому фікстуру й
число (`--example earth_orbit`, 5.9 байта кілець у надирі); D15, D11 і D12
чекають на стадію карти, як і чекали.

**Питання:** закрито **Q6** (які джерела Землі кукати — обидва). **Q3** (нахил
біля кута куба) лишається відкритим і етап не заблокував, але **ставка на
ньому виросла**: нахил тепер читає ще й правило матеріалу, тож у смузі навколо
кута двозначний і колір, а Земля додала до восьми плям Місяця ще вісім своїх.

**Що лишилось незміряним свідомо:** ціна `Ships::upload` на великому меші — у
моделі T9 їх 1115, тобто питання так і не постало (скіл `blender-assets`).

### Чого етап свідомо не робив

- **Еліпсоїда.** Сплюснутість — один-два пікселі силуету на 1080p, а коштує
  трьох перевиводів (висота події, конус патча, сферичні шари повітря).
  Лишаємо сферу; фізичний геоїд у нас і так правильний.
- **Об'ємних хмар** — PROJECT.md §7 прямо лишає на потім; двовимірний шар
  можливий, і він же найбільший візуальний ефект на гроші.
- **Моделей космодромів.** Таблиця майданчиків — це дані й геймплей (широта
  визначає досяжне нахилення); модель упирається в місцевість метрового
  масштабу й у **тягу**, якої немає.
- **Води як матеріалу.** Маска для неї тепер є (T7f), блиск і Френель дав T5,
  але це вже не про альбедо, і викликача сьогодні немає.
- **Кабіни, факела, входу в атмосферу** — решта M5, і факел чекає на тягу.

---

## Етап V. Корабель у сцені ✅

Літера `V` (vessel) — вільна, за тим самим правилом, що `R` (render), `U`
(інтерфейс), `S` (атмосфера): `M*` це мілстоуни, а етап — робота, яка їх
закриває. Нумерація `V*` стала.

Етап розблоковує решту M5 і робить першу справжню перевірку діапазонів
глибини. **Порядок усередині названий розробником: спершу третя особа.**

| Крок | Зміст | Ризик | Стан |
|---|---|---|---|
| V1 | Меш-заглушка: процедурна ракета, без ассета | 🟢 | ✅ |
| V2 | Корабель у `Scene`, `near` звідти ж | 🟡 | ✅ |
| V3 | Два проходи глибини вперше в справжній сцені | 🟡 | ✅ |
| V4 | Камера третьої особи | 🟡 | ✅ |
| V5 | D16: напрямок на світило з ефемериди | 🟢 | ✅ |
| V6 | Перемір і закриття | 🟢 | ✅ |

### V1. Меш-заглушка ✅ (2026-08-16)

`engine/src/ship.rs`, шість метрів заввишки: корпус обертанням профілю,
чотири стабілізатори, ілюмінатор. **Форма несиметрична навмисно** — куля була
б дешевшою, але поворот гладкої сфери показати не можна взагалі (R1), а
камера третьої особи стежить саме за орієнтацією.

Два оракули, обидва числа:

- **знаковий об'єм меша дорівнює тому, що кажуть таблиці**, з відносною
  похибкою 10⁻¹². Порівнюється з площею **правильного 32-кутника**
  `(n/2)·sin(2π/n)·r²`, а не з `π·r²`, тож допуску на грубість сітки немає
  ніде. Знак додатний — обхід зовнішній; збіг числа — оболонки замкнені;
- **жоден поворот, крім тотожного, не лишає корабель на місці**, і контроль —
  та сама перевірка на кулі.

Перевірено зламом, а не прогоном: викинутий трикутник дає 15.677 проти
15.798, перевернутий обхід корпусу — −0.815, а **викинутий ілюмінатор
обвалює неузгодженість крену на 90° до 8·10⁻¹⁶**. Тобто чотири стабілізатори
самі по собі лишають корабель симетричним, і без ілюмінатора перевірка крену
не означала б нічого.

Кадр зондів лишився `30812bf2…` — у рендер меш ще не входить.

### V2. Корабель у `Scene`, і `near` звідти ж ✅ (2026-08-16)

`Scene::Ship` — центр, орієнтація, висота, **габарит** і колір. Габарит полем,
а не з меша: кадру він потрібен **до** завантаження геометрії, і стабілізатори
вже виступають за корпус.

- **`near_for` тепер питає й кораблі.** Було: висота над найближчим тілом,
  поділена на 10, тобто 40 км на орбіті 400 км — корпус за п'ятнадцять метрів
  відсікався цілком.
- **Пайплайн зроблено за зразком `Lines`, а не `Planet`,** і це вибір за
  розміром задачі: 1614 вершин на корабель означають camera-relative на кожну
  щокадру за мікросекунди — дешевше, ніж другий uniform із динамічним зсувом
  під матрицю моделі. Меш — корабель **одиничної висоти**, спільний, як
  одинична сфера в тіл. Колір — атрибут вершини, з тієї самої причини, що в
  ламаних.
- ⚠ **Оракул розміру — проєкція того самого меша через `Camera::to_screen`,
  і написаний «очевидно» він був би неправильний.** Кутова оцінка дає 88.7
  пікселя, кадр малює 96: стабілізатор, повернутий до камери, стоїть на 2.28 м
  ближче за ніс і проєктується більшим. Допуск асиметричний — растеризатор
  здатен загубити вістря, але не намалювати поза геометрією.
- **Демо-анімація** (`--ship-demo`, APNG, рівно 1/60 с на кадр) малює тим
  самим `Frame`, що йде у вікно. Вона ж одразу знайшла **борг D17**.
- Кадр зондів рушія лишився `30812bf2…`: порожній список кораблів не коштує
  нічого.

### V3. Два проходи глибини вперше в справжній сцені ✅ (2026-08-16)

Досі `plan` рахував один прохід завжди: сцена зондів дає рівно один, і жоден
тест не стверджував більшого. Виявилось, що **демо-анімація V2 малювалася
двома вже вчора** — near = 1.14 м від корпусу, far = 1.31·10⁷ м від Землі,
розмах 7.06 порядка проти семи, що вміщаються в один буфер (F3). Тобто крок не
вмикав другий прохід, а вперше **стверджував** його — і питав те, про що
раніше не було кому спитати.

- **Розвилка закрита арифметикою, а не кадром: межа не потрапляє в корпус
  ніколи.** Другий діапазон з'являється лише за розмаху понад 10⁷, тож
  найменше можливе відношення сусідніх меж — `√10⁷ ≈ 3162` (три й чотири
  діапазони роблять його більшим). Межа стоїть на `near·3162`, а `near` — це
  десята частина відстані до найближчої точки корпусу, тобто межа не ближча за
  316 висот, тоді як корпус тягнеться щонайбільше на два габарити. Впритул до
  корпусу `near` впирається в поріг 0.1 м, і межа все одно лишається за сотні
  метрів. Перевірено свіпом по 40 відстаней × 3 висоти орбіти, не однією
  точкою. Габарит корабля тут не знадобився — але саме тому, що V2 уже завів
  його в `near`.
- **`plan` більше не метод.** Він не читав із `self` нічого, крім списку, у
  який пише, і саме через це «скільки тут проходів» не можна було спитати без
  GPU. Тепер список приходить параметром, а `Frame::depth_ranges` віддає
  ближні площини всіх діапазонів; довжина — їхня кількість. Числа дістаються
  з тих самих `depth_a`/`depth_b`, якими малює кадр (`lo = B/(A+1)`, однаково
  для скінченного діапазону й для нескінченного), а не рахуються вдруге.
- ⚠ **Оракул шва двічі був беззмістовним, і обидва рази виглядав робочим.**
  Спершу «нижче першого намальованого пікселя дірок немає» — але ніс корабля
  стирчить у небо, і фон під ним законний, тож перевірка падала на власній
  сцені. Потім горизонт брався **з кадру** — а дірка **на самому горизонті**
  просто зсуває той перший піксель униз, і зламані на порядок площини
  лишали перевірку зеленою. Горизонт мусить приходити з геометрії: дотичні
  точки сфери, `T(φ) = (R²/r)·up + R·√(1−R²/r²)·w(φ)`, спроєктовані тією
  самою `Camera::to_screen`.
- ⚠ **Висота камери скорочується — і сцена «з десяти метрів» перевірити шов
  не може взагалі.** `near` це десята частина висоти, `far` — приблизно
  діаметр планети, тож для камери **без корабля поруч** межа завжди виходить
  рівно `горизонт/3.16`, на якій висоті не стій. Ґрунт на такій відстані
  стоїть під 1.58 нахилу горизонту, а сам нахил на десяти метрах — 1.77 мрад
  проти пікселя в 4.09 мрад: уся ділянка другого діапазону лежить у чверті
  пікселя під горизонтом. Виміряно зламом: **розсунуті вчетверо площини не
  дають жодної дірки**. Корабель за дванадцять метрів від камери на кілометрі
  розриває це співвідношення — `near` тепер його, межа лишається на 3.5 км,
  горизонт іде на 113 км, і між ними шістдесят рядків кадру. Та сама перевірка
  на тій самій сцені ловить уже **1% розбіжності меж** (0.1% — ні). Тобто
  чутливість виросла в чотириста разів від однієї зміни висоти.
- Кадр зондів рушія лишився `30812bf2…`: його сцена як була однопрохідною,
  так і є.

### V4. Камера третьої особи ✅ (2026-08-16)

`engine/src/chase.rs`, побудований як `orbit.rs`: стан — три числа, позиція з
них виводиться, а не накопичується. Два рішення, з яких випливає решта:

- **Камера бере від корабля позицію, а не орієнтацію.** Це не спрощення, а
  суть вигляду: прив'язана до осей корабля, вона показує його нерухомим
  завжди — крен і перекладання носа в ній не видно взагалі, бо разом із
  кораблем повертається весь кадр. Перевірка кроку має сенс лише за цього
  рішення, і сторож проти зворотного стоїть окремим тестом.
- **Відстань міряється габаритами корабля, а не метрами.** Стан несе
  `ranges`, у метри це перетворюється в момент побудови `Camera`. Тоді кадр
  однаково компонує і шестиметрову заглушку, і майбутню справжню модель, а
  межа наближення (1.5 габарита — габарит це радіус обмежувальної сфери, тож
  на одиниці камера стояла б на корпусі) не переглядається з кожним новим
  кораблем.
- **`up` приходить від викликача.** Біля планети це місцева вертикаль, у
  перельоті — що завгодно стале; `Scene` не каже, біля якого тіла апарат, а
  вгадувати це з найближчого тіла означало б другу правду про те, що
  викликач знає точно.

**Виміряно, частка зміненого силуету на поворот 40°: 0.578 навколо x, 0.305
навколо y, 0.107 навколо z.** Третє менше не через камеру, а через форму:
корпус — тіло обертання, тож крен видно лише стабілізаторами й ілюмінатором,
і це рівно те, що V1 виміряв на самому меші. Кут 40°, а не 90°: стабілізаторів
чотири, тож чверть оберту навколо носа переводить силует сам у себе, і
правильна камера показала б нуль.

Перший викликач — демо-анімація V2: вона більше не будує камеру рукою, а
обходить корабель тим самим `drag`, яким її водить гравець.

### V5. Напрямок на світило зі `Scene` ✅ (2026-08-16, борг D16 закрито)

`Scene::sun` — напрямок **до** світила, світові осі. Полем сцени, а не тіла:
світло одне на кадр, і корпус, небо й поверхня мусять освітлюватись з однієї
точки; тіло з власним сонцем дозволило б їм розійтися. `game::view` бере його
з ефемериди тієї самої миті, що й тіла.

- **Сонце не в `bodies` і не буде.** `Frame::far_for` міряє розмах сцени по
  тілах, тож Сонце серед них розтягнуло б кадр на астрономічну одиницю й
  додало б проходів глибини — заради кулі, яку ніхто не малює. У снапшот воно
  йде окремим полем `sun: Option<[f64; 3]>`; `None` означає «ассет мовчить», і
  тоді кадр лишається зі своїм типовим освітленням, а не з нулем, який погасив
  би все.
- **В обертовому фреймі напрямок тільки повертається.** `Synodic::apply`
  зсуває до барицентра й ділить на відстань Земля-Місяць — для точки це
  правильно, для напрямку ні. Звідси `Synodic::direction`: той самий базис,
  без початку й масштабу.
- **Кадр зондів рушія лишився `30812bf2…`.** `Scene::new` ставить рівно той
  вектор, що був сталою `frame::LIGHT_DIR` від F5, і плюмбінг не нормалізує
  нічого: довжина зараз масштабує дифузний член, і чіпати це — робота
  матеріалів M5, а не цього кроку.
- ⚠ **Перевірка «Місяць стоїть в обертовому фреймі» була перевіркою кольору,
  і V5 її зламав** — правильно зламав: за три доби термінатор Місяця
  переповзає диск, і 686 пікселів із 732 законно змінюються. Оракул виправлено
  на **силует**: диск, що поїхав, — помилка, тінь, що поїхала, — ні.

Виміряно: за сто діб місії (межа фікстури — 120) напрямок на Сонце йде на
102°, `cos = −0.213`. Освітлений бік корпусу й освітлений бік планети
зсуваються в один бік і перевертаються разом — перевірено зламом: корпус зі
старою сталою дає −5.6 проти +5.6 пікселя, тобто рівно ту суперечність, якою
борг був небезпечний.

### V6. Перемір і закриття ✅ (2026-08-16)

`--perf-probe` дістав рядок «корабель у кадрі», 1280×720, 300 кадрів,
RTX 4070 Ti SUPER:

| висота | корабель | без, мс | з кораблем | різниця | разів |
|---|---|---|---|---|---|
| 400 км | 15 м, два проходи | 0.135 | 0.172 | +0.037 | 1.28 |
| 10⁷ м | 15 м, два проходи | 0.115 | 0.157 | +0.042 | 1.36 |
| 10⁷ м | 10⁶ м, один прохід | 0.111 | 0.134 | +0.023 | 1.21 |

**Третій рядок і є висновок.** Той самий меш, але далеко: `near` лишається
великою, проходів один — тобто це ціна самого корабля, без діапазону.
Виходить, що з 0.04 мс приблизно половина — його малювання й вивантаження, а
половина — **другий прохід глибини**, який він приводить із собою через
`near`. Читати різницю як «1614 вершин коштують 0.04 мс» було б неправильно, і
саме тому рядків три, а не два.

Абсолютні числа при цьому лишаються дрібницею: 0.17 мс проти бюджету 16.7 мс
на 60 Гц, тобто корабель забирає 0.2% кадру. Шум прогону — ±0.005 мс на перших
двох рядках і ±0.01 на третьому, тож «половина на половину» — це порядок, а не
дві значущі цифри.

**Борги й питання після етапу.** D16 закрито (V5). D17 (смуги аеральної
перспективи) лишається: етап його не чіпав, і його момент — наступна робота
над повітрям. Нових боргів етап не породив; нових питань у QUESTIONS.md теж
немає — усе, що постало (порядок кабіни й матеріалів), уже стоїть там від Q2.

### Чого етап свідомо не робить

- **Матеріалів і PBR** — це наступний крок M5, і саме він приносить пайплайн
  мешів. Заглушка лишається сірою.
- **Кабіни.** Вона впирається в прозоре скло й експозицію, тобто в M5.
- **Тяги.** Факел без неї малювати не можна; тяга — окремий етап у `/core`.
- **Слід із корабля не малюється** (рішення 2026-08-16) — ламана на пів неба
  з кабіни це не інформація.

---

## Дві розвилки, що змінюють план цілком

**1. Ефемерида не тримає точності на 200 років.** DOP853 — не симплектичний;
якщо накопичення заокруглень виявиться завеликим, впроваджуйте IAS15 саме для
офлайнового рахунку ефемериди. Це локальна заміна: рантайму вона не
торкається, бо ефемерида — готовий ассет.

**Стан: на горизонті, який репозиторій може захистити, розвилка не жива.**
До 10 років похибка ассета — це фізика, якої бракує (k ≈ 1), а не інтегратор
(k = 2), і IAS15 не купив би нічого. Оборотність за 200 років дає 16.1 м при
дуже туго заданому допуску — саме по собі не катастрофа.

Розвилка лишається відкритою рівно там, де оракула немає — на 200 роках.
Рухає її не заміна інтегратора, а відсутня фізика в кукері — найближча
названа є в D9 (лібрація Місяця); порівняння повторюється тим самим
`ex_ephspan`, який для цього й лишається в `make csv`.

**Окрема засторога проти спокуси покрутити GM.** Мінімум відносної похибки
Землі лежить на +0.3% GM гігантів (увосьмеро краще за +0.02%, фізично
передбачені), тоді як барицентрична похибка від цього монотонно гіршає.
Метрика, що покращується, і метрика, що гіршає, від одного повороту ручки —
це підгонка, а не виправлення. Брати системні GM з DE441 варто **заради
правильності даних**, і ніколи заради меншого числа проти JPL.

**2. Bindless недоступний там, де треба.** Закрито розвідкою P0: повний набір
фіч є на Vulkan, D3D12 і Metal (ліміти — у «Рішення / Рендер» вище).

---

## Вимір продуктивності

Не мілстоун і не критерій — наскрізний процес, що повторюється щоразу, як
з'являється нова вартість (нові тіла в M3.5, планети в M4). Методика й порядок
дій — скіл `perf-probe`; тут лише числа для звірки з майбутніми.

**Профіль збірки записувати обов'язково.** Між `cargo run` і
`cargo run --release` різниця в часі кадру **тринадцятикратна** (1.305 проти
0.097 мс), у CPU-проході camera-relative — сорокадвократна. Число без профілю
непорівнянне з жодним іншим. Зонд тепер друкує профіль і сцену сам.

**Історію не перезаписувати** — регресію видно лише в порівнянні.

Фізика, `make bench` (орбіта `sc_dop853`):

| | Steam Deck | RTX 4070 Ti SUPER |
|---|---|---|
| прийнятих кроків/с | 1 559 999 | 3 007 184 |
| час на прийнятий крок | 0.641 мкс | 0.333 мкс |
| vessel-кроків у бюджеті 60 Hz | 26 000 | 50 121 |

⚠ Цей рядок міряє **інтегратор**, а не силову модель: `accel_two_body`, а не
`accel_field`. Скільки апаратів витримує тік, він не каже — це рядок нижче.

Силова модель, `make bench` (`bench_field`, RTX 4070 Ti SUPER, LEO 300 км,
ассет-фікстура з десятьма тілами):

| конфігурація | ns на одне `accel_field` | проти точкових мас |
|---|---|---|
| точкові маси | 727 | — |
| + гармоніки | 752 | +3.4% |
| + SRP | 832 | +14.4% |
| + опір (повна модель) | 1183 | **+62.7%** |
| `accel_two_body` на тих самих станах | 3.5 | — |

**Повна модель коштує в 342 рази більше за `accel_two_body`** — і саме тому
рядок вище не можна читати як спроможність гри. При 15 обчисленнях на
прийнятий крок один vessel-крок коштує 17.7 мкс, тобто **929 vessel-кроків у
бюджеті 60 Hz замість 50 121**. Різниця в 54 рази, і вона вся в тому, що
міряли.

Три речі, які цей вимір з'ясував і які варто знати наперед:

- **Гармоніки другого ступеня майже безкоштовні** (+3.4%). Ціну задає
  ефемерида — десять чебишевських обчислень, — а не рекурсія Пайнса. Але це
  властивість ступеня 2, а не гармонік: на високих ступенях платить уже сама
  рекурсія (рядок про Місяць нижче).
- **Опір дорожчий за SRP удвічі** (+351 нс проти +80), і платить у ньому не
  арифметика вітру, а `eph_body_angular_velocity` — чебишевська похідна
  ступеня 36 по чотирьох каналах (виміряно окремо в K7b: 310 нс).
- **`accel_two_body` у тісному циклі — 3.5 нс**, а в `bench_dop853` виходить
  ~22 нс на обчислення. Різниця не в функції, а в тому, що всередині
  інтегратора вона йде з його власними накладними. Порівнювати між собою
  можна лише числа з одного циклу — тому обидва й міряються в одному прогоні.

**Ціна ступеня гармонік** (`bench_field`, та сама машина). Рекурсія Пайнса
строго лінійна за кількістю членів:

| ступінь | членів | нс на виклик |
|---|---|---|
| 2 | 3 | 37.4 |
| 4 | 12 | 92.2 |
| 6 | 25 | 174.4 |
| 8 | 42 | 288.1 |

Нахил (ненормована форма, стеля ступеня 8) — **6.43 нс на член + 18.2 нс**,
тобто ступінь 50 екстраполювався в ≈ 8.5 мкс. **Реальний вимір на полі GRAIL,
100 км над Місяцем: 13.5 мкс** проти 0.73 мкс на самих точкових масах — у
вісімнадцять разів, а не у вісім, бо нормована рекурсія дорожча на член
(корені в коефіцієнтах гілок). Саме тому ассет несе ступінь 24 (**4.1 мкс**),
а не 50. Урок ширший за число: екстраполяція вартості через зміну форми
запису не переноситься.

Пропускна здатність за режимами, `make bench` (`bench_prop`, та сама машина,
`tol_m` 1.0 і `h_max_s` 3600 — типові в `core-rs`, апарат 1000 кг / 20 м²):

| режим | мкс/крок | кроків на добу симуляції | стеля warp, одне ядро | історія |
|---|---|---|---|---|
| LEO 400 км, з опором | 14.5 | 171 | ×3.5·10⁷ | 24 ГіБ/год |
| LEO 400 км, без опору | 10.3 | 171 | ×4.9·10⁷ | 34 ГіБ/год |
| MEO 2000 км | 14.5 | 129 | ×4.6·10⁷ | 24 ГіБ/год |
| GEO 35786 км | 10.4 | 24.5 | ×3.4·10⁸ | 33 ГіБ/год |
| відстань Місяця | 10.4 | 24.2 | ×3.4·10⁸ | 33 ГіБ/год |

Три висновки, і третій — найважливіший:

- **Скільки кроків потрібно, вирішує орбіта, а не модель сил.** GEO коштує
  сім разів менше за LEO не тому, що там дешевші сили (10.4 проти 14.5 мкс —
  це опір), а тому, що кроків на добу вчетверо менше.
- **Запас пропускної здатності — п'ять-шість порядків.** Навіть найважчий
  режим дає ×3.5·10⁷ на одному ядрі; дев'ять апаратів ділять це на дев'ять.
  Місячний апарат із полем GRAIL просідає ушестеро (4.1 мкс проти 0.73 на
  обчислення) — і все одно лишається на шість порядків більше, ніж потребує
  гра. **Фізика не буде вузьким місцем.**
- **Колонка «історія» описує світ до етапу N** — тоді історія не
  проріджувалась ніколи (104 байти на семпл), тож warp був водночас швидкістю
  алокації. Після вікна в обертах ці числа — стеля того, що **порахується**, а
  не того, що лишиться в пам'яті: 31 апарат за сто діб тримає 1.95 МіБ.

Рендер, `--perf-probe`, 300 кадрів, release, сфера 8385 вершин:

| машина | розд. | mean, мс | p95, мс | запас до 60 Hz |
|---|---|---|---|---|
| RTX 4070 Ti SUPER | 1280×720 | 0.109 | 0.136 | +16.56 мс |
| RTX 4070 Ti SUPER | 1920×1080 | 0.110 | 0.136 | +16.56 мс |

**Головне — не запас, а те, що обидві роздільності дають однакове число.**
Кадр не залежить від кількості пікселів: чверть часу — CPU-прохід
camera-relative по 8385 вершинах, решта — синхронізація. Про M4 це не каже
нічого: там вершин буде на порядки більше, а прохід по них лишиться на CPU,
доки його не замінить зсув по патчах. Саме тому зонд міряє цей прохід окремо.

(Рядок Steam Deck по рендеру не наводиться: він рахувався в debug, на
трикутнику F2, і непорівнянний ні з чим.)

Рендер з інтерфейсом, `--perf-probe`, 300 кадрів, release, та сама сфера
(U1b, RTX 4070 Ti SUPER). **Три рядки на роздільність — з одного прогону**:
різниця між прогонами на цій машині більша за те, що коштує панель, тож
порівнювати з 0.109 мс зі старого прогону було б порівнянням шуму з панеллю.

| розд. | інтерфейс | mean, мс | p95, мс |
|---|---|---|---|
| 1280×720 | немає | 0.095 | 0.106 |
| 1280×720 | порожній прохід egui | 0.112 | 0.115 |
| 1280×720 | панель з текстом | 0.118 | 0.121 |
| 1920×1080 | немає | 0.105 | 0.103 |
| 1920×1080 | порожній прохід egui | 0.118 | 0.116 |
| 1920×1080 | панель з текстом | 0.125 | 0.127 |

- **Провід коштує більше, ніж те, що ним малюють:** +0.017 мс за порожній
  прохід і ще +0.006 мс за панель із рядком тексту. Тобто ціна — це другий
  прохід і його синхронізація, а не віджети; додавати панелі дешево, а от
  прохід має лишитися **одним**.
- **Разом це +24% часу кадру** й −0.023 мс запасу з 16.6. ⚠ Число зняте на
  **синтетичній** панелі; п'ять справжніх коштують 0.75 мс (U8, блок нижче),
  тобто «додавати панелі дешево» правда лише про прохід.

Рендер планетою з патчів, `--perf-probe`, 300 кадрів, release, RTX 4070 Ti
SUPER. **Сцена інша, ніж у двох блоках вище:** не UV-сфера 8385 вершин, а
одне тіло зі `Scene` — шість патчів 32×32 (6534 вершини), camera-relative раз
на патч (R1d) і виклик малювання на тіло (R1e).

| розд. | інтерфейс | mean, мс | p95, мс |
|---|---|---|---|
| 1280×720 | немає | 0.064 | 0.075 |
| 1280×720 | порожній прохід egui | 0.078 | 0.098 |
| 1280×720 | панель з текстом | 0.082 | 0.084 |
| 1920×1080 | немає | 0.068 | 0.071 |
| 1920×1080 | порожній прохід egui | 0.087 | 0.164 |
| 1920×1080 | панель з текстом | 0.086 | 0.094 |

CPU-прохід планети з того самого прогону (два числа без одного не означають
нічого, тому зонд друкує обидва):

| прохід | мкс | частка бюджету 60 Hz |
|---|---|---|
| UV-сфера, 8385 вершин щокадру | 26.7 | 0.16% |
| патчі, один початок на патч (R1d) | 0.015 | 0.0001% |
| патчі з поворотом і радіусом тіла (R1e) | 0.027 | 0.0002% |

**R1e подорожчав прохід удвічі й це не має значення:** дев'ять множень
повороту на початок патча знизили виграш з 1579 разів до 973, а сам прохід
лишився на чотири порядки меншим за час кадру. На N тіл він множиться на N —
шість початків на тіло, — і саме тому стеля тут не в проході, а у виклику
малювання на тіло, який зніме R6.

**Планета з LOD, відбором у compute й `draw_indirect` (R8, після R1–R6).**
Та сама методика й та сама машина; **сцена знову інша**, і цього разу вона
залежить від камери — набір патчів вибирає LOD, тож одного рядка більше не
досить. Обидві висоти над тілом радіуса Землі:

| висота | розд. | інтерфейс | mean, мс | p95, мс |
|---|---|---|---|---|
| 10⁷ м | 1280×720 | немає | 0.153 | 0.178 |
| 10⁷ м | 1280×720 | панель | 0.177 | 0.181 |
| 10⁷ м | 1920×1080 | немає | 0.162 | 0.156 |
| 10⁷ м | 1920×1080 | панель | 0.193 | 0.192 |
| 100 км | 1280×720 | немає | 0.252 | 0.267 |
| 100 км | 1280×720 | панель | 0.287 | 0.280 |
| 100 км | 1920×1080 | немає | 0.362 | 0.460 |
| 100 км | 1920×1080 | панель | 0.394 | 0.512 |

Що з цього випливає, і що ні:

- **Кадр подорожчав удвічі там, де сцена не змінилась** (0.064 → 0.153 мс на
  10⁷ м). Платить за це не геометрія — патчів там п'ять проти шести, — а
  **прохід compute плюс вибір рівня на CPU**. Тобто ціна R2–R6 сплачена
  наперед і однаково на всіх висотах;
- **З низької орбіти кадр коштує 0.25–0.36 мс**, тобто в 46 разів менше за
  бюджет 60 Hz на 1080p. Планета перестала бути найдешевшою річчю в кадрі,
  але до стелі їй далеко;
- **CPU-прохід планети не повернувся**: 0.027 мкс на тіло, те саме число, що
  на R1e. LOD і тайли не принесли per-vertex роботи назад — саме це R8 і мав
  перевірити.

**Повітря (S7, після S1–S6).** Та сама машина й та сама сцена зондів рушія;
різниця між рядками — рівно одна: чи має тіло атмосферу. 1280×720, release,
300 кадрів.

| висота | повітря | без, мс | з повітрям, мс | різниця | разів |
|---|---|---|---|---|---|
| 10 км | малюється | 0.388 | 0.497 | +0.108 | 1.28 |
| 500 км | малюється | 0.137 | 0.522 | +0.386 | 3.82 |
| 10⁷ м | малюється | 0.117 | 0.321 | +0.204 | 2.74 |
| 6.0·10⁷ м | малюється | 0.116 | 0.236 | +0.119 | 2.03 |
| 6.5·10⁷ м | пропущено | 0.109 | 0.114 | +0.005 | 1.05 |
| 10⁹ м | пропущено | 0.104 | 0.108 | +0.005 | 1.05 |

Що з цього випливає:

- **Найдорожче не з поверхні й не здалеку, а з 500 км** — уп'ятеро дорожче за
  кадр без повітря. Там об'єм аеральної перспективи марширує крізь найгустіше
  повітря на повну довжину, і водночас у кадрі ще ціла планета. Абсолютне
  число — 0.52 мс, тобто 3% бюджету 60 Hz;
- **ціна не залежить від кількості апаратів узагалі.** Жоден LUT не належить
  місії: пропускання й розсіювання рахуються раз на набір параметрів, небо й
  об'єм — раз на кадр, і камера одна. Тридцять апаратів коштують стільки ж,
  скільки один;
- **умова пропуску одна на всю роботу з повітрям**, і вона в пікселях кадру:
  шар малюється, лише коли його товщина в кадрі не менша за піксель (6.24·10⁷ м
  для Землі). Спершу умов було дві — об'єм пропускався, таблиця неба ні, — і
  різницю виміряно: 0.05 мс за диск повітря завширшки в шістнадцяту пікселя.
  Після зведення до однієї умови повітря на 10⁹ м коштує 0.005 мс.

**Справжній кадр гри — сцена місії з її панелями (U8).** Усі блоки вище міряли
`engine`: сцену рушія й **синтетичну** панель (прямокутник і рядок тексту).
Цей — перший, що міряє те, що бачить гравець: `cargo run --release -p game --
--perf-probe <діб>`, 300 кадрів, та сама RTX 4070 Ti SUPER, місія доведена до
доби 101.8 (2448 семплів історії, 0.2 МіБ).

| фрейм | вершин | розд. | інтерфейс | mean, мс | p95, мс |
|---|---|---|---|---|---|
| інерціальний | 2454 | 1280×720 | немає | 0.182 | 0.274 |
| інерціальний | 2454 | 1280×720 | **панелі** | **0.932** | 1.044 |
| інерціальний | 2454 | 1920×1080 | немає | 0.179 | 0.257 |
| інерціальний | 2454 | 1920×1080 | **панелі** | **0.924** | 1.008 |
| обертовий | 2816 | 1280×720 | немає | 0.187 | 0.243 |
| обертовий | 2816 | 1280×720 | **панелі** | **0.932** | 1.010 |
| обертовий | 2816 | 1920×1080 | немає | 0.195 | 0.283 |
| обертовий | 2816 | 1920×1080 | **панелі** | **0.929** | 1.039 |

CPU-прохід підготовки сцени з того самого прогону:

| що | інерціальний | обертовий |
|---|---|---|
| `view::build_in` цілком | 0.015 мс | 0.323 мс |
| з них крива нульової швидкості | — | **0.249 мс** на 362 вершини |

Три висновки, і жоден не той, якого чекав план:

- **Панелі коштують 0.75 мс і тепер найдорожчі в кадрі** — 0.93 проти 0.18 без
  них. Це **в тридцять разів** більше за +0.023 мс, які U1b виміряв на
  синтетичній панелі, і різниця не в віджетах як таких, а в тому, що їх п'ять
  у двох колонках із текстом, сіткою й розкладкою. Обидві роздільності дають
  те саме число, тобто платить CPU і прохід, а не растеризація. До бюджету
  60 Hz це 5.6%, тож запас лишається — але «додавати панелі дешево» (U1b)
  більше не діє: дешевий там був **прохід**, а не вміст.
- **В обертовому фреймі платить крива, а не точки.** Побудова сцени там
  дорожча в 21 раз (0.323 проти 0.015 мс) при зайвих трьох сотнях вершин, тож
  ×4 з U6a1 цього пояснити не могли. Вимір окремо: ZVC — 77% усього проходу,
  **689 нс на вершину** проти 10.56 нс у точки траєкторії, і перераховується
  вона щокадру, хоч залежить лише від `C` і `μ`. Це борг D11 нижче.
- **D7 на цій фікстурі не видно, і це вимір, а не пропуск.** Місія впирається
  в 120-денний спан ассета на добі 101.8 і дає 2816 вершин — на **два з
  половиною порядки** менше за мільйон, про який говорив борг. Бракує при
  цьому апаратів, а не спану: та сама фікстура з тридцятьма дала 831 тис.
  вершин — блок «Флот» нижче.

### Планета з рельєфом і процедурною деталлю (R7c)

RTX 4070 Ti SUPER, Vulkan, release, 1280×720, Місяць з `assets/moon.dem`;
найкращий кадр із шістдесяти, синхронний `poll(Wait)` — верхня межа, як і
скрізь у цьому розділі.

| висота | гладке тіло | рельєф + деталь | множник |
|---|---|---|---|
| 100 км | 0.161 мс | 0.557 мс | 3.5 |
| 1 км | 0.483 мс | 1.699 мс | 3.5 |
| 300 м | 0.566 мс | 2.229 мс | 3.9 |

**Що саме подорожчало.** Не растеризація: платить вибір рівня на CPU і
вибірки тайла у вершинній стадії. Рельєф увійшов у критерій рівня (R7c), тож
на 300 м патчів **219 проти 81**, а кожна вершина робить дванадцять
`Load` замість чотирьох — чотири на висоту й вісім на центральну різницю
нахилу.

З T8 **`--perf-probe` міряє й тайли** (`perf_probe::tile_cost`): він читає
`assets/moon.dem` і `assets/moon.col`, якщо вони на диску, і пропускає ці
рядки, якщо ні. Числа вище зняті окремим прогоном до того й лишаються як є —
методика та сама, але сцена інша (найкращий кадр із шістдесяти проти середнього
з трьохсот).

### Колірні тайли, і чому їхня ціна виявилась не там (T8)

Та сама машина, release, 1280×720, 300 кадрів; Місяць із `assets/moon.dem`,
різниця між стовпцями рівно одна — чи завантажений `assets/moon.col`.

| висота | без кольору | з кольором | різниця | разів |
|---|---|---|---|---|
| 100 км | 0.566 мс | 0.979 мс | +0.413 | 1.73 |
| 10⁶ м | 0.304 мс | 0.678 мс | +0.373 | 2.23 |
| 10⁷ м | 0.289 мс | 0.681 мс | +0.392 | 2.36 |
| **10⁹ м** | 0.308 мс | 0.697 мс | **+0.389** | 2.27 |

**Останній рядок і є результат кроку.** На 10⁹ м Місяць займає кілька
пікселів — а колір коштує рівно стільки ж, скільки з низької орбіти. Тобто
ціна **не у другій вибірці на фрагмент**, як записував план, і взагалі не
залежить від того, що на екрані.

Де вона тоді, показує другий вимір: та сама сцена на 10⁹ м, міняється лише
глибина піраміди, тобто **кількість текстур у bindless-масиві**.

| рівнів | тайлів | кадр, мс |
|---|---|---|
| 1 | 6 | 0.306 |
| 2 | 30 | 0.315 |
| 3 | 126 | 0.319 |
| 4 | 510 | 0.340 |
| 5 | 2046 | 0.417 |
| 6 | 8190 | 0.728 |

Лінійно за кількістю текстур вище пів тисячі: **≈51 нс на текстуру за кадр**.
Це не CPU: час кодування командного буфера виміряний окремо й однаковий з
кольором і без нього (0.067 мс), тобто платиться на боці драйвера/GPU при
прив'язці масиву.

Три наслідки, і всі три треба знати наперед:

- **Глибина піраміди коштує щокадру, а не лише пам'яттю.** T2a вибирав рівень
  6 замість 7 за пам'яттю (32 МіБ проти 256); тепер у того ж вибору є друга
  ціна — 0.42 мс кадру, і сьомий рівень коштував би близько 1.7 мс, тобто
  десяту частину бюджету 60 Hz ні за що;
- **Земля прийшла з чотирма каналами й тією ж глибиною** (T7e), тобто той
  самий масив і та сама ціна за прив'язку — вона не залежить від розміру
  текселя; переміряно на T7h;
- **лікується це резидентним набором**, а не меншою пірамідою: у кадрі
  малюються десятки патчів, а прив'язуються тисячі текстур. Це борг **D19**.

### Флот: борг D7 у числах, до етапу N і після (N1–N5)

`cargo run --release -p game -- --perf-probe <діб> --stations <n>`, 300 кадрів,
RTX 4070 Ti SUPER. Флот — `mission::fleet`: halo 1151 плюс `n` станцій на
колових орбітах 600–1300 км. Ассет той самий, 120-денний; нового не
знадобилося. Інерціальний фрейм, 1280×720, без панелей; `build_in` міряється
окремо від кадру й у грі додається до нього.

| апаратів | діб | вершин | історія | `build_in` | кадр mean | кадр p95 | `Lines::upload` |
|---|---|---|---|---|---|---|---|
| 1 (U8) | 101.8 | 2 454 | 0.2 МіБ | 0.015 мс | 0.18 мс | 0.27 мс | — |
| 11 | 101.8 | 290 477 | 28.8 МіБ | 4.05 мс | 5.16 мс | 7.66 мс | 3.40 мс |
| 31 | 50.4 | 431 320 | 42.8 МіБ | 5.15 мс | 6.54 мс | 8.63 мс | 4.76 мс |
| **31** | **101.8** | **830 920** | **82.4 МіБ** | **10.19 мс** | **13.49 мс** | **18.22 мс** | **9.28 мс** |

**Борг був реальний, і ось де саме він різав.** На тридцяти станціях за сто діб
кадр коштував `build_in` 10.19 плюс 13.49, тобто **23.7 мс** — 42 Hz замість
60, а p95 самого лише кадру вже вийшов за 16.67. В обертовому фреймі разом
**31.2 мс**, тобто 32 Hz. Це не екстраполяція й не стеля warp: це сцена, яку
розробник назвав звичайною. Найдорожче в ній — `Lines::upload`, 69% кадру, і
частка не залежить ні від роздільності (73% на 1080p), ні від фрейму (72%).

**Три числа викладки боргу виявилися неправильними, і всі — в один бік.**

- **Вершин більше, ніж обіцяла екстраполяція:** 831 тис. проти 616 тис. Причина
  не в кількості апаратів, а в допуску: `bench_prop` міряє при `tol_m` 1.0 і
  дає 171 семпл на добу, гра живе на `tol_m` 1e-2 і дає **263**.
- **Вершина коштує 11.2 нс, а не 3.** Число «3 нс» узяте з I3 і мовчки
  припускало, що прохід масштабується лінійно. Не масштабується: ціна вершини
  стала на всьому діапазоні 290–831 тис. (11.0–11.7 нс), тобто зростання вже
  сталося десь між 8 тис. і 290 тис. Найпростіше пояснення — кеш (0.3 МіБ
  проти 12); число ж виміряне.
- **Пам'ять на два порядки менша за страшну.** 82 МіБ на сто діб проти
  «гігабайтів»: 24 ГіБ/год зняті на стелі warp, у яку гравець не потрапляє.

**Та сама сцена після етапу N** (вікно 20 обертів):

| | до етапу N | після критерію й пенсії | **після вікна** |
|---|---|---|---|
| семплів історії | 830 734 | 330 467 | **19 686** |
| пам'ять історії | 82.4 МіБ | 32.8 МіБ | **1.95 МіБ** |
| вершин у кадрі | 830 920 | 319 643 | **11 948** |
| кадр разом із побудовою | 23.7 мс (42 Hz) | 6.6 мс (151 Hz) | **0.33 мс** |

І головне — **воно не росте**: на 50-й добі історія більша (32 192), ніж на
101-й (19 686). Величина залежить від режиму польоту, а не від того, скільки
часу минуло. Скільки коштує саме вікно, на тому самому флоті: 5 обертів —
1.19 МіБ, **20 (типово) — 1.95**, 100 — 4.22, 500 — 13.09. Зростання не лінійне
за обертами, бо пенсія проріджує старшу частину вікна.

⚠ **Найдорожче в кадрі — не те, що зонд називає кадром.** `view::build_in` не
входить у колонку «кадр mean»: зонд будує сцену раз, а міряє малювання. У грі
обидві роботи на одному кадрі, тож справжня ціна — сума. Тримати їх у одній
колонці не можна: проріджування б'є по обох, але в різних пропорціях, і злиття
сховало б, у якій.
