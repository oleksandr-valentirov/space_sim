# ROADMAP.md — послідовність робіт

> Що робити, як перевіряти, що робити якщо не вийде.
> Рішення — у [PROJECT.md](PROJECT.md). Тут лише порядок дій.
> Дат немає свідомо: важливий порядок, не швидкість.

---

## Принцип упорядкування

Кроки впорядковані **за спаданням імовірності успіху**, а не за важливістю.
Спочатку те, що майже напевно спрацює: воно будує інструменти, на яких потім
діагностуються складні кроки. Коли на етапі C щось не збігається, ви вже маєте
еталонні дані, тести й хеші — і шукаєте помилку, а не гадаєте.

**Єдиний виняток — дешеві розвідки.** Якщо архітектурне рішення ризиковане,
але перевірити його можна за години (`P0`, `P1`), перевірка йде рано. Сама
розвідка має високу ймовірність успіху: вона гарантовано дає відповідь, навіть
якщо відповідь неприємна.

Кожен крок має **перевірку** (як дізнатися, що зроблено) і **розвилку**
(що робити, якщо не вийшло). Розвилка — не аварійний план, а очікуваний
варіант розвитку: у чисельних задачах більшість кроків із першого разу
не сходиться.

Позначки ризику: 🟢 низький — питання часу; 🟡 середній — очікуйте день-два
на пошук причини; 🔴 високий — може змінити план.

---

## Етап A. Фундамент 🟢

Тут нічого не може піти по-справжньому не так. Мета — щоб уся подальша
діагностика вже мала інструменти.

### A1. Репозиторій і збірка

`git init`, `.gitignore`, структура каталогів за PROJECT.md §3.
Для M0 — простий `Makefile`: юніт-тести й CSV-утиліти це виконувані файли,
а крейт `cc` збирає статичні бібліотеки, тож на цьому етапі він лише заважає.
Прапорці: `-std=c11 -Wall -Wextra -Werror -ffp-contract=off -O2`.

**Перевірка:** `make test` проходить на порожньому тесті. Навмисна помилка
(невикористана змінна) ламає збірку — отже `-Werror` справді ввімкнений.

**Розвилка:** немає.

> **Борг, який треба пам'ятати:** на M1 ті самі `.c` файли збиратиме `build.rs`
> через крейт `cc`. Прапорці мусять збігатися з `Makefile` бітово, інакше
> детермінізм тихо зламається на межі. Тримайте їх в одному місці й додайте
> тест, що звіряє два набори.

### A2. Каркас детермінізму — до будь-якої фізики

Це найважливіше рішення послідовності. Детермінізм у PROJECT.md §4 —
жорстка вимога, а вимоги такого класу неможливо додати пізніше: вони або
вбудовані з першого рядка, або вам доведеться перевіряти кожен уже написаний.

Робимо:
- побітове хешування стану (хеш сирих байтів `double`, не форматованого тексту);
- еталонні хеші у репозиторії, утиліта звірки;
- **«поліція libm»**: крок збірки, що проганяє `nm` по об'єктних файлах ядра
  і падає, якщо серед невизначених символів є `sin`, `cos`, `exp`, `pow`,
  `atan2`, `log`. `sqrt` дозволений.

**Перевірка:** додайте в ядро виклик `sin()` — збірка мусить впасти. Змініть
константу в п'ятому знаку — звірка хешів мусить це побачити.

**Розвилка:** якщо `nm` поводиться по-різному на платформах — послабте до
перевірки за вихідним кодом (грепом). Гірше, але краще за нічого; повернетесь
до `nm`, коли з'явиться CI.

### A3. Базова математика

`Vec3d`, `State`, операції. Накопичення сум — у **фіксованому, явно заданому
порядку**; це не стилістика, а вимога детермінізму.

**Перевірка:** юніт-тести на точних значеннях. Тест, що сума масиву в прямому
й зворотному порядку дає **різні** біти — щоб зафіксувати, чому порядок
прописаний явно.

---

## Етап B. Ефемерида 🟢→🟡

### B1. Еталонні дані JPL Horizons — до власного інтегратора

Вивантажте вектори стану Сонця, Землі, Місяця на набір епох і покладіть
у репозиторій як фікстури.

Це перший крок не випадково: він майже гарантовано вдається і дає **оракул**,
відносно якого вимірюється все інше. Писати інтегратор, не маючи з чим
порівняти, — найдорожчий спосіб рухатись.

**Перевірка:** завантажили, розпарсили, вивели — числа збігаються з тим,
що показує вебінтерфейс Horizons.

**Розвилка:** формат чи доступ до API змінилися — вивантажте вручну один раз
і закомітьте файл. Дані статичні, оновлювати їх не доведеться.

⚠️ **Головна пастка етапу:** система координат і одиниці. Вкажіть явно
й запишіть у коментарі: барицентр Сонячної системи, ICRF, км чи м, TDB.
Більшість розбіжностей далі — саме тут, а не у фізиці.

### B2. Чебишевська апроксимація

Підгонка й обчислення. Обчислення — тільки `+` і `*` (схема Кленшоу).

**Перевірка:** підганяємо відому аналітичну функцію, максимальна похибка
падає з ростом ступеня очікувано; round-trip підгонка→обчислення дає
< 1e-14 відносних.

**Розвилка:** 🟢 стандартна чисельна задача, серйозних розвилок немає.

### B3. RK4 з фіксованим кроком і задача двох тіл

Так, вам потрібен DOP853. Але RK4 — це тридцять рядків, і він валідує
силову модель, розкладку стану й тестовий каркас **до** того, як ви почнете
шукати помилку в складному інтеграторі.

**Перевірка:** кругова орбіта замикається; для еліптичної період збігається
з аналітичним третім законом Кеплера; енергія дрейфує монотонно й пропорційно
`h⁴` — зменшили крок удвічі, похибка впала в 16 разів. Останнє — найкращий
тест: він ловить помилку в порядку методу.

**Розвилка:** не сходиться порядок — помилка або в коефіцієнтах, або в силовій
моделі. Перевірте силу окремо, без інтегратора: аналітично відома
сила на відомій відстані.

### B4. DOP853

Той самий інтегратор, що працюватиме в рантаймі (PROJECT.md §4). Адаптивний
крок, контроль похибки по позиції, допуск у метрах.

**Перевірка:** ті самі тести, що для RK4, плюс: зменшення допуску на порядок
зменшує фактичну похибку приблизно на порядок; кількість кроків на еліптичній
орбіті помітно зростає біля перицентру. Тест оборотності: 10 років вперед
і назад.

**Розвилка:** 🟡 контроль кроку — найтонше місце. Якщо крок скаче або
затискається в нуль: перевірте формулу оцінки похибки окремо від логіки
прийняття кроку, і обмежте коефіцієнт зміни кроку (не більше ніж ×5 / ÷5
за раз).

### B5. Земля–Місяць–Сонце, точкові маси, регресія на Horizons

**Перевірка — і тут важливо правильно поставити мету.** Мета цього тесту —
ловити **помилки**, а не досягати точності JPL. Помилка в системі координат
чи одиницях дає розбіжність у сотні тисяч км; відсутня фізика (інші планети,
J2) — незрівнянно меншу. Тест добре їх розрізняє.

Тому критерій відносний, а не абсолютний: виміряйте розбіжність за 10 років,
запишіть як базову лінію, і далі кожне додане збурення мусить її **зменшувати**.
Абсолютний поріг ставите після першого вимірювання, а не наперед.

**Розвилка:**
- Розбіжність гігантська (10⁵ км і більше) → це не фізика. Дивіться систему
  координат, епоху, одиниці, знак, барицентр проти геліоцентра.
- Розбіжність помітна, але правдоподібна → так і має бути. Додавайте по одному:
  точкові маси Венери й Юпітера, потім J2 Землі. Кожне додавання — окремий
  комміт з виміряним ефектом.
- Розбіжність зростає лінійно з часом → підозра на систематичну помилку
  в силі. Зростає як корінь із часу → це вже накопичення заокруглень, тобто
  норма.

#### ✅ Виконано. Що виміряно

Максимум за 10 років проти Horizons, допуск 1 м (збіжний):

| Модель | Земля в SSB | Земля відносно барицентра моделі | Місяць геоцентрично | Дрейф барицентра |
|---|---|---|---|---|
| Сонце+Земля+Місяць | 5.42·10⁹ м | 5.60·10⁷ м | 1.46·10⁵ м | 4.96·10⁹ м |
| Усі 10 великих тіл | **1.68·10⁶ м** | 6.52·10⁵ м | **9.32·10⁴ м** | 1.05·10⁶ м |

**Головне: майже вся розбіжність малої моделі — це паралельне перенесення
підсистеми, а не спотворення орбіт.** Кілька тіл несуть лише частину імпульсу
Сонячної системи, тож їхній спільний барицентр лінійно дрейфує крізь систему
SSB. Дрейф виміряно **точно однаковим** (1.0509·10⁶ м) на всіх допусках від
10⁶ до 10⁻¹ м — саме це доводить, що він фізичний, а не від інтегратора.

Лікується не інтегратором, а повнотою набору тіл: з усіма великими тілами
похибка Землі падає у 3226 разів, до 1700 км за десять років. Для моделі
з самих лише точкових мас це добре.

**Пастка, яка коштувала хибного висновку.** Похибка Місяця сходиться значно
пізніше за похибку Землі: 8.5·10⁸ → 3.7·10⁸ → 4.0·10⁷ → 3.0·10⁶ → 9.3·10⁴ м
при допуску від 10⁶ до 10⁰. На допуску 10 м геоцентрична похибка Місяця — це
ще майже цілком похибка інтегрування, і я спершу прочитав її як фізичну межу
й упевнено пояснив браком J2 Землі. Причина проста: єдиний глобальний допуск
у метрах однаково ставиться до орбіти Місяця (3.8·10⁸ м) і Нептуна
(4.5·10¹² м), а Місяць — найшвидше тіло системи, тож сходиться останнім.

**Два наслідки для B6:**
1. Ассет ефемериди має включати всі великі тіла. Інакше в нього запікається
   лінійний дрейф у мільярди метрів.
2. Допуск кукера обирати за **найшвидшим тілом**, а збіжність перевіряти
   потіло, а не за сумарною нормою.

### B6. Ефемерида як ассет

Інтегрування → чебишевські коефіцієнти → бінарний файл → завантаження.
Плюс матриці обертання тіл у той самий файл (PROJECT.md §4).

**Перевірка:** стан, зчитаний з файлу, збігається з прямим інтегруванням
у межах заданої точності апроксимації. Хеш файлу стабільний між перезапусками.

**Розвилка:** якщо файл виходить завеликий — знижуйте ступінь полінома
й скорочуйте інтервали, це компроміс розмір/точність з передбачуваною
поведінкою.

---

## Етап C. CR3BP і критерії приймання M0 🟡→🔴

Найінформативніший етап. Тут з'ясується, чи правильне ядро.

Важливо: **CR3BP — окрема, спрощена модель**, не повна ефемерида. У ній
точки Лагранжа мають аналітичні позиції, є збережена величина (константа
Якобі) і опубліковані еталонні орбіти. Саме тому halo-орбіту шукаємо спершу
тут, а не в повній моделі.

### C1. CR3BP і константа Якобі

Безрозмірна постановка, обертова система, параметр μ для Земля–Місяць.

**Перевірка:** позиції L1–L5 збігаються з відомими значеннями для цього μ;
константа Якобі зберігається до ~1e-12 відносних на 100 обертів; криві
нульової швидкості мають правильну топологію при зміні C.

**Розвилка:** 🟡 помилки тут майже завжди в знаках і конвенціях (напрямок
обертання, порядок тіл, чи включена відцентрова частина в потенціал).
Звіряйтеся з підручниковою формою рівнянь, не виводьте наново.

### C2. Halo-орбіта — спершу чужа, потім своя

**C2a. Опублікована початкова умова.** Візьміть готові дані з каталогу
періодичних орбіт JPL, проінтегруйте один період, подивіться, наскільки
орбіта замкнулась.

Це чистий тест інтегратора: жодної власної машинерії пошуку, лише
відтворення відомого результату. Імовірність успіху висока, а діагностична
цінність — теж: якщо чужа орбіта не замикається, проблема в C1, і шукати
її треба там.

**C2b. Диференціальна корекція.** Тепер знайдіть орбіту самі, стартуючи
з аналітичного наближення третього порядку (Річардсон).

**Перевірка:** нев'язка < 1e-10; продовження за сімейством працює —
змінюєте амплітуду, отримуєте сусідню орбіту.

**Розвилка — 🔴 це найризикованіший крок M0:**
- Не сходиться → **перевірте матрицю переходу стану скінченними різницями.**
  Це найкорисніший інструмент усього етапу: зсуньте компоненту початкового
  стану на ε, проінтегруйте, порівняйте з передбаченням STM. Розбіжність
  означає помилку у варіаційних рівняннях, а не в корекції.
- STM правильна, а збіжності немає → зависоко початкове наближення. Беріть
  за старт опубліковану орбіту з C2a і рухайтесь малими кроками.
- Не вдається і це → **не блокуйтеся.** Опублікованих початкових умов
  достатньо, щоб рухатись далі; поверніться до власної корекції на M3,
  коли вона знадобиться для планувальника перельотів.

#### ✅ Виконано. Що виміряно

Жодна з розвилок не знадобилася: STM зробили першою, тож корекції не було
де помилитися, крім власної алгебри. Збіжність із зміщеного насіння — 4–6
ітерацій до нев'язки ~1e-14; з грубого насіння (тільки z із каталогу,
x і vy — круглі числа) опубліковані орбіти відтворюються до 1e-13.

**STM перевірено трьома незалежними способами**, і вони падають по-різному:
скінченні різниці (2.6e-8 за півперіоду проти стовпців до 3.0), симплектичність
(7.2e-12), композиція (8.7e-12). Пастка: у обертовій системі спряжений
імпульс ≠ швидкість, тож перевірка симплектичності без заміни змінних дає
дефект 60 при елементах 10 — і не рухається при зміні допуску на три порядки.
Саме нерухомість і видає помилку означення, а не чисельну.

**Найсильніша перевірка — не окрема орбіта, а сімейство.** 40 членів
продовження дають криву, яка затискає опубліковані орбіти одночасно по
x, vy, періоду й константі Якобі. Це твердження про сімейство, і воно
взагалі не потребує допуску.

Дві поведінки, які легко прийняти за баг:

- **Сімейство складене по z.** За фолдом одному z відповідають дві орбіти,
  і Ньютон знаходить ту, до якої ближче насіння. Тому є другий вибір вільних
  змінних: тримати x замість z — він проходить фолд не помічаючи.
- **Продовження назовні по z від орбіти 0 одразу зупиняється**, бо вона біля
  екстремуму |z| гілки. Тест перевіряє цю відмову як фічу: корректор, який
  відзвітував би про успіх, був би небезпечнішим.

**Річардсон виявився рантаймовим, а не офлайновим.** Ряд тригонометричний,
але єдина фаза, яку ми обчислюємо, — перпендикулярний перетин площини xz,
де всі члени точно −1, 0 або +1. Лишаються `+ − * /` і `sqrt`, тож насіння
halo-орбіт детерміноване й доступне грі для будь-якої системи.

Дві межі ряду, виміряні: розв'язок зникає біля |z| = 0.067 для Земля–Місяць L2
(≈ третина найбільшої каталожної орбіти), і ця межа масштабується з γ —
0.168 для Земля–Місяць L2 проти 0.010 для Сонце–Земля. Та сама абсолютна
амплітуда в одній системі комфортна, в іншій неможлива. Більші орбіти —
продовженням, як і роблять на практиці.

Збіжність **найгірша на малих амплітудах** (14 ітерацій проти 5 на великих):
in-plane амплітуда halo не прямує до нуля разом з out-of-plane, тож
третій порядок саме там найдалі від дому.

### C3. Виміряти нестійкість, а не боротися з нею

Порахуйте монодромну матрицю за період і її власні значення.

**Перевірка:** є власне значення з модулем помітно більше одиниці; час зриву
орбіти, який ви спостерігаєте, узгоджується з цим показником.

Це перетворює «орбіта розвалилась» із симптому бага на виміряну властивість
системи. Ключовий крок: без нього ви шукатимете неіснуючу помилку.

#### ✅ Виконано. Що виміряно

Розв'язувач власних значень не знадобився. Матриця подібна до симплектичної,
тож характеристичний многочлен зворотний; заміна μ = λ + 1/λ зводить шостий
степінь до кубічного, одинична пара ділиться націло, лишається квадратне
рівняння. Коефіцієнти — зі слідів m, m², m³. QR-ітерація коштувала б сотні
рядків і `libm`; тут вистачає `sqrt`, тобто це працює всередині зони
детермінізму.

Індекс стабільності JPL відтворюється точно до всіх опублікованих цифр.
Виміряне зростання проти передбаченого (насіння 1e-12): орбіта 767 — 149.65
і 149.71 проти власного значення 149.717; орбіта 1151 — 594.47 і 594.08
проти 594.134.

Три речі, заради яких крок і потрібен:

1. **Індекс стабільності каталогу — це (λ + 1/λ)/2, а не λ.** Орбіта 1151:
   індекс 297, власне значення 594. Орбіта 0: індекс 1.015, власне значення
   1.19. Коментар у `test_halo.c` цитував індекс як коефіцієнт підсилення за
   оберт — виправлено тут.
2. **Слабко нестійкі орбіти не тікають експоненційно** на жодному масштабі,
   який має сенс спостерігати. Орбіта 0 мала б помножити збурення на 2.85 за
   шість обертів; виміряно 3823. Одинична пара власних значень дефектна —
   один власний вектор на два значення, — тож типове зміщення дрейфує вздовж
   сімейства й уздовж орбіти лінійно за часом, і це надовго перекриває
   множник 1.19. Це жорданова клітина, а не баг.
3. **Нев'язка кореня μ = 2 безкоштовно міряє точність самої монодромної
   матриці:** 5e-10 для добре проінтегрованих орбіт і 2e-2 для майже
   прямолінійної, що проходить під поверхнею Місяця. Тобто ця орбіта сама
   повідомляє про свою ненадійність замість упевнено невірного числа.

### C4. Перенесення в повну ефемериду 🔴

Взяти CR3BP-орбіту як початкове наближення й скоригувати під реальну
ефемериду (multiple shooting).

**Перевірка:** траєкторія лишається біля L2 протягом місяців модельного часу
з обмеженим бюджетом корекцій.

**Розвилка:** якщо чиста квазіперіодична орбіта в повній моделі не виходить —
**це не поразка, а власне гра.** Реальні місії на halo-орбітах виконують
station-keeping. Зробіть простий контролер утримання і виміряйте його бюджет
у м/с на рік — це і є та цифра, якою гравець оперуватиме в §8. Крок C4
у такому разі закривається саме так.

#### ✅ Виконано. Що виміряно

Розвилка не знадобилася для половини сімейства: **балістична траєкторія біля
L2 в повній десятитільній моделі існує й тримається вісім місяців.** Орбіта
1151, 16 обертів (233 дні): 5 ітерацій, розрив неперервності 2.3e-2 м, вузли
змістилися на 0.0097 одиниці довжини від CR3BP-наближення, віддалення від L2
ніколи не перевищило 0.123.

**Базова лінія без корекції** (скільки живе просто перенесена орбіта):
3.31 / 3.69 / 1.31 / 1.12 оберту для орбіт 0 / 383 / 767 / 1151. Порядок
слідує за нестійкістю для трьох, і ламається на орбіті 0 — найстійкішій
з чотирьох, яка живе не найдовше. Отже причина зриву — не тільки власна
нестійкість орбіти, а й розбіжність моделей.

**Три речі довелося зробити правильно, і кожна спершу була неправильна.**

1. *Надлишок свободи.* 6n невідомих проти 6(n−1) умов неперервності, і що
   робити з надлишком — це і є весь результат. Мінімізація **кроку** Ньютона
   чудово збігається (1e-4 м за 350 днів) — до траєкторії за 12 одиниць від
   L2: дрібні кроки спрямовані однаково й накопичуються. Мінімізація
   **відстані до початкового наближення** підтягує ітерацію назад на кожному
   кроці. Один доданок у правій частині — зовсім інша відповідь.
2. *Еталон.* Розкладати вузли, інтегруючи CR3BP-орбіту 16 обертів поспіль,
   означає знищити еталон до того, як його побачить корректор: орбіта 1151
   множить збурення на 594 за оберт. Орбіта періодична — рахуємо один період
   і повторюємо.
3. *Масштабування.* У метрах і м/с матриця переходу охоплює десять порядків
   між блоками, і розв'язок втрачає більшість цифр.

**Половина сімейства не піддається**, і це зафіксовано тестом як відмова:

| орбіта | власне значення | амплітуда | результат |
|---|---|---|---|
| 1151 | 594 | 0.097 | збігається, 16+ обертів |
| 767 | 150 | 0.159 | збігається, 16 обертів |
| 383 | 3.1 | 0.195 | застрягає після 2 обертів |
| 0 | 1.19 | 0.202 | застрягає після 2 обертів |

**Чим орбіта нестійкіша, тим легше вона коригується** — протилежно до
очевидного здогаду. Орбіта 0 застрягає на нев'язці ~1e6 м і лишається там
через 200 ітерацій і вчетверо більше вузлів, при цьому не тікаючи (0.23
одиниці від L2). Дві гіпотези сумісні з даними, і ці чотири орбіти їх не
розрізняють, бо амплітуда й нестійкість уздовж сімейства змінюються разом.

**Бюджет утримання** (еталон — скоригована траєкторія, ціль на пів оберту
вперед, корекція раз на пів оберту):

| похибка виведення | Δv на рік | відхилення |
|---|---|---|
| 0 | 1e-4 м/с | < 100 м |
| 1 км | 0.035 м/с | 2.5 км |
| 10 км | 0.348 м/с | 25 км |

Лінійно з точністю до відсотка. Значно менше за одиниці м/с на рік реальних
місій — і має бути: еталон точно балістичний, похибка одноразова, немає ні
навігаційної невизначеності, ні немодельованих сил.

**Головна знахідка дизайну — горизонт прицілювання.** Ціль у наступний вузол
коштує 5.5e9 м/с: змусити апарат потрапити в точку за 1/8 оберту означає
змусити його прибути туди з якою завгодно швидкістю. Пів оберту вперед
коштує 1e-4. Дев'ять порядків за одне ціле число, і ніщо в рівняннях на це
не натякає. Два оберти вперед уже не збігається: матриця переходу орбіти
з власним значенням 594 занадто чутлива.

### C5. Крос-платформна перевірка детермінізму

Потрібні Linux, Windows, macOS. Соло-розробнику простіше за все підняти
CI на безкоштовних раннерах, ніж шукати машини.

**Перевірка:** еталонний хеш фіксованого сценарію збігається на трьох
платформах.

**Розвилка:** 🟡 перший запуск часто розходиться. Порядок пошуку:
1. прапорці компілятора справді однакові;
2. «поліція libm» з A2 працює на всіх платформах, нічого не просочилось;
3. хешуйте проміжні величини й ділите сценарій навпіл, поки не локалізуєте
   першу розбіжну операцію.

**Це гейт M0.** Далі не йдемо, поки не збігається: пізніше ціна пошуку
зростає на порядок.

#### ✅ Виконано. Гейт M0 пройдено

**Чотири джоби зелені з першого разу, коли конфігурація нарешті стала
валідною:** linux-gcc, linux-clang, macos-clang, windows-mingw. Кожен звіряє
`make determinism` з тим самим `golden.txt`, тож зелений колір і **є**
твердженням, що всі порахували бітово однакові числа — окремого кроку
порівняння не треба.

Що саме покрито — з логів, а не з припущень про образи GitHub (джоб друкує
`uname -srm` і `make --version` саме для цього):

| джоб | ядро й архітектура | make | компілятор |
|---|---|---|---|
| linux-gcc | Linux 6.17, **x86_64** | 4.3 | gcc 13.3.0 |
| linux-clang | Linux 6.17, **x86_64** | 4.3 | clang 18.1.3 |
| macos-clang | Darwin 25.5, **arm64** | **3.81** | Apple clang 21.0.0 |
| windows-mingw | MINGW64_NT-10.0, **x86_64** | 4.4.1 | MinGW gcc |

Тобто чотири компілятори, три версії make і **дві архітектури**. Різні
архітектури — найсильніша частина твердження: збігаються не просто дві збірки
однією інструкцією, а два різні набори інструкцій з плаваючою комою.

І `make --version` тут не для повноти: 3.81 на macOS — це рівно та версія,
на якій впав `#` усередині `$(shell)` (пункт 2 нижче). Друк версії перетворює
той клас помилок з детективної роботи на один погляд у лог.

Всі одинадцять рядків хешів **побайтово однакові** на чотирьох джобах і на
машині розробника:

```
sc_arith 00272faa96f0960f      sc_richardson 4727f9efe7889769
sc_cheb d1404e2c8a4d356e       sc_rk4 cf327da9e36dc16c
sc_correct 25cf066032756923    sc_stm c2fa6f7a5186dc04
sc_cr3bp 507580e8ab7cf799      sc_trajectory 1ff6b8e2400c1b11
sc_dop853 8400ba159cae7209     sc_vec3 0becdeab231904ff
sc_ephemeris b8c1ec113e629def
```

**Три невдалі спроби до цього, і жодна не була про числа** — усі про
конфігурацію. Записано, бо кожна коштувала циклу:

1. `steps[*].shell` не приймає **жодного** контексту, тож
   `shell: ${{ matrix.shell }}` у кроці не просто не спрацював, а зробив весь
   файл невалідним: джоби не створилися, логів немає, а в UI workflow
   називався шляхом до файлу замість свого імені — це й була підказка.
   Правильне місце — `defaults.run.shell` на рівні джоба. Ловиться
   `actionlint`, і його варто ганяти перед кожним пушем workflow.
2. macOS: `Makefile: unterminated call to function 'shell'`. **Це був
   справжній баг збірки, а не CI.** make знімає коментарі до розбору функцій,
   тож `#` усередині `s/#.*//` починав коментар і обрізав рядок. GNU make 4.3
   (Ubuntu) це пробачає, 3.81 (macOS) падає — тобто збірка залежала від
   версії make. Символ тепер проходить через змінну `HASH`.
3. Windows: 503 при завантаженні MSYS2 з GitHub Releases — стороннє й
   тимчасове. `release: false` бере MSYS2 з образу раннера й прибирає це
   викачування взагалі.

**Знайдено дорогою — діра в самому каркасі детермінізму.** `CFLAGS` бралися
через `$(shell sed ...)`, а `$(shell)` про помилку не повідомляє: якщо sed
падає, прапорці просто порожні й збірка тихо йде з дефолтними, тобто **без
`-ffp-contract=off`**. Детермінізм ламався б без жодного повідомлення, а
падала б потім звірка хешів за кілометр від причини. Тепер це `$(error)`,
плюс окрема перевірка присутності самого прапорця.

**Виміряно локально до CI** — те, на чому тримається вся аргументація:

> Хеші **всіх** сценаріїв бітово однакові при `-O1`, `-O2`, `-O3`, `-Os`,
> `-march=native` і `-funroll-loops`. А з `-ffast-math` — розходяться.

Тобто в ядрі немає нічого, що компілятор міг би переасоціювати, і перевірка
має зуби, а не просто збігається сама з собою.

Побічно: **збірка вимагає щонайменше `-O1`**. При `-O0` gcc кличе `sqrt` як
функцію libm замість апаратної інструкції, і сценарії, які свідомо лінкуються
без `-lm`, не злінкуються. «Зібрати без оптимізації для дебагу» тут не працює;
`-O1 -g` дає бітово те саме.

<details>
<summary>Як це було зроблено</summary>

**Зроблено:**

- `.github/workflows/determinism.yml` — матриця з чотирьох джобів: Linux/gcc,
  Linux/clang, macOS/clang, Windows/MSYS2. Окремого кроку «порівняти платформи»
  немає й не треба: усі звіряються з одним закоміченим `golden.txt`.
- **Закрито прогалину покриття.** Сценарії не діставали до рантаймового шляху
  читання ассета — саме того, від якого залежить гра. Перешкода була реальна:
  сценарій лінкується без `libm`, тож не може скукати ассет сам, бо
  чебишевська підгонка — це тригонометрія. Розв'язок і є архітектурою:
  ассет закомічено (`data/fixture/earth_moon.eph`, 50 КБ). Друга причина
  важливіша за першу — кукер рахує коефіцієнти через `cos()`, тож дві машини
  скукали б **різні** ассети, і звірка падала б на кукері, а не на рантаймі.
- Два нові сценарії, навмисно окремі, щоб їх можна було ділити навпіл за
  процедурою вище: `sc_ephemeris` (читання ассета, синодична система, апарат
  у полі десяти тіл) і `sc_trajectory` (multiple shooting і утримання —
  найбільш розгалужений код, де кількість ітерацій і вибір ведучого елемента
  вирішуються порівняннями чисел з рухомою комою).

**Виміряно локально, без інших платформ** — це те, на чому тримається вся
аргументація детермінізму:

> Хеші **всіх** сценаріїв бітово однакові при `-O1`, `-O2`, `-O3`, `-Os`,
> `-march=native` і `-funroll-loops`. А з `-ffast-math` — розходяться.

Тобто в ядрі немає нічого, що компілятор міг би переасоціювати, і перевірка
має зуби, а не просто «щось збігається сама з собою».

Побічно: **збірка вимагає щонайменше `-O1`**. При `-O0` gcc кличе `sqrt` як
функцію libm замість апаратної інструкції, і сценарії, які свідомо лінкуються
без `-lm`, не злінкуються. «Зібрати без оптимізації для дебагу» тут не працює;
`-O1 -g` дає бітово те саме.

</details>

---

### Стан Milestone 0

Критерії приймання з PROJECT.md §10 — усі шість:

| # | Критерій | Де виміряно | Результат |
|---|---|---|---|
| 1 | Константа Якобі, 100 обертів | `test_cr3bp.c` | дрейф падає з допуском, ~1e-12 |
| 2 | Оборотність, 10 років вперед і назад | `test_dop853.c` | 4.11e-2 м за 134 оберти |
| 3 | Диференціальна корекція, нев'язка < 1e-10 | `test_correct.c` | ~1e-14 за 4–6 ітерацій |
| 4 | Регресія на Horizons, Місяць за 10 років | `test_nbody.c` | 9.32e4 м |
| 5 | Хеш детермінізму на Linux/Windows/macOS | CI | збігається на 4 джобах |
| 6 | Halo тримається N обертів, N узгоджений з Ляпуновим | `test_stability.c`, `test_frame.c` | зростання збігається з λ до 2e-3 |

І остання поставка мілстоуна, не критерій: C6 нижче. Milestone 0 закрито
повністю. Наступний крок — етап D, і саме там Rust з'являється вперше.

---

### C6. Вивід у CSV і графіки — поставка M0 ✅

Не критерій приймання, а остання поставка мілстоуна. Ядро рахувало все
потрібне, але міряло себе лише твердженнями в тестах. Тест каже «пройдено»;
він не каже, **що саме** пораховано.

```
make csv       чотири експортери -> build/csv/*.csv   (~0.6 с)
make plots     scripts/plot.py   -> build/plots/*.png
```

**Зроблено:**

- `core/export/` — чотири програми й спільний писар `csv.c`. Лінкуються як
  тести, з обома бібліотеками й `-lm`: це діагностичні драйвери, а не рантайм.

  | Програма | Файли | Що показує |
  |---|---|---|
  | `ex_cr3bp` | `cr3bp_points`, `halo_family`, `stability` | точки лібрації, п'ять орбіт каталогу, зростання збурення проти λⁿ |
  | `ex_trajectory` | `halo_inertial`, `station` | та сама орбіта в полі десяти тіл, у трьох системах відліку; ціна утримання проти горизонту |
  | `ex_accuracy` | `jacobi`, `jacobi_growth`, `reversibility` | критерії 1 і 2 як криві, а не як числа |
  | `ex_horizons` | `horizons` | критерій 4: десять тіл проти JPL за 10 років, і три тіла поруч як контроль |

- `scripts/plot.py` — шість сторінок PNG. Скрипт **нічого не рахує**: уся
  фізика лишається в C, тут тільки читання CSV і осі. Якщо графік показує
  дурницю, дивитися треба в ядро.

**Що це вже спіймало.** Нічого в ядрі — і це сам по собі результат: числа на
графіках збіглися з тими, що записані в коментарях тестів (4.11e-2 м за 133
оберти, дрейф Якобі 1.5e-7…1.5e-14 за п'ятьма допусками, 9.2e4 м по Місяцю).
Спіймало натомість дві помилки **подання**, і обидві варті запису, бо вони з
тих, що роблять графік переконливим і хибним:

> ⚠ **Місячне число тут застаріле, і саме воно потім спіймало помилку.**
> 9.2e4 м — це показник при допуску 1 м, який не збігся; збіжне значення
> 1.55e5 м. «Числа збіглися з коментарями тестів» лишається правдою — просто
> хибними були обидва однаково. Що з цим сталося далі — у розвилці 1 нижче
> й у блоці допусків у `core/test/test_nbody.c`.

1. **Нуль на логарифмічній осі.** Похибка на нульовій вибірці рівно нуль, і
   дрейф збереженої величини теж буває рівно нуль. Перша версія підміняла їх
   підлогою 1e-18 — вісь розтягувалася на двадцять порядків, уся справжня
   динаміка стискалася в лінію під заголовком, а внизу малювалася пилка з
   чисел, яких ніхто не міряв. Тепер такі точки просто не малюються.
2. **`stability` каталогу — це не λ.** Каталог JPL публікує індекс
   (λ + 1/λ)/2, тож для орбіти 1151 індекс 297, а множник за оберт — 594.
   Перший варіант підписував індекс як λ. Про цю пастку прямо попереджає
   `stm.h`, і вона все одно спрацювала.

**Побічно, з графіка `stability`:** орбіти 0 і 1534 ростуть **швидше** за свої
λ = 1.19 і λ = 1. Це не помилка інтегратора — це дефектна пара одиничних
власних значень (жорданова клітина), і `test_stability.c` це вже фіксував
текстом. Поруч із пунктиром λⁿ воно видно за секунду замість абзацу.

**CSV не входять у звірку детермінізму й не можуть входити:** друк `double` у
десятковий текст — справа libc, а не IEEE. У CI `make csv` є, але лише як
перевірка «збирається й доходить до кінця», без порівняння виводу.

---

## Етап D. Межа C↔Rust (M1) 🟢

Ризик низький: обсяг маленький, помилки шумні.

### D1. Перенесення збірки C у `build.rs` ✅

`cc::Build` з тими самими прапорцями, що в `Makefile`. **Борг з A1 закрито.**

**Зроблено:**

- Робоча область із одним членом — `core-sys`. Решта крейтів з PROJECT.md §3
  додаються, коли з'являться: порожній крейт у `members` ламає `cargo build`.
- `core-sys/build.rs` збирає `core/*.c` у `libcore.a`, читаючи прапорці з
  `core/cflags.txt` — того самого файлу, що й `Makefile`. Обидві перевірки
  Makefile (порожній список, відсутній `-ffp-contract=off`) повторені там
  дослівно.
- Сценарії детермінізму збираються **з тих самих об'єктних файлів**, що йдуть
  у Rust, і без `-lm`. Це важливо: інакше перевірялися б прапорці, а не
  бібліотека, яку насправді використає крейт.
- `core-sys/tests/determinism.rs` запускає їх і звіряє з
  `core/scenario/golden.txt` — тим самим еталоном, що й `make determinism`.

**Перевірка пройшла з першого разу:** усі одинадцять хешів збіглися. І звірка
має зуби — з дописаним `-ffast-math` розходяться дев'ять з одинадцяти
(`sc_arith` і `sc_vec3` лишаються, у них нема чого переасоціювати).

**`-fPIC` — єдиний прапорець поза `cflags.txt`,** і в плоскому списку його не
виразити: він залежить від платформи. Виконувані файли Rust на Linux за
замовчуванням PIE, тож об'єкти без нього не злінкуються. На арифметику він не
впливає — і це не припущення, а наслідок перевірки: еталон рахувався збіркою
`make` **без** `-fPIC`, а хеші збіглися.

**Знахідка, варта запису: `no_default_flags(true)` прибирає не все.** Фактичний
виклик у cc 1.4.2 —

```
cc -I core -Wall -Wextra <наші прапорці> -fPIC -o … -c …
```

тобто `-Wall -Wextra` крейт вставляє від себе, і вставляє **перед** нашими.
На числа не впливає (вони й так у `cflags.txt`), але висновок ширший: повного
контролю над командною строкою цей метод не дає, і вважати його гарантією не
можна. Гарантією лишається звірка хешів — будь-який прапорець, який `cc`
колись додасть і який змінить арифметику, зсуне хеші й буде спійманий.

**Профіль cargo у C не протікає.** `cargo test --release` дає ті самі хеші —
рівень оптимізації Rust на ядро не впливає, бо його прапорці задає
`cflags.txt`. У CI це окремий крок, а не припущення.

**Розвилка (якщо колись розійдеться):** дивитися прапорці.

```
make flags
cargo run -q --example flags                 # те саме на боці cargo
CC_ENABLE_DEBUG_OUTPUT=1 cargo build -vv     # повний виклик компілятора
```

**Борг «Windows під cargo» — закрито 2026-08-13.** Тулчейн Rust там за
замовчуванням MSVC, а `core/cflags.txt` написаний під gcc — потрібен
`x86_64-pc-windows-gnu`. `build.rs` це виявляє й падає з поясненням, а не
збирає щось інше мовчки — і саме так і сталося на першій спробі: повідомлення
одразу вказало на потрібний тулчейн, правити `cflags.txt` чи `build.rs` не
довелося.

Дочекалися машини розробника, а не CI-раннера. Поставлено: `rustup toolchain
install stable-x86_64-pc-windows-gnu` + `rustup override set` на корені
репозиторію (глобальний дефолт лишився MSVC, чіпає лише цей каталог);
`gcc`/`binutils`/`make`/`diffutils`/`coreutils`/`findutils`/`gawk`/`grep`/`sed`
— з MSYS2 (`pacman`, набір UCRT64-пакетів, той самий, що ставить джоб
`windows-mingw`).

`cargo build --workspace`, `cargo test --workspace` (debug і `--release`),
`cargo clippy --all-targets -- -D warnings`, `cargo fmt --check` — усе чисто.
Головне: `hashes_match_the_committed_golden` пройшов — `cargo` з mingw-gcc на
Windows дає **той самий бітовий результат**, що еталон, порахований на
Linux. Це перша перевірка саме `cargo` (а не тільки `make`) на реальному
Windows-залозі: крок нижче, «Cargo determinism», у CI на Windows й далі
пропущений свідомо — вмикати його там залишається окремим рішенням, не
зробленим тут.

**Знахідка з G1, задокументована тут, бо це діра саме в D1: `watch()` у
`build.rs` не стежив за самими каталогами `core/` і `core/scenario/`, лише
за файлами, які в них були на момент, коли `build.rs` востаннє виконався.**
Коли `core/scenario/sc_uncertainty.c` з'явився в дереві (комміт з механікою
невизначеності), жоден уже відстежений файл не змінився — і `cargo test`
у сесії з не порожнім `target/` мовчки звірявся зі старим набором
сценаріїв (10 замість 11), тобто не з тим, що фактично лежить у дереві.
`make determinism` цього не ловить узагалі: він завжди перечитує
`$(wildcard core/scenario/*.c)` наново. Розбіжність не в числах — у тому,
**які сценарії взяті до звірки**, і саме тому вона мовчазна: обидва шляхи
кажуть «збігається», порівнюючи різні набори.
Виправлено додаванням `cargo:rerun-if-changed` на самі каталоги, не лише
на файли в них (`core-sys/build.rs`, функція `watch`). Порожній `target/`
(як у CI без кешу) цю діру не показав би — перше виконання `build.rs`
завжди бачить дерево таким, яке воно є.

### D2. `core-sys` — три функції вручну ✅

`eph_load`, `eph_body_state`, `eph_free`. Без bindgen (PROJECT.md §5).

**Зроблено:** `core-sys/src/lib.rs` — `Vec3d`, `State`, непрозорий
`EphemerisCtx` і три `extern "C"`. Жодного `unsafe`-блоку: тут лише
оголошення, наш `unsafe` живе в `core-rs` (D3).

**`CoreResult` — це `c_int`, а не `#[repr(C)] enum`,** і це не спрощення, а
коректність. Rust-енум зі значенням поза переліком — невизначена поведінка, а
не помилка, яку видно; сирий шар має віддавати рівно те, що сказав C.
Перетворення на справжній `enum` із гілкою «щось інше» — робота D3, там для
цього є місце.

**Перевірка бітова, і це принципово.** Помилка в межі не падає: переплутані
поля `State`, `int` замість `size_t`, `*mut` замість `*const` — усе це
компілюється й повертає числа, схожі на координати. Тому `core-sys/oracle.c`
друкує те, що `eph_body_state` дає в C (`%.17g`, три тіла × три моменти
фікстури), а `tests/ffi.rs` викликає ту саму функцію через FFI і звіряє
`to_bits()`.

**Оракул, а не числа, вписані в тест.** Літерали зловили б ту саму помилку, але
протухли б при першому `make cook` і не мали б способу оновитися. Оракул
перезбирається разом з ассетом. Лежить у крейті, а не в `core/scenario/`: там
він змінив би `golden.txt`, а це риштування межі, не сценарій детермінізму.

**Звірка має зуби:** поміняв місцями `r` і `v` у декларації `State` — тест упав
одразу, прочитавши швидкість замість положення (−1.07e9 проти 9.31), і сказав
у повідомленні, що це розкладка, а не фізика.

Ще два тести, кожен про свою помилку:

- **коди повернення доходять як коди.** Час поза проміжком ассета і невідоме
  тіло мусять давати `CORE_ERR_INVALID_ARG`. Якщо код повернення читається
  неправильно, екстраполяція чебишевської підгонки виглядатиме як успіх — і
  це найгірший результат, бо траєкторія вийде правдоподібна. Плюс перевірка,
  що всередині проміжку таки `CORE_OK`, інакше попередній тест «проходив» би
  й на повністю зламаному коді.
- **`eph_free(NULL)` дозволений** — так каже `core/ephemeris.h`. Дрібниця, на
  яку D3 буде спиратися: RAII звільняє в `Drop` беззастережно, і якби обіцянка
  була неправдива, впало б не тут, а при вивантаженні сцени.

### D3. `core-rs` — безпечна обгортка ✅

RAII над хендлом, `Result`. Єдине місце з нашим `unsafe`.

**Зроблено:** `Ephemeris` — володіння хендлом, звільнення у `Drop`,
`CoreError` замість кодів, `Result` замість вихідних параметрів. Слайсів поки
немає, бо немає функцій із буферами: вони приходять разом із `prop_run`
(PROJECT.md §5), і правило для них уже записане — Rust дає буфер і місткість,
C повертає фактичну кількість.

**Дві обіцянки перевіряє компілятор, а не прогін.** `compile_fail`-доктести
в `src/lib.rs`: використання після звільнення не збирається (тип не `Copy`
і не `Clone`, після `drop` значення переміщене), звільнити двічі нема чим
(`eph_free` не реекспортується, поле приватне).

**Пастка `compile_fail`, і як вона перевірена.** Такий доктест проходить, якщо
код не зібрався **з будь-якої** причини — друкарська помилка теж «проходить».
Тому кожен фрагмент прогнано ще раз без порушувального рядка й з `no_run`:
обидва зібралися. Отже причина була саме та, яка потрібна, а не випадкова.

**Пам'ять — valgrind у CI, а не Miri й не ASan.** Не відступ через брак
інструмента, а правильніший інструмент: алокації живуть у C (`malloc` в
`ephemeris.c`), Miri в C не бачить узагалі — роадмап це й передбачав, — а ASan
вимагав би перезібрати ядро з `-fsanitize=address`, тобто з прапорцями,
відмінними від `core/cflags.txt`. Valgrind не потребує перезбірки й дивиться
саме на ту купу.

І він ловить те, чого не ловлять типи: `compile_fail` доводить, що звільнити
двічі не можна, але **не** доводить, що звільнення взагалі відбувається.
Витік — не помилка типів. Тест `loading_and_dropping_repeatedly_is_clean`
робить п'ятдесят циклів саме щоб дати valgrind що виміряти.

**`Send` і `Sync` — обіцянка, обґрунтована читанням C.** `eph_body_state` бере
`const EphemerisCtx*`, торкається лише полів контексту й купи, якою той
володіє, і не має ні статиків, ні кешу (перевірено в `ephemeris.c` і
`cheb.c`); після `eph_load` контекст не змінюється взагалі. Потрібно це буде,
щойно фізика поїде у свій потік — і краще, щоб обіцянку писав той, хто щойно
прочитав C, ніж той, кому вона зрештою знадобиться.

**Числа не змінилися по дорозі:** обгортка звіряється з сирим викликом бітово.
Разом із D2 (сирий шар проти C) це замикає ланцюжок C → `core-sys` → `core-rs`.

---

## Етап E. Розвідки перед графікою 🟡

Робити **до** написання рендера. Кожна займає години і закриває питання,
переробка якого коштувала б тижні.

### P0. Чи є bindless у wgpu на ваших цілях ✅

`tools/gpu-probe` перелічує адаптери й читає, що вони самі про себе кажуть.
Пристрій не запитується свідомо: `adapter.features()` показує, що **можна**
попросити, а це і є питання; створення пристрою додало б причин впасти, не
додавши відповіді.

```
cargo run -p gpu-probe        # локально
```

Windows і macOS — окремий workflow `gpu-probe.yml`, бо цих машин немає.

**Таблиця. wgpu 30.0.** «маси́ви» = текстур / буферів / storage-ресурсів,
«неодн.» = неоднорідна індексація, «частк.» = часткова зв'язаність.

| Бекенд | Адаптер | текст. | буф. | storage | неодн. | частк. | елем. | семпл. |
|---|---|---|---|---|---|---|---|---|
| Vulkan | RTX 4070 Ti SUPER, NVIDIA 595 | + | + | + | + | + | 1 048 576 | 1 048 576 |
| Vulkan | AMD Raphael iGPU, RADV | + | + | + | + | + | 8 388 606 | 8 388 606 |
| Vulkan | llvmpipe (програмний) | + | + | + | + | + | 1 000 000 | 1 000 000 |
| Metal | Apple Paravirtual (раннер) | + | + | + | + | + | 1 000 000 | **1 024** |
| Dx12 | Microsoft Basic Render (WARP) | + | **–** | + | + | + | 1 048 576 | **2 048** |
| Dx12 | RTX 4070 Ti SUPER, справжнє залізо | + | **–** | + | + | + | 1 048 576 | **2 048** |
| Dx12 | AMD Radeon iGPU, справжнє залізо | + | **–** | + | + | + | 1 048 576 | **2 048** |
| Gl | RTX 4070 Ti SUPER | – | – | – | – | – | 0 | 0 |

**Рішення: варіант (а) — нативні Vulkan / D3D12 / Metal.** Запасний шлях не
потрібен, бо повний набір є скрізь, крім GL, а GL ніколи не був ціллю
(PROJECT.md §2 обирає wgpu заради кросплатформності нативних API, не заради
браузера). Тонка абстракція над wgpu лишається — але як страховка від переїзду
на Vulkan 1.3, а не як другий шлях рендера.

Три фічі перевірялися окремо, бо відмовляють окремо й кожна відмова означає
інше. Масив без неоднорідної індексації змушує індекс бути константою рівня
draw call. Масив без часткової зв'язаності треба заповнювати щокадру — а це
рівно та ціна, заради уникнення якої bindless і беруть.

<details>
<summary>Дві знахідки, які змінюють не рішення, а конструкцію</summary>

**1. D3D12 не дає масивів uniform-буферів.** Єдиний мінус у таблиці. Решта
фіч на місці, `STORAGE_RESOURCE_BINDING_ARRAY` теж — бракує саме
`BUFFER_BINDING_ARRAY`.

Це **не** зачіпає задуманого. PROJECT.md §7 каже «один descriptor heap на всі
текстури, індекси в буферах»: масив потрібен для текстур, а дані на об'єкт
лежать у storage-буфері, який прив'язується звичайно. Тобто конструкція, яку
вже обрано, випадково опинилася рівно всередині того, що є всюди.

Виміряно спершу на WARP, програмному адаптері раннера. **Перевірено на
справжньому залізі 2026-08-13** (перша Windows-машина розробника, ROADMAP
D1): RTX 4070 Ti SUPER і вбудована AMD Radeon дають рівно той самий результат
— масивів uniform-буферів немає, решта фіч на місці, ті самі ліміти семплерів.
Здогад підтвердився, і рішення (а) від цього не змінюється.

**2. Масиви семплерів малі, і це справжня межа.** 1024 на Metal і 2048 на
D3D12 проти мільйона на Vulkan — три порядки різниці, і найменше число
визначає конструкцію.

Наївний bindless «масив пар текстура+семплер» упреться в Metal на 1024
текстурах. Отже семплерів має бути **кілька** (за режимами фільтрації й
обгортання), вони індексуються окремо від текстур, і масштабується лише масив
текстур. Це стандартний спосіб, але його треба знати наперед: переробляти
шейдери, коли на macOS раптом закінчуються семплери, — це та сама переробка
всього рендера, якої §7 велить уникати.

</details>

**Що лишилося неперевіреним:** справжній GPU під Metal (D3D12 перевірено вище,
2026-08-13). У таблиці на цьому рядку — паравіртуальний пристрій раннера,
тобто відповідь про те, що підтримує **wgpu на цьому API**, а не що потягне
залізо гравця. Для рішення (а) цього досить: воно спирається на наявність
фіч у бекенді. Реальні ліміти конкретних карт — питання телеметрії, а не
розвідки.

### P1. Slang → який вихід ✅

**Рішення: беремо WGSL.** `slangc -target wgsl` → `wgpu` → трикутник на
екрані, перевірено по пікселях. Шлях через SPIR-V зараз закритий, причина
нижче.

```
sh scripts/fetch_slang.sh     компілятор, 24 МБ, у git не тримаємо
cargo run -p slang-probe
```

Зонд проходить увесь ланцюжок і зупиняється лише на останньому кроці:
`slangc` → модуль у wgpu → пайплайн → малює в текстуру 64×64 → пікселі
читаються назад. Останнє й є суттю: модуль, який створився й не намалював
нічого, — це пройдена перевірка і зламаний рендер, і саме так виглядає
розбіжність семантик або порядку локацій.

**Знахідка: SPIR-V відхиляється, і винна одна конструкція.**

```
Shader parsing error: UnsupportedCapability(DrawParameters)
```

`naga` не приймає capability `DrawParameters`, яку Slang оголошує заради
`SV_VertexID`. **Той самий шейдер без `SV_VertexID` wgpu приймає** — це
третій випадок у зонді, і він там саме заради цього розрізнення. Тобто шлях
не зламаний узагалі; він зламаний на прийомі, без якого не обходиться жоден
повноекранний трикутник.

Висновок практичний: навіть якби SPIR-V був потрібен, обходити довелося б
відмовою від найпоширенішого способу задавати вершини. WGSL цього не
вимагає, тож вибір не є компромісом.

**Що це коштує.** Нічого з того, заради чого Slang брали: модулі, дженерики
й один вихідний текст лишаються, бо вони — властивість мови, а не цілі. WGSL
на виході читабельний (`build/slang/triangle.wgsl`), що знадобиться, коли
доведеться дивитися, у що розгорнувся дженерик.

**Виміряно на:** Slang v2026.14.1, wgpu 30.0, Vulkan/NVIDIA. `slang-probe.yml`
повторює на Linux і macOS. Windows не покрито — `fetch_slang.sh` вміє tar.gz,
а там zip; відповідь від платформи не залежить (naga розбирає WGSL однаково),
тож гілка не написана свідомо.

<details>
<summary>Дрібниця, яка ледь не стала хибним висновком</summary>

Перша версія перевірки брала контрольні точки рівно на вершинах трикутника —
і падала на цілком правильному рендері: пікселя точно на вершині растеризатор
законно не зафарбовує. Вивід виглядав як «WGSL не малює», тобто як провал
розвідки.

Точки зсунуто всередину, а звіряється тепер **переважання каналу**, а не
близькість до чистого кольору: трикутник інтерполює вершинні кольори, тож
будь-який поріг «схоже на червоний» був би підгонкою під конкретні
координати.

</details>

---

## Етап F. Перша картинка (M2) 🟢→🟡

Порядок навмисно дрібний: кожен крок або працює, або локалізує проблему.

### F1. Вікно, очищення кольором ✅

`winit` + `wgpu`. Крейт `engine`.

```
cargo run -p engine                          вікно
cargo run -p engine -- --frames 200          вікно, N кадрів і вихід
cargo run -p engine -- --shot build/f1.png   кадр у PNG, без вікна
```

**Кадр один, шляхи два.** `engine::frame` не знає про вікно нічого — лише про
текстуру, у яку писати. Далі той самий кадр іде або в поверхню вікна, або в
PNG. Це не подвійна робота, а єдина причина, з якої рендер узагалі можна
перевірити: «вікно відкрилось і не впало» перевіркою не є, бо чорний кадр
виглядає так само. Знімок звіряється по пікселях і в `engine/tests/shot.rs`.

Колір заливки навмисно не чорний — з тієї ж причини.

**Знахідка, і вона коштувала години: обмежений прогін вішався на vsync.**

Симптом: `--frames 40` зупинявся рівно на двадцятому кадрі й висів без
помилки, без паніки, без жодного повідомлення. `--frames 20` проходив.

Хибний слід: спершу здалося, що винна програмна зміна розміру посеред
прогону — вона стоїть якраз на середині. Прогін без неї вішався так само,
на тому ж кадрі.

Причина: під X11 вікно процесу, який не має фокуса, може фактично не
показуватися. Тоді черга `Fifo` ніколи не звільняє кадр, і
`get_current_texture` блокується **назавжди**. З `AutoNoVsync` ті самі 200
кадрів проходять за частку секунди.

Висновок у код: `--frames` вимикає vsync, `--vsync` вмикає назад. Перевірка,
результат якої залежить від того, чи компонувальник справді показує вікно, —
не перевірка рендера.

**Побічно виправлено:** переконфігурація поверхні після `Outdated` брала
збережений розмір, а не фактичний розмір вікна. Якби вікно на той момент уже
змінилося, поверхня лишалася б `Outdated` вічно — кадри не малюються,
лічильник не росте, помилки немає. Тепер розмір читається з вікна.

**Що лишилося за вами:** подивитися на вікно живцем і посмикати його рамку.
Автоматично перевірено все, крім самого віконного менеджера.

### F2. Трикутник ✅

Видно. Перший справжній вжиток зв'язки Slang → WGSL з розвідки P1.

**Шейдер компілюється офлайн, WGSL комітиться.** Це та сама архітектура, що
з ассетами: кукер працює один раз на машині розробника, у білд їде результат
(PROJECT.md §4). Інакше кожна збірка рушія — і кожен джоб CI — вимагали б 24
МБ компілятора Slang. Побічна користь: згенерований WGSL видно в діфі, тож
дженерик, який розгорнувся не в те, помітно в рев'ю, а не в кадрі.

```
sh scripts/build_shaders.sh    engine/shaders/*.slang -> *.wgsl
```

Протухнути це може — хтось правитиме `.slang` і забуде перезібрати. Тому
workflow `slang-probe` перезбирає й дивиться `git diff --exit-code`: там
slangc уже викачаний, тож перевірка коштує секунду.

**Пайплайн прив'язаний до формату цілі,** тож `Frame` створюється під формат
і живе поруч із поверхнею. З цього ж випливає, що вікно й знімок дають трохи
різні пікселі: поверхня вікна sRGB, ціль знімка лінійна. Кольори в шейдері
однакові — різне те, що з ними робить апаратура на записі. Тому тест звіряє
**переважання каналу**, а не точний колір.

Контрольні точки — всередині трикутника, не на вершинах: пікселя точно на
вершині растеризатор законно не зафарбовує. Ця помилка вже коштувала часу на
P1, тож тут вона врахована відразу.

### F3. Reversed-Z і нескінченна далека площина ✅ — з поправкою до критерію

Зроблено. Але **критерій у буквальному прочитанні недосяжний**, і це не вада
реалізації, а арифметика float32. Перевірку переформульовано на «де саме
проходить межа», і межа виміряна.

```
cargo run -p engine -- --depth-probe
```

**Чому 1 м на 10⁸ м неможливий.** Reversed-Z з нескінченною далекою площиною
дає `z_ndc = near / z`. Тоді:

```
Δz_ndc = near·Δz / z²        ulp(near/z) ≈ (near/z)·2⁻²⁴
⇒  Δz_min ≈ z · 2⁻²⁴ ≈ z · 6·10⁻⁸
```

**`near` скорочується.** Роздільність — стала частка відстані, і ніякою
близькою площиною її не покращити. На 10⁸ м межа ≈ 8 м; метр там не роздільний
за жодних налаштувань.

**Виміряно на GPU, частка кадру, де ближча поверхня попереду.** 1.000 — глибина
роздільна; 0.000 — виграв порядок малювання; між — те саме мерехтіння.

| відстань | зазор | reversed-Z | звичайна | межа (оцінка згори) |
|---|---|---|---|---|
| 10⁴ м | 1 м | 1.000 | 1.000 | 0.001 м |
| 10⁵ м | 1 м | 1.000 | **0.000** | 0.012 м |
| 10⁶ м | 1 м | 1.000 | 0.000 | 0.119 м |
| 10⁷ м | 1 м | **1.000** | 0.000 | 1.19 м |
| 10⁸ м | 1 м | **0.000** | 0.000 | 11.9 м |
| 10⁸ м | 100 м | 1.000 | **0.000** | 11.9 м |

Три речі, які видно лише в цій таблиці разом:

- **Звичайна проєкція здається вже на 10⁵ м** — і не «трохи гірше», а повністю,
  включно зі 100-метровим зазором на 10⁸ м. Далеку площину їй дали щедру
  (×10 від відстані), тож справа не в поганому налаштуванні.
- **Reversed-Z виграє п'ять порядків** — від 10⁵ до 10⁷ м там, де звичайна вже
  не працює.
- **Оцінка межі консервативна разів у 1.4.** На 10⁷ м вона дає 1.19 м, а зазор
  1 м GPU ще розрізняє: `f32::EPSILON` — найгірший ULP у двійковому проміжку,
  фактичний гуляє між 2⁻²⁴ і 2⁻²³. Оцінка згори тут навмисна.

**Що це означає для плану.** Нічого нового — PROJECT.md §7 уже вимагає
багатопрохідного рендера по діапазонах саме з цієї причини: «Один depth buffer
не покриє 10¹¹ м». F3 перетворює це з припущення на виміряне число: один
прохід тримає приблизно **сім порядків** відстані, тож чотири оголошені
діапазони — не запас, а необхідність.

Межа зафіксована тестом (`engine/tests/depth.rs`), і тест на неї падає, якщо
метр на 10⁸ м раптом стане роздільним: це була б добра новина, яка суперечить
розрахунку, і причину треба було б знайти.

**Довимірено на реальному залізі (Steam Deck, `AMD RADV VANGOGH`).** Клітинка
10⁷ м / 1 м у таблиці вище виявилася межею ще в одному сенсі: на CPU-
растеризаторі (`lvp`) звичайна проєкція там програє (0.000, як задокументовано
вище), а на апаратному Vulkan того самого GPU — раптом **вигравала** (1.000).
Причина не в коді рушія: `z_ndc` обох поверхонь у double відрізняється на
частку однієї ULP float32, тобто результат порівняння вирішує округлення
останнього біту — FMA у GPU-компіляторі шейдера чи апаратний `rcp` замість
честивого ділення, деталь конкретного растеризатора, а не гарантія IEEE-754.
`-ffp-contract=off` з CLAUDE.md §2 рятує від цього класу проблем у C-ядрі; на
WGSL воно не діє — межа детермінізму (PROJECT.md §4) навмисно проходить по
пропагації, не по рендеру, і цей випадок якраз показує, чому.

Тест `a_conventional_projection_loses_the_same_case_entirely` брав саме цю
межову клітинку (зазор 1 м) і став на GPU-залежний. Перевірено трьома
бекендами на одній машині (`cargo run -p engine -- --depth-probe`,
`VK_ICD_FILENAMES` для вибору адаптера):

| бекенд | адаптер | 10⁷ м / 1 м, звичайна |
|---|---|---|
| Vulkan | `llvmpipe` (CPU) | 0.000 |
| Vulkan | `AMD RADV VANGOGH` (апаратний) | **1.000** |
| GL | `radeonsi` (той самий GPU) | **1.000** |

Висновок не міняється — звичайна проєкція категорично не тримає 10⁷ м, — але
доводити це варто в точці з запасом, а не на самій межі округлення. Тест
переведено на зазор 100 м у тому самому кадрі (10⁷ м): на ньому всі три
бекенди одностайні (0.000), а твердження «навіть стометровий зазор звичайна
проєкція тут не бачить» доводить те саме категоричніше, ніж метровий.

**Поправка: зазор був не при чому, і 100 м не полагодили нічого.** Наступний
же джоб `macos-clang` упав на тому самому тесті з зазором 100 м — `share =
1.000`, тобто на Metal звичайна проєкція «раптом упоралася» рівно так само,
як до того на RADV із метровим. Причину знайдено арифметикою, а не ще одним
підбором клітинки. У строгому f32 обидві поверхні там дають **той самий
біт**:

```
near = 0.1, far = 10×відстань, z_ndc = (m22·z_view + m32) / −z_view

10⁷ м, зазор 1 м     0x3f800000 == 0x3f800000
10⁷ м, зазор 100 м   0x3f800000 == 0x3f800000
```

Обидві впираються рівно в далеку площину: `1 − near/z` на 10⁷ м давно за
межею мантиси. Для `near = 0.1` це починається вище **~1.7·10⁶ м**, тож
жодний зазор у цьому кадрі — ні 1 м, ні 100 м, ні 1000 м — з насичення не
виводить. Діагноз «випадок стоїть на межі ULP» був хибний: він не на межі,
він **за нею**, і 100 м лише перетасували те, як конкретний драйвер
розв'язує нічию.

Друге, що показала та сама арифметика, — **зайва точність растеризаторів**.
Рядок 10⁴ м / 1 м у таблиці вище: звичайна проєкція виграє кадр (1.000),
хоча в строгому f32 обидві поверхні дають один біт (`0x3f7fff69`). Отже
залізо тягне через інтерполяцію й перспективний поділ більше, ніж f32
шейдера, і скільки саме — не наша гарантія. Це остаточно закриває питання,
чому клітинки з нічиєю не можна перевіряти на GPU.

**Що змінено.** Негативні твердження («глибина цього не розрізняє») переїхали
з `engine/tests/depth.rs` в юніт-тести `engine::depth`, до нової
`depth::ndc()` — та сама пара кроків, що в шейдері, у строгому f32, однаково
на всіх платформах:

| твердження | де тепер | чому там |
|---|---|---|
| звичайна не тримає 100 м на 10⁷ м | `depth::tests` | збіг бітів — це і є твердження |
| reversed-Z ту саму пару розводить | `depth::tests` | дзеркало, 113 ULP |
| метр на 10⁸ м не роздільний і в reversed-Z | `depth::tests` | теж нічия, теж був GPU-залежний |
| reversed-Z тримає метр на 10⁷ м | `tests/depth.rs` (GPU) | різниця справді є |
| зазор ×10 від межі роздільний на 10⁸ м | `tests/depth.rs` (GPU) | різниця справді є, 8 ULP |

Таблиця вимірів на початку розділу лишається як є — це історія заміру на
llvmpipe, і тепер видно, що частина її нулів і одиниць говорить про драйвер,
а не про глибину. Числа, на яких стоїть висновок про сім порядків, це не
зачіпає: він тримається на позитивних клітинках reversed-Z, а вони бітово
неоднакові.

⚠ `reversed_z_resolves_a_metre_at_ten_million` лишається на GPU, але стоїть
на різниці рівно в **1 ULP** (`0x322bcc77` проти `0x322bcc78`). Це і є той
край, який F3 міряв, тож клітинку лишено свідомо — але якщо тест колись
почервоніє на новому залізі, дивитися треба сюди, а не в рушій.

### F4. Camera-relative ✅

Трансформації в `double` на CPU, у шейдер їде `float` у камерному просторі.
PROJECT.md §7, рішення 1: світові координати НІКОЛИ не потрапляють у `float`.

```
cargo run -p engine --example f4
```

Обидва шляхи закінчуються однаково — той самий шейдер, той самий пайплайн.
Різниця лише в арифметиці на CPU:

```
camera-relative   (світ_об'єкта − світ_камери) as f32
наївний            світ_об'єкта as f32 − світ_камери as f32
```

**Виміряно.** Об'єкт за 10 м від камери, обидва — на відстані D від початку
координат. Камера рухається кроками по 0.1 м (~1.2 пікселя):

| D, м | ULP f32, м | camera-relative, сер/макс px | наївний |
|---|---|---|---|
| 10³ | 0.0001 | 3.15 / 3.54 | 3.15 / 3.54 |
| 10⁵ | 0.012 | 3.15 / 3.54 | 3.15 / 3.54 |
| 10⁷ | 1.19 | 3.15 / 3.54 | 3.15 / 3.54 |
| 10⁸ | 11.9 | 3.15 / 3.54 | **3.92 / 4.24** |
| 10⁹ | 119 | 3.15 / 3.54 | **немає в кадрі** |
| 10¹¹ | 11 921 | **3.15 / 3.54** | **немає в кадрі** |

**Головне — стовпець camera-relative однаковий до останньої цифри на всіх
восьми порядках.** Не «похибка мала», а відстані до початку координат немає
в рівнянні взагалі. Це і є те, що крок мав дати.

**Поправка до формулювання перевірки.** Критерій казав «камера зсувається на
міліметр — вершини не тремтять», але міліметр на десяти метрах — це 0.004
пікселя, нижче за будь-яке вимірювання: обидва шляхи дають нуль, і тест не
розрізняє нічого. Крок узято 0.1 м, щоб рух був видимий у пікселях; тоді
«плавно» і «стоїть, тоді стрибає» стають різними числами.

**Наївний шлях не тремтить — він зникає.** На 1 а.о. ULP у `f32` ≈ 16 км
проти сцени в десять метрів: обидві координати лягають на 16-кілометрову
ґратку, різниця перетворюється на нуль, об'єкт опиняється в камері й
відсікається ближньою площиною. Тремтіння — м'який випадок, і воно видно
лише у вузькій смузі коло 10⁸ м, де ULP лише трохи більший за сцену.

Порогом виявилося **10⁸ м** — на порядок ближче, ніж 1 а.о. з критерію. Тобто
camera-relative потрібен не лише для міжпланетних відстаней, а вже для
навколоземних операцій.

### F5. Сфера в реальному масштабі ✅

Земля справжнього радіуса, камера від поверхні до орбіти без розривів.

```
cargo run -p engine -- --flight-probe
```

**Перевірка:** безперервний проліт від 10 м до 10⁷ м без стрибків і z-fighting.

**Перше справжнє геометричне тіло, а не намальоване з `SV_VertexID`.**
Триангуляційна сфера (`engine::sphere`, UV-сітка 64×128, 8385 вершин, 16384
трикутники) з нормалями, вершинний і індексний буфери — саме те, на що
`triangle.slang` натякав коментарем ще з F2: «Буфери прийдуть із F5, коли
з'явиться справжня сфера».

**Camera-relative тепер per-vertex, а не per-object.** F4 довів принцип на
одній точці — об'єкт малий, тож досить одного зсуву на весь draw call.
Планета — ні: камера може стояти за 10 м від однієї вершини меша і за
1.6·10⁷ м від протилежної **одночасно**. `engine::camera::Camera::relative`
робить те саме віднімання й переносить у камерний базис у `double`,
окремо для кожної вершини, і лише тоді звужує до `f32`. Це і є те, що M4
доведеться замінити на зсув по патчах: перераховувати ввесь меш планети
щокадру на CPU можна для 8 тисяч вершин налагоджувальної сфери, не для
мільйонів вершин LOD.

**Перевірка — не «на око», а число проти числа**, той самий інструмент, що
в F3 (`resolvable_gap`) і F4 (стійкість зсуву). Сфера опукла, тож кут, під
яким видно її край із зовнішньої точки, має точну формулу без наближень:
`half_angle = asin(R/(R+висота))`. Звідси — очікувана частка кадру, яку
займає диск силуету, коли він цілком у кадрі. `flight_probe::sweep`
рахує 15 висот, логарифмічно від 10 м до 10⁷ м, і звіряє виміряне
покриття кадру з цією формулою.

**Виміряно на Steam Deck (AMD RADV VANGOGH, Vulkan), меш 64×128, 512×512:**

| висота, м | покриття (виміряне) | аналітично | різниця |
|---|---|---|---|
| 10 … 1.4·10⁶ | 1.000 | 1.000 | 0.0000 на кожному з 13 кроків |
| 3.7·10⁶ | 1.000 | — (диск торкається кутів, формула не визначена) | — |
| 1.0·10⁷ | 0.420 | 0.421 | −0.0003 |

**Покриття кадру монотонно не зростає на всіх 15 кроках** — це і є
формальний варіант «без стрибків»: будь-який ривок угору в цій величині
неможливий для опуклого тіла, що віддаляється, і сигналізував би про саме
ту помилку камери-relative чи глибини, яку крок мав спіймати.

**Чому 10⁷ м — єдина точка, де диск взагалі влазить у кадр.** При полі
зору 60° і радіусі Землі 6371 км сфера заповнює весь кадр аж до висоти
понад ~3·10⁶ м: з 10 м до 1.4·10⁶ м камера фактично дивиться на майже
пласку поверхню, обрій ще далеко за краєм екрана. Це не вада перевірки, а
геометричний факт, який перевірка й зафіксувала — «покриття 1.000
тринадцять разів підряд» так само красномовне твердження, як і збіг на
орбіті, бо воно означає, що сфера не зникла, не проскочила крізь ближню
площину і не з'явилась дірками через z-fighting на жодному з цих кроків.

**Ближня площина (`near = 1` м) — на порядок менша за найближчий проліт
(10 м), а не «про всяк випадок».** Останнє число, яке дав би F3, якби
його застосувати тут напряму, — це не якась довільна безпечна відстань:
на висоті 10 м камера стоїть за 10 м від найближчої вершини меша, і near
мусив лишити запас, а не впертися в нього.

**Освітлення — тимчасовий ламберт з невеликим ambient (`sphere.slang`),
не фінальна модель PROJECT.md §7.** Досить, щоб форма сфери читалась оком
на знімку (`build/f5_flight.png`, `build/f5_surface.png`); нуль ambient і
SH-проба від кольору планети чекають на справжнє сонце й атмосферу.

**Без відсікання граней (`cull_mode: None`), як і в `depth_quad`.**
Коректність тримається на тесті глибини — сфера опукла, тож найближча
поверхня для кожного пікселя виграє незалежно від порядку обходу вершин,
— а не на тому, що обхід вершин меша вгаданий правильно. Одна менш
переконлива умова для коректності того самого висновку.

### F6. Траєкторії в обертовій системі ✅

Лінії, перетворення фрейму у вертексному шейдері (PROJECT.md §7).

```
cargo run -p engine -- --trajectory-probe
```

**Перевірка:** halo-орбіта з етапу C виглядає замкненою петлею біля L2;
перемикання інерціальний ↔ обертовий фрейм миттєве.

**Розвилка (з тексту кроку): 🟡 шейдеру потрібні позиції двох тіл на момент
кожної вершини.** Саме так і зроблено — не знадобилось ні заливати кути
семплами, ні інтерполювати: дані вже семпльовані з кроком ~2.7 год (той
самий крок, яким `ex_trajectory` експортує CSV), і цього вистачає для
центральної різниці, що дає нормаль орбітальної площини (нижче).

**Дані — фікстура, не FFI.** `engine` не лінкує `core-rs`: це окреме,
більше рішення (справжній зв'язок фізики й рендера через `prop_run`),
не те, що варто вирішувати попутно з кроком про рендер-техніку. Halo-
орбіта з C4 (`data/fixture/halo_inertial.csv`, урізана версія експорту
`core/export/ex_trajectory`, ROADMAP C6) — комітиться так само, як
`earth_moon.eph`. Деталі — `data/fixture/README.md`.

**Геоцентричний anchor замість camera-relative.** У F4/F5 CPU віднімає
позицію КАМЕРИ в `double` до звуження у `f32`. Тут камери немає — є
апарат і система Земля-Місяць, а Земля сама пролітає мільярди метрів
геліоцентрично за 102 доби місії. Anchor — Земля-в-момент-семпла: вершинні
атрибути це `vessel − earth` і `moon − earth` для ТОГО САМОГО t. Апарат
ніколи не відходить далі кількох мільйонів метрів від Місяця, тож числа
лишаються малі на всій місії, хоч Земля тим часом проходить помітну частку
геліоцентричної орбіти. Той самий принцип, що camera-relative, — інша
точка відліку.

**Перетворення на GPU звірене з C-оракулом, не «на око».** `sx,sy,sz` у
фікстурі — вихід `frame_from_inertial` (C, `core/frame.h`), безрозмірні
одиниці CR3BP. `engine::trajectory::rotating_position` рахує ту саму
формулу в Rust (`double`), і `cargo test -p engine` звіряє її з оракулом
на всіх 1345 семплах: **найгірша розбіжність 3.48·10⁻⁷**, на семплі 0 —
де центральна різниця для нормалі орбітальної площини вироджується в
однобічну (крайня точка ряду). Шейдер — та сама формула ще раз, у `f32`.

**Нормаль площини — центральною різницею, не через `h = d × ḋ` з
аналітичних швидкостей.** `core/frame.h` рахує нормаль з реальних
швидкостей тіл на кожен момент. Тут швидкостей немає (CSV їх не несе),
тож `z = normalize(d_i × (d_{i+1} − d_{i−1}))` за сусідніми семплами.
Похибка 3.5·10⁻⁷ показує, що на кроці ~2.7 год проти 27-денного місячного
місяця цього достатньо з великим запасом.

**Знахідка, варта запису: `mix()` у вертексному шейдері мовчки читав
uniform неправильно — лише у вершинній стадії, лише на цьому GPU.**
Перша версія мала одну точку входу з `chosen = lerp(geocentric, rotating,
uniforms.rotating)`, де `rotating` — 0 чи 1 з CPU. На AMD RADV VANGOGH
(ACO) рендер із `rotating=1` стабільно давав **порожній кадр**: пайплайн
збирався без жодної помилки чи попередження (`RUST_LOG=trace` — теж
чисто), `draw` виконувався, і єдиним симптомом був чорний PNG.

Локалізовано методом виключення, кожен крок відсікав один шар:
- геометрія коректна — обчислена паралельно в Rust (`double`), NDC у межах
  кадру;
- глибина не винна — `depth_compare: Always` нічого не змінив;
- `mix()` як функція не винна — той самий виклик із літералом
  `vec3<f32>(1.0f)` замість `vec3<f32>(uniforms.rotating)` малював коректно;
- **фрагментний шейдер читає той самий uniform правильно** — вивід
  `uniforms.rotating` прямо в колір показав точне `1.0`;
- копіювання значення в локальну змінну перед `mix()` не допомогло.

Тобто дані в буфері правильні, `mix()` як примітив працює, і саме
**поєднання читання uniform у вершинній стадії з `mix()`** на цьому
GPU/драйвері давало неправильний результат. Причина лишилась
нез'ясованою — це не той рівень, де варто зупиняти крок про рендер-техніку
заради GPU-компілятора.

Обхід — архітектурно чистіший, не милиця: **дві точки входу**
(`vertex_geocentric`, `vertex_rotating`) замість однієї з розгалуженням.
Обидва пайплайни компілюються по одному разу при першому виклику `render`,
вершинні буфери спільні для обох, перемикання фрейму — це вибір готового
пайплайна на CPU, а не перерахунок вершин чи навіть інша гілка в тому
самому шейдері. «Перемикання миттєве» з критерію тримається саме на
цьому.

**Регресія застрахована тестом, а не тільки оком.** `both_frames_draw_visible_pixels`
(`engine/tests/trajectory_render.rs`) рахує непорожні пікселі в обох
рендерах — той самий клас перевірки, що впіймав би саме цей баг
автоматично. Пройдено і на апаратному Vulkan (RADV VANGOGH), і на
`llvmpipe`/GL — тому самому бекенді, що в CI.

**Камера — спільний кут огляду, не окремий для кожного фрейму.** Обидва
режими проєктують на (x, y, z) — базис Земля-Місяць лінії й миттєвої
нормалі площини — і дивляться вздовж «y»: розкид орбіти там на порядки
менший, ніж по x і z (виміряно на реальних даних: `sy` семпла 0 — 0.008,
`sx` — 1.17, `sz` — −0.09), тож саме під цим кутом видно форму, а не
тонку лінію. Межі кадру (центр і радіус обмежувальної сфери) рахуються в
Rust на реальних даних (`geocentric_framing`, `rotating_framing`), а не
підбираються руками.

**Результат — два знімки з ОДНИХ вершинних буферів:**
`build/f6_geocentric.png` показує сім відкритих пелюсток — апарат, що сім
разів облітає Землю за 102 доби, кожен раз по-іншому (Земля тим часом
пройшла геліоцентрично, тож пелюстки не збігаються);
`build/f6_rotating.png` показує ту саму траєкторію як щільну **замкнену
петлю** біля L2 — саме критерій кроку, і саме те, для чого halo-орбіти
взагалі шукають в обертовій системі.

---

### Стан Milestone 2

Критерії приймання з PROJECT.md §10 — три:

| # | Критерій | Де зроблено | Результат |
|---|---|---|---|
| 1 | `wgpu` + `winit`, camera-relative + reversed-Z | F1, F3, F4 | вікно й кадр без вікна звіряються по пікселях; reversed-Z тримає ~7 порядків відстані (виміряно F3); camera-relative однаковий до останньої цифри від 10³ до 10¹¹ м (F4) |
| 2 | Одна текстурована сфера в реальному масштабі | F5 | сфера радіуса Землі, camera-relative per-vertex; покриття кадру збігається з аналітичною формулою `asin(R/(R+висота))` на 15 висотах від 10 до 10⁷ м |
| 3 | Рендер траєкторій в обертовій системі координат | F6 | halo-орбіта з C4 — замкнена петля біля L2 в обертовому фреймі проти семи розімкнених пелюсток у геоцентричному; перемикання фреймів миттєве (дві точки входу, не гілка в шейдері) |

**Критерій 2 виконаний не буквально.** Мета формулювання — довести, що
точність рендера тримається на сфері реального масштабу, і саме це виміряно
(таблиця покриття кадру, F5). Image-текстура (колірна карта) не накладена:
F5 прямо документує освітлення як тимчасовий ламберт, "не фінальна модель"
(PROJECT.md §7 планує SH-пробу від кольору планети, коли з'явиться сонце й
атмосфера). Накладати текстуру заради формулювання, а не заради перевірки,
означало б роботу без критерію — вона природно лягає на M4/M5, де вже буде
DEM і PBR.

**Milestone 2 закрито.** Борг з D1 (Windows під cargo) теж закрито заднім
числом 2026-08-13, див. вище. Далі — «Далі — грубо» нижче: після M2 достатньо
Rust і вимірювань, щоб деталізувати M3, коли до нього дійде черга.

---

## Вимір продуктивності (інфраструктура, не крок)

Не мілстоун і не критерій приймання окремого етапу — наскрізний процес,
що повторюється щоразу, як з'являється нова вартість (нова геометрія
в етапі F, нові тіла в M3.5, планети в M4). Методика й порядок дій —
скіл `perf-probe`; тут лише базові числа для звірки з майбутніми.

Два вимірювачі, кожен на своєму боці межі C↔Rust. Разом, а не один
замість іншого: `prop_run` ще не оголошений у `core-sys` (лише `eph_*`,
D2), тож фізичний бік міряється прямо в C, без FFI.

- **`make bench`** — пропускна здатність DOP853, рантаймового інтегратора
  (`core/bench/bench_dop853.c`): прийняті/відхилені кроки й обчислення
  прискорення за секунду, і похідне з них — скільки vessel-кроків влазить
  у бюджет кадру 60/30 Hz. Не звіряється з `golden.txt`: час стінного
  годинника апаратно залежний за визначенням, а не деталь, яку можна
  зробити детермінованою.
- **`cargo run -p engine -- --perf-probe`** — час кадру рендера
  (`engine/src/perf_probe.rs`): синхронний `submit`+`poll(Wait)` без
  вікна й без vsync, min/mean/p95/max за 300 кадрів, запас до 16.7 мс
  (60 Hz) і 33.3 мс (30 Hz). Міряє те, що `Frame::draw()` малює просто
  зараз — і далі трикутник F2, не сфера.

  **Уточнення проти того, що тут писалося до F5.** План був «після F5
  `Frame::draw()` малюватиме сферу», але F5 зроблено тим самим способом,
  що й F3/F4: окремий, самодостатній шлях (`sphere_render`,
  `flight_probe`), а не заміна вмісту інтерактивного `Frame`. Причина та
  сама, що тримала F3/F4 осторонь від `app.rs`, — `Frame` ще не має
  depth-текстури, що переживає resize, ні камери, якою можна керувати,
  і вирішувати це способом, який потім переробить frame graph (PROJECT.md
  §7), зарано. Отже перемір після F5 нічого не покаже понад те, що вже є
  в таблиці нижче; він стане змістовним, коли сфера (чи планета M4) стане
  вмістом `Frame`, а не раніше.

### Базові числа (Steam Deck, AMD RADV VANGOGH, Vulkan headless)

⚠ **Профіль збірки тут не записаний, і це робить рендер-рядок непорівнянним**
з будь-яким іншим. З'ясувалося на I3: між `cargo run` і `cargo run --release`
різниця в часі кадру тринадцятикратна. Рядок лишається як історія; наступний
вимір на цій машині треба зробити в release і записати профіль (зонд тепер
друкує його сам).

DOP853, `make bench`, бюджет 2 с, орбіта sc_dop853 (ексцентрична, похила):

| | |
|---|---|
| прийнятих кроків/с | 1 559 999 |
| відхилених кроків | 16.5% спроб |
| обчислень прискорення/с | 23 352 507 |
| час на прийнятий крок | 0.641 мкс |
| vessel-кроків у бюджеті 60 Hz | 26 000 |
| vessel-кроків у бюджеті 30 Hz | 52 000 |

Рендер, `--perf-probe`, 300 кадрів, сцена — трикутник F2 (не сфера):

| розд. | min, мс | mean, мс | p95, мс | max, мс | fps | запас до 60 Hz |
|---|---|---|---|---|---|---|
| 1280×720 | 0.355 | 1.064 | 3.671 | 4.197 | 940 | +15.6 мс |
| 1920×1080 | 0.534 | 1.177 | 3.201 | 4.268 | 850 | +15.5 мс |

**Рендер-числа зараз не показують вартість сцени** — сцена й далі трикутник
F2: F5 додав сферу окремим шляхом, не вмістом `Frame` (пояснення вище).
Числа показують стелю CPU↔GPU синхронізації одного кадру на цій машині:
навіть порожній кадр коштує ~1 мс через синхронний `poll(Wait)`, а розрив
mean↔p95/max (1 мс проти 3–4 мс) — джиттер планувальника й компонувальника
Steam Deck, не рушія. Переміряти, коли сфера чи планета (M4) стане вмістом
`Frame`, — тоді число почне означати щось про сцену, а не про сам метод
виміру.

---

## Етап G. Планування (M3) 🟢→🟡

M2 закритий формально (вище). Деталізується покроково, а не наперед: перший
крок зроблено, решта M3 (диференціальна корекція як частина флайт-планера,
porkchop, редактор маневрів) — коли до них дійде черга.

### G1. Lambert-солвер ✅

`core/planning/lambert.c` — `lambert_solve(r1, r2, dt, mu, prograde, n_revs,
&v1, &v2)`, точно за ескізом PROJECT.md §5. Нуль обертів (`n_revs = 0`) лише:
багатооборотний випадок має інший корінь того самого рівняння часу польоту
і власну схему брекетингу, якої тут немає.

```sh
make test    # core/test/test_lambert.c у складі юніт-тестів
```

**Нова, третя бібліотека в графі збірки.** `core/planning/` стоїть поряд
з `core/offline/`, але це не кукер: результат Lambert — план, який гравець
підтверджує, а не ассет, що їде в білд. `libm` дозволений (PROJECT.md §4:
межа детермінізму — по пропагації, не по плануванню), і на відміну від
`core/offline/`, ця бібліотека лінкується в рантайм — `scripts/check_no_libm.sh`
свідомо сканує лише `build/core` верхнього рівня, і коментар про майбутній
`core/planning` там був написаний ще на A2, до того як тут з'явився хоч
рядок коду.

**Перевірка — той самий прийом, що C2b: звірка з уже довіреною машинерією,
а не з переписаним у пам'яті числом з підручника.** Замість вбивати в тест
очікувані `v1`/`v2` з чужого підручникового прикладу (ризик — здогадана
цифра пройде тест і нічого не доведе), `test_lambert.c` бере державу
(r1, v1) на справжній двотільній орбіті, летить нею вперед через **DOP853**
(уже перевірений на B4) до (r2, v2), і подає в Lambert лише (r1, r2, dt):
розв'язок мусить відтворити v1 і v2, які й так вже відомі. Плюс перевірка,
що не залежить від DOP853 узагалі — енергія й момент імпульсу, порахований
окремо з (r1, v1) і з (r2, v2), мають збігатися, бо обидві точки лежать
на одному конічному перерізі.

**Прапорець `prograde` — це не «яка з двох гілок», а знак z-компоненти
моменту імпульсу.** Тест це виводить, а не постулює: для довільної орієнтації
орбіти `cross(r1, r2)` завжди паралельний до `h = r×v` (обидві точки лежать
у площині орбіти), і те, яким боком — тим самим чи протилежним — залежить
лише від знаку `h.z`, а не від того, коротким чи довгим шляхом летить
апарат. Звідси `prograde = (h.z ≥ 0)`, перевірено на шести геометріях:
кругова й ексцентрична орбіта, кожна коротким і довгим шляхом, плюс орбіта,
нахилена на 120° (щоб `h.z` дійсно був від'ємний, а не лише в теорії),
і одна на геліоцентричному масштабі — той самий z-допуск на z (нижче)
однаково збігається на 7·10⁶ м і на 1.5·10¹¹ м, бо він безрозмірний.

**Знахідка, яка коштувала переписування солвера: C(z) не падає нижче
нуля при z > 4π², а підручникове читання формули каже, що мало б.**
`C(z) = (1 - cos√z)/z` — чисельник ніколи не від'ємний, тож `C(z) ≥ 0`
для будь-якого `z > 0`, не лише на «еліптичній гілці». Перша версія
(звичайний Ньютон від z=0, як у Куртіса) це проґавила: на геометрії
«довгий шлях», 252°, перший же крок Ньютона стрибнув на +65 у z — далеко
за 4π² = 39.478, у корінь **іншого** рівняння (гілка з одним повним
обертом, яка так само розв'язна й так само проходить перевірку `c > 0,
y > 0`, просто описує іншу траєкторію). Звідти ітерація хаотично
перестрибувала між гілками сотню разів і не збігалася — не через
неправильну похідну, а через відсутність межі, за яку не можна заходити.

Розв'язано брекетингом: `find_bracket` розширюється від z=0 в той бік,
де F(z) прямує до нуля, з жорсткою стелею `LAMBERT_Z_HI` (трохи нижче
4π²), а сам корінь шукається safeguarded Ньютоном усередині вже знайденого
брекета — Ньютонів крок береться, лише якщо не виходить за межі; інакше
крок бісекції. Той самий прийом, що вже є в `core/correct.c`. Після цього
всі шість геометрій (і «короткий», і «довгий» шлях на кожній) збігаються
за ~1e-10 у z.

**Похідну dF/dz не взято з пам'яті готовою формулою.** Побудована через
добуток і ланцюгове правило з `y(z), C(z), S(z)` та їхніх похідних,
а рекурентні співвідношення `C'(z), S'(z)` звірені з рядом Тейлора при
z→0 прямо в коментарі коду — тобто перевірені, а не процитовані. Головна
ж перевірка коректності — не довіра до формули похідної окремо, а те, що
весь розв'язок (до якого ця похідна лише прискорює збіжність) відтворює
незалежно порахований DOP853.

**Чого свідомо немає:** багатооборотний Lambert (`n_revs ≠ 0` — інша
гілка того самого рівняння, інший брекет), і FFI на межу C↔Rust
(`core-boundary`: нова функція на межу — окрема робота, коли `game` чи
`engine` дійсно матиме що з нею робити; поки жодна ланка Rust не викликає
планування).

### G2. Porkchop-плот ✅

`core/planning/porkchop.c` — `porkchop_compute`: сітка (t1, tof) × Lambert
на кожну клітинку, PROJECT.md §8 «porkchop-плоти як UI для вибору вікна».
Патч-конічний, без поправки на повну ефемериду — та поправка (диференціальна
корекція) застосовується пізніше до однієї обраної гравцем траєкторії,
не до всіх клітинок сітки, яких можуть бути тисячі.

```sh
make test    # core/test/test_porkchop.c у складі юніт-тестів
```

**Дизайн — той самий приймач стану, що вже є для прискорення.**
`BodyStateFunc` — вказівник на функцію `(t, ctx) → (r, v)`, той самий
принцип, що `AccelFunc` у `core/integrator.h`: модуль не прив'язаний
до конкретного джерела ефемериди (скукований ассет, синтетична орбіта,
тестова фікстура). Буфер виходу — виклик дає буфер і місткість, C заповнює
й повертає фактичну кількість (CLAUDE.md інваріант 6, той самий контракт,
що `refdata_load_vectors`).

**Клітинка, де Lambert не зійшовся, пропускається, а не провалює всю
сітку.** Заборонені зони — очікувана, не аварійна частина порчкоп-плоту
(траєкторії, яких не існує при цьому часі польоту), і саме плот — інструмент
їх побачити. `CORE_ERR_BUFFER_TOO_SMALL` — єдина помилка, яку сітка може
повернути після старту, і `out_count` при ній також показує, скільки
записано, а не залишає це невідомим.

**Перевірка — аналітичний Хоманів переліт, а не довільний поріг.**
Круговий, компланарний Земля(1 а.о.)–Марс(1.524 а.о.) навколо Сонця,
синтетичні `circular_state` замість реального ассета — та сама причина,
що в G1: точний оракул, порахований у самому тесті, а не задубльоване
число. Хоманів переліт — мінімально-енергетичний двоімпульсний перехід
між двома круговими орбітами за вільної фази, тож жодна клітинка сітки
не може дати менше. Сітка 120 (час старту, більше синодичного періоду)
× 60 (час польоту, 0.5–1.5 Хоманового) клітинок: **усі 7200 зійшлися**,
і найкраща клітинка дала 5595.9 м/с проти аналітичних 5593.6 — розбіжність
0.042%, це крок сітки за фазою старту, не недолік солвера.

### G3. Диференціальна корекція для флайт-планера ✅

`core/target.h/c` — `target_hit`: скоригувати швидкість старту так, щоб
під реальним полем (не спрощеною двотільною моделлю, яку припускає Lambert)
апарат прийшов рівно в задану точку в задану мить. Одна ціль замінює
Lambert-наближення на факт: PROJECT.md §8 «Lambert solver → початкове
наближення → диференціальна корекція / multiple shooting → уточнення».

**Не нова математика — той самий алгоритм, що вже мовчки жив у
`core/station.c` (C4).** У `station.c` була приватна функція `target()`
з тим самим Ньютоном на 3×3 блоці STM (позиція-від-швидкості-старту), яку
`station_keep` викликає, щоб прицілитись у контрольну точку попереду.
Замість писати другу копію того самого — той самий Ньютон, той самий 3×3
розв'язок Гауса з частковим піводенням — код винесено в спільний
`core/target.c`, і `station.c` тепер викликає його. **Рефактор перевірено
бітово:** хеш `sc_trajectory` (єдиний сценарій, що йде через
`station_keep`) не зрушив ні на біт до і після винесення.

**Живе в `core/` верхнього рівня, не в `core/planning/`.** На відміну від
Lambert, тут нема жодної тригонометрії — суто `+ - * /` і `sqrt` через STM
(`core/stm.h`), тож немає причини виносити за межу детермінізму, навіть
якщо один із двох викликачів (уточнення Lambert-плану) сам поза нею.
Детермінізм — властивість коду, а не викликача.

```sh
make test    # core/test/test_target.c у складі юніт-тестів
```

**Перевірка — повний трубопровід §8 на реальних даних фікстури,
Земля → Венера.** `lambert_solve` дає двотільне (лише Сонце) наближення
швидкості старту з позиції Землі до позиції Венери за 70 діб; `target_hit`
під справжнім полем восьми тіл (десять тіл фікстури мінус Земля й Венера —
нижче) підправляє її так, щоб апарат дійсно прийшов, а не туди, куди
привело б наближення. Зійшлося за 3 ітерації, нев'язка 1.0·10⁻² м.

**Пастка, яка одразу дала NaN: власна гравітація точки старту.**
`field_all_bodies` бере всі десять тіл фікстури, а `r1` — це буквально
позиція Землі з `eph_body_state`: відстань до «Землі» в сумі поля виходить
рівно нуль, і `mu/0³` — NaN на першому ж виклику `accel_field`, ще до
жодної ітерації Ньютона. Той самий нюанс і на прильоті: `r2` — позиція
Венери. Виправлення — не костиль, а правильний обсяг моделі: контекст поля
збирається вручну (поля `FieldCtx` публічні) без Землі й Венери, бо власна
яма тяжіння тіла відправлення чи прибуття — окрема, не змодельована тут
фаза, той самий обсяг, у якому Lambert і так рахує. `field_all_but`
виключає лише одне тіло, тому для двох довелось зібрати контекст напряму.

**Виміряно: поправка — уточнення, а не переспроб.** Наближення 37405.0
м/с, поправка 246.0 м/с — 0.66% відносно. Сонце лишається в полі (одне
з восьми тіл, що лишились) і домінує, тож решта сімох планет за 70 діб
лише підправляють відповідь, не замінюють її.

### G4. Криві нульової швидкості ✅

`cr3bp_zvc_radius` у `core/cr3bp.c` — межа забороненої зони для заданої
константи Якобі C, для карти гри (PROJECT.md §7 «Карта — це наша графіка»).
Насправді доробок етапу C (CR3BP), не M3-планування — топологію ZVC C1
уже перевіряв непрямо (сканом уздовж осі для пастки L1), тут з'явилась
перша перевикористовувана функція, а не лише перевірка в тесті.

```sh
make test    # блок ZVC у core/test/test_cr3bp.c
```

**Уся фізика — одна нерівність, не крива.** `v² = 2Ω − C`, тож заборонено
(v² < 0) рівно там, де `2Ω(r) < C`. Функція лише знаходить, де ця нерівність
переходить у рівність уздовж одного променя — сканом на знак, потім
бісекцією, той самий прийом, що `find_crossing` у `correct.c`. Викликач,
що обходить сітку кутів, збирає з цього ламану для рендера.

**Напрям — вектор, не кут.** `core/cr3bp.c` — верхній рівень `core/`,
тобто детермінована зона без `libm` (CLAUDE.md інваріант 3): `cos`/`sin`
там заборонені так само, як в усьому іншому файлі. Переклад кута в напрям —
робота викликача, а не цього модуля.

**Бісекція, не Ньютон — свідомо, за прецедентом із цього ж файлу.**
`cr3bp_lagrange` (нижче в тому самому cr3bp.h) уже аргументує: рахується
кілька разів на кадр карти, ніколи в гарячому циклі, і не може розійтися.
Той самий аргумент застосовано тут без зміни.

**Перевірка не додає нового невіряного числа — будується на вже
перевіреному C1 факті** (мінімум `2Ω` уздовж осі між тілами дорівнює
C(L1), пастка L1). Зі сторони баріцентра вздовж осі до другого тіла:
при C трохи більшому за C(L1) («ворота» зачинені) межа знаходиться до L1
і саме з очікуваного боку; при C трохи меншому («ворота» відчинені) на
цьому промені взагалі нема перетину — `CORE_ERR_TOLERANCE_NOT_MET`, і це
теж відповідь про топологію, не помилка; і коли C наближається до C(L1),
розрив до L1 звужується як `√delta` — квадратичний мінімум це передбачає.
Виміряно: delta зменшено у 100 разів, розрив зменшився у 10.46 раза
(очікування — 10).

**Сценарій детермінізму розширено, не додано новий.** `sc_cr3bp` тепер
хешує ще й `cr3bp_zvc_radius` — та сама бісекція, що вже там для L1–L3,
інший виклик. Хеші решти сценаріїв не зрушили ні на біт;
`make determinism-bless` зроблено свідомо, різниця — один рядок.

### Далі в M3

- Редактор маневрів («план = список маневрів, каскадний перерахунок») —
  стан гри, потребує старту крейта `/game`. Окреме архітектурне рішення,
  не наступний маленький крок.
- FFI на `lambert_solve`/`porkchop_compute`/`target_hit`, коли гра вперше
  матиме інтерфейс, що їх викликає.

---

## Етап H. Жива межа 🟢→🟡

Найбільший борг після M2: `engine` малює фікстури, а не фізику. Ядро вміє
рахувати траєкторії, межа вміє читати ефемериду — і ці дві половини ще
жодного разу не бачили одна одну. Етап їх з'єднує.

Порядок той самий, що на етапі D: спершу C, потім сира декларація, потім
обгортка, і лише тоді споживач. Кожна ланка звіряється з попередньою бітово,
бо помилка на межі не падає — вона повертає правдоподібні числа.

### H1. `prop_run` у C ✅

`core/prop.h/c` — `prop_create`/`prop_free`/`prop_run`, друга половина
C API з PROJECT.md §5. Під ним усе вже було й було перевірене:
`field_all_bodies` (десять точкових мас), `accel_field` (сума),
`dop853_integrate` (кроки). Нове тут — форма, якої потребує межа, і одна
властивість, яку треба довести, а не сподіватись на неї:

> **Прогноз, порізаний буфером на шматки, бітово дорівнює одному прогону.**

Це інваріант 5 CLAUDE.md («прогноз і фізика — один інтегратор і один
допуск») у виконуваному вигляді. Планер, що малює лінію, якою апарат потім
не полетить, — це не помилка малювання.

```sh
make unit        # core/test/test_prop.c
make determinism # core/scenario/sc_prop.c
```

**Семпли — це прийняті кроки інтегратора, а не рівномірна сітка.** Причина
не естетична: приземляти кроки на обрані часи означає забрати крок у
контролера похибки, і кукер уже виміряв, наскільки далеко це заходить —
з примусовими вузлами зміна допуску на два порядки не змінювала **нічого**
(«Допуск оновлено: 1 м → 1e-6 м»). До того ж адаптивний крок сам густішає
там, де траєкторія загинається, тобто рівно там, де ламаній потрібні
вершини.

Щоб це стало можливим, `dop853` отримав **спостерігача кроку**
(`StepObserver`, `dop853_integrate_obs`): його кличуть після кожного
прийнятого кроку, і він може зупинити прогін. Колбек лишається всередині
C — через межу ходить буфер, не функція (інваріанти 6 і 7). Рефактор
довелося зробити бітово нейтральним, і це перевірено: усі дванадцять
наявних хешів не зрушили.

**Виміряно** (дві доби геостаціонарної орбіти, допуск 1 см, поле десяти тіл):

| що | результат |
|---|---|
| прогін без семплів проти прямого `dop853_integrate` | бітово те саме |
| семплювання проти прогону без семплів | бітово те саме |
| кількість семплів | 101 = `n_accepted` прямого прогону |
| 26 ланок по 4 семпли проти одного прогону | бітово те саме, **включно з перенесеним кроком** |
| той самий прогін, але крок між ланками викидається | 7148 семплів замість 101, розбіжність 1.9 мм |

Останній рядок — це причина, з якої PROJECT.md §4 вимагає крок у сейві.
Ціна не в тих двох міліметрах, а в **сімдесятикратній** роботі: інтегратор
щоразу вгадує початковий крок наново й піднімається до природного заново.

**Знахідка, яка не видно у відповіді: стеля кроку мусить бути заданою.**
Якщо `h_max_s = 0`, стелю обирає інтегратор — і обирає довжину ланки, яку
йому дали (`core/dop853.c`). Тоді остання ланка зшитого прогону отримує
меншу стелю, ніж бачив безперервний. Виміряно: **траєкторія лишається
бітово однаковою, а перенесений крок — ні** (5493.85 с проти 6440.34 с).
Гірша половина саме та, що не проявляється у відповіді: наступний виклик
продовжить кроком, якого ніхто не обирав. Тест фіксує це як нерівність —
якщо колись почне збігатися, значить змінилося, як обрізаний крок годує
контролера, і про це варто почути.

**`VesselParams` свідомо немає.** Маса, площа, `cr`, `cd` з ескізу §5
нікому не потрібні, доки немає SRP і опору (M3.5), а структура, жодне поле
якої не читається, гірша за свою відсутність: викликач її заповнює, нічого
не відбувається, і ніщо про це не каже.

**Заповнений буфер — причина зупинки, а не помилка** (`CoreStopReason`):
нічого не зламалось, прогін дійшов до точки, з якої можна продовжити. Той
самий підхід, що з пропущеними клітинками porkchop.

**`sc_prop` хешує семпли, а не лише кінцеву точку** — тобто хешує саму
послідовність кроків. Платформа, яка інакше вирішила б долю одного
відхиленого кроку, прийшла б у ту саму кінцеву точку в межах допуску й
розійшлася б тут. Перевірка має зуби: з `-ffast-math` хеш розходиться.

### H2. Події ✅

Зупинка **рівно на події**, а не в кінці кроку, який її перетнув
(PROJECT.md §4). Три види: `CORE_EVENT_PERIAPSIS`, `CORE_EVENT_APOAPSIS`,
`CORE_EVENT_DISTANCE`. Події описуються даними, пошук кореня живе в C —
разом це і робить інваріант 7 (жодних колбеків з C у Rust) здійсненним,
а не лише проголошеним.

**Чому не «раз на крок».** Перемикання режиму на межі кроку відбувається в
момент, що залежить від розміру кроку, — і два прогони того самого плану
запалюють двигун у різних місцях. Виміряно, що зупинка на події від кроку
не залежить: **зменшення стелі кроку вчетверо зсуває перицентр на 8.3 мкс**
при кроці в сотні-тисячі секунд.

**Ціна названа чесно.** Зупинка на події змінює послідовність кроків після
неї — траєкторія продовжується з моменту, якого контролер не обирав. Це і є
задумана поведінка, а не послаблення інваріанта 5: той самий план,
відтворений ще раз, зупиняється в той самий момент і продовжується з того
самого стану. Зламало б детермінізм протилежне — дозволити кроку вирішувати,
де сталася подія.

**Оракул — форма самої траєкторії, а не число звідкись.** Перицентр
перевіряється тим, що за хвилину до й хвилину після апарат далі від Землі
(19843.115 км проти 19843.771 з обох боків); період з двох апоцентрів
дорівнює подвоєній відстані до перицентра між ними. Жодна перевірка не знає
відповіді наперед.

| що виміряно | результат |
|---|---|
| перицентр знайдено | t₀+7.546 год, r = 19843.115 км (справжній мінімум) |
| стеля кроку ÷4 зсуває його на | 8.3 мкс |
| апоцентр, і наступний за ним | t₀+15.091 год, далі рівно через 15.091 год |
| попадання в задану відстань (30000 км) | промах 6.8·10⁻⁶ м всередину, 1.1·10⁻⁶ м назовні |
| дві озброєні події: сфера перша, тоді перицентр | той самий перицентр з точністю 1.3 мкс |

**Знахідка, що коштувала окремого поняття: подія, на якій прогін
стартував, — це не подія.** Прогін, зупинений на апоцентрі, віддає стан, де
`g = d·ḋ` нуль з точністю пошуку кореня, а знак там — що вийде з останньої
арифметики. Поновити з тим самим озброєним апоцентром — і перший же крок
читає цей знак як «зараз перетнемо» й доповідає про апоцентр, на якому
щойно спинилися, у момент, невідрізненний від «тепер». Виміряно: наступний
апоцентр опинявся за 0.000 год. Гравець, що крокує від апоцентра до
апоцентра, не зрушив би з місця.

Лікується не порогом, вигаданим з повітря, а класифікацією з величезним
запасом: `|g| < 10⁻¹² · |d|·|ḋ|`. Для цієї орбіти це `|g| < 0.12`, тоді як
за **мікросекунду** до апоцентра `g ≈ 10`, а за секунду — `10⁷`. Вісім
порядків між «на події» і «майже на події»; це розпізнавання, а не
підкручена стала.

**Чого немає і чому саме:** `SHADOW_ENTRY` потребує моделі тіні, що
приходить із SRP (M3.5); `STATION_RISE` — матриць обертання тіл, яких ассет
ще не несе. `CORE_EVENT_DISTANCE` — відстань від **центра**, не висота: в
ассеті лежать ім'я і `mu`, радіуса немає, і подія про висоту мусила б його
вигадати. Стане висотою, коли радіуси приїдуть в ассет (M4).

`sc_prop` розширено фазою з подіями: це найрозгалуженіший рантаймовий код
за межею — скільки ітерацій зробить Ньютон і чи візьме крок замість
бісекції, вирішується порівнянням чисел з рухомою комою.

### H3. FFI — `core-sys` ✅

`prop_create`, `prop_free`, `prop_run` — вручну, без bindgen, як і перші три.
Плюс типи: непрозорий `PropagatorCtx`, `PropConfig`, `CoreEvent`, і три
переліки як `c_int` з константами — з тієї самої причини, що й `CoreResult`:
Rust-енум зі значенням поза переліком це UB, а не помилка, яку видно.

**Дванадцять аргументів `prop_run` — це дванадцять способів розійтися тихо.**
Дві структури (`PropConfig`: `enum`, два `double`, `long`; `CoreEvent`: `enum`,
`int`, `double` — саме там вирівнювання й розходиться), три вихідні
вказівники, буфер від Rust і пара `out_cap`/`out_count`, яку легко переплутати
місцями. Жодна з цих помилок не падає — усі повертають числа, схожі на
траєкторію.

Тому оракул (`core-sys/oracle.c`) розширено з «трьох тіл у три моменти» до
двох повних прогонів: один із семплами до заданого часу, другий — до
перицентра, тобто через `CoreEvent` і через код зупинки. `tests/ffi.rs`
повторює обидва через FFI й звіряє **біти**: кожен семпл, кінцевий стан,
перенесений крок, код зупинки й індекс події.

**Звірка має зуби, і це перевірено, а не заявлено:** переставлені місцями
`tol_m` і `h_max_s` у декларації `PropConfig` валять тест одразу — навіть не
на числах, а на кількості семплів.

`max_steps` — `c_long`, бо в C це `long`: 64 біти на Linux і macOS, 32 на
Windows. `c_long` іде за платформою так само, тож обидва боки згодні; це межа
кількості кроків, а не арифметика.

Апарат в оракулі заданий літералами, а не порахований з `sqrt`: оракул
лінкується **без `libm`**, точно як сценарії детермінізму.

### H4. `core-rs` — `Propagator` ✅

RAII, `Result`, зрізи замість (вказівник, довжина). Тепер обгортка має обидві
половини межі: `Ephemeris` каже, де тіла, `Propagator` веде крізь їхнє поле
апарат.

**Ефемерида — `Arc`, а не лайфтайм.** Контекст у C зберігає сирий вказівник
на неї, тобто вона мусить пережити пропагатор. Лайфтайм у структурі виразив
би те саме — і заразив би собою все, що пропагатор зберігає (CLAUDE.md:
жодних лайфтаймів у структурах). `Arc` каже те саме без цієї ціни.

**Перший тип межі, де енум кращий за структуру.** `Event` — це
`Periapsis { body }`, `Apoapsis { body }`, `Distance { body, metres }`:
задати перицентр із відстанню тут неможливо, бо такого варіанту немає. У C
це одна структура з полем `param`, яке для двох видів із трьох не означає
нічого.

**Порожній зріз ≠ порожній буфер, і на цьому легко зламатися.** Порожній
зріз у Rust — вирівняний **висячий** вказівник, не нуль. Якби він поїхав у C
як буфер, той визнав би це помилкою викликача (нуль місця = нуль поступу), і
цикл ланок не закінчився б ніколи. Тому порожність перекладається явно, і на
це є тест.

**Що перевірено:**

| твердження | як |
|---|---|
| обгортка не змінює жодного біта | той самий прогін через `core-sys` напряму, бітова звірка семплів, кінцевого стану й кроку — ланцюжок C → `core-sys` → `core-rs` замкнено |
| зшиті ланки = один прогін | через обгортку, бітово, разом із перенесеним кроком |
| подія доходить з індексом | старт рівно в апоцентрі, озброєні обидві події, чекаємо індекс **1** — нуль за замовчуванням тут не пройшов би |
| use-after-free і double-free неможливі | два `compile_fail`-доктести |
| вони падають саме з тієї причини | кожен прогнано ще раз без порушувального рядка — обидва зібралися |
| звільнення таки відбувається | 50 циклів create/run/drop під valgrind у CI (витік — не помилка типів) |

`Integrator::Rkn` оголошений і повертає `InvalidArg`: PROJECT.md §4 вимагає
поле вибору інтегратора з першого дня, щоб RKN колись означав зміну виклику,
а не переписування шару.

### H5. `engine` малює живий прогноз ✅

Рушій лінкує `core-rs`. Лінія в кадрі — вихід `prop_run`, а не колонка
тексту. Борг, записаний у F6 («`engine` не лінкує `core-rs` — це окреме,
більше рішення»), закрито.

```sh
cargo run -p engine -- --live-probe
```

**Що робить зонд:** бере стан апарата з першого семпла фікстури, веде його
`core_rs::Propagator` крізь поле десяти тіл ассета — 2448 семплів за 39
викликів `prop_run` — і малює тим самим рендером, що й F6. Поруч, у тому
самому кадруванні, малюється сама фікстура.

**Фікстурі повернули швидкості.** `vx,vy,vz` прибрали при трімі з
аргументом «рушію лінія без швидкостей» — правдивим рівно доти, доки рушій
лінію лише читав. Позиція без швидкості не є станом, і продовжити її
неможливо.

**Головне вимірювання — не «збігається», а «як довго».** Фікстура це
розв'язок multiple shooting: сім ланок, кожна зі свого вузла, з розривами
на швах. Живий прогноз безперервний, тож на орбіті з множником 594 за оберт
дві криві **зобов'язані** розійтися:

| розбіжність | коли |
|---|---|
| 10³ м | 30.7 доби |
| 10⁵ м | 41.2 доби |
| 10⁷ м | 50.9 доби |
| 10⁹ м | 67.9 доби |

**Темп — ×763 за оберт проти ×594, передбачених монодромною матрицею
в C3.** Це і є доказ, що криві розходяться через нестійкість орбіти, а не
через помилку в стані, одиницях чи епосі: така помилка дала б лінійне
зростання або зовсім інший темп. Збіг у межах 30% для лінійного передбачення,
застосованого до скінченного відхилення, — це збіг.

І це видно оком, без жодного числа: `build/h5_reference.png` — замкнена
петля всі 101.8 доби; `build/h5_live.png` — та сама петля, з якої апарат
на середині місії йде геть. Саме тому реальні місії на halo-орбітах роблять
station-keeping (C4).

**«Прогноз і політ — одна траєкторія» перевірено з боку гри.** H1 міряв це
в C; тепер те саме через увесь ланцюжок: ланки по 16 семплів проти одного
прогону дають бітово однаковий кінцевий стан і однаковий перенесений крок.

**Шлях до ассета — `CARGO_MANIFEST_DIR`, і це свідомо тимчасово.** `cargo
test` запускає бінарник з каталогу крейта, `cargo run` — з поточного, тож
відносний шлях означав би різне. Грі шлях дасть застосунок; вигадувати шар
конфігурації зараз — робота без критерію.

---

## Етап I. Інтерактивний кадр 🟢

Етап F міряв по одному твердженню за раз, і кожен крок робив це **окремим
самодостатнім шляхом**: `depth_probe`, `camera_probe`, `sphere_render`,
`flight_probe`, `trajectory_render` — кожен зі своїм пайплайном, своєю
depth-текстурою й PNG на виході. Так і треба було: крок, який міряє, не
повинен ділити код із кроком, який його ламає.

Ціна виявилась у тому, що `Frame` — те, що справді потрапляє у вікно —
лишався трикутником F2. Отже жодна виміряна властивість не була перевірена
на шляху до гравця, а `--perf-probe` міряв стелю синхронізації замість сцени.

### I1. Сфера як вміст кадру ✅

`Frame` отримав depth-текстуру, що переживає зміну розміру, буфери сфери й
камеру як параметр малювання.

```sh
cargo run -p engine -- --shot build/i1_frame.png
cargo run -p engine -- --frames 40      # вікно, зі зміною розміру посеред
```

**Depth-текстура належить `Frame`, не `app`.** Спокуса була покласти її поруч
із поверхнею — там же, де обробляється `Resized`. Але тоді та сама логіка
знадобилася б і в `shot.rs`, а два місця розходяться. `Frame` і далі не знає
про вікно нічого: він знає про текстуру, її формат і розмір, а розмір і так
приходить аргументом.

**Один метод `draw`, а не пара «оновити / записати».** Роздільні виглядають
чистіше й дозволяють забути перше — отримати кадр із учорашньою камерою або
з depth-текстурою чужого розміру. Тут це неможливо за конструкцією.

**Ближня площина рахується з висоти камери, а не задана сталою.** Десята
частина висоти — той самий порядок запасу, що F5 узяв руками (near = 1 м при
прольоті на 10 м). Підбирати її заради z-fighting не треба й нема сенсу:
F3 виміряв, що роздільність глибини від near не залежить взагалі
(`Δz ≈ z·6·10⁻⁸`, near скорочується).

**Перевірка — не «намалювалось», а число проти числа.** Оракул той самий, що
у F5: частка кадру, яку зобов'язаний зайняти диск силуету,
`asin(R/(R+висота))`. Камера за замовчуванням стоїть на 10⁷ м саме тому, що
це єдина висота з таблиці F5, де диск цілком влазить у кадр — при 60° поля
зору сфера заповнює все аж до ~3·10⁶ м.

**Головний тест — один `Frame` у три різні розміри поспіль** (128×128,
256×128, 100×100), кожен звіряється зі своїм аспектом. Він ловить обидві
помилки, а не одну: якщо depth не переслідує розмір — падає валідація wgpu,
якщо переслідує, але проєкція не перерахувалась — розходиться покриття.
Зуби перевірені: з вимкненим перестворенням текстури тест падає з
«Attachments have differing sizes: … (128, 128, 1) … (256, 128, 1)».

**Що зникло:** перевірка трикутника в `engine/tests/shot.rs`. Вона питала
«який канал переважає» — найслабше, що можна спитати, і саме тому вона там
була до появи справжньої геометрії.

Зонди етапу F лишаються як були. Вони міряють по одному твердженню й роблять
це краще, ніж міг би інтерактивний кадр; спільним лишається все, що робить
арифметику — `sphere::Mesh`, `camera::Camera`, `depth`, той самий
`sphere.wgsl`.

### I2. Керована камера ✅

Орбітальна: ліва кнопка обертає, колесо міняє висоту, Esc виходить.

```sh
cargo run -p engine            # вікно; керування друкується при старті
```

**Стан — три числа, позиція з них виводиться, а не накопичується.** Камера,
яка додає зсуви до власної позиції, поступово сповзає зі сфери, і моменту, в
який це стало помітно, не існує. Тут yaw, pitch і висота — єдине, що
змінюється; позиція перераховується щокадру.

**`orbit` не знає ні про `winit`, ні про GPU.** На вході числа, на виході
`Camera`. Тому все, що модуль обіцяє, перевірено без адаптера, а `app`
лишився тонким перекладачем подій.

Три межі, кожна з причиною, а не «про всяк випадок»:

| межа | звідки число |
|---|---|
| висота ≥ 10 м | найближча точка прольоту F5; нижче меш 64×128 — це грань під носом камери |
| висота ≤ 10¹¹ м | докуди F4 виміряв, що camera-relative тримається до останньої цифри |
| нахил < 90° − 10⁻³ | рівно на полюсі напрямок погляду збігається з орієнтиром «вгору», їхній векторний добуток нуль, базис камери стає NaN |

Останню варто назвати окремо: це не теоретичний ризик, користувач доводить
камеру до полюса за секунду тягнення. Тест тягне на сто кроків через полюс і
назад і вимагає скінченних чисел.

**Масштаб геометричний, не додаванням.** Від 10 м до 10¹¹ м десять порядків;
сталий крок у метрах або нерухомий на одному кінці, або непридатний на
іншому. Двадцять клацань туди й назад повертають висоту з точністю 10⁻⁹.

**Перевірка, що арифметика доїжджає до кадру** (`engine/tests/shot.rs`):
обертання не має міняти покриття взагалі — сфера з усіх боків однакова, —
а наближення має, і рівно на стільки, скільки каже `asin(R/(R+висота))`.
Той самий оракул, що в I1, тобто перевіряється не «картинка змінилася», а
«змінилася рівно так, як зобов'язана».

**Камера за замовчуванням — та сама, на якій міряють кадр** (`Orbit::default`
проти `frame::default_camera`, звірка позицій). Інтерактивний шлях
починається рівно в тій точці, де його перевіряють.

### I3. Перемір продуктивності ✅

Уперше число про сцену — і воно одразу показало, що метод виміру мав діру.

```sh
cargo run --release -p engine -- --perf-probe    # release, а не cargo run
make bench
```

**Машина інша, ніж у базових числах.** Ті рахувались на Steam Deck (RADV
VANGOGH); ці — на NVIDIA RTX 4070 Ti SUPER, Vulkan. Порівнювати їх між собою
означало б міряти дві машини, а не сферу, тож базовий рядок лишається як був,
а це — окремий блок (скіл `perf-probe`: не перезаписувати, історія робить
регресію видимою).

**Діра в методі: профіль збірки не записувався.** Різниця виявилась не
стилістичною:

| профіль | час кадру, 1280×720 | CPU camera-relative |
|---|---|---|
| debug (`cargo run`) | 1.305 мс | 1.015 мс (78% кадру) |
| release (`cargo run --release`) | 0.097 мс | 0.024 мс (25% кадру) |

Тринадцятикратна різниця в часі кадру й сорокадвократна в CPU-проході.
Число без профілю непорівнянне з жодним іншим — а базовий рядок Steam Deck
профілю не називає (1.064 мс середнього **на трикутник** дуже схоже на
debug). Тому зонд тепер друкує профіль і сцену поруч із числами: пастку
прибрано з процесу, а не з пам'яті.

**Виміряно (release, RTX 4070 Ti SUPER, сфера 8385 вершин / 16384
трикутники):**

| розд. | min мс | mean мс | p95 мс | max мс | запас до 60 Hz |
|---|---|---|---|---|---|
| 1280×720 | 0.091 | 0.109 | 0.136 | 0.354 | +16.56 мс |
| 1920×1080 | 0.093 | 0.110 | 0.136 | 1.480 | +16.56 мс |

**Головне — не запас, а те, що обидві роздільності дають однакове число.**
Кадр не залежить від кількості пікселів, бо його визначає не растеризація:
чверть часу — CPU-прохід camera-relative по 8385 вершинах, решта —
синхронізація. Тобто сфера в бюджет влазить із запасом у 150 разів, і це
нічого не каже про M4: там вершин буде на порядки більше, а прохід по них
лишиться на CPU, доки його не замінить зсув по патчах.

Саме тому зонд тепер міряє цей прохід **окремо** — це єдина частина кадру,
про яку заздалегідь відомо, що вона тимчасова, і в загальному часі кадру на
швидкій машині вона тоне.

**Фізика на цій машині** (`make bench`, той самий сценарій): 3 007 184
прийнятих кроки/с проти 1 559 999 на Steam Deck — тобто вдвічі швидше ядро,
0.333 мкс на крок, 50 121 vessel-крок у бюджеті 60 Hz.

---

## Етап J. Гра і потоки 🟢→🟡

Крейт `/game` і модель потоків з PROJECT.md §6. Етап архітектурний: він не
додає ні фізики, ні картинки, а вирішує, **хто чим володіє** — і зробити це
кодом наосліп означало б переписувати все, що на цьому стане.

Порядок той самий, що завжди: спершу однонитково, потім потоки. Нитка,
додана до нерозв'язаної задачі, не розв'язує її, а робить діагностику вдвічі
дорожчою.

**Рішення етапу, з якого випливає решта** (записане в PROJECT.md §6):
годинник не входить в інтегратор, `t_end` у `prop_run` ніколи не походить
від годинника, обсяг роботи міряється **ланками**. Ланка — одиниця
інтегрування, поділу зі снапшотом, інвалідації та відвантаження на GPU.

### J1. Крейт `/game`, типи стору, межа з рушієм ✅

`Leg`, `Trajectory`, `WorldSnapshot`; тік **в одній нитці**;
`engine::scene::Scene` і `engine::window::Target`. Гра стає бінарником і
володіє циклом подій; рушій отримує сцену й про гру не знає нічого.

```sh
cargo run -p game                          # вікно
cargo run -p game -- --shot build/j1.png   # повний прогноз у PNG, без вікна
```

**Головна перевірка пройшла: 2448 семплів, бітово ті самі, що в H5** —
час, позиція, швидкість, позиції Землі й Місяця, кожен семпл. Між
`prop_run` і кадром тепер стоїть шар (ланки, стор, снапшот, сцена), і
кожна його ланка могла б тихо зіпсувати криву так, що вона лишилася б
правдоподібною.

**Розмір ланки тут інший, ніж в оракула, і це головне в перевірці:** 256
проти 64 у `engine::live`, тобто 10 викликів `prop_run` проти 39.
Однакові числа зробили б звірку тавтологією; різні — роблять її
твердженням «робота міряється ланками, а ланка на числа не впливає»
(інваріант 9; у C це виміряв H1).

**Зуби перевірено:** з викинутим перенесенням кроку (`tip_step = 0` перед
кожною ланкою) виходить **2491 семпл замість 2448** — і тест падає на
першому ж порівнянні. Заодно це число варте запису поруч із H1: там
скидання кроку щоланки коштувало ×70 роботи, тут ланок лише десять, тож
надлишок — 1.8%. Тобто ціна залежить не від самого скидання, а від того,
як часто воно трапляється; на дрібних ланках прогнозу вона знову стане
сімдесятикратною.

**Тік по колу між апаратами.** Бюджет у ланках витрачається порівну, а не
першим у списку: інакше дев'ятий апарат гравця (PROJECT.md §4, warp як
розклад) не рахувався б ніколи. Перевірено окремо, що розмір бюджету на
числа не впливає взагалі — 1 ланка за тік проти 1000 дають бітово те саме.
Це J1-версія головної перевірки J2, і мати її **до** появи годинника варто
саме тому, що потім підозрюваних було б двоє.

**Три рішення в межі `engine ↔ game`, кожне з причиною:**

1. **Ламані йдуть тим самим camera-relative, що й вершини сфери.**
   Спокуса була взяти готовий `trajectory_render` (F6) — але той
   прив'язаний до пари Земля-Місяць і має власну «камеру» одним зсувом,
   без базису, тож обертання гравця його не зачіпає. Новий шейдер
   (`line.slang`) робить рівно проєкцію, а `double`-віднімання лишається
   на CPU. Тест `the_camera_moves_the_prediction_too` ловить саме цю
   помилку.
2. **Колір ламаної — атрибут вершини, а не поле uniform.** Ламаних у
   кадрі кілька, а всі записи в чергу відбуваються **до** проходу: з
   uniform виграв би останній запис, і всі лінії стали б одного кольору.
   Шістнадцять байтів на вершину дешевші за цей клас помилки.
3. **`Scene` не має списку тіл.** Сфера лишилася всередині `Frame`: в
   ассеті немає радіусів тіл (це M4), тож список був би структурою, яку
   хтось заповнює, а рушій ігнорує — та сама причина, з якої H1 не завів
   `VesselParams`.

**`Plan` перенесено в J3, і це свідомо.** План, який ніхто не читає, — це
рівно та структура, проти якої проєкт уже висловився двічі. У J1 його
нічим було б прочитати: `t_end` береться з кінця місії, а Δv нема куди
застосувати. Він приїде разом із сегментним циклом, який його споживає.

**Що це дає подивитися очима** (`build/j1_game.png`): та сама halo-орбіта,
що в H5, але геоцентрично й в інтерактивному кадрі — кілька витків біля
L2, а тоді довга лінія геть. Це не новий результат, а той самий зрив
нестійкої орбіти, який H5 виміряв у числах; тепер він у тому кадрі, який
іде у вікно.

**Розвилка не знадобилася.**

### J2. Курсор часу, warp, горизонт ✅

Курсор, warp, стеля горизонту в ланках, інтерполяція Ерміта до точного `t`,
`stall` у снапшоті.

```sh
cargo run -p game                                 # пробіл — пауза, «.» і «,» — warp
cargo run -p game -- --shot build/j2.png --day 55 # курсор посеред місії
```

**Головна перевірка пройшла, і в чотирьох варіантах:** 5 fps проти 500 fps;
рівні кадри проти смиканих (16 мс з провалами до 330 мс і нульовим кадром);
сталий warp проти половинного зі зміною посеред; пауза на 50 кадрів. Усі
дають **бітово ту саму** траєкторію й закінчуються рівно в той самий момент.

**Зуби перевірено, і саме вони роблять цей крок вартим окремої перевірки.**
З `t_end`, порахованим від курсора (котючий горизонт «30 діб уперед від
зараз» — цілком природний спосіб це написати), падають усі три перевірки
частоти кадрів: у двох розходиться навіть кількість семплів, у третій —
семпл 976 із різницею в часі. Тобто інваріант 9 не теоретичний: найочевидніша
реалізація горизонту його порушує, і помилка не падає сама — вона просто
робить траєкторію функцією частоти кадрів.

**Горизонт міряється в ланках, і саме тому це працює.** `LEAD_LEGS = 4` —
тримати чотири ланки прогнозу попереду курсора. Скільки це діб, залежить від
того, як густо інтегратор ставить кроки, тобто від самої траєкторії; змінити
число безпечно, бо воно вирішує, **скільки** прогнозу існує, і ніколи —
які в нього числа.

**Порядок усередині кроку: спершу порахувати, потім рушити час.** Навпаки
означало б, що на високому warp курсор упирається в горизонт, який цього ж
кадру мав би вирости, — гра «затиналася» б через кадр при цілком достатній
пропускній здатності.

**Виміряно попутно: warp на цій місії в пропускну здатність не упирається
взагалі.** Одна ланка — близько одинадцяти діб траєкторії, а кадр на стелі
warp (10⁷) — 1.85 доби. Інтегратор випереджає годинник ушестеро навіть з
однією ланкою на кадр, тож спровокувати `Stall::Horizon` чесною роботою тут
неможливо. Це не хиба тесту: на вільному польоті ядра з 3·10⁶ кроків/с (I3)
просто забагато. Стеля стане реальною на тязі та в атмосфері, де крок
фіксований і малий. Перевіряється натомість сам механізм: світ із нульовим
бюджетом не рухає час ні на біт і каже, чому.

**Ерміт, а не пряма, і різниця виміряна:** у вузлах інтерполяція точна
(похибка рівно 0 — кубіка проходить через семпл за побудовою), а посередині
між семплами вона відхиляється від хорди на **17.4 км**. Тобто лінійна
інтерполяція між кроками інтегратора малювала б орбіту з похибкою в
сімнадцять кілометрів — саме тому в кожному вузлі зберігається й швидкість.
Тест вимагає обох тверджень: точності у вузлі й **не**виродженості
посередині. Друге ловить помилку, від якої перше не рятує: кубіка, у яку
швидкості не потрапили, — це та сама хорда.

**Історія й прогноз — одні й ті самі ланки**, розділені порівнянням
`sample.t` з курсором. Ні перерахунку, ні копіювання, ні руху в сторі: саме
це й означає правило 5 з PROJECT.md §4, і тепер його видно буквально —
у `build/j2_mid.png` межа між блакитним і оранжевим стоїть рівно там, де
маркер апарата.

**Стеля на `dt` кадру** (0.25 с) — не заради плавності. Вікно, згорнуте на
хвилину, дало б перший кадр із `dt` у хвилину, і курсор поїхав би на тижні
вперед від того, що користувач перемкнув вкладку. Зламатися від цього нічого
не може (час усе одно впреться в горизонт), але це не те, чого гравець
просив.

**Розвилка не знадобилася.**

### J3. План маневрів → послідовність `prop_run` ✅

`Plan` і `Manoeuvre` (перенесені сюди з J1: у J1 їх не було чим читати),
імпульсний Δv у фреймі, сегментний цикл, каскадна інвалідація.

```sh
cargo run -p game -- --demo-plan --shot build/j3.png --day 40
```

**Як план стає викликами `prop_run`:** межа сегмента — це час наступного
незастосованого маневру (або кінець місії), і саме він іде в `t_end`.
Усередині сегмента ланки нарізає буфер. Дійшли до межі — додали Δv до
швидкості, взяли наступний маневр за індексом, пішли далі. Ніде в цьому
ланцюжку немає годинника (інваріант 9).

**Головна перевірка: світ робить рівно те, що зроблять руки.** Оракул —
та сама послідовність через `core-rs`, зшита в тесті без стору, без
снапшотів, без індексу застосованих маневрів і без каскаду. Бітово те саме
на всіх 2000+ семплах трьох сегментів.

**Виміряно, каскадний перерахунок** (курсор на 20-й добі, правиться маневр
на 25-й): ланок було 5, лишилося 3, перераховано 3, збережено **605 семплів
бітово незмінними**. Горизонт після правки 50.7 доби проти 40.0 до неї — і
це не помилка: горизонт міряється в **ланках**, а межі «буфер заповнився»
після іншого Δv лягають в інших місцях. Вимагати тієї самої секунди
означало б вимагати, щоб інша траєкторія мала ту саму густину кроків.

**Крок через маневр: переносимо.** Виміряно на п'яти добах після імпульсу
12 м/с: перенесений крок — 365 кроків, скинутий — 370, розбіжність у кінці
1.6·10⁻⁴ м. Тобто скидання дорожче лише на 1.4%, а не в сімдесят разів, як
у H1, — бо тут воно одне на маневр, а не одне на ланку. Обидві траєкторії
правильні; перенесення обрано як дешевше й таке, що ніколи не буває
помилковим (завеликий крок контролер відхилить, замалий виросте).

**`Leg` отримала `entry` — стан, з якого ланка почалася.** Без цього
інтерполяція Ерміта на першому відрізку після маневру брала б лівим вузлом
останній семпл попередньої ланки, тобто швидкість **до** імпульсу, і
малювала б дугу, якою апарат не летів. Тепер розрив стоїть рівно там, де
він фізично є. Перевірено окремо: позиція на маневрі неперервна бітово,
стрибок швидкості дорівнює |Δv| з точністю 10⁻⁹ (подвійне застосування дало
б рівно вдвічі більше).

**Правки в минулому відхиляються** (`PlanRejected::InThePast`), і саме на
цьому стоїть недоторканність історії: переписуються тільки ланки прогнозу,
бо інших правити не дозволено.

**Зуби перевірено двічі, і друга перевірка знайшла справжню ваду.**

1. Скидання кроку на маневрі валить звірку з ручним прогоном — і лише її,
   що правильно: решта тестів не порівнюють із незалежним зшиванням.
2. Вимкнений маневр мусив завалити три тести — а натомість **завісив
   прогін назавжди**. Причина: `t_end` лишався тим самим моментом, ланка
   виходила порожня, і цикл крутився без поступу. Виправлено тим, що тік
   рахує **поступ, а не спробу**; у правильному коді такого не буває, тож це
   сторож, а не механізм. Вада була б невидима без перевірки зубів:
   зависання в грі виглядає як «щось задумалось», а не як помилка.

**Оракул Lambert відкладено, і це варто назвати прямо.** ROADMAP обіцяв
зовнішню перевірку — дві імпульси з Lambert (G1) приводять апарат у цільову
точку. `lambert_solve` живе в C і на межу не винесений: `core-sys` має рівно
шість функцій, а винести сьому — це окремий крок етапу D з власним
C-оракулом і бітовою звіркою, а не рядок у тесті плану.

Що від цього втрачено: **фізика імпульсу не перевіряється проти зовнішньої
задачі.** Але сам імпульс — це три додавання; єдине, де в ньому можна
помилитися, — базис фрейму, і його перевіряють юніт-тести проти явно
виписаних векторів (кругова орбіта в площині xy: «вперед» мусить лягти на
+y, «нормаль» на +z, «назовні» на +x). Непокритою лишається не арифметика, а
питання, чи має обраний Δv фізичний сенс, — на це й потрібен Lambert, і він
приїде разом із флайт-планером M3.

**Фрейм Δv — обов'язково відносно тіла.** `Frame::Vnb { body }`, а не просто
«вперед»: у барицентричних координатах швидкість апарата біля Землі — це
переважно швидкість самої Землі навколо Сонця, і «вперед» означало б уздовж
земної орбіти. Окремий тест саме на це: тіло, що летить швидше за апарат, не
має права зсунути базис.

**Видно оком** (`--demo-plan`, гальмування 12 м/с на 10-й добі): апарат
сходить з halo-орбіти не геть, як без маневру, а в зв'язану орбіту навколо
Землі. Той самий кадр без плану лишає траєкторію, яка йде в нікуди.

**Розвилка не знадобилася.**

### J4. Нитка симуляції ✅

`crossbeam-channel` для команд, `arc-swap` для снапшотів, `select!` з
тіком.

**Код світу не змінився жодним рядком.** `World::step` як була звичайною
функцією, так і лишилась; змінилося тільки те, хто її кличе. Саме заради
цього J1–J3 і робилися однонитково — і саме тому весь крок вийшов
маленьким.

**Головна перевірка пройшла: траєкторія з нитки бітово дорівнює
однонитковій**, усі 2000+ семплів. Це не самоочевидно: нитка міряє власний
`dt`, крутиться зі своїм тіком (8 мс) і робить іншу кількість роботи за
прохід. Усе це J2 оголосив безпечним — тут воно перевірене в тій формі, у
якій справді працюватиме.

**Чого перевіряти не довелося — і це головне.** Гонки даних немає не тому,
що ми її шукали, а тому, що спільного мутабельного стану немає взагалі:
світ належить нитці, назовні йдуть незмінні снапшоти, всередину — команди
каналом. `Send`, але не `Sync` у `core_rs::Propagator` (H4) тут працює як
задумано: компілятор просто не дав би поділити його між нитками.

**Виміряно, читання снапшоту:** 1 889 288 читань за 300 мс при насиченій
нитці, найгірше **13.8 мкс**. Під навантаженням (усі п'ять тестів разом,
п'ять ниток симуляції плюс тестові) найгірше — 3 мс, і це вже планувальник
ОС, а не очікування: `ArcSwap::load_full` не має на кого чекати. Поріг у
тесті — 50 мс, щоб ловити блокування, а не завантажений раннер.

Порівняння, заради якого це міряється: під `Mutex` кадр чекав би на тік
симуляції, і 60 fps трималися б рівно доти, доки нитка не візьметься за
довгу ланку. Тут не тримається жоден замок, бо його немає.

**Команди не перетинаються з кроком.** Канал вичерпується **до**
`world.step`, тож коміт плану ніколи не приходить усередину інтегрування.
Це не оптимізація, а причина, з якої `commit_plan` може безпечно
переписувати `tip` і `applied`.

**Події доповідаються один раз.** Апарат, що вперся в межу ассета, шле
`VesselFailed` одного разу, а не щотіку: канал не має перетворюватися на
потік того самого повідомлення. Прапорець скидається, якщо новий план
повернув апарат до життя.

**Нитка спиняється разом із ручкою** — `Drop` шле `Shutdown` і чекає.
Виміряно: 52 мкс. Без цього закрите вікно лишало б живу нитку, яка й далі
рахує, і помітно це стало б лише в диспетчері задач.

**Розвилка не знадобилася.**

### J5. Нитка планувальника ✅

Спекулятивні прогони, скасування за `id`, коміт пропозиції у світ.

```
у грі: «p» — показати гальмування через 5 діб, Enter — летіти ним
```

**Головна перевірка пройшла: прев'ю з іншої нитки бітово дорівнює тому, що
потім порахував `Sim`** — три ланки, 725 семплів, кожен біт позиції,
швидкості й часу, плюс перенесений крок на виході кожної ланки. Це і є
обіцянка «лінія, яку ви бачили, і є лінія, якою полетите»: без неї
планувальник маневрів безглуздий, бо гравець обирав би за однією кривою, а
летів іншою.

**Спільного коду тут більше, ніж власного, і це рішення, а не лінощі.**
Планувальник не має свого сегментного циклу: він будує звичайний `World` на
**тій самій** ефемериді, з тим самим `PropConfig`, і кличе той самий `step`.
Точку перезапуску обидва беруть з однієї функції (`leg::restart_at`). Дві
реалізації того самого правила розійшлися б — і розійшлися б саме там, де
ціна найвища.

**Ділити ефемериду можна, і це передбачено давно.** `Ephemeris` — `Sync`,
доведено читанням C у D3, коли жодної другої нитки ще не існувало.
`Propagator` — `Send`, але не `Sync`, тож у кожної нитки він свій; тут
компілятор просто не дав би зробити інакше.

**Виміряно, чому точка перезапуску мусить нести крок:** те саме прев'ю з
перенесеним кроком дає 981 семпл, з «обери крок сам» — 986. П'ять семплів
різниці — це не округлення, а інша послідовність кроків, тобто інша
траєкторія (H1). Прогін із довільної точки дав би правдоподібну криву, яка
просто не та; окремий тест тримає цю різницю видимою.

**Скасування — між ланками, а не після прогону.** Гравець тягне вузол, і
запити летять десятками за секунду; нитка кидає роботу, щойно в каналі
з'явився новіший запит. Перевірено двадцятьма запитами поспіль: доходить
рівно останній.

**Дві пастки, знайдені тестами, і обидві не про код, а про те, як перевіряти
нитки.**

1. Перша версія перевірки чекала «доки з'явиться дві ланки після точки
   перезапуску» — і проходила **до** того, як коміт узагалі обробився:
   потрібних ланок було досить уже в старій траєкторії. Ознака, що
   перерахунок таки стався, мусить бути такою, якої **не могло бути
   раніше**; тут це ланка, що закінчується рівно на маневрі. Правило: чекати
   на слід нової роботи, а не на кількість, яка й так уже є.
2. **Тест брав момент маневру круглим числом — і впав на macOS, пройшовши на
   Linux.** Команда `TogglePause` долітає не миттєво, а на максимальному warp
   кожен тік симуляції — це майже дві доби ігрового часу; на повільнішому
   раннері курсор устигав проїхати обрану 30-ту добу, і план чесно
   відхилявся як «у минулому». Виправлено тим, що тест читає, де курсор
   **справді** спинився, і бере маневр відносно цієї точки. Заодно відмова
   тепер валить перевірку одразу, а не ховається за таймаутом.

Це не хиба конструкції: гра поводилася правильно в обох випадках. Але
записати варто, бо клас помилки повторюється — **у тесті з нитками не можна
припускати, коли команда подіє.**

### J6. Сейв і завантаження ✅

Курсор, стан, крок інтегратора, план (PROJECT.md §4, правило 4).

```sh
cargo run -p game -- --shot build/a.png --demo-plan --day 40 --save build/j6.save
cargo run -p game -- --shot build/b.png --load build/j6.save
у грі: F5
```

**Головна перевірка пройшла:** зберегти на 30-й добі, підняти наново,
догнати кінець місії — **1832 семпли бітово ті самі**, що в прогону, який
ніхто не переривав. Правило 4 нарешті перевірене, а не проголошене.

**Зуби:** сейв без кроку дає 1836 семплів замість 1832. Це та сама перевірка,
що в H1 і J3, але з найгіршою ціною: різниця не в роботі, а в тому, що
завантажена гра летить не туди, куди летіла збережена, — і жодного
повідомлення про це немає.

**Точка збереження — остання межа ланки не пізніша за курсор, і жодне з
трьох слів тут не зайве.**

- Не кінець порахованого: перша версія зберігала `tip`, і завантажена гра
  стрибала вперед на весь прогноз — у виміряному випадку з 30-ї доби на
  66-ту. Прогноз у сейв не входить (§4), тож відновлювати з нього нічого.
- Не сам курсор: з довільної точки продовжити бітово неможливо, бо крок
  інтегратора зберігається на межах ланок і тільки там.
- Отже **зерно сейву — ланка**, і скільки це часу, залежить від траєкторії.

Курсор при цьому зберігається точно. Після завантаження горизонт наздоганяє
його першими ж тіками, а щоб годинник не відкотився назад, у `Clock`
з'явилося правило: **курсор не ходить назад ніколи**. Доти снапшот чесно
каже `Stall::Horizon` — тобто «рахую», а не «підгальмовую».

**`applied` лежить у файлі числом.** Стан до й після імпульсу мають
**однаковий час**, тож правило «застосувати все, що не пізніше» виконало б
маневр удвічі, а «все, що раніше» — жодного разу, якби точка перезапуску
колись стала пост-імпульсною. Окремий тест зберігається рівно на межі, що
збігається з маневром, і вимагає, щоб після завантаження стрибок швидкості
був той самий, що був.

**Числа у файлі — шістнадцяткові біти, десяткові поруч коментарем.** Сейв
мусить відтворювати гру бітово, а десятковий друк — це домовленість між
форматувальником і парсером. У Rust вона надійна, але C6 уже фіксував
протилежний випадок (друк `double` — справа libc, і саме тому CSV не входять
у звірку детермінізму). Ціна помилки тут вища за читабельність, тож парсер
читає біти, а око — коментар. Один сейв з одним апаратом і одним маневром —
сім рядків.

**Чого етап свідомо не робить:** UI на `egui` (це M3), local tier і Jolt,
скінченні горіння (потребують тяги в C), `rayon` будь-де.

### Стан етапу J

Усі шість кроків закриті. Що з цього діє далі:

| Твердження | Де виміряно |
|---|---|
| Ігрові типи не змінюють жодного біта проти прямого прогону | J1, 2448 семплів |
| Частота кадрів, warp і пауза не доходять до чисел | J2, чотири варіанти |
| План — це послідовність `prop_run`, і машинерія нічого не додає | J3, звірка з ручним зшиванням |
| Нитка не змінює нічого, крім того, хто рахує | J4, бітово |
| Прев'ю з іншої нитки — це майбутній політ | J5, 3 ланки / 725 семплів |
| Сейв відновлює гру бітово | J6, 1832 семпли |

Три числа, які варто пам'ятати поза контекстом кроків:

- **Читання снапшоту — 13.8 мкс у найгіршому випадку** при насиченій нитці
  (1.9 млн читань за 300 мс). Під `Mutex` тут було б очікування.
- **Warp на вільному польоті в пропускну здатність не упирається**: ланка —
  це ~11 діб траєкторії, кадр на стелі warp — 1.85 доби. Упреться на тязі
  та в атмосфері.
- **Ерміт проти прямої — 17.4 км** посередині між кроками інтегратора. Саме
  тому кожен вузол несе швидкість.

Одна вада, знайдена перевіркою зубів і виправлена: тік рахував **спроби**, а
не поступ, тож апарат, який не рухається, зациклював прогін назавжди.
Зависання читається як «щось задумалось», а не як помилка, — це найгірший
спосіб бути неправим, і без навмисного ламання воно б не знайшлося.

Борг, який лишається названим: **оракул Lambert для J3.** `lambert_solve`
живе в C і на межу не винесений; винести його — окремий крок етапу D з
власним C-оракулом. Без нього фізика імпульсу перевірена лише зсередини
(базис фрейму — юніт-тестами, решта — арифметика з трьох додавань), а не
проти зовнішньої задачі. Приїде разом із флайт-планером M3.

---

## Етап K. Повна модель сил (M3.5) 🟢→🟡

Точкової маси досить було рівно до цього місця. PROJECT.md §4 називає
чотири ефекти (J2 Землі, маскони Місяця, SRP, опір) і одну діагностику
(`prop_run_stm`), і етап мусить закритись **до** M6: без SRP і гармонік
у механіки невизначеності немає фізичної основи (PROJECT.md §8), вона
нав'язується штучно.

Цей етап заодно рухає відкриту розвилку з розділу «Дві розвилки» вище:
похibka ефемериди проти Horizons зростає як `t^1.02` (Земля) і `t^0.87`
(Місяць), а не `t²` — це підпис відсутньої фізики, названої там же:
**J2 Землі й Місяця**, ще до IAS15.

Як і з G1 у M3: деталізувати наперед усі кроки немає сенсу, кожен
отримає свій розгляд, коли до нього дійде черга. Зараз розписаний лише
перший.

### K1. Гармоніки Пайнса — ізольований модуль

Рекурсія Пайнса (1973), а не Каннінгема: Каннінгем ділить на
`sqrt(x²+y²)`, що сингулярне на полюсі, а PROJECT.md §4 explicitly
вимагає «немає сингулярності на полюсах». Пайнс працює через напрямні
косинуси `s=x/r, t=y/r, u=z/r` — жодного ділення на щось, що прямує
до нуля.

Модуль `core/harmonics.c/h`: `harmonics_accel` (прискорення) і
`harmonics_potential` (потенціал, для перевірки й майбутньої
енергетичної діагностики) від коефіцієнтів `C_nm, S_nm` (ненормовані,
довільного degree/order, трикутний масив) у власній системі відліку
тіла. Прив'язку до фактичного тіла (полюс, обертання, коефіцієнти з
ассета) робить K2 — тут лише математика, як `richardson.c` і `stm.c`
до неї.

**Ланцюжок рекурсій:**
- `R_m + iI_m = (x+iy)^m` — без нормування на `r`, щоб степінь `r`
  згорнулась в один спільний показник із радіальним членом;
- `A_nm(u)` — трикутна рекурсія Лежандра з винесеним сингулярним
  множником `(1-u²)^{m/2}` (сама ідея Пайнса);
- `Ad_nm(u) = dA_nm/du` — та сама рекурсія, продиференційована прямо,
  а не взята з довідника: похідна від точної формули, а не окреме
  джерело помилки.

**Перевірка:**
- рекурсія проти підручникових приєднаних функцій Лежандра, виписаних
  явно (17 пар `(n,m)` до 6-го степеня × 3 точки): найсильніша перевірка
  саме загальної гілки `n > m+1`, бо замкнена форма J2 закріплює рівно
  одну пару `(2,0)`, а звірка градієнта з потенціалом зсунула б обидва
  разом і мовчала б. **Спершу цієї перевірки не було** — ROADMAP заявляв
  її, а в `test_harmonics.c` її не написали; додано при ревізії, разом
  із лічильником перевірених пар, бо тихо порожній цикл «проходить»
  (і він одразу спіймав власну помилку на три в моєму ж лічильнику);
- поле з єдиним `C_20=-J2` проти класичної формули J2 у декартових
  координатах (Vallado) у кількох точках — відносна похibka на рівні
  машинної точності, не наближення;
- обертальна інвариантність зонального поля навколо полюса: поворот
  на рівно 90° — це `(x,y)→(-y,x)`, без жодної тригонометрії, тож
  перевірку можна зробити з тим самим допуском, що бітову;
- скінченні різниці `harmonics_potential` проти `harmonics_accel` для
  довільного багаточленного (тесерального) поля — той самий інструмент,
  яким C2b перевірив STM;
- на самому полюсі трансверсальне прискорення точно нуль для `m=0`
  і `m≥2`, і ненульове лише для `m=1` — це випливає прямо з того, що
  `R_m(0,0)=I_m(0,0)=0` при `m≥1`, а не з довідника, і є другою
  незалежною перевіркою тієї самої рекурсії;
- `make check-libm`: нового невизначеного символу нема, лише
  `+ - * / sqrt`.

**Розвилка:** 🟢 математика виведена символьно тут-таки (додаткова
рекурсія для `Ad_nm` — це пряме диференціювання рекурсії `A_nm`, а
формула прискорення звірена проти класичної J2 аж до знаку), а не взята
з пам'яті без перевірки. Якщо тест не сходиться — спершу секторальний
член (`m=n`): його похідна мусить бути точним нулем; далі — сусідній
`A_{m+1,m}`, і лише потім загальна гілка `n>m+1`.

### K2. J2 Землі в кукері

Зональний член вісесиметричний — потрібен лише напрям полюса тіла, не
повне обертання, тож поки що це найдешевший спосіб рухати розвилку
з розділу «Дві розвилки»: без чекання на K3.

**Область звужена вже на старті, і це варто сказати прямо.** Задум був
«J2 Землі й Місяця», але `data/horizons/obj_moon.txt` (та сама сторінка,
з якої вже взято GM Місяця) J2 не публікує — лише GM, радіус, період
обертання. Вигадати число суперечило б власному правилу README
(`data/horizons/README.md`: «не вигадувати число»), а справжні коефіцієнти
Місяця — це GRAIL, реальний файл з ліцензією й нормуванням, тобто K5, а
не рядок у коміті. Земля натомість має цитоване значення просто в уже
закомутему `obj_earth.txt`: `J2 (IERS 2010) = 0.00108262545`, `Equ. radius,
km = 6378.137`. K2 звужено до Землі; Місяць явно лишається для K5.

**Полюс — вісь z інерціальної рамки, без обертання.** ICRF за побудовою
близький до середнього екватора Землі на епоху J2000, тож похибка від
цього спрощення — прецесія за десятиліття, кутові хвилини — на порядки
менша за те, що взагалі вимірюється тут. Справжня орієнтація (полюс,
меридіан, чебишевським поліномом) — K3; ставити її раніше, ніж вона
комусь потрібна, означало б готувати абстракцію наперед.

**Механіка, а не тільки додавання сили.** J2 Землі тягне Місяць — і за
третім законом Ньютона тягне назад Землю, з тим самим доданком,
масштабованим на `-mu_moon/mu_earth`. У коді це явно (`nbody.c`,
`NBodySystem::has_j2/j2_body/j2_field`), не припущено: пропустити реакцію
означало б непомітно ламати збереження імпульсу. `nbody_energy` отримав
відповідний доданок потенціалу (`-mu_j·harmonics_potential(d)`), виведений
із тієї самої похідної, що й сила, — не окрема формула, яку могло
розійтися зі своєю ж силою.

**Перевірка (`test_nbody.c`, новий блок):**
- композиція: прискорення на Місяць = точкова маса + `harmonics_accel`,
  побітово (`CHECK_BITS_EQ`) — нічого не губиться і не додається по дорозі;
- реакція на Землю — та сама трійка чисел, масштабована точно так, як
  масштабує сам `nbody.c` (`vec3_add_scaled`), теж побітово;
- збереження імпульсу від самого J2-доданка: `mu_earth·a_earth +
  mu_moon·a_moon ≈ 0`. Не бітово — субтракція двох повних прискорень, що
  на вісім порядків більші за реакцію, яку вони ізолюють, з'їдає точність
  до ~9.5·10⁻¹¹ від масштабу, і це виміряно, а не здогадано;
- збереження енергії на короткій інтегрованій дузі: якби реакція чи
  доданок потенціалу були неправильні за знаком чи величиною, дрейф
  енергії був би помітний, а не на рівні 1e-9, як в інших інтеграторних
  перевірок цього файлу.

#### ✅ Виконано. Що виміряно

Перекуковано `data/fixture/earth_moon.eph` (розмір і похибка підгонки не
зрушили: 50848 байт, 0.0462 м на 120 днях — J2 не додав ваги файлу, лише
точності). Хеші `sc_ephemeris`, `sc_prop`, `sc_trajectory`
переблагословлено; решта дев'ять сценаріїв (включно з `sc_harmonics`)
не зрушили ні на біт — ізоляція підтверджена, а не лише задумана.

**`ex_ephspan` за 10 років, той самий інструмент, що виміряв розвилку,
тепер з `has_j2` увімкненим для Землі:**

| | до K2 | після K2 |
|---|---|---|
| Місяць геоцентрично (10р) | 1.396·10⁵ м | **3.454·10⁴ м** |
| Земля відносна (10р) | 6.519·10⁵ м | 6.521·10⁵ м |
| показник зростання, Місяць (`k`, від 1 року) | 0.87 | 1.01 |
| показник зростання, Земля відн. (`k`, від 1 року) | 1.02 | 1.02 |

**Місяць — у 4.04 рази точніший**, рівно там, де й мало бути: геоцентрична
геометрія Місяця — це те, що тримає на собі гра (PROJECT.md §4), а не
геліоцентрична позиція Землі, яку власний J2 Землі майже не чіпає (6.519 →
6.521·10⁵ м, у межах шуму) — цей залишок належить іншим відсутнім ефектам
(планетні J2, вищі гармоніки), не Місяцю.

**Показник зростання Місяця не впав до k≈0, він став чистішим — з 0.87
до 1.01.** Це не провал гіпотези, а її уточнення: 0.87 було сумішшю двох
механізмів зі своїми показниками (J2 Землі, тепер прибраний, і те, що
лишилось), яка на log-log графіку не є чистим степеневим законом. Після
вилучення домінантного члена лишається один ясний підпис — `k≈1`,
«стала відсутня сила», а не «інтеграторне заокруглення» (`k=2`). Це саме
той підпис, який мала лишити фізика K5: гравітаційне поле Місяця вищих
степенів, а не форма Землі.

**Далі — грубо, кожен пункт деталізується, коли до нього дійде черга:**

### K3a. Кватерніони й модель IAU — ізольована математика

Той самий поділ, що K1/K2: спершу математика окремо й перевірена, потім
(K3b, нижче) — ассет і кукер. Полюс і меридіан за елементами IAU дають
обертання тіла; PROJECT.md §4 хоче його чебишевським поліномом в ассеті
саме тому, що будувати його з sin/cos (`α0(T), δ0(T), W(d)`) можна лише
офлайн, а рантайм має лишитися без тригонометрії.

**Дані звужені так само, як GM барицентрів і J2 Місяця раніше.** Задум
був просто «елементи IAU», але звіряючись із джерелом виявилось: звіт
IAU WGCCRE 2015 явно **прибрав** наближені вирази для Землі й
низькоточну серію для Місяця «щоб уникнути плутанини» — вони очікують
IERS-точні моделі там, де важлива точність. Ці наближені вирази й досі
живуть у `pck00011.tpc` NAIF (реальний, версійований, широко вживаний
файл, а не забутий чернетка), процитовано звідти з обома застереженнями,
не мовчки:
- Земля: сам файл каже, що похibka положення нульового меридіана —
  щонайменше 150 кутових секунд від цієї формули;
- Місяць: жодної фізичної лібрації (переодичного хитання, яке повна
  модель IAU виправляє, порядку кутових хвилин) — лише середні полюс
  і обертання.

Це не вигадані числа з відсутнім джерелом (як застережено в
`data/horizons/README.md`), а цитована середня модель зі свідомо
названою й обмеженою відсутньою частиною — інша річ, ніж здогад.

**Кватерніон, не матриця, як представлення для чебишевської підгонки.**
9 компонент матриці, підігнаних незалежно, дрейфують від ортогональності
між вузлами підгонки; 4 компоненти кватерніона — лишається один інваріант
(одинична норма), і відновити його — `+ - * /` і `sqrt`, той самий
допустимий набір, що й решта рантайму. `core/quat.h`: `quat_rotate`
(стандартний розклад `v + 2w(u×v) + 2u×(u×v)`, дешевший за повне
кватерніонне множення) і `quat_normalize`.

**Побудова обертання (`core/offline/body_rotation.c`) — через явну
матрицю, не через ручний символьний розклад.** Класична послідовність
3-1-3 з орбітальної механіки (`Ω=90°+α0, i=90°-δ0, ω=W`), три елементарні
матриці перемножені в коді (`mat3_mul`), а не вручну розкриті на 27
доданків — ризик арифметичної помилки лишається в множенні матриць, яке
механічне й самоочевидне, а не в тому, що я міг переплутати доданок при
розкритті на папері. Матриця → кватерніон — метод Шеппарда (стандартний,
розгалужений по найбільшому діагональному елементу, щоб `sqrt` ніколи не
брав корінь з майже нуля).

**Перевірка (`test_body_rotation.c`) — не довіра до виводу формули, а
дві незалежні звірки:**
- напрям полюса: `quat_rotate(q, (0,0,1))` мусить дорівнювати
  `(cosδ0 cosα0, cosδ0 sinα0, sinδ0)`, порахованому в тесті **окремо**,
  без жодної матриці — той самий трюк, що звірка Ω/i з напрямом моменту
  імпульсу в класичній механіці, яким я й перевірив саму послідовність
  обертань нижче;
- швидкість обертання: кінцева різниця кватерніона на ±1 с навколо річної
  епохи дає кутову швидкість, яка мусить збігтися з **уже закомутеним**
  числом — `data/horizons/obj_earth.txt` («Rot. Rate (rad/s) =
  0.00007292115») і `obj_moon.txt` («Sid. rot. rate, rad/s =
  0.0000026617») — зовнішній оракул, який не довелося вигадувати, він уже
  лежав у репозиторії з B1.

**Розвилка:** 🟢 послідовність обертань звірена аналітично (напрям полюса
з Ω/i відтворюється формулою орбітального моменту імпульсу — той самий
підручниковий результат, яким уже користується планування) до того, як
писався код; після — обидві щойно описані перевірки, з різних боків
(геометрія полюса й швидкість обертання), обидві пройшли з першого разу.
Якби не пройшли — спершу перевірити напрям обертання найпростішого
елемента (`mat3_rz`, `mat3_rx`) на осьовому векторі вручну, потім знак у
`quat_conjugate` на межі (обертання «інерціальна→власна тіла» проти
«власна тіла→інерціальна» — найлегше переплутати саме тут).

#### ✅ Виконано (K3a). Що виміряно

`core/quat.h` (усе інлайн, як `vec3.h` — і сценарій `sc_quat` за тим самим
приводом, що й `sc_vec3`: перевіряти арифметику модуля напряму, не
чекаючи справжнього рантайм-викликача), `core/offline/body_rotation.h/c`.
П'ять тестів у `test_quat.c` (тотожність, поворот на 90°, round-trip через
спряжений, збереження довжини, і що ненормований кватерніон довжину **не**
зберігає — навіщо взагалі потрібна нормалізація) і дев'ять у
`test_body_rotation.c` (немодельоване тіло → точна тотожність; полюс і
швидкість обертання для Землі й Місяця на трьох епохах). Усі пройшли з
першого разу після виправлення в самому виведенні (нижче).

**Одна знахідка під час виведення, спіймана до написання коду, а не
тестом.** Пряме символьне розкриття `R = R3(W)·R1(i)·R3(Ω)` дало формулу
для x- і z-компонент прискорення J2 (K1) — там це спрацювало, бо кінцева
формула порівнювалася з підручниковою J2. Тут порівнювати обертання
цілком нема з чим підручниковим, тож і розкриття вручну не робилось
узагалі — рівно та обережність, якої бракувало на першому проході K1.

`sc_quat` — новий хеш, решта дванадцяти не зрушили ні на біт. `make
check-libm` чистий: `body_rotation.c` живе в `core/offline/` (лінкується
з `-lm`, як `nbody.c`), `quat.h` — на рантайм-боці й не використовує
нічого, крім `+ - * / sqrt`.

**Дві межі моделі виміряні, а не оцінені — і саме вони перевизначають,
навіщо потрібен K3b.**

1. **Нахил полюса в K2 не має значення.** Припущення «полюс Землі = вісь z»
   точне на J2000 за побудовою й розходиться на 0.557°/століття: 6.6 кутових
   секунд на закомутеній 120-денній фікстурі, 3.3 кутові хвилини на
   десятирічній діагностиці. Ціну виміряно прямо — тимчасовим нахилом усього
   десятирічного куку на найгірші 3.3′: геоцентрична похибка Місяця
   3.454·10⁴ → 3.485·10⁴ м, **менше за 1%**. Отже вісь z — не те, що обмежує
   модель, і акуратна орієнтація тіл не купить точності ефемериди.
2. **А нульовий меридіан Землі гірший, ніж каже застереження NAIF.** Файл
   каже «щонайменше 150 кутових секунд»; виміряно проти справжнього
   Earth Rotation Angle на J2000 (280.4606°) — модель дає 280.1470°, тобто
   **1129 кутових секунд, 0.31°, ≈34 км на екваторі**. Для зонального поля
   (усе, що робить K2 і робитиме K4) це рівно нуль: там входить лише полюс,
   а меридіан скорочується. Для тесеральних членів K5 і атмосфери K7 — ні.

**Отже K3b — не про точність куку, а про систему відліку тіла.** Раніше
цей крок стояв у списку як «потрібне для K5 і K7» з підозрою, що заодно
покращить ефемериду; перше підтвердилось, друге спростовано числом.
Наслідок для порядку робіт: K3b не блокує нічого з уже зробленого й не
дає виграшу сам по собі — його вартість оплачує K5, і робити його варто
впритул перед ним, а не зараз. **Наступний крок — K4** (гармоніки в
`field.c`, тобто те саме поле для апарата, а не лише для кукера): він
спирається лише на K1, дає гравцю J2 під ногами й не потребує обертання
взагалі, бо зональне поле його не питає.

**Що саме лишиться на K3b:** ассет-формат (нова версія `EPH_VERSION`,
чотири додаткові чебишевські канали на тіло на інтервал), `eph_build`/
`cook_fixture.c` рахують і підганяють кватерніон поряд із позицією,
рантаймовий читач у `ephemeris.c` — і, перш ніж на нього спиратимуться
тесеральні члени, краща модель меридіана за ту, чию похибку щойно
виміряно.

- **K4. Гармоніки у `field.c`.** Те саме ядро K1, тепер у полі, яке
  відчуває апарат у грі, не лише в кукері.
- **K5. Маскони Місяця (GRAIL, ~50 ступінь).** Реальні коефіцієнти:
  джерело даних, ліцензія, нормування (K1 працює з ненормованими
  `C_nm/S_nm`, GRAIL публікує нормовані — конверсія офлайн, у кукері).
- **K6. SRP з моделлю тіні.** Залежить лише від позицій (Сонце, тіло,
  апарат) — функція тіні без `acos`, алгебраїчна апроксимація конуса.
- **K7. Атмосферний опір, таблична густина.** Потребує K3 (швидкість
  відносно атмосфери, що обертається) і радіуса тіла — його теж поки
  немає в ассеті.
- **K8. `prop_run_stm` на межі C↔Rust.** `stm_integrate` уже є (C2b);
  тут — лише FFI, потрібний і диференціальній корекції M3, і еліпсам
  невизначеності M6.

---

## Далі — грубо

Деталізувати зараз немає сенсу: G1 показав, що навіть один крок M3 вартий
свого підрозділу; решта віддається на той самий розгляд, коли до неї
дійде черга.

- **M4 Планети.** Кубосфера, DEM, horizon culling, frame graph.
- **M5 Атмосфера й матеріали.**
- **M6 Механіка невизначеності.**

---

## Дві розвилки, що змінюють план цілком

**1. Ефемерида не тримає точності на 200 років.** DOP853 — не симплектичний;
якщо накопичення заокруглень виявиться завеликим, впроваджуйте IAS15 саме
для офлайнового рахунку ефемериди. Це локальна заміна: рантайму вона
не торкається, бо ефемерида — готовий ассет.

**Виміряно (2026-08-13), рішення ще не прийняте.** Той самий інструмент,
що вже в `core/export/ex_accuracy.c` (`export_reversibility`: двотільна
орбіта на відстані Місяця, `tol_m=1e-6`, вперед-назад), прогнаний не до
10, а до 200 років — той самий метод, довший горизонт, нічого не
вигадано заново:

| років | обертів | похибка round-trip | дрейф енергії |
|---|---|---|---|
| 10 | 133 | 4.11·10⁻² м | 7.4·10⁻¹⁴ |
| 100 | 1331 | 4.15 м | 8.5·10⁻¹³ |
| 200 | 2661 | 16.1 м | 1.7·10⁻¹² |

Похибка round-trip росте **квадратично** з часом (×101 за 10-кратне
збільшення часу, ×3.9 за подвоєння — обидва ≈ N²), а дрейф енергії —
майже лінійно. Це очікуваний підпис несимплектичного інтегратора: локально
енергія тримається чудово, але мала систематична похибка семімажорної осі
накопичується в похибку періоду, а та — у квадратично зростаючу похибку
фази вздовж орбіти. Розвилка була гіпотезою («якщо виявиться завеликим») —
тепер за нею є число й швидкість росту, не лише підозра.

**Це не робить рішення очевидним.** 16 м за 200 років при дуже туго
заданому допуску — саме по собі не катастрофа, а фікстура зараз покриває
лише 120 діб, не 200 років. Заміна DOP853 на IAS15 в кукері виправдана,
коли/якщо гра реально потребуватиме безперервного ассета чи тривалого
пропагування на десятиліття-століття ігрового часу — не раніше.

#### Заперечення «фікстура покриває лише 120 діб» знято (2026-08-13)

`core/export/ex_ephspan.c` жене кукер на послідовності спанів, що
подвоюються від закомічених 120 діб, і читає кожен ассет назад через
`eph_body_state` — рантаймовим шляхом, не внутрішнім станом інтегратора.

**Межу задає оракул, не смак.** За останньою епохою `data/horizons` немає
чому бути неправим, а крива без оракула під нею міряла б самоузгодженість,
яку й так міряє похибка підгонки. Тому послідовність подвоюється, доки не
перескочила б оракул, і останнім кроком лягає рівно на нього — і число
береться із завантажених даних, а не з константи, яка може тихо розійтися
з `data/horizons`. Інтервал (8 діб), ступінь (14) і допуск (1 м) —
фікстурні; спан лишається єдиною змінною.

| спан | років | інтерв. | кБ | кроків | похибка підгонки | Земля відн. | Місяць геоц. |
|---|---|---|---|---|---|---|---|
| 120 д | 0.33 | 15 | 50 | 350 | 4.6·10⁻² м | 1.23·10⁴ м | 6.53·10³ м |
| 240 д | 0.66 | 30 | 99 | 695 | 5.2·10⁻² м | 4.95·10⁴ м | 1.33·10⁴ м |
| 480 д | 1.31 | 60 | 197 | 1385 | 7.0·10⁻² м | 7.24·10⁴ м | 2.47·10⁴ м |
| 960 д | 2.63 | 120 | 394 | 2765 | 7.0·10⁻² м | 1.70·10⁵ м | 4.59·10⁴ м |
| 1920 д | 5.26 | 240 | 788 | 5525 | 7.6·10⁻² м | 3.21·10⁵ м | 8.44·10⁴ м |
| 3630 д | 9.94 | 454 | 1490 | 15884 | 7.6·10⁻² м | 6.52·10⁵ м | 1.396·10⁵ м |
| контроль, той самий допуск | 9.94 | — | — | 10950 | — | 6.52·10⁵ м | 1.396·10⁵ м |

(кроки — вже після зміни допуску, див. нижче; при 1 м їх було 350…10447)

У таблиці немає колонки `earth_m`, яку програма друкує: баріцентрична
похибка Землі змішує зсув підсистеми з орбітальною фазою, і на цих епохах
вона немонотонна (1.35·10⁴ м на 960 добі проти 9.15·10⁴ м на 480-й). Це не
покращення точності, а те, де Земля опинилася на орбіті. Саме заради цього
поруч і рахується `earth_rel_m` — з відкинутим зсувом баріцентру моделі.

Чотири висновки, кожен вимірюваний:

- **Довший ассет продовжує коротший, а не переписує.** Перевірено бітовою
  рівністю на кожній спільній епосі. Єдиний виняток — власний кінець спану,
  де закритий інтервал обчислюється на правому краї, а в довшому ассеті та
  сама мить припадає на лівий край наступного: два поліноми, одна мить,
  розбіжність **2.55 см** — це шов між інтервалами, він є в кожному ассеті
  на кожній межі й до довжини спану стосунку не має. Практичний наслідок:
  подовження закоміченої фікстури не змінить хеші сценаріїв, які читають
  лише перші 120 діб.
- **Похибка підгонки зі спаном не росте** (4.6·10⁻² → 7.6·10⁻² м і виходить
  на полицю). Вона властивість інтервалу й ступеня, а не довжини. Тобто
  чебишевський шар — не те, що обмежує спан.
- **Ассет збігається з контролем при тому самому допуску** (1.396·10⁵ проти
  1.396·10⁵ м, нахили однакові до третьої цифри). Отже зростання в колонках —
  не похибка представлення й не дрейф кукера, а розбіжність самої моделі
  з JPL. Показник росту `ex_ephspan` міряє числом: у `похибка ~ t^k`,
  від першого року, **k = 1.02 для Землі й 0.87 для Місяця**. Стала
  відсутня сила дає рівно k = 1; заокруглення несимплектичного інтегратора
  дало б k = 2 — саме таке міряє оборотність вище (16.1 м за 200 років,
  квадратично). Одне число розводить два механізми, і воно каже: це фізика.
  Яка саме — названо в PROJECT.md §4: J2 Землі й Місяця.

#### Допуск оновлено: 1 м → 1e-6 м (2026-08-13)

Це виявилось окремою помилкою, більшою за саму розвилку.

**Метр не діяв.** Кук на 10 років робив однакові 10447 кроків при допуску
1, 0.1 і 0.01 м: крок задавали примусові приземлення на вузли чебишевської
підгонки, а не допуск. Точність кука виходила з кадансу вузлів, і зміна
`INTERVAL_DAYS` чи `DEGREE` тихо змінила б її — `max_fit_error_m` не
повідомив би нічого, бо він поінтервальний.

**А там, де метр діяв, він брехав у приємний бік.** `ex_horizons` і
`test_nbody` приземляються лише на епохи звірки, без вузлів підгонки. Там
десятирічна геометрія Місяця при 1 м дає 9.32·10⁴ м, а збіжна — 1.549·10⁵ м.
Це не «грубіше»: показник при 1 м на **40% НИЖЧИЙ за правду**, бо числова
похибка частково гасила відсутню фізику. Послідовність по допуску
(8.5·10⁸ → 3.7·10⁸ → 4.0·10⁷ → 3.0·10⁶ → **9.32·10⁴** → 1.417·10⁵ →
1.539·10⁵ → 1.549·10⁵) перетинає правильну відповідь по дорозі вниз.
`test_nbody.c` уже мав коментар про цю пастку — і сам у неї потрапив
на порядок нижче, записавши 1 м як точку збіжності.

Змінено: `cook_fixture.c`, `ex_horizons.c`, `ex_ephspan.c`, `test_nbody.c`.
Ціна — 526 кроків замість 350 на фікстурі, 0.04 с на десятирічному куці.
Значення закоміченого ассета зрушили щонайбільше на **2.2 см** (Місяць,
за 120 діб) — менше за похибку підгонки 4.6 см, тобто фізика та сама,
змінилася лише її залежність від випадковості. Хеші `sc_ephemeris`
і `sc_trajectory` переблагословлено; решта дев'ять не зрушили.

**Урок, ширший за це місце:** межу «нижче X» не можна перевіряти одним
прогоном. Незбіжне число, яке вийшло замалим, виглядає як успіх і проходить
будь-який поріг. Збіжність доводиться **згодою двох допусків**, і саме такий
`CHECK` тепер стоїть у `test_nbody.c`.

#### Баріцентр закріплено (2026-08-13)

Найдешевше з того, що взагалі можна було зробити з розбіжністю з JPL.

**Дрейф баріцентру — це не дрейф, а імпульс.** Сума `μᵢvᵢ` початкових умов
дає баріцентру десяти тіл швидкість **3.35 мм/с**; за 10 років це 1.057·10⁶ м
проти виміряних 1.051·10⁶ м. Збіг під відсоток — отже дрейф пояснено
повністю, і жодного «десь накопичується» не лишилось.

`nbody_anchor_barycentre` віднімає цю швидкість з усіх тіл. Це зміна системи
відліку, а не фізики: справжній баріцентр Сонячної системи спочиває за
побудовою, а наш рухається лише тому, що набору бракує маси. Прибирається
**тільки швидкість** — зсув баріцентру від початку SSB (3.4·10⁵ м) лишається
свідомо: стала не накопичується, а прибрати її означало б зрушити всі тіла
з опублікованих початкових умов, які є найкращими наявними даними.

| | без анкера | з анкером |
|---|---|---|
| дрейф баріцентру за 10 років | 1.051·10⁶ м | **9.1 мкм** |
| Земля, баріцентрична (максимум) | 1.679·10⁶ м | 8.743·10⁵ м |
| Земля, відносна | 6.519·10⁵ м | 6.519·10⁵ м |
| Місяць, геоцентричний | 1.549·10⁵ м | 1.549·10⁵ м |

Дві останні колонки не зрушили **взагалі** — і це головна перевірка, а не
розчарування: анкер зобов'язаний міняти лише розташування системи цілком.
Прямий доказ у фікстурі: після перекуку **всі десять тіл зрушили однаково**,
на 3.474·10⁴ м за 120 діб при 3.351 мм/с. Чистий перенос.

Виграш на 10 роках скромний (48% на баріцентричній Землі, нуль на решті),
але він у **швидкості росту**, а не в поточному числі: прибрана складова
росла лінійно й без межі. На 200 роках вона дала б ≈2.1·10⁷ м; анкер
лишає 3.4·10⁵ м назавжди.

**Перемикач збірки** — `make ANCHOR_BARYCENTRE=0`. Це не налаштування гри,
а спосіб зміряти ефект проти власної відсутності; типово ввімкнено, і ассет
їде анкерований. Свідомо не поле `EphBuildConfig`: два виклики, що обрали
по-різному, дали б дві правдоподібні ефемериди. Ім'я штампа в `build/`
несе значення, тож зміна перемикача перезбирає кукер — інакше `make cook
ANCHOR_BARYCENTRE=0` після звичайного `make` тихо скукував би не тим кодом.

**`test_ephemeris` спіймав це першим.** Він звіряє ассет із незалежним
інтегруванням «з того самого старту», і старт перестав бути тим самим —
впав одразу на порозі 1e3 м. Тест мав рацію, звірка була неповна.

#### Пункт «GM барицентрів» переоцінено: він більше не окремий крок

`data/horizons/README.md` тримає відкрите питання: для барицентрів газових
гігантів у `gm.csv` лежить GM **планети**, тобто заниження на 0.01–0.02%.
Виміряно чутливістю (масштабувати GM чотирьох гігантів на частку `f` і
дивитися, куди йде десятирічна похибка):

| `f` | Земля, відносна | Місяць | дрейф баріцентру, **без** анкера |
|---|---|---|---|
| +0% | 6.52·10⁵ м | 1.5486·10⁵ м | 1.051·10⁶ м |
| +0.01% | 6.31·10⁵ м | 1.5486·10⁵ м | 5.54·10⁵ м |
| +0.02% | 6.10·10⁵ м | 1.5485·10⁵ м | **5.77·10⁴ м** |
| +0.05% | 5.47·10⁵ м | 1.5484·10⁵ м | 1.44·10⁶ м |

**Головне тут — остання колонка.** Виправлення GM рівно на фізично
передбачену величину (+0.02%) валить дрейф баріцентру у 18 разів. Тобто
хибні GM барицентрів і **були** тим залишковим імпульсом. Це заодно
незалежне підтвердження самої гіпотези README: помилка справді там і справді
такого порядку, і це не «бракує астероїдів».

Але анкер уже прибирає цей симптом до 9 мкм, безумовно й не потребуючи
шукати число. Тож від пункту лишається те, що анкер не покриває:

- **Земля, відносна: 3–6%** (652 → 631…610 км). Мало.
- **Місяць: нуль.** 1.5486·10⁵ → 1.5485·10⁵ м, тобто метр із 155 км.
  А Місяць — це геометрія, на якій стоїть гра.

**І окрема засторога проти спокуси покрутити далі.** Мінімум відносної
похибки Землі лежить не на +0.02%, а на **+0.3%** (76.9 км, увосьмеро
краще) — вп'ятнадцятеро далі за все, що можна виправдати масою супутників.
При цьому баріцентрична похибка Землі від +0% і далі гіршає монотонно
(8.7·10⁵ → 1.8·10⁷ м при +1%). Метрика, що покращується, і метрика, що
гіршає, від одного повороту ручки — це підгонка, а не виправлення. Брати
системні GM з DE441 варто **заради правильності даних**, і ніколи заради
меншого числа в порівнянні з JPL.

Отже порядок робіт незмінний: далі J2 (M3.5), а не GM.

**Заодно закрито діру в Makefile.** `$(ACTUAL)` залежав лише від бінарників
сценаріїв, тож після `make cook` наступний `make test` звіряв старий
`actual.txt` і мовчки проходив. Перевірка, яка існує заради ловіння тихих
змін, пропускала найтихішу з них. Тепер `$(ACTUAL)` залежить і від
`data/fixture/*.eph`.

**Що це робить з розвилкою.** На горизонті, який репозиторій може
захистити, вона **не жива**: до 10 років похибка ассета — це фізика, якої
бракує, а не інтегратор, і IAS15 не купив би нічого. Розвилка лишається
відкритою рівно там, де й була, — на 200 роках, де оракула немає.
Наступним її рухає J2 в кукері (M3.5), а не заміна інтегратора; після нього
те саме порівняння варто повторити — `ex_ephspan` для цього й лишається
в `make csv`.

**2. Bindless недоступний там, де треба** (P0). Змінює §7 і, можливо, вибір
цілей. Тому й перевіряється до того, як написано хоч рядок рендера.

---

## Прототип механіки невизначеності ✅

Було відкритим питанням (текст нижче — історія рішення, не інструкція):
**прототип механіки невизначеності одразу після M0.** Текстовий, у Python,
поверх CSV з ядра: один переліт, три сеанси трекінгу, одна корекція, рішення
«палити зараз чи чекати». Приблизно тиждень, без графіки.

Аргумент за: це головна ставка дизайну (PROJECT.md §8), а в поточному порядку
вона перевіряється останньою, після всієї графіки. Аргумент проти: відволікає
від межі C↔Rust, коли інерція є саме в ядрі.

**Що змінилося після C6:** «поверх CSV з ядра» більше не потребує роботи.
`halo_inertial.csv` уже містить повний вектор стану з часом, а `station.csv` —
ціну утримання за різних горизонтів прицілювання; прототип читав би їх тим
самим `load()` зі `scripts/plot.py`. Вартість аргументу «за» впала на кілька
днів, вартість аргументу «проти» не змінилася.

**Зроблено інакше, ніж написано вище — не Python-скрипт, а розширення
C-ядра.** `stm.h` сам це передбачав дослівно: «The same matrix is what the
uncertainty machinery needs later (PROJECT.md section 8)». Поширення
коваріації — лінійне (`P' = Φ P Φᵀ`), і STM для цього вже є, вже перевірена
(C2b) й уже пропущена через реальне 10-тільне поле (`accel_field_var`, C4).
Писати це заново в Python означало б довіряти новій математиці збоку замість
уже протестованої в ядрі — тому `core/uncertainty.c/h`, а не скрипт.

```
make csv && make plots     # build/plots/uncertainty.png
./build/export/ex_uncertainty
```

**Що саме зроблено:**

- `core/uncertainty.h/c` — `uncertainty_propagate` (`Φ P Φᵀ`, через
  `stm_multiply`, який уже є), `uncertainty_scale` (прохід трекінгу — один
  коефіцієнт, не модель каналу зв'язку), `uncertainty_position_sigma` /
  `uncertainty_velocity_sigma` (ізотропний підсумок — слід діагоналі, не
  власні значення: ті для симетричної 3×3 без наближень вимагають `libm`,
  за межею інваріанту 3), `uncertainty_symmetry_defect` (діагностика).
  Лише `+ - * /` і `sqrt` — лишається в детермінованій зоні, хоч і не
  використовується рантаймом.
- `core/test/test_uncertainty.c` — чотири перевірки. Точна алгебра
  (одинична СТМ, Φ=2I → P'=4P, бітово) ловить помилку індексації без
  жодної траєкторії. Головна: лінійне передбачення звірене з **реальною
  нелінійною динамікою** — 12 детерміновані точки (±eps по кожній з 6 осей,
  та сама ідея, що фінітні різниці C2b для самої STM) проінтегровані
  `dop853_integrate`, вибіркова дисперсія проти діагоналі `Φ P₀ Φᵀ`.
  Найгірша розбіжність **2.9·10⁻¹⁰** — допуск тесту звужений до `1e-6`,
  запас у чотири порядки, не довільна цифра.
- `core/scenario/sc_uncertainty.c` — детермінізм-сценарій (20 legs, прохід
  кожні 5), додано в `golden.txt`. Хеші решти сценаріїв не зрушили ні на
  біт — новий модуль інертний, поки щось його явно не викликає. Звірено, що
  перевірка має зуби: з `-ffast-math` хеш розходиться.
- `core/export/ex_uncertainty.c` — той самий референс (орбіта 1151, C4),
  що `ex_trajectory.c`; дублює його `lay_out()`/`shoot()` замість спільного
  модуля — діагностичний код, третя майже-копія дешевша за Makefile-хірургію
  заради спільної трансляційної одиниці в `core/export/`. Дає
  `build/csv/uncertainty.csv` і друкує приклад PROJECT.md §8 з реальними
  числами.

**Головна знахідка — не число, а форма кривої.** На малюнку —
пряма лінія на логарифмічній осі: геометричне зростання, без жодного
згладжування. Ілюстративний прохід (дисперсія ÷10, σ ÷√10≈3.16) не встигає
за нестійкістю орбіти 1151 (594× за оберт, C3): на перших проходах
масштаб — кілометри, той самий порядок, що приклад PROJECT.md §8 (±40 км);
до кінця семиобертової місії σ сягає ~10¹⁴ м — числа, з якими вже нема сенсу
рахувати. Це не помилка моделі, а її межа: **сталий коефіцієнт стиснення
проходу — недостатня модель трекінгу біля сильно нестійкої орбіти.** Реальні
місії тому стежать частіше, що ближче до критичної події, — і тепер це
виміряний, а не здогаданий, наслідок.

**Рішення «палити зараз чи чекати» — на першому проході (8-ма доба),
де числа ще осмислені, а не на останньому:**

| | σ до проходу | σ після проходу | Δv/рік | маневрів |
|---|---|---|---|---|
| без сеансу трекінгу | 25.8 км | — | 1.947 | 13 |
| після сеансу трекінгу | — | 8.2 км | 0.616 | 13 |

Стиснення похибки в 3.16× дає **втричі дешевшу корекцію** — Δv майже
лінійна в похибці виведення (той самий факт, що C4 виміряв «з точністю до
відсотка» для station-keeping). Тобто відповідь на приклад PROJECT.md §8
конкретна: чекати на сеанс вигідно рівно настільки, наскільки той сеанс
стискає еліпс, і тепер це число, а не інтуїція.

**Чого прототип свідомо не робить** (задокументовано в `uncertainty.h`, щоб
не сплутати з планом): жодної моделі вимірів, жодного справжнього orbit
determination, жоден коефіцієнт стиснення не походить з розрахунку каналу
зв'язку. Це наступний крок, коли/якщо механіка піде в M6.
